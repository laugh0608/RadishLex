# RadishLex product Rime configuration
# encoding: utf-8
# Distributed under Apache-2.0.

config_version: "1.0"

schema_list:
  - schema: radishlex_pinyin

menu:
  page_size: 5

ascii_composer:
  good_old_caps_lock: true
  switch_key:
    Caps_Lock: clear
    Shift_L: commit_code
    Shift_R: noop
    Control_L: noop
    Control_R: noop
