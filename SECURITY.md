# RadishLex 安全策略

本文说明 RadishLex 安全漏洞的私下报告方式、受理范围与披露原则，面向安全研究者、用户和贡献者。本文不公布未修复漏洞细节，不承诺固定响应 SLA，也不替代根目录 `LICENSE`、隐私设计或具体平台 runbook。

RadishLex 涉及系统输入法、个人化学习、端到端加密同步、设备授权、用户数据和产品安装事务。可能暴露真实输入、密钥、用户词库或系统控制权的问题应优先私下处理。

## 私下报告

远程仓库已启用 [GitHub Private Vulnerability Reporting](https://github.com/laugh0608/RadishLex/security/advisories/new)，请使用该私密入口提交安全漏洞。若该入口不可用，可发送邮件至 `laugh0608@foxmail.com`，主题包含 `[RadishLex Security]`。

请不要为未修复漏洞创建公开 Issue 或 Pull Request，也不要在首次联系中附带真实用户数据、密钥或可直接利用的完整材料。报告尽量包含：

- 受影响的提交、版本、组件、平台和部署条件；
- 使用合成数据得到的最小复现步骤、预期行为与实际行为；
- 对机密性、完整性、可用性、隐私或系统安全边界的影响；
- 已脱敏的日志、崩溃信息、调用链或测试样例；
- 已知利用前提、临时缓解措施和建议披露时间；
- 是否可以公开致谢及希望使用的署名。

不得发送真实输入历史、联系人、密码、支付或证件信息、用户词库、私密上下文、恢复码、设备私钥、同步域密钥、生产凭据或未经授权取得的第三方数据。若复现确实依赖敏感材料，请先说明材料类型，等待维护者安排合适的传输方式。

## 安全问题范围

以下问题通常应按安全漏洞私下报告：

- secure text、密码、支付、证件、隐私模式输入或其他 P0 数据被学习、记录、展示、同步或写入诊断材料；
- P1 原始选择、上下文或输入事件未经明确设计和授权离开本地，或 P2 对象未保持端到端加密；
- 同步服务能够解密用户数据、进入按键热路径，或暴露超出必要范围的 metadata；
- 设备授权、恢复、撤销、密钥轮换、签名验证或重放防护可被绕过；
- tombstone、冲突合并、旧设备或备份恢复使已删除数据复活；
- 输入法平台壳、Manager、本地数据库、Keychain、Keystore、日志或诊断接口发生越权访问或敏感数据泄露；
- FFI 的 ABI、所有权、生命周期、线程、UTF-8 或错误处理缺陷导致越界访问、任意代码执行、状态混淆或安全失败被静默忽略；
- 安装、升级、修复、移除、回滚或 startup gate 可绕过身份、owner、mode、link、hash、receipt、staging、guard 或运行状态校验；
- 恶意词库、schema、同步对象、网络请求或其他输入造成路径穿越、注入、越权 I/O、持久破坏、拒绝服务或资源失控；
- 依赖、构建、CI、签名、打包、分发或更新链路存在供应链完整性问题。

普通功能缺陷、无安全影响的性能问题、文档错误和功能建议可以使用公开 Issue；不确定时优先选择私密报告。

## 处理与披露

项目当前不承诺固定响应 SLA、长期支持版本范围或公开修复周期。维护者会尽力确认接收、评估影响和复现条件，并与报告者协调缓解、修复和披露。修复或可靠缓解可用前，请不要公开可直接利用的细节。

安全修复仍遵循仓库分支与验证规则：修复从私有或独立主题分支进入 `dev`，阶段性稳定后再由 `dev` 晋级 `master`；`master` 合并结果必须回流 `dev`。漏洞样本、测试 fixture、日志和截图必须使用合成或充分脱敏的数据，不得把报告中的秘密或用户内容带入长期仓库历史。
