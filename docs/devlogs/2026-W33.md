# 2026-W33 开发周志

本文记录 2026-W33 的阶段事实、验证证据、风险和交接事项，读者是 RadishLex 维护者与后续协作者。本文不替代 `docs/status/current.md`、架构边界或 Linux L6 runbook，也不构成 VM、package mutation、清理、推送或发布授权。

## 本周目标

- 从冻结 `S2-source-data-512e8ab-0c2cefd6` 逐步验证 source→target upgrade，严格维持单 VM、断网、逐 mutation 授权与 XDG 保留边界。
- 任何真实环境、artifact、receipt、process 或数据指纹漂移立即失败关闭并保留现场；不自动进入 repair、crash/retry 或 P05C。
- 将实机暴露的根因回收到 production 代码与 release-pair 构建门禁，再以新 pair 和独立 clone 继续，不热替换旧现场。

## 2026-08-10：S2 preflight SQLite 副作用与恢复

- 工作从干净且与 `origin/dev` 对齐的 `bf2e4f2` 开始；七台注册 VM 起始全部停止。只启动第三套 L6 guest，立即关闭 `enp0s1` 并确认 IPv4/IPv6 路由为空，其余 VM 保持停止。
- 首轮所谓只读 preflight 通过 Python SQLite `mode=ro` 与 `PRAGMA query_only=ON` 打开 userdb；SQLite 仍创建 `userdb.sqlite3-wal` 与 `userdb.sqlite3-shm`，使冻结 XDG fingerprint 漂移。发现后立即停止，没有生成 upgrade operation ID、没有运行 maintenance CLI 或 dpkg，也没有删除副作用文件冒充恢复。
- 经授权先以 APFS COW 保存漂移现场 `S2-preflight-wal-drift-512e8ab-8fe4d9a1`。config/EFI/qcow2 SHA-256 为 `5111741c54a49068dbbacfd131891b0990a531001769db21942eb88ac6767d2b`、`c496eae6567e551da959684db8c1533335eeabe5cd1b8cfa35c21433c2494345`、`8fe4d9a14c9c53e0b38a2ca4540d7fedd06a22fcefa0cc8ae01207f580f08d53`，local evidence SHA-256 为 `c16bc62e0fc63472e2e86c5f0cf5f6e95f7b28ec484ec5b306a1cd1d086aa199`。
- 在所有 VM 停止且 qcow 无打开句柄时，从原始 S2 COW sibling 原子恢复第三个 guest；config/EFI/qcow2 与冻结 S2 精确匹配。第二轮 preflight 不再打开 SQLite，只按固定节点、mode/owner/link/size/hash 与 WAL/SHM absence 核对；package、receipt、dpkg status、依赖、字体、startup decision、XDG 和产品进程/映射全部通过。

## 2026-08-10：首次真实 upgrade 在 receipt 前失败关闭

- 经单步系统 mutation 授权生成新的 32 hex operation ID，原值未输出或写入外部证据，只登记 SHA-256 `f3306a5a0d4a78459fce4277e89bbc7d548b2dc17b1e63b57b13074edc8a9fd1`。只调用一次 production maintenance `upgrade`，未调用 acceptance controller，也未重试。
- 外层 UTM transport 返回 0 不能代表成功；脱敏调用报告证明 CLI 自身 exit 1、stdout 0 bytes、stderr 34 bytes。maintenance/dpkg 进程均已退出，operation 目录仍只有历史 install 一项，没有新 receipt、staging、guard 或 package mutation。
- package 保持 `26.7.1+38-1`；receipt SHA-256 `e58b144e2d148a7bc37790045c27ef6746308cfdea2e4e0924aa9ed9afa63ab8`、dpkg status SHA-256 `33c4973d4bcccc1932de35b2b326c61140037ee46613c7a925f2cbdbd3d5cff1` 及 userdb/settings/privacy 三份内容哈希均与 mutation 前相同。XDG 没有 WAL/SHM/profile 漂移，用户数据未改动。
- 根因由同一 Rust production parser 离线复现：source actual `.deb` 通过，target 返回 `ProductManifestInvalid`。`ProductManifestV1::validate_profile` 将 expected package version 写死为 `product_version+build_number-1`，因此合法 revision 2 在 store bootstrap 之前被拒绝。旧 release-pair builder 只调用各 root 的 Python artifact verifier，没有以 target production parser 逐侧读取 pair。
- guest 正常关机后七台 VM 全部停止。第三个 failure disk 无打开句柄，config/EFI/qcow2 SHA-256 为 `5111741c54a49068dbbacfd131891b0990a531001769db21942eb88ac6767d2b`、`8f36df35444b83ca414a1fe9b6c7be57c94478830a48909f2aa45e65bb5ae1ee`、`0cb75b38c62fd8d5731f452eb91717ffdfac2103d597e667c675e96c95c0f387`。旧 target ELF、pair、handoff、guest input 和 failure disk 均不热替换、不恢复或重试。

## 2026-08-10：revision profile 修复与 pair 双侧门禁

- Rust manifest 校验改为先验证 canonical positive Debian revision，再精确绑定 package version 中的 product version/build number 与 manifest 字段；不把 revision 限死为 1，也不放宽 product/build/data contract。
- 合成 actual relationship fixture 新增 revision 1→2 upgrade 正向路径，并继续拒绝 manifest product version/build 漂移。Linux product 113 项测试全部通过。
- 新增只读 `radishlex-linux-artifact-verifier`。L6 pair builder 从 target clean root 与 production maintenance 一起编译它，在形成 record 前分别解析 source/target actual `.deb` 与 evidence；任一侧失败都不发布 handoff。release-pair 17 项行为测试与 5 项源码合同测试通过。
- 修复后的同一 Rust parser 对第三套冻结真实 source/target 包只读复验均通过，两份 Linux artifact identity 可构造且 data contract 完全相同。该结果只证明代码根因与修复方向，不使旧 target ELF 或失败现场重新有效。
- 本批代码与门禁已提交为 `56dd4de fix(linux): 接受相邻 Debian revision`，未推送。

## 2026-08-10：第四套真实 ARM64 pair 与独立 handoff

- 七台注册 VM 起始全部停止，只启动 2223 隔离 builder `E2F5624C-B0F9-4102-B149-5F72C1851D49`。SSH Ed25519 host key 与已登记的 clean-base 2222/2224 链一致；builder 为 Debian 13.6 AArch64，旧 bundle、roots 与三套输出均未覆盖。
- host 生成完整 Git bundle，SHA-256 `3d1bd6db9f42c17219f2922509fda9fbb36aff8f8f838cd9ec891ef51370f475`、大小 4,694,460 字节。builder 以显式 `umask 0022` 从 source `55351f21536d6dca3f90ab053c2a81e2b9bea354` 与 target `56dd4de57313a27ff5af288977f9f1530b64736d` 创建两个全新 clean root；构建在 user/network namespace 内只启用 loopback 且无路由。
- 构建使用冻结 Rust/Cargo 1.85.0、CMake 3.31.6、Flutter 3.44.0/Dart 3.12.0 与本地 cache。除 Cargo 自身可变 `.global-cache` 索引外，8,687 项 crate 实体、11,179 项 pub cache 与 1,514 项 Flutter bin cache 在构建前后均匹配冻结清单；没有下载依赖或访问外网。
- 第四个 canonical record SHA-256 为 `70a394eae293cf95924fa250bca8daf0e11fe45c46342bc8e6f94a03db38139a`。source/target package 分别为 `55fba51b05970e6726e24b7485b65bf608317fd9b086dec4b4a9fe20572c280f`、`58ba35891492a864f36d44df37ab30a9bac5cdee18dd4105ba3a2a56a6452814`，artifact evidence 分别为 `c5a805d24fc0b1e9eaf9f2046221373987f0972fed5988a981e7481f5eb40ad6`、`838afa00554d0e5e9d4c4eb094921a6dc4134ecf7194de49903043fed70fe64e`。
- production/acceptance ELF SHA-256 分别为 `3bb2925fca8a88cc7a1c2f107aab7a072faf1f90c68dca1c506040185bcf401c`、`29cb1b1a4c4cab73c80a7dd25803053d9065c07017c897622d8a4d848f69ae76`；均为 ELF64/AArch64、loader `/lib/ld-linux-aarch64.so.1`，对应 production no-default-features 与 compile-isolated acceptance identity。target production Rust verifier 对 source/target actual `.deb` 均返回通过。
- builder Python verifier、host 当前仓库 verifier、8 项逐哈希、mode/link/ELF 与双 root clean 状态一致。pair 经 host 临时目录接收和独立复验后，同父目录原子发布为 `RadishLex-L6-Handoff-56dd4de`；旧三套 handoff 不变。builder 正常关机后七台注册 VM 全部停止。
- 本批没有创建或克隆新 L6、没有写入 guest artifact input、没有生成 operation ID、运行 maintenance/acceptance CLI、调用 `/usr/bin/dpkg` 或改动用户 XDG，也没有推送、tag、Release、公开发布或真实同步。

## 2026-08-10：第四套独立 clone 与断网 preflight

- 创建第八台 VM 前先确认原七台全部停止。从第三套注册 VM 使用 plain `utmctl clone` 建立 UUID `A3F757B1-CE75-4F23-9509-CAD033260AA1` 的 `RadishLex-Debian13-ARM64-L6-56dd4de`，在首次启动前只替换新 clone 的 EFI/qcow2 为冻结 S2 精确字节；源 VM、S2、旧 handoff 与 failure disk 均未修改。
- 首次启动后立即关闭 `enp0s1` 并清空 IPv4/IPv6 路由。guest 实见 UTM 注册缓存保留的源 MAC `7a:5b:65:2e:9c:cd`，与曾直接编辑的磁盘配置 `…:ce` 不一致；因此在输入传输前正常关机。`utmctl clone` 不支持 MAC/端口参数，最终将新 clone 磁盘配置恢复为注册运行态的 `…:cd`/2226；单 VM 与全程断网消除了重复离线 MAC/端口的并发风险，第二次启动后三方 identity 一致。
- 第三套 `/var/tmp/radishlex-l6-inputs` 的 8 项哈希先全部复验。第四套文件只经断网 guest agent 写入独立 `.incoming`；record/package/evidence/production/acceptance/build-environment 8 项哈希全部匹配 handoff后，统一收紧为 root ownership、根 `0700`、目录 `0755`、普通文件 `0644`、两个 ELF `0755` 与单链接。旧输入原子保留为 root `0700` 的 `radishlex-l6-inputs-512e8ab-preserved`，第四套再切入固定路径。
- 只读 preflight 保持 package `26.7.1+38-1`、receipt `e58b144e…ab8`、dpkg status `33c4973d…ff1` 与 dpkg config `fead43b8…a015`；`dpkg -V` 返回 0、audit 为空，历史 install operation 仍只有 source package/evidence 两个 staged slot。20 项直接依赖、DejaVu/Noto 字体解析和 package owner、manifest `b1322933…67b`、两份 FFI `d845abfe…a74` 均通过。
- XDG preflight 禁止 SQLite API，只按固定节点 metadata/size/hash 复验 fingerprint `f3df287f…b86b`；WAL/SHM 与 Fcitx profile absent。Manager/Fcitx sibling FFI startup gate 均返回 status 0、`AllowedProduct(2)`、`InstalledReceiptVerified(2)`、`completed(6)`；调用后 receipt/dpkg/XDG 不变且产品进程映射为 0。
- 本批未生成 operation ID，未运行 production maintenance 或 acceptance ELF，未调用 dpkg mutation，也未启动 Manager、Fcitx、Flutter、Engine 或桌面会话。正常关机后八台注册 VM 全部停止，qcow2 打开句柄为 0；clone config/EFI/qcow2 SHA-256 为 `61daca925e0080d0718aa4b54e1c9633b74bcef9e375bac1e8a73b95c77f9239`、`8f36df35444b83ca414a1fe9b6c7be57c94478830a48909f2aa45e65bb5ae1ee`、`55575c8e3de0afc87f3727c0ef0e7785362e19238e79785a8a583f0b177db114`。

## 2026-08-10：第四套 upgrade 在 operation ID 前停止

- 获得仅执行一次 source→target upgrade 的单步授权后，先确认八台注册 VM 全停，只启动第四套 clone，并立即关闭 `enp0s1`、清空 IPv4/IPv6 路由；MAC、link down 与空路由符合冻结前态。
- 在生成 operation ID 或运行 CLI 前，对照 current terminal receipt 与第四套 record：S2 已安装 source package/evidence 为 `09ed122804b11767b8ac7cd69c323c1f6eef511fd6ae7284d75756fb60569bec`/`fe3d6297c08dccd8cacba13d50aa44dbb1c94b0ca8c2ab4df3a5c605466fcf94`，第四套重建 source 则为 `55fba51b05970e6726e24b7485b65bf608317fd9b086dec4b4a9fe20572c280f`/`c5a805d24fc0b1e9eaf9f2046221373987f0972fed5988a981e7481f5eb40ad6`。
- production `LinuxInstallReceipt::can_replace` 要求新 operation 的 source artifact 与 current installed artifact 精确相同；相同 package version、revision 和 data contract 不能替代 artifact identity。该失败关闭保护 receipt chain，不应削弱，也不能把旧 source 与新 target 跨 pair 混搭为未记录输入。
- 本批没有生成 operation ID、运行 maintenance/acceptance ELF、创建 guard/receipt/staging、调用 dpkg 或启动产品，receipt/package/dpkg/XDG 均未触碰。正常关机后八台 VM 全停，qcow2 打开句柄为 0；第四套 clone config/EFI/qcow2 SHA-256 为 `61daca925e0080d0718aa4b54e1c9633b74bcef9e375bac1e8a73b95c77f9239`、`d32181b0867378a95ed2511e956090160407115e508b8d58ab0f16a50a3d1960`、`5afb335680bc424ae84ffd9a61c6d74acc9090acd27db9ee5754d79a8b3eb6d0`。
- 正确下一步是让 release-pair builder/verifier 显式接受并验证 prior-terminal source anchor：精确复用 S2 receipt 的 source package/evidence，只从修复后的 clean target 形成 revision 2，拒绝同版本 source 重建漂移，再原子冻结新的 canonical handoff并使用另一独立 S2 clone。第四套 handoff/clone 继续作为 mismatch 现场保留。

## 2026-08-10：prior-terminal source anchor repository 门禁

- `packaging/linux/l6-release-pair.json` 现精确绑定 S2 terminal source package/evidence 的文件名、size、SHA-256 `09ed1228…bec`/`fe3d6297…cf94` 与 `prior-terminal-installed-artifact-v1` policy；只同版本、revision 或 data contract 相同不再足以形成 pair source。
- `build-linux-l6-release-pair.sh` 的输入改为 frozen source package、frozen source evidence、单一 clean target root 与 absent output。builder 不再 checkout/build source；先验证并冻结 source，再只构建 target Manager/addon/rootfs/`.deb`、production verifier/maintenance 与 compile-isolated acceptance ELF。
- 新增浅职责 `l6_source_anchor.py`：只接受 absolute canonical `0644` single-link regular files，交叉 canonical evidence 内 package identity，按 committed size/hash 校验后以 `O_EXCL` 复制并同步 file/directory。`l6_release_pair.py` 在 record 与 published verify 阶段再次绑定 source anchor，target production Rust verifier 仍逐侧解析 actual `.deb`。
- release-pair 21 项行为测试与 6 项源码合同测试通过，新增同版本 source 重建、source evidence 漂移、anchor staging 与 source-build 回退拒绝。专项入口不运行 package/guest/system mutation；本批未启动任何 VM、未生成第五套真实 pair/handoff、operation ID 或调用 dpkg。

## 停止线与下一步

- current/roadmap/runbook/协作入口同步与完整仓库门禁已通过；本批只作本地提交，不推送、不创建 tag/Release。
- 下一步另行授权独立 ARM64 builder，以精确 source anchor 与新的 clean target commit 构建、复验并 absent-output 原子发布第五套 handoff；不在同一步启动 L6 guest。
- 第五套 handoff 完成后再另建 S2 clone/preflight，并逐次取得 upgrade、repair、rollback、remove、reinstall、crash/retry、产品启动和重启授权。四个 failure/mismatch guest、四套旧 handoff、S0/S1/S2 与 WAL-drift snapshot 全部保留。

## 2026-08-10：第五套 chain-continuous ARM64 handoff

- 起始复验 `dev` clean、HEAD `1ebbdab` 且八台注册 VM 全停；只启动 2223 隔离 builder `E2F5624C-B0F9-4102-B149-5F72C1851D49`。实时 Ed25519 host key 与冻结 2222/2224 基线一致，Debian 13/AArch64、Rust/Cargo 1.85.0、CMake 3.31.6、Flutter 3.44.0/Dart 3.12.0 与既有冻结环境一致。
- host 生成只含 `dev` ref 的完整 Git bundle，SHA-256 `f2820ac7e0076e5d78987a52bf5d1e0d50fd04d937d7b15ee61d0b9e589eaed6`、大小 4,726,507 字节。builder 从 bundle 创建唯一 clean target `1ebbdabeec804eec6a430266b5be341d79526476`；source 不 checkout/重建，只输入 S2 terminal package/evidence `09ed1228…bec`/`fe3d6297…cf94` 的精确 `0644` single-link 字节。
- 构建运行在 user/network namespace 中，只有 loopback 且 IPv4/IPv6 路由为空。冻结清单中的 8,687 项 Cargo stable file、11,179 项 pub cache entry 与 1,514 项 Flutter bin cache 在构建后继续逐哈希通过；本次只新增一个 pub `active_roots` 指针，没有下载依赖或改变 hosted package body。
- canonical record SHA-256 为 `d2661cc0b7bc3b2ad85dccd11f589baf704d285d99f5c528bc3fea612ac7ed15`。source/target package 为 `09ed122804b11767b8ac7cd69c323c1f6eef511fd6ae7284d75756fb60569bec`/`cdac2f32e7828165b5c3bb3bbb9839df6cd462f965e88b57b4f97a6374567c26`，artifact evidence 为 `fe3d6297c08dccd8cacba13d50aa44dbb1c94b0ca8c2ab4df3a5c605466fcf94`/`7786847ce5dea6fe135f8549d78445565605111ae6e7fe07cee9dd48a2cb2d5f`。两侧依赖摘要保持 20 项/`2ee2b7e5…5743`，manifest 为 `b1322933…67b`/`eaa0a06e…e47`。
- production/acceptance ELF 为 `3bb2925fca8a88cc7a1c2f107aab7a072faf1f90c68dca1c506040185bcf401c`/`29cb1b1a4c4cab73c80a7dd25803053d9065c07017c897622d8a4d848f69ae76`。target production Rust verifier 对 source/target actual `.deb` 均通过；builder 与宿主 Python verifier、8 文件 mode/link/size/hash、source 与第三套 terminal input 的逐字节比较全部一致。
- builder run 先从 `.incoming` 同父目录原子冻结；宿主也只向 absent `.RadishLex-L6-Handoff-1ebbdab.incoming` 复制，独立复验后原子发布 `RadishLex-L6-Handoff-1ebbdab/release-pair-1ebbdab`。旧四套 handoff、四个 failure/mismatch guest、S0/S1/S2 与 WAL-drift snapshot 未改动。
- 本批未启动 L6 guest，未生成 operation ID、运行 maintenance/acceptance CLI、调用 dpkg、创建 `/var/lib/radishlex` 或读写用户 XDG。builder `dpkg --audit` 为空、RadishLex package/state absent；正常停止后八台注册 VM 全部 `stopped`。

## 当前停止线与下一步

- 下一步另行授权从原始 S2 建立新的独立 clone，只写入第五套 handoff并完成单 VM、断网、package/receipt/dependency/font/startup/XDG/process preflight；不在同一步执行 upgrade。
- preflight 冻结后再单独授权 source→target upgrade；repair、rollback、remove、reinstall、crash/retry、产品启动和重启继续分别授权。旧资产、远端推送、tag/Release、公开发布和真实同步保持关闭。

## 2026-08-10：第五套独立 S2 clone 与只读 preflight

- 起始复验仓库 clean、HEAD `e5edd6a` 且八台注册 VM 全停。从第三套 stopped VM clone 出第九台 `RadishLex-Debian13-ARM64-L6-1ebbdab`，UUID `9C5638D7-0F97-4BD8-8A83-ABCDFCADAC6C`；首次启动前以 APFS COW 将冻结 S2 EFI/qcow2 精确复位为 `365b5a17…261bc`/`0c2cefd6…f3eea`，原 VM、S2 与旧四个现场未修改。
- clone 磁盘配置已移除 Network，但 UTM 首次运行仍从注册态缓存带出源 MAC 网卡，guest 短暂取得 `10.0.2.15` 与默认路由。此时尚未写入 handoff、运行产品命令或生成 operation ID；立即将 `enp0s1` down并确认 IPv4/IPv6 路由为空，正式 input switch/preflight 只在该边界之后执行。该偏差未隐藏为“从启动起全程离线”。
- 原 S2 固定输入八项先复验为第三套 record `2f2deaed…697`、source/target `09ed1228…bec`/`6382003e…ca5`、production/acceptance `f716fad3…de62`/`c3ac9c1a…bc2`。第五套 handoff 以 SHA-256 `e5a1b081…6c97`、85,403,136 字节的无扩展属性 USTAR 经 guest agent 进入独立 incoming；脚本先验证 S2 receipt/dpkg/staging/XDG 与旧输入，再验证新 8 文件 hash/mode/link，原子保留旧输入并切换固定路径。
- input-switch local evidence SHA-256 为 `7083334457f8d617695d7737c0889fcbb84987f93690f49f9bdd502ccc3870f8`。第五套 record/source/target 为 `d2661cc0…ed15`/`09ed1228…bec`/`cdac2f32…7c26`；terminal receipt `e58b144e…ab8`、dpkg status `33c4973d…ff1` 与 XDG `f3df287f…b86b` 在切换前后不变。
- readonly preflight evidence SHA-256 为 `6e44f027355b4bd1f3a555929f4a796a38856a6c17f8f18249d24b929c6a6e48`。`radishlex 26.7.1+38-1` installed、`dpkg -V`/audit clean，20 项直接依赖与摘要 `2ee2b7e5…5743`、DejaVu/Noto owner 和 Latin/CJK glyph、manifest `b1322933…67b`、双 FFI `d845abfe…a74`/不同 inode 全通过。
- Python `ctypes` 只从两份精确 sibling FFI 调用 read-only startup gate，不启动 Manager/Fcitx/Flutter/Engine；Manager/Fcitx 均为 status/version/decision/reason/receipt `0:1:2:2:6`。pair source 精确等于 terminal installed artifact，target 为相邻 revision；调用前后产品映射为 0，receipt/dpkg/XDG 不变，WAL/SHM/Fcitx profile absent。
- transfer tar 与三份临时校验脚本在证据回读后从 guest 精确删除，可由宿主原件恢复；root-owned 第五套输入、独立保留的第三套输入和两份 `0600` guest evidence 保留。没有运行 maintenance/acceptance CLI、调用 dpkg mutation、生成新 operation ID 或启动产品。
- host 以 absent `.RadishLex-L6-Preflight-1ebbdab.incoming` 接收三份脱敏 evidence，逐哈希/mode/link/JSON 通过后原子发布 `RadishLex-L6-Preflight-1ebbdab`。local evidence SHA-256 为 `74932b8ca0703f4ecd388da0fe627c450dac5b0b912db12552cf64b68f794f51`。
- clone 正常关机；config/EFI/qcow2 SHA-256 为 `27cbca50e65ef1bae2cf016c25d6108760cd32e578405304fc66f6f2bfe2cc3c`、`c496eae6567e551da959684db8c1533335eeabe5cd1b8cfa35c21433c2494345`、`77c434de5a591886085897920b36bb2dc0ab3407c297ff82dae3f075a465dd1f`，qcow2 零打开句柄；九台注册 VM 全部 stopped。

## 当前停止线与下一步

- 第五套 stopped clone 是下一次 package mutation 的唯一连续现场。下一步仍需单独授权：重新验证九台中仅该 VM 运行、`enp0s1` down/双路由为空、pair/receipt/dependency/XDG/process 静止，生成新的 operation ID并只执行一次 source→target upgrade。
- upgrade terminal postflight 后立即停止，不自动进入 repair、rollback、remove、reinstall、acceptance crash/retry、产品启动或重启。旧资产、远端推送、tag/Release、公开发布和真实同步保持关闭。

## 2026-08-10：收尾复核与 2026-08-11 明日事项

- 收尾前今日已形成八个本地提交：`56dd4de` 修复 Debian revision profile并加入 target production Rust 双侧 actual-package verifier，`1ebbdab` 将 release-pair 改为 prior-terminal source anchor与 target-only builder；其余六个提交依次冻结第三套失败、第四套 pair/clone/chain mismatch、第五套 handoff与只读 preflight证据。本节将随第九个收尾文档提交冻结；逐提交复核未发现未记录的协议、隐私、许可或平台职责变化。
- 代码与文档对照后，boundary、runbook、current、technical plan、repository layout、packaging/platform README 均已覆盖 actual verifier、source anchor、target-only build和停止线；本次补正 roadmap 中仍残留的“双 clean commit 构建”目标口径，并把 UTM 注册缓存可能恢复网卡的运行态检查提升为协作与 runbook 规则。
- 收尾审阅开始时工作区 clean，九台注册 VM 全部 stopped；第五套 clone、四个旧 failure/mismatch disk、所有 handoff/S0/S1/S2、WAL-drift snapshot与证据均保持原样。没有执行新的 operation、maintenance/acceptance CLI、dpkg mutation、产品启动、远端推送或资产清理。
- 明日开始先复验仓库 clean、九台 VM 全停和第五套 evidence/pair/receipt/XDG未漂移。另行获得单步授权后，只启动第五套 clone；guest 内确认接口 down、IPv4/IPv6 路由为空后再做 package/dependency/process preflight，不能仅凭磁盘配置判断断网。
- preflight 全通过后生成新的 operation ID，只运行一次 production source→target upgrade；以 terminal receipt、dpkg status/audit、完整 inventory、双 startup gate 与 XDG fingerprint postflight 判定，并立即停止 VM。不得顺带进入 repair、rollback、remove、reinstall、acceptance controller、crash/retry、产品启动或 P05C。

## 2026-08-11：第五套 UTM 注册书签异常，upgrade 前停止

- 工作从 clean `bbaf4ff` 开始。宿主 `utmctl list` 只列出八台可用 VM 且全部 stopped，未包含文档登记的第五套 UUID `9C5638D7-0F97-4BD8-8A83-ABCDFCADAC6C`；`utmctl status` 对该 UUID 返回 `Virtual machine not found`。UTM 库面板仍显示九个条目，但第五套为 unavailable，因此没有把磁盘目录存在误判为已注册可启动。
- 第五套 `.utm` 路径、内嵌 UUID、owner/mode/link 与 plist lint 通过。config/EFI/qcow2 SHA-256 精确保持 `27cbca50e65ef1bae2cf016c25d6108760cd32e578405304fc66f6f2bfe2cc3c`、`c496eae6567e551da959684db8c1533335eeabe5cd1b8cfa35c21433c2494345`、`77c434de5a591886085897920b36bb2dc0ab3407c297ff82dae3f075a465dd1f`，qcow2 无打开句柄。
- 经额外单步授权，只尝试恢复现存未漂移 package 的 UTM 注册书签。Finder 精确打开与 UTM 自身 `打开…` 面板精确选择均返回“未能读取数据，因为数据丢失”；系统日志只证明 UTM 尝试解析/创建 security-scoped bookmark，不能证明注册成功。未点击 unavailable 条目的 `移除`，未移动、复制、重建或修改 `.utm`。
- 两次注册入口后 `utmctl list` 仍只有原八台 stopped，UTM 面板仍将第五套标为 unavailable；三哈希再次精确匹配且 qcow2 仍无打开句柄。没有启动任何 VM、改变网络、写入 guest input、生成 operation ID、运行 maintenance/acceptance CLI、调用 dpkg 或读写 XDG。
- 当前失败关闭在宿主注册层，早于 guest/operation/package mutation。下一步须另行设计和授权可恢复的 UTM 注册修复：先保护现存 package 与三哈希，不直接点击 `移除`；只有同一 UUID 重新可由 plain `utmctl` 查询为 stopped、九台全停且三哈希不变后，才重新排一次 source→target upgrade。

## 2026-08-11：第五套冷加载根因修复并恢复注册

- 注册修复开始前 UTM 已正常退出，完整 preference 与第五套 config 先独立备份。对照 UTM 4.7.5 官方源码确认 registry bookmark 位于 package 外部；尝试生成新 security-scoped bookmark 时宿主 agent 返回 Code 256，清空 bookmark 后旧条目仍 unavailable。随后只移除第五套 registry/VMEntryList 登记、保留 `.utm`，UTM 默认 Documents 扫描仍未加入该 package，证明根因不在 registry。
- 第五套与可冷加载的第四套 config 逐字段比较后，除名称/UUID外唯一结构差异是第五套整体缺失 `Network`。UTM 4.7.5 `UTMQemuConfiguration.init(from:)` 对该字段使用必填 `decode([UTMQemuConfigurationNetwork].self, forKey: .networks)`；第五套此前只在 UTM 注册态缓存对象仍存活时启动，磁盘 config 从未通过 app 冷启动解析。通用“数据丢失”错误被更正为配置冷加载失败，不再记录为已证实的 bookmark 损坏。
- 经授权从已备份 config 生成唯一差异候选 `Network=[]`，lint 与移除该键后的结构等价检查通过；候选先以同目录 incoming 暂存并复验 owner/mode/link/hash，再原子替换 `config.plist`。config SHA-256 从 `27cbca50e65ef1bae2cf016c25d6108760cd32e578405304fc66f6f2bfe2cc3c` 更新为 `402a584095e9150543023a155575b3492053371efc39ee2fd0d03616534c83e9`；EFI/qcow2 继续为 `c496eae6567e551da959684db8c1533335eeabe5cd1b8cfa35c21433c2494345`/`77c434de5a591886085897920b36bb2dc0ab3407c297ff82dae3f075a465dd1f`，qcow2 无打开句柄。
- UTM 重开后从默认 Documents 冷加载第五套，使用 package 内同一 UUID `9C5638D7-0F97-4BD8-8A83-ABCDFCADAC6C` 重建唯一 registry/VMEntryList 条目并生成新 bookmark；Library 不再 unavailable，plain `utmctl list` 精确枚举九台且全部 stopped。没有点击删除、移动或重克隆 package，也没有启动 VM、改变 guest 网络、生成 operation ID、运行 maintenance/acceptance CLI、调用 dpkg 或读写 XDG。
- 修复前 local evidence `74932b8c…4f51` 与旧 config `27cb…c3c` 保留为历史，不冒充修复后 host/guest preflight。下一步须另行授权只启动第五套，重新证明接口 down、双路由为空、pair/receipt/dependency/font/startup/XDG/process 静止并冻结新 config 身份；通过前不生成 operation ID，通过后才单独授权一次 upgrade。

## 2026-08-11：第五套修复后断网只读 preflight

- 从 clean `9a95fdc` 开始；九台注册 VM 均 stopped，第五套 config/EFI/qcow2 为 `402a5840…3e9`/`c496eae6…345`/`77c434de…d1f`，qcow2 零打开句柄。只启动 UUID `9C5638D7-0F97-4BD8-8A83-ABCDFCADAC6C` 一台，启动后立即确认仅 `lo`、IPv4 `127.0.0.1/8`、IPv6 `::1/128`，IPv4/IPv6 路由为空；本次从启动起未出现虚拟网卡、DHCP 或默认路由。
- 第五套固定输入仍为 8 文件/4 目录、root-owned、固定 mode/size/link；record `d2661cc0…ed15`，source/target package `09ed1228…bec`/`cdac2f32…7c26`。receipt `e58b144e…ab8`、dpkg status/config `33c4973d…ff1`/`fead43b8…a015`、audit/verify 与 20 项依赖摘要 `2ee2b7e5…5743` 均通过；state 仍只有 receipt 和单一匹配历史 operation，tmp/guard absent。
- DejaVu/Noto owner/glyph、manifest `b1322933…67b`、双 FFI `d845abfe…a74`/不同 inode 均通过。Manager/Fcitx startup 正向合同都为 `0:1:2:2:6`；无效 component 9 返回 exit 5 与 `1:0:0:0:0`，证明调用器能区分拒绝。XDG 只按 metadata/size/hash 检查，fingerprint `f3df287f…b86b`，WAL/SHM/profile 与 acceptance evidence root absent，产品/dpkg 进程和映射前后均为零。
- UTM guest-agent 对部分命令出现 exit status/空 stdout 不一致，较长或文件输入的 stdin payload 也未可靠执行；Python stdout/exit status 因而不作为证据。40 MB package、receipt 与 dpkg status 通过 guest file pull 回读宿主哈希，结构检查使用可靠 Perl 退出合同。startup 使用只写 guest tmpfs 的 C probe，source/binary SHA-256 为 `232101d6…0382`/`f2a044dd…b2a`；正负向完成后两者已删除并确认 absent。任何空输出或单一返回码均未独立用于成功判定。
- 修复后证据冻结到 `RadishLex-L6-Preflight-1ebbdab-post-repair`：目录 `0700`，readonly/JSON 文件 `0600`，SHA-256 `9ecd7f54b88601e977a401d622890d8f785d890436b05bb7edabf9b4a9f63089`/`bf2e05912aa96acd73b5e3d49f3b462691f5424dd2003b030e30fc36b26dd09f`。证据只保存既有 operation ID 的 SHA-256，不暴露原值，也不是 canonical session evidence。
- 网络 postflight 仍为 loopback-only/双路由为空；正常关机后 config 不变为 `402a5840…3e9`，本次 boot 后 EFI/qcow2 冻结为 `bcdab060…ce8`/`fc552276…8f10`，零打开句柄，九台 VM 全停。未生成新 operation ID、运行 maintenance/acceptance CLI、调用 dpkg mutation、启动产品或写用户 XDG。

## 当前停止线与下一步（修复后 preflight）

- 第五套 stopped clone 是下一次 package mutation 的唯一连续现场。下一步仍需单独授权：只启动该 clone，mutation 前重验单 VM、从启动起 loopback-only/双路由为空、pair/receipt/dependency/XDG/process 静止，生成新的 operation ID并只执行一次 production source→target upgrade。
- upgrade terminal postflight 后立即停止，不自动进入 repair、rollback、remove、reinstall、acceptance controller、crash/retry、产品启动或 P05C。旧资产、远端推送、tag/Release、公开发布与真实同步保持关闭。

## 2026-08-11：第五套单次 upgrade 失败并自动恢复 source

- 从 clean `e2e78da` 开始，九台 VM 全停且第五套 config/EFI/qcow2 精确为 `402a5840…3e9`/`bcdab060…ce8`/`fc552276…8f10`。只启动第五套，启动即仅 `lo`、双路由为空；record/source/target、旧 terminal receipt/dpkg、精确 20 项依赖、字体、startup 正负向、XDG 与进程静止全部通过。
- mutation 前发现未曾成功使用的汇总诊断脚本有两个口径/调用错误：把 production relationship 摘要 `2ee2…5743` 当作 resolved package 文本摘要，且把 Manager ELF 作为 Python 脚本参数。没有用 guest-agent 返回 0 或该脚本失败冒充通过；实际 20 行逐项匹配且文本摘要为 `28fe1065…6aac`，其余 gate 通过分段回读、shell 退出合同和 C probe 独立闭合。
- 生成新的 32 hex operation ID，证据只记录 SHA-256 `3926c1d0…bf6a`；production maintenance 只调用一次。真实 exit 为 0、stderr 为空、stdout 为 `maintenance_outcome=rolled_back`。dpkg log 证明 `38-2` 在 22:08:07 installed，22:08:09 自动恢复 `38-1` installed；未重试或启动其他 operation。
- 新 receipt SHA-256 `55171bef…6611`，语义为 `upgrade/target_newer/rolled_back`，failure `target_validation_failed` after `package_mutating`，operation chain 2、source proof installed、target proof null、manual recovery false。dpkg status 精确恢复 `33c4973d…ff1`；Manager/Fcitx startup 均为 `0:1:2:2:11`，XDG `f3df287f…b86b`、WAL/SHM/profile absence、网络与进程静止通过。
- failure/recovery evidence 冻结到 `RadishLex-L6-Upgrade-1ebbdab-rolled-back`，目录 `0700`、八份文件 `0600`；text/JSON SHA-256 `98319405d53c294c95ea2331a2b866610475124d399f195a18c9c867f0862378`/`64d5395f618adbc0c22ff5f8723446c2a6e6e069758078d255491855a74be3d8`。正常关机后 disk 为 `402a5840…3e9`/`cb8a697b…bd65`/`e684f829…8904`，qcow2 零打开句柄、九台全停。

## 当前停止线与下一步（upgrade rolled back）

- 第五套现在是 target-validation failure 与自动 source recovery 现场，不再作为可原地重试 guest。精确失败条件尚未隔离；现有证据只把边界收窄到“target 已 installed 后、target proof 写入前的 production validation”。
- 下一步只在宿主离线对 actual target package、manifest/inventory、dpkg snapshot/relationship 与 installed-product observer 建立可重复合成失败；根因修复和回归门禁闭合后，重新形成 pair/handoff 与独立 clone再逐步授权。
- repair、rollback operation、remove、reinstall、acceptance controller、crash/retry、产品启动、P05C、推送、tag/Release、公开发布、清理与真实同步保持关闭。

## 2026-08-11：收尾复盘与 2026-08-12 明日事项

- 收尾前今日已形成四个本地提交：`b031d0b` 记录第五套 UTM 注册停止线，`9a95fdc` 冻结 `Network=[]` 冷加载修复，`e2e78da` 冻结修复后断网只读 preflight，`da73475` 冻结单次 upgrade 的 target-validation failure 与自动 source recovery；本节将随第五个本地文档提交冻结。今日提交都只修改文档；仓库内没有 Rust、Go、Flutter、C++、脚本、协议、隐私、许可或产品载荷代码提交。
- 按今日事实复核活跃文档，发现 technical plan、roadmap、repository layout、Linux 安装/平台/Manager boundary 以及 packaging/platform README 仍停在第五套 preflight 或“等待 upgrade”口径；本次收尾统一改为 target `38-2` 已安装、production validation 失败、source `38-1` 自动恢复、terminal `rolled_back`，且不得原地重试。历史 devlog 段继续保留当时判断，不回写为新事实。
- 收尾审阅开始时工作区基线为 `dev` ahead 4，九台注册 VM 全部 `stopped`；五套 L6 现场、pair/handoff/S0/S1/S2、WAL-drift snapshot、修复备份与 failure/recovery evidence 均保持原样。今晚不再启动 VM、生成 operation ID、运行 maintenance/acceptance CLI、调用 dpkg、启动产品、清理资产或推送远端。
- `./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与完整 `./scripts/check-repo.sh` 均通过；仅保留既有 `docs/sync-server-api-storage.md` 508 行预算 warning，本批未修改该文件。

### 明日事项（2026-08-12）

1. 开始时复验 Git clean、九台 VM 全停，以及第五套 failure/recovery evidence 和已登记 disk identity 未漂移；只做宿主只读检查，不启动任何 guest。
2. 唯一主切面是离线隔离 `target_validation_failed`：以现有 actual target package/evidence、product manifest/inventory、dpkg log/status snapshot、dependency relationship 与 installed-product observer 为输入，先建立可重复的最小合成失败，并确定具体失败字段、检查顺序和证据缺口。
3. 根因未被测试稳定复现前不修改 production transaction；如确认代码缺口，先补失败回归与边界说明，再做最小一致性修复，运行 Rust 定向测试、Linux product/L6/release-pair 门禁和完整仓库门禁。
4. 修复闭合后只更新 committed contract 与文档，不在同一天顺带启动第五套。新的真实推进必须重新形成合规 pair/handoff 与独立 clone，并重新取得逐步系统写入授权。
5. 继续关闭第五套原地重试、repair、rollback operation、remove、reinstall、acceptance crash/retry、产品启动、P05C、旧资产清理、远端推送、tag/Release、公开发布与真实同步。

## 2026-08-12：离线定位并修复 target validation revision 漂移

- 开始时仓库为 clean `8d811b6` 且与 `origin/dev` 同步，plain `utmctl list` 复验九台 VM 全部 `stopped`。第五套 failure evidence `98319405…2378`/`64d5395f…e3d8` 与 target package/evidence `cdac2f32…7c26`/`7786847c…2d5f` 未漂移；只读提取 target manifest 确认为 product `26.7.1`、build `38`、package `26.7.1+38-2`，未启动 guest 或修改冻结资产。
- production target validation 在 dpkg snapshot 匹配后依次重建 staged relationship、校验 dpkg relationship 并调用完整 installed-product inventory。actual-package manifest parser 已接受 canonical positive Debian revision，但 startup manifest validator 仍独立构造 `product+build-1`；因此 revision 2 target 即使 manifest 与 artifact 精确一致也必然返回 `PackageIdentityChanged`，再映射为 `target_validation_failed`。既有 startup system fixture 只生成 revision 1，未覆盖这一跨层规则漂移。
- 先把完整 `validate_system_product` 的 `26.7.1+38-2` 回归加入 production crate；修改前该测试稳定失败为 `PackageIdentityChanged`。随后把 relationship 的 canonical release validator 提升为 crate 内共享规则，startup 不再硬编码 revision；正向覆盖 revision 2，负向覆盖 revision `0`、前导零、product mismatch 与 build mismatch，保持 package/artifact、ABI、inventory、owner/mode/link/hash 等既有失败关闭条件不变。
- 完整 crate 115 项测试、Linux package transaction/startup、metadata/layout/deb artifact 与 L6 contract/controller/release-pair 门禁均通过；controller feature 下 production crate 120 项和 acceptance crate 10 项测试通过，完整 `./scripts/check-repo.sh` 通过且只保留既有 `docs/sync-server-api-storage.md` 508 行预算 warning。第五套仍只作 failure/recovery 取证，不原地重试；下一步只能从本修复的 clean commit 重新形成 pair/handoff 与独立 S2 clone，再另行取得真实系统推进授权。

## 2026-08-12：形成第六套 chain-continuous ARM64 handoff

- 从 clean `80e49ce`、source anchor `55351f2` 和九台 VM 全停开始；只启动 2223 隔离 builder。其 Ed25519 host key 与冻结的 2222/2224 指纹一致，guest 为 Debian 13.6 ARM64，Rust/Cargo 1.85.0、CMake 3.31.6、Flutter 3.44.0；正式输入前将 `enp0s1` 置为 down并确认 IPv4/IPv6 路由为空。
- guest-agent 首次把 40,806,592-byte source package 截断为 24,477,696 bytes，未用该文件构建。改为五个固定 chunk 传输，逐块校验后重组为 exact SHA-256 `09ed1228…bec`；source evidence 与完整 Git bundle `829c8d25…adaf` 也在 guest 侧逐项复验。早期 prepare 因 Cargo root/cache 清单和 bundle verify 调用假设失败关闭，只保留在本次新 run，不产生 target pair。
- 第一个正式 target 在 `unshare --user --map-root-user --net` 中完成载荷构建，但 record 阶段因 Flutter root warning 污染 combined machine output而以 `invalid flutter version output` 失败；release-pair output 保持 absent，target/log 原样保留。随后从同一 exact bundle 新建 clean r2 target，使用 `--map-current-user --net` 的零路由 namespace；Cargo/pub/Flutter 稳定 cache 清单前后不变，target-only builder 成功。
- 第六个 record 为 `cda70afa89b3f0ee05235b95dcc346eaeea9805c4b87af9d451ce1900f00659b`。source package/evidence 保持 `09ed1228…bec`/`fe3d6297…cf94`；target package/evidence 为 `b211d940…d09c`/`2a1132c6…0e1b`，production/acceptance ELF 为 `b060c240…7d81`/`b24d103b…a6c`，双侧 dependency digest 均为 `2ee2b7e5…5743`。target production Rust verifier、builder/宿主 Python verifier 与 8 文件 owner/mode/link/hash 全部一致。
- pair 以 canonical USTAR `f2e975b8…5f59` 分 11 块回读宿主并逐块复验，在临时目录完成同一 verifier 后，从 absent `.RadishLex-L6-Handoff-80e49ce.incoming` 同父目录原子发布到独立 `RadishLex-L6-Handoff-80e49ce/release-pair-80e49ce`；既有冻结资产均未覆盖或清理。builder 的 dpkg audit 为空、RadishLex package 与 `/var/lib/radishlex` absent，正常关机后九台 VM 全停。

## 当前停止线与下一步（第六套 handoff）

- 本批未启动任何 L6 guest、创建 clone、写 artifact input、生成 operation ID、运行 maintenance/acceptance CLI、调用 dpkg mutation或触碰用户 XDG。第五套 `rolled_back` clone、staging、receipt、preflight/upgrade evidence 与 disk identity继续原样保留，绝不原地重试。
- 下一步只从冻结 `S2-source-data-512e8ab-0c2cefd6` 创建独立 clone，切入第六套 handoff并完成断网只读 preflight；通过后仍须另行授权真实 transaction。repair、rollback operation、remove、reinstall、acceptance crash/retry、产品启动、P05C、清理、推送、tag/Release、公开发布与真实同步保持关闭。

### 明日事项（2026-08-13）

1. 开始时复验 Git clean、九台 VM 全停、第五套 failure/recovery 现场未漂移，以及第六套 record `cda70afa…659b` 和 8 文件 handoff identity 完整；只做宿主只读检查。
2. 取得当次授权后，只从冻结 `S2-source-data-512e8ab-0c2cefd6` 创建一台独立 clone；不修改、启动或恢复第五套 `rolled_back` clone，也不复用任何旧 failure/mismatch guest。
3. 新 clone 每次启动前确认其余 VM 全停；启动后、写入 handoff 前先证明目标接口 down 且 IPv4/IPv6 路由为空，再原子切入第六套输入并完成 package/receipt/dependency/font/startup/XDG/process 的只读 preflight。
4. preflight 任一 identity、网络或系统状态不匹配即保留现场并停止；全部通过也只冻结 clone、input-switch/preflight evidence 与关机后 disk identity，不生成 operation ID，不运行 maintenance/acceptance CLI 或 `dpkg`。
5. 修复后 source→target upgrade 必须留到后续独立授权批次；repair、rollback operation、remove、reinstall、crash/retry、产品启动、P05C、旧资产清理、推送、tag/Release、公开发布与真实同步继续关闭。

## 2026-08-13：第六套独立 S2 clone 与断网只读 preflight

- 从 clean `e6f36cd`、九台 VM 全停开始，宿主先只读复验冻结 S2 config/EFI/qcow2 `5111741c…d2b`/`365b5a17…261bc`/`0c2cefd6…f3eea`、第五套 rolled-back disk `402a5840…3e9`/`cb8a697b…bd65`/`e684f829…8904`、failure evidence `98319405…2378`/`64d5395f…e3d8` 与第六套 8 文件 handoff identity。record/source/target 为 `cda70afa…659b`/`09ed1228…bec`/`b211d940…d09c`，production/acceptance ELF 为 `b060c240…7d81`/`b24d103b…a6c`；production verifier 和宿主 verifier 均通过，旧资产未修改。
- 从冻结 S2 以 APFS COW 创建 `RadishLex-Debian13-ARM64-L6-80e49ce`，UUID `193179D5-2595-4628-A063-9EFB73F8EC05`。首次启动前 config/EFI/qcow2 为 `00bac87d…456`/`365b5a17…261bc`/`0c2cefd6…f3eea`，QEMU config 使用必填 `Network=[]`；注册后只启动该 VM，其余九台保持 stopped。
- guest 启动后只存在 `lo`，IPv4/IPv6 路由为空；在写入第六套 handoff 前再次把 network、boot 与 fixed input 状态写入 `/run` 并回读宿主，pre-input evidence SHA-256 为 `40742f44…8991`。guest-agent 的命令退出/空输出不作为单一成功证据，关键状态都由 guest 文件回读和正负向对照复验。
- source/target package 各拆为五个固定 chunk 传入 `/run`，逐块校验 size/hash 后在 private stage 重组；guest-agent push 产生的 `0666` mode 先作为诊断记录，再在进入 stage 前归一为 `0600`。完整 8 文件通过 owner/mode/link/hash 和 pair verifier 后，以同文件系统 rename 原子保留旧输入到 `/var/tmp/radishlex-l6-inputs-512e8ab-preserved` 并切换 fixed input。stage/input-switch evidence 为 `d8271e0c…e8e`/`a20dc4f…f63`。
- package `radishlex|arm64|26.7.1+38-1|install-ok-installed`、terminal receipt、dpkg status/config/audit/verify、20 项依赖、字体 owner/glyph、manifest与两份同 hash/不同 inode FFI 全部通过；pair relationship digest 为 `2ee2b7e5…5743`，installed FFI 为 `d845abfe…6a74`。
- 第一份 startup 诊断错误地把 Manager ELF 当作 Python source，稳定失败并保留；修正为 `python3 -B -` 后 Manager/Fcitx 均返回 `0:1:2:2:6`。C probe 首次放在 `noexec` `/run`，执行以 exit 126 失败并冻结 diagnostic `bf5586ee…f2c3`；随后同一 source 在 `/tmp` private tmpfs 编译，正向两组件 exit 0/`0:1:2:2:6`，负向 component 9 exit 5/`1:0:0:0:0`，证明不是由错误执行环境伪造通过。probe source/binary 已精确删除并以 evidence `8646021f…273a` 证明 absent。
- preflight 前后 XDG fingerprint 均为 `f3df287f…b86b`，WAL/SHM、Fcitx profile、acceptance evidence root 均 absent；receipt/dpkg 不变、产品映射和 dpkg 进程为零、guest 仍仅 loopback且双路由为空。没有创建 operation ID、运行 maintenance/acceptance CLI、调用 dpkg mutation、启动产品或写用户 XDG。readonly evidence SHA-256 为 `9e84eaf2…e3f`。
- host 以 absent `.incoming` 接收并核对十份脱敏 evidence，原子发布 `RadishLex-L6-Preflight-80e49ce`；目录 mode `0700`，全部文件 `0600`、单 link，local JSON SHA-256 为 `dcdd5acb…df36`。正常关机后 clone config/EFI/qcow2 为 `00bac87d…456`/`7ba7b6bd…ab17`/`0c0c4aea…8d5f`，qcow2 零打开句柄；S2、第五套和其他现场 identity 不变，十台注册 VM 全部 stopped。

## 当前停止线与下一步（第六套 preflight）

- 第六套 clone 是下一次 package mutation 的唯一候选现场，但本批只形成只读 preflight，不把它写成 target terminal 或 transaction 成功。下一步仍须独立授权：启动前复验十台仅该 VM 可运行，启动后重新证明仅 loopback/双路由为空，并在 operation ID 前复验 pair、receipt、dependency、XDG 与进程静止；随后只执行一次 source→target upgrade。
- upgrade terminal postflight 后立即停止，不自动进入 repair、rollback operation、remove、reinstall、acceptance crash/retry、产品启动、P05C 或旧资产清理。远端推送、tag/Release、公开发布和真实同步继续关闭。

## 2026-08-13：第六套单次 upgrade 完成 target terminal

- 从 clean `e88b5d7`、十台 VM 全停与第六套 preflight evidence `9e84eaf2…e3f`/`dcdd5acb…df36` 开始；宿主复验 handoff record/ELF、clone config/EFI/qcow2 `00bac87d…456`/`7ba7b6bd…ab17`/`0c0c4aea…8d5f` 和 qcow2 零打开句柄，只启动 UUID `193179D5-2595-4628-A063-9EFB73F8EC05`，其余九台保持 stopped。
- guest 启动后回读证据只含 `lo`、IPv4/IPv6 路由为空。mutation 前再次复验 record/source/target、旧 source terminal receipt/dpkg、20 项依赖、字体/双 FFI、Manager/Fcitx startup `0:1:2:2:6`、invalid component 9 负向、XDG `f3df287f…b86b`、WAL/SHM/profile absence和产品/dpkg 进程静止；preflight/stage4 tar 为 `7536d306…400c`/`c4f46d38…035e`。
- operation ID 在 guest 内以 16-byte 随机数生成，宿主证据只保存 SHA-256 `d22cad12…1b04`。单次 executor 先原子创建 `/run` invocation marker，再只调用一次 production maintenance；exit 0、stderr 空、stdout `maintenance_outcome=completed`，开始/结束为 `11:52:22Z`/`11:52:30Z`，未重试或调用 acceptance。
- dpkg log 证明 `38-1`→`38-2` 在 19:52:28 installed。receipt SHA-256 `ccbc4cd0…1e60`，语义为 `upgrade/target_newer/completed`、operation chain 2、source/target staging齐全、target proof 精确绑定 package/evidence `b211d940…d09c`/`2a1132c6…0e1b`，failure null、manual recovery false。
- terminal postflight 证明 package `38-2`、dpkg status `c09365b3…cece`、audit/verify clean、20 项依赖、manifest `9f08c7fb…0196`、双 FFI `f51dc0f1…50d3` 且不同 inode、startup 正向/负向、XDG 零漂移、WAL/SHM/profile absence、无产品/dpkg 进程，网络仍仅 loopback/零路由。postflight tar SHA-256 `aa355ca5…21b5`。
- 正常关机后 config/EFI/qcow2 为 `00bac87d…456`/`0846b1d3…9741`/`6b22499b…b137`，qcow2 零打开句柄，十台 VM 全停。host 从 absent `.incoming` 原子发布 `RadishLex-L6-Upgrade-80e49ce-completed`，目录 `0700`、9 个文件 `0600` 单 link；command/local evidence `044c7a25…64e`/`9cc50665…f695` 不含 raw operation ID、raw receipt 或完整 dpkg log。

## 当前停止线与下一步（第六套 target completed）

- 第六套 stopped clone 是唯一 target terminal 现场，本批没有执行 repair 或第二次 maintenance invocation。下一步须另行授权，只从当前 stopped disk 冻结 S3 target-installed snapshot并绑定 receipt/package/XDG/disk identity；不在同批启动 VM。
- S3 冻结后，repair 仍需新的独立授权和 mutation 前 preflight。rollback、remove、reinstall、crash/retry、产品启动、P05C、清理、推送、tag/Release、公开发布与真实同步继续关闭。

## 2026-08-13：冻结第六套 S3 target-installed snapshot

- 从 clean `35e667f`、十台 VM 全停与第六套 target-completed disk 开始；未启动、挂载或注册 VM。宿主重新计算 config/EFI/qcow2 SHA-256 `00bac87d…456`/`0846b1d3…9741`/`6b22499b…b137`，与 upgrade postflight evidence 精确一致，source qcow2 零打开句柄。
- 只在同一 APFS device 上以 clonefile 把三项恢复文件复制到 absent `S3-target-installed-80e49ce.incoming`；逐文件全量 SHA-256、size、plist、inventory、mode、单 link 与脱敏 JSON 语义通过后，同父目录原子发布 `RadishLex-L6-Snapshots/S3-target-installed-80e49ce`。没有复制 screenshot、raw operation ID、raw receipt 或完整 dpkg log。
- S3 identity 为 `S3-target-installed-80e49ce-6b22499b`。local evidence SHA-256 `79a3a170fa9e0c60a2de954cf8129e3fed5578a3bffb69d3f77ea42010e7494b`，绑定 parent S2 evidence `d164de0f…5c74`、receipt `ccbc4cd0…1e60`、target package/evidence `b211d940…d09c`/`2a1132c6…0e1b`、manifest/FFI/dpkg/startup 与 XDG `f3df287f…b86b`。
- 发布后目录/Data 为 `0700`、evidence 为 `0600`、三项恢复文件为 `0644`，四个文件均 single link；`.incoming` absent，source 与 snapshot qcow2 零打开句柄，十台 VM 仍全停。repair、acceptance、dpkg、产品进程与用户 XDG 均未触碰。

## 当前停止线与下一步（S3 已冻结）

- 第六套 terminal 原盘与 S3 都只作恢复真相源，不直接执行 repair。下一步须另行授权，只从 S3 创建独立 repair clone，强制 `Network=[]` 并完成 mutation 前只读 preflight；本步不生成 operation ID、不运行 maintenance/acceptance CLI。
- repair transaction 仍需再一次独立授权。rollback、remove、reinstall、crash/retry、产品启动、P05C、清理、推送、tag/Release、公开发布与真实同步继续关闭。

## 2026-08-13：第六套独立 repair clone 断网只读 preflight

- 从 clean `c48a1da`、十台 VM 全停与冻结 S3 开始。plain `utmctl clone` 使用三项恢复字节与 S3 精确一致的 registered target-terminal VM 创建 `RadishLex-Debian13-ARM64-L6-80e49ce-repair`，UUID `A3022255-ED08-4C66-92D3-075A6BB93107`；启动前 EFI/qcow 与 S3 精确一致，config 仅 Name/UUID 不同且 `Network=[]`，注册 VM 增至十一台。
- 只启动 repair clone；`utmctl start` 虽返回 OSStatus `-10004`，注册状态独立证明目标已 started、其余十台仍 stopped。第一条 guest 命令证明 Debian 13 ARM64/root、仅 `lo`、IPv4/IPv6 路由均为空，之后才把三份已哈希只读输入写入 noexec `/run`。
- 首次直接执行 `/run` stage1 因 noexec 返回 126，target 从未运行；宿主没有把 guest-agent 空 stdout 当成功。改由 `/bin/sh` 解释后 stage1 通过，target 在本批新增的 `4e00-9fff` charset 字符串假设失败。只读诊断证明 `fonts-noto-cjk 1:20240730+repack1-1` installed，owner 与 `Noto Sans CJK SC` family 正确，TTC 实际声明 `4e00-9fef`；删除这条超出仓库正式 package/owner/family 合同的假设，正式门禁未放宽，两次诊断均未修改产品状态。
- 最终 guest 文件回读与逐文件 SHA-256 证明 stage1/target exit 均为 0。record/source/target、package `38-2`、receipt `ccbc4cd0…1e60`、dpkg audit/verify、20 项依赖、字体、manifest/双 FFI、startup 正向 `0:1:2:2:6` 与 component 9 负向、XDG `f3df287f…b86b`、WAL/SHM/profile absence、进程与断网状态全部通过；operation 目录仍为 2、guard absent，未生成 operation ID、运行 maintenance/acceptance CLI、调用 dpkg mutation、启动产品或写用户 XDG。
- 正常 guest-request 关机后十一台 VM 全停，repair clone config/EFI/qcow2 为 `584bf2b5…f59b`/`a73a3266…9155`/`ee6cedf8…5d68`，qcow2 零打开句柄。host 从 absent `.incoming` 原子发布 `RadishLex-L6-Repair-Preflight-80e49ce`；目录 `0700`，三文件 `0600`、single link，local/accepted/diagnostic evidence SHA-256 为 `aa10e919…69e8`/`180c19ad…0cd`/`39733904…4097`，不含 raw operation ID、raw receipt 或完整 dpkg log。

## 当前停止线与下一步（repair preflight 已冻结）

- repair clone 是下一次 package mutation 的唯一候选现场，但本批没有生成 operation ID 或执行 repair。下一步仍需独立授权：启动前确认十一台仅该 clone 可运行，启动后重新闭合 loopback-only/双路由为空及 pair/receipt/dependency/XDG/process mutation preflight，再生成新 operation ID并只调用一次 production repair。
- 第二次 repair、rollback operation、remove、reinstall、acceptance crash/retry、产品启动、P05C、清理、推送、tag/Release、公开发布与真实同步继续关闭。

## 2026-08-13：第六套单次 repair 在 staged preflight 失败关闭

- 从 clean `331b2c6`、十一台 VM 全停与 repair-preflight 冻结身份开始。repair clone 启动前 config/EFI/qcow2 仍为 `584bf2b5…f59b`/`a73a3266…9155`/`ee6cedf8…5d68`，qcow2 零打开句柄；preflight local/accepted/diagnostic evidence `aa10e919…69e8`/`180c19ad…0cd`/`39733904…4097` 未漂移。
- 只启动 UUID `A3022255-ED08-4C66-92D3-075A6BB93107`。第一条 guest 证据即证明 Debian 13 ARM64、仅 `lo`、IPv4/IPv6 路由为空；boot ID 只保存 SHA-256 `7030cd31…1caa`。其余十台保持 stopped。
- mutation preflight 逐文件回读通过：stage1/target exit 都为 0、stderr 空；pair/input、package `38-2`、receipt `ccbc4cd0…1e60`、operation 目录 2、guard absent、dpkg/audit/verify、20 项依赖、字体、manifest/双 FFI、startup 正负向、XDG `f3df287f…b86b`、WAL/SHM/profile、产品/dpkg 进程与映射均满足冻结值。此时仍未生成 operation ID或执行 CLI/dpkg。
- guest 内生成唯一 32 hex operation ID，host 只保存 SHA-256 `249b531d…84c7`。one-shot marker 后只调用一次 production maintenance `start --kind repair`，target 仅为冻结 package/evidence `b211d940…d09c`/`2a1132c6…0e1b`。CLI exit 0、stderr 空，stdout 为 `maintenance_outcome=aborted_preserved`；没有第二次 invocation、resume或其他 operation。
- 新 receipt SHA-256 `ba7a9637…f17c`、4749 bytes，语义为 `repair/same_release/aborted_preserved`、failure `version_relation_invalid` after `artifacts_staged`、manual recovery false；operation chain/目录均为 3，只 stage target，source/target artifact 都绑定现有 `38-2`，target/source proof 均 null。
- `/var/lib/dpkg/status` 继续为 `c09365b3…cece`；`/var/log/dpkg.log` 继续为 `0b963d92…c2da`/882804 bytes，与调用前逐字节一致，证明 coordinator 在 quiescence和package mutation前失败关闭。package仍为`38-2` installed，audit/verify clean；Manager/Fcitx startup均为`0:1:2:2:7`、negative component 9仍拒绝，XDG/网络/进程零漂移。
- 首次 postflight 脚本仍固定期待 completed receipt-state code 6，因实际 aborted-preserved code 7 在 manager-startup 项失败；独立回读证明 decision/reason仍为 AllowedProduct/ProductVerified。保留该失败证据后修正只读期望，最终 postflight exit 0、stderr 空，未掩盖或覆盖首次口径失败。
- production 根因定位到 `LinuxDpkgTransactionPort::validate_staged_operation`：repair 按 receipt 合同只物理 staging target，host prepare 已把 target 作为 effective source；system staged verifier却把 absent source直接传给`validate_operation_relation`，因此真实repair必然得到`version_relation_invalid`。修复提交 `b0197f5` 让 operation/staged validation共用repair effective-source投影，并新增single-target system-port回归。
- targeted test、`cargo test -p radishlex-linux-product-install` 116 tests和`cargo clippy -p radishlex-linux-product-install --all-targets -- -D warnings`均通过。此结果只证明源码修复，冻结 handoff 内 ARM64 maintenance ELF `b060c240…7d81` 仍是旧字节，不能热替换后复用失败clone。
- 正常关机后十一台 VM 全停，config/EFI/qcow2 为 `584bf2b5…f59b`/`3b117def…8f0`/`adcc92fd…3a4`，qcow2 零打开句柄。host evidence 冻结到 `RadishLex-L6-Repair-80e49ce-Aborted-Preserved`：目录 `0700`、4文件 `0600` single link；terminal JSON/final bundle SHA-256 `87190852…881f`/`2d300ae6…c8da`，不含 raw operation ID、raw receipt或完整dpkg log。

## 当前停止线与下一步（repair staged-preflight failure 已冻结）

- repair clone 是 `aborted_preserved` 失败现场，不 resume、重试、恢复、热替换或复用；第六套 terminal 原盘与 S3 继续只作真相源。十一台 VM 全停。
- 现有 release-pair v1 要求 executable commit等于target release commit，并由clean target同批构建package与双executable；它不能表示冻结`80e49ce` target artifact与`b0197f5`修复后ELF的组合。下一步先固定maintenance-only refresh合同，锚定旧record `cda70afa…659b`与既有installed target package/evidence `b211d940…d09c`/`2a1132c6…0e1b`，只允许较新production maintenance ELF进入独立handoff；不得改写旧pair或重建同版本package。
- handoff 通过后仍须另行授权从未改写 S3 创建独立 clone并完成只读 preflight；真实repair、rollback、remove、reinstall、acceptance crash/retry、产品启动、P05C、清理、推送、tag/Release、公开发布与真实同步继续关闭。

## 2026-08-13：收尾审阅与 2026-08-14 明日事项

- 收尾复核了当天六个既有提交：`e88b5d7`/`35e667f`/`c48a1da`/`331b2c6`依次冻结第六套只读preflight、upgrade终态、S3和repair preflight；`b0197f5`修复repair effective-source投影并补直接回归；`0a11bc6`冻结单次repair失败关闭现场。代码、receipt语义、真实证据与current/roadmap/technical plan/Linux boundary/runbook/目录说明/双README的描述已逐项对照。
- 审阅发现的唯一新缺口不是transaction实现，而是修复后执行载体合同：`scripts/linux-product/l6_release_pair.py`明确拒绝executable commit与target release commit不同；直接复用release-pair builder会要求重建target package，违背installed artifact chain anchor。今晚只修正文档顺位，不实现新合同、不启动VM、不生成handoff。
- `check-docs`、文本卫生、L6 contract/release-pair专项门禁和完整`check-repo`均通过；Linux transaction crate为116 tests。仅保留既有`docs/sync-server-api-storage.md` 508行预算warning，未扩大为本批修改。

### 明日事项（2026-08-14）

1. 先做只读基线复核：Git clean/HEAD、十一台VM全停、第六套S3和repair failure disk/evidence身份均未漂移；不启动guest。
2. 固定并实现maintenance-only refresh合同与verifier：至少绑定base release-pair record、既有target package/evidence、修复源码commit、production maintenance ELF identity/size/hash/mode/link和独立原子发布语义；拒绝改写旧record、重建或替换package、混入acceptance权限。
3. 为合同补正负向合成测试和文档门禁；先在宿主完成代码审阅、格式化、targeted test、clippy、L6 contract/release-pair与全仓验证。
4. 上述闭合后才申请隔离builder授权，离线构建并复验新的ARM64 production maintenance handoff；builder步骤不启动L6 transaction guest。
5. handoff冻结后再单独授权从未改写S3创建新clone和只读preflight。真实repair与其余matrix、P05C、发布、推送、清理、真实同步及其他平台继续关闭。

## 2026-08-15：闭合 maintenance-only refresh v1 合成合同

- 从 clean `54e227c` 开始，`dev` 与 `origin/dev` 一致，`b0197f5` 是 HEAD 祖先；其后 production 相关源码只有等价 Rust lint 调整，Linux product metadata 与 `80e49ce` 逐字段相同。本批未读取真实 handoff/S3、未调用 `utmctl`、未启动 VM，也未生成 ARM64 ELF 或 package。
- 新增 `packaging/linux/l6-maintenance-refresh.json`，精确锚定第六套 record `cda70afa…659b`、target package/evidence `b211d940…d09c`/`2a1132c6…0e1b`、target commit `80e49ce`、旧 production ELF `b060c240…7d81` 与 repair fix祖先 `b0197f5`。refresh root必须是包含修复的clean descendant且product metadata不得漂移；package rebuild固定为false，唯一输出身份是`production-maintenance`。
- 新增 `l6_maintenance_refresh.py` 及浅职责 environment/I/O helper。base record先按canonical JSON与完整SHA-256验证，再由既有release-pair verifier校验结构并从其中取得target size/dependency/manifest identity；package/evidence需absolute canonical、`0644`、single link且逐字节匹配record。新ELF需`0755`、single link、ELF64 AArch64、固定loader、不含acceptance markers且hash不同于旧ELF；evidence拒绝路径、operation/PID/proc/dpkg原文和额外字段。
- 新增 `build-linux-l6-maintenance-refresh.sh`：只在Debian 13 ARM64接受三份frozen input、clean refresh root与absent output；offline Cargo只构建production maintenance和临时actual-package verifier，不调用Manager/addon/rootfs/`.deb`构建。Python以`O_EXCL`、file/directory `fsync`形成私有staging，Rust verifier重验旧target actual package，完整inventory通过后同文件系统rename、父目录`fsync`并发布后再验；builder没有guest、dpkg、receipt、acceptance或运行时override入口。
- 新增独立仓库门禁并接入`check-repo.py` required paths/执行顺序。20项行为测试覆盖record/package/evidence/旧ELF篡改、acceptance、错误架构、本机路径、unknown evidence、metadata/clean/ancestry/build-output、stage mode/link、publication inventory/rehash/hardlink与内核级no-replace原子发布；6项源码合同测试固定anchor、repair ancestry、production-only build、actual verifier、构建顺序、exclusive I/O与禁止package/guest/runtime mutation。
- refresh专项、相邻L6 matrix/release-pair、文档/文本卫生、`git diff --check`与完整`./scripts/check-repo.sh`均通过；完整仓库基线覆盖Linux transaction 116 tests、controller feature下121 tests、acceptance 10 tests及workspace/Go门禁。只保留既有`docs/sync-server-api-storage.md` 508行预算warning，本批未扩大该文件。

## 当前停止线与下一步（refresh 合同已闭合）

- 当前只形成repository contract、builder与合成证据，不存在新的ARM64 handoff；不得把宿主测试写成修复后production载体。旧pair、target package/evidence、旧handoff、第六套terminal/S3与repair failure clone均保持原边界。
- 下一步须另行授权隔离Debian 13 ARM64 builder，以冻结三输入和包含`b0197f5`的clean descendant root形成absent-output refresh handoff；验证actual package、commit、ELF/mode/link/hash、canonical evidence与原子发布后立即停止，不启动transaction guest。
- handoff冻结后仍须另行授权从未改写S3创建全新clone与只读preflight。真实repair、rollback、remove、reinstall、acceptance crash/retry、产品启动、P05C、清理、推送、tag/Release、公开发布与真实同步继续关闭。

## 2026-08-15：形成 ARM64 maintenance-refresh handoff

- 从 clean `b891ed1`、十一台 VM 全停和既有第六套 frozen input开始。base record/target package/target evidence SHA-256 `cda70afa…659b`/`b211d940…d09c`/`2a1132c6…0e1b` 先在宿主复验；完整 `dev` Git bundle `e1805ade…a19` 与 target package分块逐哈希送入唯一启动的 `RadishLex-L6-PairBuilder-2fa1b8c-v2`。
- builder第一项有效 guest 动作关闭 `enp0s1`，其后只在 user/network namespace、loopback down、IPv4/IPv6 零路由和 `CARGO_NET_OFFLINE=true` 下构建 production maintenance与临时actual-package verifier。Debian 13.6 ARM64、Cargo/rustc 1.85.0和Git 2.47.3身份已记录；RadishLex package与`/var/lib/radishlex`在前后均 absent。
- 实际 build只执行一次。refresh record/新 maintenance ELF/build environment SHA-256 为 `4b41d1c0d998cd2a9349904ef52c768848403960cc4bacddb9a42f1f5f5fb2cd`/`9a657510737aa4df1e30850b33c8575615c6fb1770282af6fe720f5266554585`/`adcc6e30de383dcd58c8b104c36abee347ae07904615250fbee5b6f703d0c863`；ELF 为 1,787,712-byte AArch64、loader `/lib/ld-linux-aarch64.so.1`。target package/evidence继续精确等于 `b211d940…d09c`/`2a1132c6…0e1b`，旧pair/package、Manager/addon/rootfs和acceptance executable均未构建或改写。
- canonical USTAR `33a02bfb229a8ceb5117fb39ae4674f54f926ddbe3b3db07557d5e8f1aaaa2e2` 分 6 块回读宿主并逐块复验；临时目录和正式目录均通过 `l6_maintenance_refresh.py verify`，随后从 absent incoming同父目录原子发布 `RadishLex-L6-Maintenance-Refresh-b891ed1/maintenance-refresh-b891ed1`。10-file local evidence `a495e6a234f949226a95bc641d92be03f3e245b74df2361cd0d94308a6001cbb` 以`0600` single-link文件冻结。
- prepare runner首次在 clone前错误调用`git bundle verify`，postflight runner首次以root读取luobo-owned clean root；两者均失败关闭并保留，前者发生于clean root/output/build之前，后者只影响只读收尾。修正没有重跑实际 build，也没有触发 package/system mutation。
- builder postflight再次证明网络down/零路由、package/state absent、dpkg audit为空且无build进程，随后正常关机。最终 config/EFI/qcow2 SHA-256 为 `1f509c73…d4f`/`e00eff6e…fd2`/`d3a820d4…4792`，qcow2零句柄；十一台 VM 全停。没有启动 L6 transaction guest、创建 S3 clone、生成 operation ID、运行 maintenance/acceptance CLI、调用dpkg或写用户XDG。

## 当前停止线与下一步（refresh handoff 已冻结）

- `b891ed1` production-only refresh handoff已形成；旧release-pair、target package/evidence、第六套terminal/S3和repair failure clone保持原边界，不覆盖、不清理、不复用失败clone。
- 下一步须另行授权，仅从未改写S3创建全新clone、强制`Network=[]`并完成断网只读preflight；本步仍不得生成 operation ID、运行maintenance/acceptance CLI或调用dpkg。
- 真实repair仍需其后再次单步授权。其余matrix、P05C、发布、推送、tag/Release、清理、真实同步及其他平台继续关闭。
