# 2026-W27 开发日志

本文档记录 2026-W27 周内 RadishLex 的阶段性推进事实、验证证据和后续建议，读者是项目维护者和后续 AI / 人工协作者。本文不承载长期架构设计、接口字段完整参考或历史推演；长期边界应回写到对应专题文档。

## 本周目标

- 继续推进 Phase 3 自部署同步服务端前置治理。
- 在真实 HTTP handler 和远端上传下载前，补齐 Go server storage 层的签名验签门禁。
- 保持服务端默认不可信，只验证 metadata、设备状态、签名、版本冲突和密文完整性。

## 2026-06-29 推进记录

- 已在 `server/sync-server/internal/storage` 新增 Ed25519 签名验证抽象，使用与 Rust `radishlex-signature-v1` 对齐的 length-prefixed canonical field list。
- Go storage 写入前已验证 signed object manifest、device authorization、device revocation 和 recovery record，公钥来源为 `devices.signing_public_key`，并校验 `signature_schema_version`、`signature_algorithm`、`signature_key_id`、签名设备状态和签名时间窗口。
- SQLite metadata schema 已补签名 schema、算法、key id、authorization `join_short_code` 和 recovery KDF 参数字段，用于覆盖签名 canonical bytes 所需 metadata。
- Storage conformance tests 已改为使用确定性 Ed25519 fixture，内存 store 与 SQLite store 共用同一套真实签名 happy path 和字段篡改失败测试。
- 新增篡改拒绝测试覆盖 object manifest、device authorization、device revocation 和 recovery record；签名失败返回 `invalid_signature`，不会提交 metadata 或 blob。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步从签名验证调整为 wrapped device key bytes 承载、recovery wrapped material 读取接口、metadata API 和错误语义验证。
- 已在 storage interface 中将设备授权写入收敛为 `DeviceAuthorizationUpload`，让 authorization metadata、wrapping metadata 和 encrypted wrapped key bytes 作为一个事务输入提交。
- 已为内存 store 和 SQLite store 补齐 wrapped key bytes blob 承载、`DeviceWrappedKey` 读取接口、长度 / `ciphertext_hash` 复验，以及 SQLite 缺失 wrapped key blob 时的 `storage_unavailable` 失败路径。
- 已扩展 blob ref 路径安全测试，覆盖 wrapping blob ref 不泄漏原始 ID / hash 分隔符。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 recovery wrapped material 读取接口、metadata API 和错误语义验证。
- 已补 `LatestRecoveryWrappedMaterial` storage 接口，内存 store 与 SQLite store 可返回当前 active recovery metadata 和 encrypted wrapped material bytes，并复验 `wrapped_material_len` / `ciphertext_hash`。
- 已补 storage conformance happy path 和 SQLite 缺失 recovery wrapped material blob 的 `storage_unavailable` 测试。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 metadata API、统一错误响应、限速和日志脱敏验证。
- 已补 `internal/api.Handler` 的 recovery latest 路由，`GET /api/v1/domains/{domain_id}/recovery-records/latest` 复用 `LatestRecoveryWrappedMaterial`，返回 recovery metadata 与 encrypted wrapped material。
- 已补 API handler 测试，覆盖统一 JSON 错误响应、`not_found` 到 HTTP 404、恢复读取限速到 HTTP 429、限速请求不触发 storage 读取，以及响应不泄漏内部 `blob_ref`。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 domain / device / join request metadata API、审计日志和对象上传下载前错误语义验证。
- 已补 domain / device / join request 基础 metadata handler，覆盖 `POST /api/v1/domains`、`GET /api/v1/domains/{domain_id}/state`、`GET /api/v1/domains/{domain_id}/devices/{device_id}` 和 `POST /api/v1/domains/{domain_id}/join-requests`。
- 已补 API handler 闭环测试，使用内存 store 验证 domain 创建、domain state 读取、active device 读取、pending join request 创建、pending device 状态读取和非法 JSON 的 `invalid_request` 响应。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为审计日志、panic recovery、request id、join request 列表、authorization handler 和对象上传下载前错误语义验证。
- 已补 API handler 外层 request id、panic recovery 和非持久 `AuditSink` hook；handler 会透传 / 生成 `X-Request-ID`，panic 时返回结构化 `storage_unavailable`，并记录 route、domain、device、result code、HTTP status、byte count、server time 和 latency。
- 已补测试覆盖 request id 响应头、审计事件字段、审计事件不包含请求体公钥字段，以及 panic recovery 的结构化错误和审计记录。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 join request 列表、authorization handler、SQLite audit_events 写入和对象上传下载前错误语义验证。
- 已补 `PendingJoinRequests` storage 接口，内存 store 与 SQLite store 均只返回 pending join request，并按创建时间和 join id 稳定排序。
- 已补 `GET /api/v1/domains/{domain_id}/join-requests` 和 `POST /api/v1/domains/{domain_id}/join-requests/{join_request_id}/authorization`，authorization handler 将 signed authorization、wrapping metadata 和 encrypted wrapped key bytes 映射到 storage upload。
- 已补 storage conformance，覆盖授权前可列出 pending join request、授权后不再列出、设备激活和 wrapped key bytes 读取；API 测试覆盖 join request 列表响应和 authorization handler 到 storage upload 的路径字段绑定。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 SQLite audit_events 写入和对象上传下载前错误语义验证。
- 已补 storage `AuditEvent` 模型和 SQLite `RecordAuditEvent` 写入；handler 会在底层 store 支持时自动把非敏感 API 审计事件映射到 storage audit model。
- 已补 SQLite audit_events 行写入测试和 handler 自动调用持久审计 recorder 测试；持久审计只保存 event type、domain、device、object、version、result code、byte count 和 server time。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 encrypted object 上传下载和版本冲突 HTTP 语义。
- 已补 encrypted object version handler，接入 `POST /api/v1/domains/{domain_id}/objects/{object_id}/versions`、`GET /api/v1/domains/{domain_id}/objects/{object_id}/versions/{version}` 和 `GET /api/v1/domains/{domain_id}/objects/{object_id}/versions/{version}/payload`。
- 上传 handler 复用 `PutObjectVersion`，当前请求体使用 JSON metadata + `payload` byte 字段承载 encrypted bytes；metadata 响应不返回 payload 或内部 `blob_ref`，payload 读取接口返回 `application/octet-stream`。
- 已补 API handler 测试覆盖成功上传后读取 metadata / payload、payload length / ciphertext hash mismatch、stale base version 返回 409 和 latest metadata、同 version 同 hash 幂等、同 version 不同 hash 冲突、revoked / pending / unknown device 禁止上传、plaintext 字段拒绝，以及错误响应 / audit event 不包含 payload bytes 或请求体字段。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 server main 前运行时日志脱敏、部署入口或 Rust 远端客户端接线边界确认。
- 已补 `cmd/radishlex-sync-server` 启动入口和 `internal/runtime` 装配层，负责加载配置、打开 SQLite metadata store、应用嵌入 migration、创建 local blob store、注入 API handler、配置 HTTP timeout 和关闭 SQLite 连接。
- SQLite migration 已改为 idempotent，runtime 测试覆盖同一 metadata 文件重复打开，避免进程重启后因 `CREATE TABLE` 重复执行失败。
- API handler 已接入 `MaxObjectBytes`，对象上传在进入 storage 前按 `encrypted_payload_len` 和实际 `payload` bytes 返回 `payload_too_large`，避免过大对象继续进入签名验证和 blob transaction。
- 已补 runtime audit logger，只消费 handler 产生的非敏感 `AuditEvent`，测试覆盖日志不包含请求体公钥、encrypted payload bytes、base64 payload 或 signature。
- 已补 config 测试，覆盖默认值、环境变量 override 和非法数值拒绝。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为真实同步 runbook、server smoke、Docker Compose 边界或 Rust 远端客户端接线边界确认。
- 已补 `docs/runbooks/sync-server-local-smoke.md`，明确自动化 smoke、人工本机启动、允许 / 禁止日志字段和停止线；人工启动继续要求开发者明确执行，不由 AI 默认保留后台服务。
- 已补 `TestLocalServerSmokeUploadsReadsAndConflicts`，使用临时 SQLite、local blob store 和短生命周期 `httptest` server 走真实 HTTP 请求，覆盖 domain 创建、encrypted object 上传、metadata 读取、payload 下载、stale base version 409 和 runtime audit log 脱敏。
- 已同步 `docs/sync-server-api-storage.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 Docker Compose 边界或 Rust 远端客户端接线边界确认。
- 已补 Rust `ime-sync` remote client DTO / transport trait，上传入口以 `AssembledSyncObject` 和 `SignedSyncObjectManifest` 为输入，生成 Go server 兼容的 JSON metadata + base64 encrypted payload 请求。
- Remote client 已覆盖 `POST /api/v1/domains/{domain_id}/objects/{object_id}/versions`、`GET /api/v1/domains/{domain_id}/objects/{object_id}/versions/{version}` 和 binary payload 下载路径；不内置真实 HTTP transport，不启动 server。
- 已补 remote client 测试，覆盖上传请求不包含 plaintext / input code / reading / ranker detail 字段、manifest 与 encrypted object metadata mismatch 拒绝、stale base version latest metadata 映射、forbidden device 错误码映射、metadata 后二进制 payload 下载和 payload length mismatch 拒绝。
- 已同步 `docs/sync-server-api-storage.md`、`docs/sync-payload.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为真实 HTTP transport 与两客户端端到端同步边界，或 Docker Compose 边界确认。

## 2026-06-29 提交回顾

今日 `dev` 相对 `origin/dev` 新增 12 个提交，均围绕 Phase 3 自部署同步推进：

- `4de580a feat(server): verify sync metadata signatures`：补 Go storage 层 Ed25519 metadata 签名验证和篡改拒绝测试。
- `0b8bb23 feat(server): store device wrapped key blobs`：补设备授权 wrapped key bytes blob 承载和读取验证。
- `9879659 feat(server): read recovery wrapped material`：补 recovery wrapped material 读取接口和 hash / length 复验。
- `664324b feat(server): add recovery latest handler`：补 recovery latest HTTP handler、统一错误响应、限速和 `blob_ref` 不外泄。
- `dbae79b feat(server): add sync metadata handlers`：补 domain、device、join request metadata API。
- `a0fba91 feat(server): add handler request auditing`：补 request id、panic recovery 和非敏感 audit hook。
- `5f6918c feat(server): add join authorization handlers`：补 join request 列表和 authorization handler。
- `0d1dadc feat(server): persist audit events`：补 SQLite `audit_events` 持久化。
- `165d524 feat(server): add object version handlers`：补 encrypted object version 上传、metadata 读取、payload 下载和冲突语义。
- `8a3cf7c feat(server): add sync server runtime`：补 server main、runtime 装配、HTTP timeout、对象大小门禁和脱敏 audit logger。
- `d3d8b4d test(server): add local sync server smoke`：补本机 smoke runbook 和短生命周期 HTTP smoke。
- `6bdbc75 feat(sync): add remote object client boundary`：补 Rust `ime-sync` remote DTO / transport trait、错误映射和脱敏测试。

## 文档对齐检查

- `docs/sync-server-api-storage.md` 已覆盖 Go server API / storage / runtime / smoke、encrypted object handler、Rust remote DTO / transport trait、错误语义和停止线。
- `docs/sync-payload.md` 已覆盖 Rust 侧 remote client DTO / transport trait 当前边界，并明确不内置真实 HTTP transport、不提供 plaintext 上传入口。
- `docs/technical-plan.md` 已覆盖 Phase 3 当前状态、同步方向和停止线，下一步口径指向真实 HTTP transport 与两客户端端到端同步边界，或 Docker Compose 边界。
- `docs/roadmap.md` 已覆盖 Phase 3 当前完成证据和未完成项，Phase 3 退出标准仍要求两台客户端同步加密用户词库、服务端无法读取明文。
- `docs/repository-layout.md` 已覆盖 `crates/ime-sync`、`server/sync-server` 和 runbook 的当前职责。
- `docs/runbooks/sync-server-local-smoke.md` 已作为本机 smoke 入口保留，不要求 AI 默认启动长期运行 server。

## 验证记录

```text
go test ./...
cargo test -p radishlex-ime-sync
git diff --check
./scripts/check-repo.sh
```

结果：

- `server/sync-server/internal/api` 通过。
- `server/sync-server/internal/storage` 通过，覆盖内存 store 与 SQLite store 的签名验签 conformance。
- `crates/ime-sync` 通过，覆盖 remote client DTO、错误映射、payload 下载和脱敏边界。
- 仓库级文本卫生、文档预算、Go server 测试、Rust workspace 测试和 doc-tests 均通过。

## 风险与未完成项

- 当前仍不实现 Docker Compose、Rust 真实 HTTP transport、两客户端端到端同步、Flutter manager 或平台壳。
- 当前未启动长期运行 server 做真实联调，也未编写 Docker Compose 或部署配置。
- Device wrapping record 已保存 wrapped key metadata 与 encrypted wrapped key bytes；真实授权 handler 对外开放时仍必须只返回密文 bytes 和服务端可见 metadata。
- Recovery latest handler 已能返回 metadata 与 encrypted wrapped material bytes，并覆盖限速与内部 `blob_ref` 不外泄；API 外层已补 panic recovery、request id、非持久审计 hook 和 SQLite audit_events 写入；runtime audit logger、本机 runbook 和短生命周期 HTTP smoke 已覆盖请求体脱敏和对象上传下载复验。
- Go recovery record 现在持久化 KDF 参数以支持验签；后续 HTTP DTO 和 Rust 真实 HTTP transport 接线时必须继续保持这些字段只描述加密恢复材料，不包含恢复码明文或 KDF 输出。

## 明天事项（2026-06-30）

- 优先推进 Rust 真实 HTTP transport 边界：在 `ime-sync` 现有 `SyncRemoteTransport` 上增加可测试实现，继续只发送 encrypted payload 和 signed manifest，不引入 plaintext payload、P1 原始事件或 ranker 明细。
- 真实 HTTP transport 的第一组验证应使用短生命周期测试服务或等价测试 harness，覆盖成功上传 / metadata 读取 / binary payload 下载、`conflict_stale_base_version` latest metadata、`conflict_object_version`、`forbidden_device`、`storage_unavailable`、payload hash / length mismatch 和错误 / debug 脱敏。
- HTTP transport 通过后，再确认两客户端端到端同步的接线边界：本地 userdb P2 payload 加密上传、另一客户端下载解密、合并写回 SQLite、冲突后重新组装新版本；该阶段仍不接 Flutter manager、平台壳或长期运行服务。
- Docker Compose 可以后置到真实 HTTP transport 和端到端测试边界清楚之后；如果明天选择部署方向，应先写清环境变量、SQLite 路径、blob 路径、日志脱敏和本机启动停止线。

## 2026-06-30 推进记录

- 已在 `crates/ime-sync` 新增 std-only `http://` `HttpSyncRemoteTransport`，实现 `SyncRemoteTransport`，复用既有 `SyncRemoteRequest` / `SyncRemoteResponse` 边界传递 Go server object API 请求与响应。
- HTTP transport 当前支持不含凭据、query 和 fragment 的 `http://` base URL，支持可选 base path 拼接、短连接 HTTP/1.1、JSON request、binary payload response、`Content-Length` 和 chunked response；当前不引入第三方 HTTP 依赖，不支持 HTTPS / TLS。
- 组合测试已通过真实本地 TCP harness 复验 `SyncRemoteClient<HttpSyncRemoteTransport>` 的 encrypted object upload、metadata 读取、binary payload 下载、stale conflict latest metadata 映射、base path 拼接、chunked response 解码和 transport 错误脱敏。
- 沙盒内本地 TCP bind 会返回 permission denied；相关测试在无法绑定端口时自动跳过，以保证普通单测不因沙盒网络权限失败。已用真实环境提权复验同一 `cargo test -p radishlex-ime-sync`，确认本地 TCP harness 实际执行通过。
- 已同步 `docs/sync-payload.md`、`docs/sync-server-api-storage.md`、`docs/privacy-sync.md`、`docs/sync-key-management.md`、`docs/crypto-boundary.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/repository-layout.md` 和 `docs/production-recovery-flow.md`，将下一步调整为两客户端端到端同步边界，或 Docker Compose 边界确认。
- 已补 `crates/ime-userdb/tests/two_client_sync.rs`，使用合成 userdb、sync master key、test-memory signing key store 和内存 remote harness 复验两客户端 userdb 同步边界。
- 测试覆盖设备 A 导出 P2 payload 并加密上传 `dictionary.user_terms`、`ranker.weights` 和 `dictionary.deleted_terms`，设备 B 下载二进制密文后解密、解码并合并写回 SQLite。
- 测试覆盖本机 tombstone 阻断旧远端词条、ranker weight 写回、stale base version 409 latest metadata 映射，以及设备 B 基于最新 `base_version = 1` 重新组装并上传 v2。
- 已为 `crates/ime-userdb` 添加 `base64ct` dev-dependency，仅用于 integration test 中模拟 Go JSON byte 字段的 base64 payload。
- 已同步 `docs/sync-payload.md`、`docs/sync-server-api-storage.md`、`docs/privacy-sync.md`、`docs/sync-key-management.md`、`docs/crypto-boundary.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 Go server 两客户端真实 HTTP 联调边界，或 Docker Compose 边界确认。
- 已优化 `docs/sync-server-api-storage.md` 篇幅，将可见数据模型字段清单从多行 code block 压缩为紧凑字段行，文档从 532 行降至 402 行，消除仓库级文档预算 warning。
- 已扩展 `server/sync-server/internal/runtime` 的短生命周期 HTTP smoke：通过真实 HTTP API 创建第二设备 join request、提交 signed authorization、复验第二设备 active，然后让 device-a 上传 v1、device-b stale 上传被拒、device-b 基于 latest `base_version` 上传 v2 并读取对应 payload。
- 已同步 `docs/runbooks/sync-server-local-smoke.md`、`docs/sync-server-api-storage.md`、`docs/privacy-sync.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将下一步调整为 Rust HTTP transport 直连 Go server 的跨语言联调边界，或 Docker Compose 边界确认。
- 已补 `crates/ime-sync/tests/go_server_http.rs`，通过短生命周期 `go run ./cmd/radishlex-sync-server`、临时 SQLite metadata 和 local blob dir，验证 Rust `HttpSyncRemoteTransport` 直连 Go server。
- 跨语言测试覆盖 raw HTTP domain 初始化、Rust signed encrypted object 通过 Go object API 上传、metadata 读取、binary payload 下载、payload 仍为密文，以及 stale base version 409 latest metadata 映射。
- 跨语言测试暴露并修正对象 `ciphertext_hash` 口径不一致：Go storage 新增 `ObjectCiphertextHash`，按 Rust `ime-crypto` envelope 的 `radishlex-ciphertext-hash-v1` domain separator、公开 object AAD、AAD 长度、encrypted payload 长度和 encrypted payload bytes 复验；device wrapping 和 recovery wrapped material 继续使用裸密文 bytes 的 hash / length 校验。
- 已同步 Go storage / API / runtime 测试 helper，确保对象版本签名绑定新的 Rust envelope hash；签名篡改测试改为篡改 nonce，避免被 hash mismatch 提前拦截。
- 已同步 `docs/sync-server-api-storage.md`、`docs/privacy-sync.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将 Rust HTTP transport 直连 Go server 调整为已验证项，下一步转向 Docker Compose、本机部署 runbook 或更完整两客户端真实 HTTP 同步边界。
- 已补并迭代 `deploy/sync-server/docker-compose.local.yaml`、`deploy/sync-server/docker-compose.yaml`、`server/sync-server/Dockerfile` 和 `server/sync-server/.dockerignore`，提供 Go sync server Docker Compose 入口。
- 当前 Compose 已按兄弟 Radish 项目部署口径拆成本地容器验证和部署态两套入口：本地入口通过 Caddy internal TLS 暴露 `https://localhost:7319`，部署态入口只暴露 `sync-server` HTTP 上游 `http://127.0.0.1:7319`，两者使用同一个对外端口，外部反代负责生产 TLS。
- 已新增 `deploy/sync-server/.env.example`、`deploy/sync-server/caddy/local.Caddyfile` 和 `deploy/sync-server/nginx.prod.conf`；部署态 `.env.example` 参考 Radish 的 `.env.example -> .env` 模式，并将持久化目录放到 `RADISHLEX_SYNC_DATA_PATH`。本地 compose 不再使用单独 env 文件，显式指定 `docker-compose.local.yaml` 即进入本地 / 测试 HTTPS 入口。
- 本地 Compose 使用 named volume 保存 SQLite metadata、encrypted blob dir 和 Caddy internal CA / config；部署态 Compose 默认使用宿主机目录 `../../DeployData/SyncServer` 保存 SQLite metadata 和 encrypted blobs。两种入口都开启 read-only root filesystem、`/tmp` tmpfs 和 `no-new-privileges`。
- 已重写 `docs/runbooks/sync-server-compose.md`，说明本地 HTTPS、部署态 HTTP 上游、外部 Nginx TLS 终止示例、人工启动 / 停止、volume / 持久化目录清理、配置覆盖、允许 / 禁止日志字段和停止线；当前不把 Compose 视作完整生产部署或真实用户同步入口。
- 已同步 `docs/sync-server-api-storage.md`、`docs/privacy-sync.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/repository-layout.md` 和 `docs/runbooks/sync-server-local-smoke.md`，将 Docker Compose 本地 / 部署态入口调整为已落地项，下一步转向容器实际启动 smoke、外部 TLS / 认证 / 备份部署边界或更完整两客户端真实 HTTP 同步边界。
- 本轮尝试补齐 Docker Compose 容器实际启动 smoke。`docker compose version` 可用，`docker context ls` 显示当前 context 为 `desktop-linux`，但沙盒内和真实环境复验的 `docker version --format '{{.Server.Version}}'` 均无法连接当前 Docker Desktop socket，报错为 Docker API socket 不存在，判断为当前 Docker daemon 不可用。
- 因 Docker daemon 不可用，本轮没有执行 `docker compose up --build -d`、`curl` 到达性检查或容器内日志检查；不能记录本地 HTTPS / 部署态 HTTP 容器启动通过。未启动容器，因此无需 `down` 清理；未创建临时 `.env`、临时数据目录、真实数据、容器生成数据或新的对外端口。
- 已补充 `docs/runbooks/sync-server-compose.md` 的容器实际启动 smoke 步骤，固定本地 HTTPS `curl -k https://localhost:7319/api/v1/domains/smoke-compose/state`、部署态 HTTP `curl http://127.0.0.1:7319/api/v1/domains/smoke-compose/state`、预期结构化 `404 not_found` 响应、`down` / `down -v` 清理要求和 Docker daemon 不可用时的记录项。
- Docker App 启动后已复验 Docker Compose 容器实际启动 smoke。`docker version --format '{{.Server.Version}}'` 返回 `29.5.2`，本地 HTTPS 模式 `docker-compose.local.yaml` 成功 `up --build -d`，`curl -k https://localhost:7319/api/v1/domains/smoke-compose/state` 返回 `HTTP/2 404`、`via: 1.1 Caddy` 和 sync-server 结构化 `{"error_code":"not_found" ...}`，证明请求经 Caddy internal TLS 到达 sync-server。
- 本地模式日志记录 `route="domains.state"`、`domain_id="smoke-compose"`、`result_code="not_found"` 和 `status=404`，未出现 payload、signature、wrapped material 或明文输入数据。完成后已执行 `docker compose -f deploy/sync-server/docker-compose.local.yaml down -v`，容器、network 和本地 named volume 均已删除。
- 部署态 HTTP 模式使用仓库外临时 env 覆盖 `RADISHLEX_SYNC_DATA_PATH`，未创建仓库内 `.env` 或 `DeployData`。`docker-compose.yaml` 成功 `up --build -d`，`curl http://127.0.0.1:7319/api/v1/domains/smoke-compose/state` 返回 `HTTP/1.1 404 Not Found` 和 sync-server 结构化 `not_found` JSON，证明部署态 HTTP upstream 可直达 sync-server。
- 部署态模式日志同样只记录允许的 request metadata，完成后已执行 `docker compose -f deploy/sync-server/docker-compose.yaml --env-file <temporary-env> down`，随后删除临时 env 与临时数据目录。最终 `docker ps --filter name=radishlex-sync` 无运行容器，`docker compose -f deploy/sync-server/docker-compose.local.yaml ps` 为空。
- 已同步 `docs/technical-plan.md` 和 `docs/roadmap.md`，将 Docker Compose 容器实际启动 smoke 调整为已验证项；后续推进焦点转为外部 TLS / 认证 / 备份部署边界，或更完整两客户端真实 HTTP 同步边界。
- 已补 `crates/ime-userdb/tests/two_client_go_http_sync.rs`，通过短生命周期 Go sync server、临时 SQLite metadata、临时 blob dir、真实 `HttpSyncRemoteTransport` 和 test-memory signing key store，验证 Rust userdb 两客户端经 Go HTTP API 同步。
- 测试覆盖 device B join request / signed authorization、`dictionary.user_terms` / `ranker.weights` / `dictionary.deleted_terms` 三类 P2 对象上传下载、远端 payload 保持密文、客户端下载后解密 / 解码 / SQLite 写回、本机 tombstone 阻断、stale conflict latest metadata、按 `base_version = 1` 上传 v2、device A 下载 v2 写回和 runtime 日志脱敏。
- 测试构建 Go sync server 到仓库外临时目录并直接启动二进制，结束后 kill / wait server、删除临时 SQLite、blob dir 和二进制目录；loopback bind 或 Go 工具不可用时记录 skip，不伪造通过证据。
- 已为 `crates/ime-userdb` 添加 `sha2` dev-dependency，仅用于 integration test 中复现 Go wrapping / encrypted object hash 字段。
- 已同步 `docs/sync-payload.md`、`docs/sync-server-api-storage.md`、`docs/sync-key-management.md`、`docs/crypto-boundary.md`、`docs/technical-plan.md` 和 `docs/roadmap.md`，将两客户端真实 Go HTTP 同步调整为已验证项；后续推进焦点转为外部 TLS / 认证 / 备份部署边界，或真实平台私钥 backend 验证边界。
- 已补 `docs/runbooks/sync-server-production-deployment.md`，把部署拓扑、外部 TLS、认证 / 访问控制、数据目录权限、冷备份、恢复、升级回滚、验证证据和真实用户开放停止线从 Compose runbook 中拆成独立生产部署边界。
- 已同步 `docs/runbooks/sync-server-compose.md` 和 `deploy/sync-server/nginx.prod.conf`，明确 Compose 只负责容器入口，Nginx 示例只是 TLS 终止骨架；不新增第二个 env 文件，不新增第二个对外端口，部署态仍是 `docker-compose.yaml + .env.example` 的 HTTP upstream。
- 已同步 `docs/sync-server-api-storage.md`、`docs/privacy-sync.md`、`docs/technical-plan.md` 和 `docs/roadmap.md`，将生产部署边界调整为已固定项；生产访问控制、备份恢复演练、外部 TLS 验证和平台私钥 backend 仍是用户可用同步前的停止线。
- 已补 `docs/runbooks/apple-keychain-signing-backend.md`，选定 `apple-keychain-v1` 作为首个真实平台私钥 backend 验证对象，固定创建、加载、签名、删除 / 撤销、锁屏 / 权限、备份迁移、日志脱敏、gated smoke 和停止线。
- 已同步 `docs/adr/0003-device-signing-key-storage.md`、`docs/adr/0004-platform-private-key-storage-backend.md`、`docs/sync-key-management.md`、`docs/crypto-boundary.md`、`docs/privacy-sync.md`、`docs/production-recovery-flow.md`、`docs/technical-plan.md` 和 `docs/roadmap.md`，将 Apple Keychain 平台 runbook 调整为已固定项；默认 Rust workspace 仍不访问 Keychain，当时尚未接 macOS backend，也未运行真实 Keychain smoke。
- 已在 `ime-crypto` 增加 `apple-keychain` feature、`AppleKeychainDeviceKeyStore`、macOS Security / CoreFoundation FFI 接线和 ignored `apple_keychain_smoke`，覆盖 Keychain item 创建、加载、公钥读取、Ed25519 签名、删除 / revoke 后阻断和 fresh store not found 的可复验入口；默认测试和启用 feature 的普通测试均不运行 smoke，不触碰本机 Keychain。
- 已补 `UnsupportedSignatureAlgorithm` 错误语义和 `apple-keychain-v1` capability 默认测试，当前默认声明 `exportable = false`、`hardware_backed = false`、`user_presence_required = false`、`backup_migratable = false`；已同步 `docs/runbooks/apple-keychain-signing-backend.md`、`docs/adr/0004-platform-private-key-storage-backend.md`、`docs/sync-key-management.md`、`docs/crypto-boundary.md`、`docs/privacy-sync.md`、`docs/technical-plan.md` 和 `docs/roadmap.md`，明确真实 Keychain smoke 已运行但阻塞于 `ed25519-v1` 创建。
- 已为 `apple_keychain_smoke` 增加失败路径 cleanup guard；测试使用合成 `device_id` / `signing_key_id`，异常返回时会尝试删除同一合成 Keychain item，降低真实本机 smoke 失败后的残留风险。
- 已补 `docs/adr/0005-apple-platform-signing-strategy.md`，固定 Apple 平台签名策略：保留 `ed25519-v1` 协议，不把 Keychain seed 存储 fallback 混入 `apple-keychain-v1`，不为单个平台阻塞切换全链路签名算法。
- 已调整 `apple-keychain-v1` backend status，在真实 smoke 通过前明确阻断生产签名；保留 gated smoke 作为平台调查入口，普通 feature 测试只验证编译、capability metadata 和状态门禁，不创建 Keychain item。
- 已确认生产部署 runbook 此前只固定“生产必须认证 / 访问控制”的停止线，没有固定具体 Go server 内建认证方案；本轮将首个生产访问控制方案收敛为单用户自部署 bearer access token。
- 已在 `server/sync-server` 增加 `RADISHLEX_SYNC_ACCESS_TOKEN` 配置，非空时要求至少 32 bytes 且不含空白字符；API handler 在业务 storage 前校验 `Authorization: Bearer <token>`，失败返回结构化 `401 unauthenticated`，并保持响应、audit event 和 runtime log 不记录 token。
- 已让 runtime 装配透传 access token，部署态 Compose 继续使用唯一 `deploy/sync-server/.env.example`，只新增同一个 token 变量，不新增第二个 env 文件或第二个对外端口；部署态仍是 HTTP upstream，本地测试态仍由 Caddy internal TLS 负责 HTTPS。
- 已在 Rust `HttpSyncRemoteTransport` 增加可选 bearer token header，继续拒绝 URL credentials / query token / fragment token，并补 `unauthenticated` server error code 映射；Debug 不打印 token。
- 已同步 `docs/sync-server-api-storage.md`、`docs/runbooks/sync-server-production-deployment.md`、`docs/runbooks/sync-server-compose.md`、`docs/sync-payload.md`、`docs/privacy-sync.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将生产访问认证调整为已有 server / transport 实现证据；备份恢复演练、外部 TLS 真实验证和平台私钥 backend 仍是用户可用同步前停止线。
- 本轮未运行 Apple Keychain smoke，未触碰本机输入法、Keychain、证书、系统环境或长期运行服务；未提交真实 secret、本机绝对路径、容器生成数据或真实用户数据。

## 2026-06-30 验证记录

```text
gofmt -w server/sync-server/internal/config/config.go server/sync-server/internal/config/config_test.go server/sync-server/internal/api/handler.go server/sync-server/internal/api/handler_test.go server/sync-server/internal/runtime/runtime.go server/sync-server/internal/runtime/runtime_test.go
cargo fmt
cargo fmt --check
cargo test -p radishlex-ime-sync
cargo test -p radishlex-ime-userdb
cargo test -p radishlex-ime-userdb --test two_client_go_http_sync -- --nocapture
go test ./internal/runtime -run TestLocalServerSmokeUploadsReadsAndConflicts -count=1
go test ./...
cargo test -p radishlex-ime-sync --test go_server_http -- --nocapture
docker compose -f deploy/sync-server/docker-compose.local.yaml config
docker compose -f deploy/sync-server/docker-compose.yaml --env-file deploy/sync-server/.env.example config
docker compose version
docker context ls
docker version --format '{{.Server.Version}}'
docker compose -f deploy/sync-server/docker-compose.local.yaml up --build -d
curl -k -i https://localhost:7319/api/v1/domains/smoke-compose/state
docker compose -f deploy/sync-server/docker-compose.local.yaml ps
docker compose -f deploy/sync-server/docker-compose.local.yaml logs --no-color --tail=80 sync-server sync-gateway
docker compose -f deploy/sync-server/docker-compose.local.yaml down -v
docker compose -f deploy/sync-server/docker-compose.yaml --env-file <temporary-env> up --build -d
curl -i http://127.0.0.1:7319/api/v1/domains/smoke-compose/state
docker compose -f deploy/sync-server/docker-compose.yaml --env-file <temporary-env> ps
docker compose -f deploy/sync-server/docker-compose.yaml --env-file <temporary-env> logs --no-color --tail=80 sync-server
docker compose -f deploy/sync-server/docker-compose.yaml --env-file <temporary-env> down
docker ps --filter name=radishlex-sync --format '{{.Names}} {{.Status}} {{.Ports}}'
docker compose -f deploy/sync-server/docker-compose.yaml --env-file deploy/sync-server/.env.example config
docker compose -f deploy/sync-server/docker-compose.local.yaml config
git diff --check
cargo test -p radishlex-ime-crypto
cargo test -p radishlex-ime-crypto --features apple-keychain
cargo test -p radishlex-ime-crypto --features apple-keychain --test apple_keychain_smoke -- --ignored --nocapture
./scripts/check-repo.sh
```

结果：

- `cargo test -p radishlex-ime-sync` 通过 41 个单元测试、`go_server_http` integration test 和 doc-tests；新增 integration test 实际启动 Go sync server 并完成跨语言 upload / metadata / payload / stale conflict。
- `cargo test -p radishlex-ime-userdb` 通过 30 个库内测试、`p2_payload_crypto` integration test、`two_client_sync` integration test 和 `two_client_go_http_sync` integration test。
- `cargo test -p radishlex-ime-userdb --test two_client_go_http_sync -- --nocapture` 在真实环境通过，实际构建并启动短生命周期 Go sync server，完成两客户端授权、三类 P2 对象真实 HTTP 上传下载、解密写回、stale conflict 和 v2 重新上传；测试结束已停止 server 并清理临时目录。沙盒不允许 loopback bind 或缺少 Go 工具时，该测试会明确 skip。
- `go test ./internal/runtime -run TestLocalServerSmokeUploadsReadsAndConflicts -count=1` 通过，覆盖 Go runtime 双设备 HTTP smoke。
- `go test ./...` 通过 Go sync server 所有 package 测试。
- `docker compose -f deploy/sync-server/docker-compose.local.yaml config` 通过，确认本地入口解析为 `https://localhost:7319` / `127.0.0.1:7319`。
- `docker compose -f deploy/sync-server/docker-compose.yaml --env-file deploy/sync-server/.env.example config` 通过，确认部署态入口解析为 `http://127.0.0.1:7319` / `127.0.0.1:7319`。
- `docker compose version` 返回 Docker Compose v5.1.3；`docker context ls` 返回 `desktop-linux` 当前 context，endpoint 为 Docker Desktop socket。
- `docker version --format '{{.Server.Version}}'` 在沙盒内和真实环境复验均失败，错误为当前 Docker Desktop socket 不存在；本轮 Docker Compose build/up 和 `curl` 到达性 smoke 阻塞，不能作为容器启动通过证据。
- Docker App 启动后，真实环境 `docker version --format '{{.Server.Version}}'` 返回 `29.5.2`。
- 本地 HTTPS compose `up --build -d` 通过，`sync-server` 与 `sync-gateway` 均为 `Up`，端口映射为 `127.0.0.1:7319->7319/tcp`；真实环境 `curl -k -i https://localhost:7319/api/v1/domains/smoke-compose/state` 返回 `HTTP/2 404`、`via: 1.1 Caddy` 和 sync-server 结构化 `not_found` JSON。
- 本地 HTTPS 模式已执行 `down -v`，删除 `radishlex-sync-local` 容器、network 和 `sync-server-data` / `sync-gateway-data` / `sync-gateway-config` named volume。
- 部署态 HTTP compose 使用仓库外临时 env 通过 `up --build -d`，`sync-server` 为 `Up`，端口映射为 `127.0.0.1:7319->7319/tcp`；真实环境 `curl -i http://127.0.0.1:7319/api/v1/domains/smoke-compose/state` 返回 `HTTP/1.1 404 Not Found` 和 sync-server 结构化 `not_found` JSON。
- 部署态 HTTP 模式已执行 `down`，临时 env 与临时数据目录已删除；`docker ps --filter name=radishlex-sync` 无运行容器，本地 compose `ps` 为空。
- 生产部署边界补文档后，`docker compose -f deploy/sync-server/docker-compose.yaml --env-file deploy/sync-server/.env.example config` 和 `docker compose -f deploy/sync-server/docker-compose.local.yaml config` 通过，确认仍只有部署态 HTTP upstream、本地 HTTPS 入口和同一个外部端口 `7319`。
- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `cargo test -p radishlex-ime-crypto` 通过 34 个单元测试、0 个默认 Apple Keychain smoke；默认 workspace 未访问系统 Keychain。
- Apple 策略调整前，`cargo test -p radishlex-ime-crypto --features apple-keychain` 通过 34 个单元测试，`apple_keychain_smoke` 保持 ignored；该命令只验证 feature 编译和普通测试，不创建 Keychain item。
- `cargo test -p radishlex-ime-crypto --features apple-keychain --test apple_keychain_smoke -- --ignored --nocapture` 已在沙盒和提权真实环境各运行一次，均在 Keychain Ed25519 key 创建阶段失败，错误为 `UnsupportedSignatureAlgorithm { algorithm: "ed25519-v1" }`；测试未进入签名成功、同步上传或用户可用路径。本轮未修改本机输入法配置、未安装证书、未启动长期服务，也未提交本机绝对路径或真实用户数据。
- 失败后尝试用 `security find-key` 做非敏感只读确认，但命令返回参数无效，不能作为残留结论；当前可信证据是 smoke 未完成 key 创建成功路径，且测试侧已补同一合成 item 的 cleanup guard。
- Apple 签名策略调整后，`cargo fmt --check` 通过；`cargo test -p radishlex-ime-crypto --features apple-keychain` 通过 35 个单元测试，并显示 `apple_keychain_smoke` 为 ignored。新增断言覆盖 `apple-keychain-v1` status 在当前策略下返回 unavailable，`ensure_production_signing_allowed()` 必须失败。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- `./scripts/check-repo.sh` 通过，包含文本卫生、文档预算、Go server 测试、Rust workspace 测试和 doc-tests。
- 本轮 access token 实现后，`go test ./...` 在 `server/sync-server` 通过，覆盖配置解析、API bearer 门禁、runtime 配置透传、认证失败不进入业务 storage 和日志脱敏。
- 本轮 access token 实现后，`cargo test -p radishlex-ime-sync` 通过 43 个单元测试、`go_server_http` integration test 和 doc-tests，覆盖可选 bearer header、token Debug 脱敏和 `unauthenticated` 错误映射。
- 本轮修改 Compose 后，`docker compose -f deploy/sync-server/docker-compose.local.yaml config` 与 `docker compose -f deploy/sync-server/docker-compose.yaml --env-file deploy/sync-server/.env.example config` 均通过，确认未新增第二个 env 文件、未新增第二个对外端口，部署态仍是 HTTP upstream。
- 本轮未运行 Docker `up` / `curl` 容器启动 smoke，未启动长期服务；未运行 Apple Keychain smoke，未触碰本机 Keychain、证书、输入法或系统环境。
- 本轮 `git diff --check` 通过，`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。

## 下一步建议（2026-07-01）

- 优先推进备份恢复演练或外部 TLS 真实验证证据；生产部署 runbook 已固定边界，单用户 bearer access token 已有 server / transport 实现证据，但真实 token 部署复验、备份恢复演练和外部 TLS 验证仍未形成运行证据。
- Apple 平台若继续推进，应单独做原生非导出 Ed25519 支持矩阵 spike；如果要引入 Keychain seed 存储 fallback 或 P-256 等新算法，必须先新增独立 backend / signature algorithm ADR，再改 Rust / Go 协议实现。
- 真实 Go HTTP 两客户端同步、生产部署边界、单用户 bearer access token、Apple Keychain 平台 runbook、Apple 签名策略 ADR 和 feature-gated macOS backend 编译验证已具备证据，但真实 Keychain smoke 未通过，真实 token 部署复验、备份恢复演练和外部 TLS 验证也未完成；这些停止线解除前，仍不得接 Flutter manager、平台壳或用户可用同步 UI，不启动长期运行服务，不放宽平台私钥 backend 停止线。
