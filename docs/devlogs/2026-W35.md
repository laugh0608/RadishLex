# 2026-W35 开发周志

本文记录 2026-W35 的阶段推进、真实验证证据和停止线，供维护者复核 M5-P05B 决策。本文不替代当前状态、Linux 安装维护边界或 L6 runbook，不保存 raw operation ID、用户输入、密钥或可直接执行系统 mutation 的授权。

## 2026-08-24：v4 `install_artifacts_staged` exact resume v1 repository-only控制闭合

- 本批从clean `dev`的`f95865a`开始，只修改和验证仓库；没有调用真实`utmctl`、进入guest、读取raw operation ID、运行maintenance/dpkg、执行resume、启停VM或触碰冻结guest/host证据。用户此前提出的候选词中英释义点子只保留在未来规划语义，不进入本批，也未改变当前M5-P05B顺位。
- 新binding逐项固定checkpoint manifest `3aca0576…0a4f7`的49个成员、原checkpoint clean `7bf6e04`、request/terminal/guest terminal与12类双回读artifact，并递归绑定network `40be3f3f…38383`、transfer `d1090e8d…dc09`、resolution `7bab8f20…4e4a`、negative preflight `aba59811…0d7a`、canonical source 92,825,600 bytes/`7bbeb291…403c`、attempt链和v4 target。最终clean committed HEAD的只读复验确认49项、operation ID hash `21041a89…111a`、checkpoint `9cb4acb8…25e0`、crash-state `55f70579…af1a`、receipt `c759b5c6…5d34`、同一boot/guest/preflight/dpkg身份全部吻合。
- host one-shot要求exact resume、既有secret只传hash、一次resume+一次postflight、禁止retry/cleanup/stop/quit/next checkpoint四项独立授权。通过clean committed HEAD、source/target/process/QEMU句柄、冻结network/checkpoint live artifact双回读后，才可建立固定create-new guest根，将单一driver从incoming归一化为root-owned `0600`并逐字回读，然后只调用一次；marker、terminal、phase及成功artifact均双回读并与guest terminal hash/size互证。host所有证据扫描raw 32-hex token，始终不接收operation ID正文。
- guest driver把attempt和control root固定为`d75818f-v4-install-artifacts-staged-resume-20260824-v1`，未验证目录不写terminal。它只在guest内读取32-byte secret并核对hash，复验receipt、operation chain、唯一target staging、合法未锁guard、package absent、dpkg status/config/log/audit/updates、startup input/FFI、`ActiveGuard`双startup、XDG、product/dpkg进程与映射、仅`lo`和双main route为空。通过后从冻结case派生只暴露`resume`与`postflight`的terminal dispatch，最多各调用一次；成功再验证maintenance输出、resume result、terminal postflight、receipt/dpkg delta/log身份。
- 失败分类固定为resume前`resume-rejected/artifacts-staged-preserved`、resume开始后`state-indeterminate`，均无自动retry/cleanup/stop/quit。7项host与9项driver测试、默认L6合同、package transaction、startup gate、L6 controller、release pair、maintenance refresh、文档/文本和repository门禁用于验证仓库合同；它们不代表真实resume或完整case已通过。

## 真实调用前停止线

1. 保持checkpoint及全部前序manifest冻结；真实授权前仍从clean committed HEAD重新执行同一只读binding，不访问guest、查询或操作VM。
2. 若只读绑定仍一致，真实调用也必须另行精确授权，且只允许attempt `d75818f-v4-install-artifacts-staged-resume-20260824-v1`、UUID `50B75F88…8038`和absent host根`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-v4-Exact-Resume-v1`。任一前置漂移不resume；resume开始后的缺证不重试。
3. 真实exact resume不包含受控停止、cleanup、quit、next checkpoint或下一case。只有resume终态独立闭合后，才为停止与磁盘冻结另行请求授权；其余crash、连续L6、P05C、发布、推送和其他平台继续关闭。

## 2026-08-24：v4 exact resume在host handle门失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-resume-20260824-v1`、absent host根`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-v4-Exact-Resume-v1`及guest内部至多一次resume和一次postflight；不含retry、cleanup、stop、quit、next checkpoint或其他VM动作。调用前HEAD `7309313` clean、49项checkpoint binding通过且新根absent。
- committed control先闭合binding、source、target文件和host process门；process观察exit 0且相关角色为0。随后固定target EFI/qcow2的`lsof`观察exit 1，`parse_target_handles`在任何guest observation/write之前拒绝，terminal为`precondition-rejected`、reason `target-handles-preflight-exit-1`、transaction `artifacts-staged-preserved`。
- terminal固定file pull/push、guest exec、maintenance resume和postflight全部为0；plain list/status/start、automatic retry/cleanup/stop/quit/next checkpoint均未执行。没有进入guest、读取raw operation ID、运行maintenance/dpkg或形成新的guest evidence，故不能把case记为resume，也不能推断package或receipt发生变化。
- create-new host根包含7个manifest成员，SHA-256 `a436677c28a65e6abb995f0d038086e09ccec6edb3a0c5f510d5ff192638adc7`逐项复核通过；根/8文件为`0700`/`0600`、single-link、零xattr，raw 32-hex operation ID token为0。该根与原checkpoint现场现冻结，attempt和输出根均已消费，不覆盖、补写或重试。
- 相关进程为0且target handles缺失只证明host观察时backend资格不成立，不证明VM为stopped、saved或不可恢复。此前plain list/status后backend可能再次出现，因此本批不以“只读诊断”为名调用它们，也不擅自激活或停止VM。

## 2026-08-24：v4 backend状态消歧repository-only控制闭合

- 本批只修改并验证仓库，递归绑定exact resume失败manifest `a436677c…adc7`的7项固定成员、原clean `7309313`语义及checkpoint/source/target/attempt链；没有调用真实`utmctl`、查询VM、进入guest、执行resume/dpkg、启停backend或改写冻结证据。
- 新控制固定attempt `d75818f-v4-install-artifacts-staged-backend-resolution-20260824-v1`和未来create-new根`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-v4-Backend-Resolution-v1`。三个授权开关分别覆盖本case的backend消歧、唯一一次可能重新拉起backend的plain `status UUID`、以及禁止list/start/resume/guest/retry/stop/quit；不能沿用此前exact resume授权。
- `status`前须再次闭合clean committed binding、source/target、零相关进程和target句柄精确absent。调用后只做有界`ps`/target `lsof`：`started`加QEMU backend和EFI/qcow2双句柄才为`runtime-reactivated`；`stopped`加全窗口静止才为`registered-stopped`；`started`但无backend为`registered-started-runtime-unavailable`，明确不把它猜成saved。partial、漂移或status异常均`state-indeterminate`，且不自动resume或停止。
- 7项合成回归覆盖runtime延迟出现、registered stopped、started但backend缺失、status失败后置观察、调用前句柄拒绝、partial backend及授权/路径隔离；默认L6合同通过。所有测试均为repository-only，不构成真实backend状态或系统动作证据。

## 2026-08-24：v4 backend状态消歧闭合registered-stopped

- 用户新授权只覆盖commit `92e82e2`、固定UUID/attempt、absent `…-Backend-Resolution-v1`根及一次可能激活backend的plain `status`；不含list/start、guest、resume、retry、stop、quit或后续动作。调用前HEAD clean、全部冻结binding通过且输出根absent。
- control复验source/target、零相关进程和target句柄absent后只调用一次`utmctl status UUID`，exit 0、空stderr并返回`stopped`。随后不再调用UTM命令；10轮host process均为0，同轮target句柄均为absent，target身份前后不变，故terminal为`registered-stopped`。该结论只固定registered status与有界静止，不推断saved状态或下次start会恢复原boot。
- create-new根含30个manifest成员，SHA-256 `0193b4350fa97ba1d00f159fd2384e2cdbe4f790b61d6a8cd98f3740b80363e9`逐项复核通过；根/31文件为`0700`/`0600`、single-link、零xattr且raw ID token为0。file pull/push、guest exec、maintenance resume、plain list/start、retry/cleanup/stop/quit全部为0，transaction保持`artifacts-staged-preserved-no-resume`。
- attempt和输出根均已消费并冻结，不循环status或原地补证。当前backend不具备exact resume资格，旧resume attempt也已消费；下一批先repository-only绑定新证据并设计registered-stopped后的重新激活/boot分流控制，真实激活、全新resume与停止继续拆分授权。

## 2026-08-24：v4 reactivation与boot分流repository-only控制闭合

- 本批commit `924118c`只修改仓库，没有调用真实`utmctl`、查询/启动VM、进入guest、读取raw boot ID或operation ID、运行maintenance/dpkg、resume、stop、retry、cleanup或改写冻结证据。新binding逐项固定backend resolution manifest `0193b435…63e9`的30个成员与原clean `92e82e2`完整语义，递归重验全部上游，并绑定v7二十台全停基线、v4 prepared、UTM 4.7.5 build 118及foreground AppleScript transport。
- clean committed HEAD纯读取复核确认30项、`registered-stopped`、v7 baseline 20/expected 21和原boot hash `18f1ba06…022a`一致。该复核只读取Git、冻结证据、target文件和UTM应用定义，不调用UTM CLI或观察VM运行状态。
- 未来one-shot固定attempt `d75818f-v4-install-artifacts-staged-reactivation-20260824-v1`与absent根`…-v4-Reactivation-v1`。前置source/target、零相关进程和target句柄absent后只允许一次可能带起backend的plain `list`；清单须为v7二十台stopped加唯一v4。list若已使target started且QEMU/双句柄闭合则禁止重复start；target仍stopped时只有整个settle和ready门持续静止才允许一次foreground AppleScript start。任何其他VM active、partial backend、漂移或异常均失败关闭，不再status/list或自动retry/stop。
- host runtime闭合后，唯一guest动作是隔离Python校验canonical boot UUID并只输出SHA-256，不向host暴露raw boot ID；同冻结hash闭合`original-boot-restored`，不同canonical hash闭合`new-boot-started`。它发生在任何业务guest、secret、resume或dpkg动作之前；两种结果都不自动resume或停止。10项回归与L6/全仓门禁通过，完整门禁仅保留既有runbook和sync文档行数预算提醒。

## 2026-08-24：v4 reactivation真实调用失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-reactivation-20260824-v1`、absent `…-v4-Reactivation-v1`根、一次潜在副作用`list`、必要时一次foreground start和一次guest boot hash；不含第二次list、status、resume、业务guest、retry、stop、quit或cleanup。调用前HEAD `e6305e8` clean、全部冻结binding通过且新根absent。
- control复验source/target、零相关进程和target句柄absent；唯一`utmctl list`证明其余20台all-stopped且v4 canonical stopped。10轮settle与ready持续静止后只调用一次foreground AppleScript，exit 0、stdout/stderr空；随后没有再调用UTM CLI。
- 30轮target `lsof`均稳定返回一个`QEMULauncher`记录并精确持有EFI/qcow2各一次，payload hash一致；但同轮固定`ps ... ucomm=`解析的相关进程始终为0。启动返回码、target句柄和全局进程观察不能共同闭合bounded target runtime，故terminal失败关闭为`state-indeterminate/post-start-runtime:foreground-start-without-bounded-target-runtime`，guest boot hash调用0次。VM/UTM可能仍在运行，不能从本证据推断终态。
- create-new根含94个manifest成员，SHA-256 `2a477730bc3f10167add9afee125c7aed8e8dc1df3d4aad1c8a1953f131c5c4c`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr，raw operation ID token为0。attempt与根已消费并冻结；terminal后未补查、重试、进入guest或停止VM。

## 2026-08-24：v4 runtime resolution repository-only控制闭合

- clean `b056402`的新binding逐项固定reactivation manifest `2a477730…c5c4c`的94个成员、原clean `e6305e8` request/binding/source/target/list/settle/ready/foreground transport、30轮矛盾观察及terminal语义，并递归重验全部上游、v7/prepared、attempt链与原boot hash `18f1ba06…022a`。clean HEAD离线复核确认94项、旧`state-indeterminate`和原boot hash一致；该复核没有调用UTM CLI、观察进程/句柄、进入guest或操作VM。
- 新控制固定attempt `d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v1`。它先重验source/target并排除活动`utmctl`，只调用一次可能激活backend的plain `list`；只有v4 canonical started且其他20台stopped才继续。随后从精确target EFI/qcow2发现唯一`QEMULauncher` PID，连续3次用`lsof -a -p PID`绑定同一PID、command和双文件，每次再排除活动`utmctl`，并复验live target descriptor。
- 全部身份稳定后才允许一次guest boot hash，之后再做终态PID、进程和target文件复核；hash相同/不同分别闭合`original-boot-restored`/`new-boot-started`。任何list、成员、PID、command、handle、process、target或hash异常在list后均为`state-indeterminate`，不执行start/status/resume/业务guest/file transfer/retry/stop/quit，也不读取或生成operation ID。
- 11项合成回归覆盖原/新boot、target stopped、其他VM active、句柄缺失、PID漂移、错误command、活动utmctl、hash异常、终态漂移及授权/路径隔离，并进入默认L6门禁；全仓基线通过，只有既有runbook和sync文档行数预算提醒。本批未执行新的真实runtime resolution。

## 2026-08-24：v4 runtime resolution在boot hash格式门失败关闭

- 用户新授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v1`、absent `…-v4-Runtime-Resolution-v1`根、一次潜在副作用`list`、固定PID观察、一次guest boot hash及终态复核；不含start/status/resume、业务guest、file transfer、retry、stop或quit。调用前HEAD `ab151a9` clean、94项reactivation binding通过且新根absent。
- 唯一`utmctl list` exit 0、空stderr，证明v4 canonical started且其他20台stopped。target双句柄发现得到唯一`QEMULauncher` PID `61666`；连续3次`lsof -a -p 61666`的argv、PID、command、EFI/qcow2各一次、378-byte stdout hash `bdd4dcc3…52ec`完全一致，同轮host进程观察均无活动`utmctl`。target config/EFI/qcow2 descriptor在boot调用前稳定。
- 唯一guest boot hash调用已发生，但输出没有满足精确64个小写hex加换行的合同；control在保存成功boot observation/classification前以`guest-boot-id-hash-output-invalid`失败关闭。terminal为`state-indeterminate`、observed hash null、inventory 1、identity observation 3、guest hash 1；终态targeted PID、host process与target postflight未执行。invalid output不能推断原boot、新boot或guest状态，也不授权重跑。
- create-new根含17个manifest成员，SHA-256 `bc83a806e635cb21b945524a2733263fde55dc4c53849f3be614fc606816c766`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr，raw operation ID token为0。source postflight保持92,825,600 bytes/`7bbeb291…403c`；foreground start、plain status/start、resume、业务guest、file pull/push、retry/cleanup/stop/quit均为0。attempt与根已消费并冻结，terminal后未再查询或操作VM，VM仍可能运行。

## 2026-08-24：runtime resolution v2先落盘boot observation

- 从clean `32475d6`只修改repository。v2 binding固定旧runtime resolution manifest `bc83a806…c766`的17个成员、原clean `ab151a9`、request/binding/source/target/list、PID `61666`发现/3次确认、host process、invalid boot hash terminal与全部禁止动作，并递归重验reactivation 94项及更早上游；新attempt固定为`d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v2`。
- 控制在唯一boot hash命令返回后、调用严格parser前，先独立持久化`guest-boot-id-hash-observation.json`；文件只含argv、exit/timeout、stdout/stderr字节数、SHA-256与截断标志，明确`raw_output_persisted=false`。解析成功后才另写classification；格式错误仍保留脱敏observation、无classification且不重试。terminal分别记录observation/classification是否落盘。
- 12项回归覆盖原boot、新boot、stopped/其他VM active、句柄缺失、PID/command/utmctl/终态漂移、无效hash、授权/路径隔离及“parser执行前observation已存在”；默认L6合同通过。代码提交为`e21c518`。随后在clean HEAD使用现有Python 3.14对真实冻结根做纯离线binding，闭合17项、旧`state-indeterminate`、旧observation未落盘及原boot hash `18f1ba06…022a`；没有创建新证据根、调用`utmctl`、观察进程/句柄、进入guest或操作VM。

## 2026-08-24：v2确证guest exec成功空输出并失败关闭

- 新授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v2`、absent `…-v4-Runtime-Resolution-v2`根、一次潜在副作用`list`、固定PID/3次确认、一次guest boot hash及终态复核；不含start/status/resume、业务guest、file transfer、retry、stop或quit。调用前HEAD `61b24a9` clean，17项v1 binding、原boot hash、source三元组、target config与新根absence全部通过。
- 唯一`list`再次闭合v4 started且其他20台stopped；target发现及3次targeted观察均为同一`QEMULauncher` PID `61666`、EFI/qcow2各一次、378-byte stdout SHA-256 `bdd4dcc3…52ec`，四轮host相关进程数均0，target descriptor在boot前稳定。
- 唯一guest boot hash调用exit 0、未超时，stdout/stderr均为0 bytes与空串SHA-256。raw-free observation已在parser前落盘且`raw_output_persisted=false`；没有64-hex可解析，故无classification，并以`guest-boot-id-hash-output-invalid`闭合`state-indeterminate`。终态PID/process/target postflight未执行，空输出不能证明原boot、新boot或guest状态。
- create-new根含18个manifest成员，SHA-256 `e2e4123578bf51d28b4dad6257773fcb9cb2d7158c34ad9253ba55db4e4ec49e`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr且raw operation token为0。source postflight保持92,825,600 bytes/`7bbeb291…403c`；foreground start、plain status/start、resume、业务guest、file transfer、retry/cleanup/stop/quit均为0。attempt与根冻结，terminal后未再查询或操作VM，VM仍可能运行。

## 当前下一步

1. 保持v2 manifest `e2e41235…c49e`及全部前序现场冻结；不得把成功空输出改写为原boot、新boot或guest已进入，也不得沿用授权补查或重跑。
2. 下一批先repository-only绑定18项，并为exec成功空输出设计独立boot transport resolution；任何未来真实动作继续使用新attempt、absent根和精确授权。
3. 只有新控制闭合`original-boot-restored`才另行设计和授权exact resume；`new-boot-started`进入独立恢复决策，`state-indeterminate`不得继续。受控停止继续独立，其余crash、连续L6、P05C、发布、推送和其他平台继续关闭。

## 2026-08-24：日终提交与文档复核

- 今日从`847cb0e`推进至`c7dd6c8`，共13个提交、21个文件净变更、9,639行新增与15行删除。路线图提交`f95865a`只记录未排期的候选词双语释义构想；五个实现提交`7309313`、`92e82e2`、`924118c`、`b056402`、`e21c518`依次建立exact resume、backend resolution、reactivation、runtime resolution及parser前raw-free boot observation；其余七个文档提交逐次冻结真实失败关闭证据和当前顺位。
- 代码—文档交叉复核确认：新增实现全部位于既有`scripts/linux-product/`验收控制边界，对应回归均接入`check-linux-l6-contract.sh`，仓库总门禁继续调用该入口；L6 runbook已记录各控制合同、授权边界、真实结果、manifest和停止线，`current`保持唯一阶段入口。今日没有改变产品runtime、安装事务语义、FFI ABI、隐私/同步、依赖、平台顺序或目录职责，因此无需改写README、技术方案、仓库结构、Linux稳定Boundary、FFI或隐私专题。
- 今日真实链路从exact resume的target handle前置拒绝，经registered-stopped消歧与一次foreground reactivation，最终两次runtime resolution均在boot identity前失败关闭。v2进一步证明guest exec exit 0、未超时但stdout/stderr均为0 bytes，且raw-free observation先于严格parser持久化；这仍不能证明guest已执行命令或判断原/new boot，transaction保持`artifacts_staged`，VM可能运行，全部attempt和证据根冻结。

## 明日事项（2026-08-25）

1. 只在仓库内实现runtime resolution v3 binding，闭合v2的18项manifest并递归重验v1、reactivation与全部上游；加入成功空输出、observation/classification顺序及禁止重试的精确回归。
2. 设计并评审独立boot transport resolution：guest侧只向私有create-new证据写入boot ID SHA-256，host侧以独立双回读和严格parser交付分类，并在成功后复核同一PID/双句柄/target identity；不保存raw boot ID，不让stdout空值触发fallback或猜测。
3. 明日先完成实现、合成测试、默认L6与仓库门禁、文档提交，不调用`utmctl`、不进入guest、不查询、启动、恢复或停止VM。任何未来实机动作重新固定新attempt、absent host/guest根、动作次数和单独授权；exact resume与受控停止仍分别关闭。

## 2026-08-25：v3 binding与独立boot transport resolution闭合

- 本批从clean `b85c35c`开始，只修改和验证仓库。代码提交`dbce3a5`新增独立binding、host control与guest probe，并将8项host和3项probe回归接入默认L6门禁；没有修改production transaction、startup、FFI、隐私/同步、依赖或平台边界。
- v3 binding先复用既有v2 binding递归重验runtime resolution v1的17项、reactivation的94项与全部上游，再精确绑定v2 manifest `e2e41235…c49e`的18项。新语义明确要求原exec exit 0、未超时、stdout/stderr均0 bytes、`raw_output_persisted=false` observation先落盘、classification文件不存在、terminal保持`state-indeterminate`且retry/start/status/resume/stop/quit均未执行。
- guest probe只接受canonical attempt/target/probe identity与精确私有`0700`控制根，以`O_EXCL|O_NOFOLLOW`创建root-owned `0600` marker/result；它读取canonical boot ID后只持久化SHA-256，不保存或输出raw boot ID。第二次attempt不能覆盖既有文件，非法boot只保留marker且不产生result。
- host control在未来clean binding、source/target、一次可能激活backend的plain `list`、同一QEMULauncher PID三次双句柄确认后，才允许一次probe delivery/execution。probe经root-owned `0600`归一化和逐字回读，result必须两次独立pull完全一致、canonical且无未知字段；分类后再次复核同一PID、双句柄、无活动`utmctl`及target identity。任一缺失、漂移、格式或终态异常均失败关闭，不fallback、不自动retry，也不进入resume。
- 代码提交后在clean `dbce3a5`上直接调用binding读取真实冻结证据，返回`18 / exit 0 / stdout 0 / classification not-generated`；该调用没有创建host输出根、调用`utmctl`、观察live process/handle、进入guest或操作VM。`./scripts/check-linux-l6-contract.sh`、package transaction、startup gate、`./scripts/check-repo.sh`、文本检查与diff检查通过；全仓只保留既有L6 runbook和sync文档行数预算提醒。

## 后续停止线

1. v1/v2 runtime resolution attempt、manifest和全部上游继续冻结；repository-only binding不能表述为boot已识别或真实transport已通过。
2. 真实boot transport必须使用新attempt `d75818f-v4-install-artifacts-staged-boot-transport-20260825-v1`、absent host/guest根和新的逐项授权；不包含start/status/resume、业务guest、retry/stop/quit或cleanup。
3. 只有`original-boot-restored`及终态target稳定同时闭合后才另行设计exact resume；`new-boot-started`进入独立恢复决策，`state-indeterminate`不得继续。受控停止仍须单独授权。

## 2026-08-25：真实boot transport在stopped inventory失败关闭

- 授权前在最终clean `8f59825`复验v2冻结根及全部上游，binding返回18项、exec exit 0、stdout 0、classification not-generated；canonical source仍为92,825,600 bytes与SHA-256 `7bbeb291…403c`，新host根不存在。
- 唯一attempt `d75818f-v4-install-artifacts-staged-boot-transport-20260825-v1`只执行一次可能激活backend的plain `list`。调用exit 0且观察21台注册VM全部stopped，target `50B75F88…8038`未满足started门，因此以`target-not-registered-started-for-boot-transport`闭合`state-indeterminate`。
- 控制在inventory gate停止：PID发现/三次确认均未开始，guest exec/probe、file push/pull、result双回读均为0；没有生成boot classification，没有创建guest控制根，也未执行start/status/resume、业务guest、retry/stop/quit或cleanup。transaction仍为`artifacts-staged-preserved-no-resume`。
- 新host证据根`…-v4-Boot-Transport-v1`包含8个manifest成员，SHA-256 `6a1ad09f1db4b5e0b5d9edb9c4a9d1102fa8dfbf333a819f9961e759352e5140`逐项通过，权限为`0700`/`0600`、uid 501/gid 20且single-link。attempt与根冻结；单次registered stopped不证明boot、saved state或连续终态，不能补查、重试或直接resume。
- 下一步先repository-only评审保持stopped或授权一次新boot的恢复分流。任何新boot、后续transport、exact resume与受控停止分别使用新attempt、absent根和独立授权。

## 2026-08-25：boot start恢复分流repository-only闭合

- 恢复分流选择把一次启动与boot transport合并为单一失败关闭控制；保持stopped只冻结现场，不能识别boot或推进首个crash。新控制不修改production transaction、startup、FFI、隐私/同步、依赖或平台运行时。
- 代码提交`38b1bb8`新增boot start binding/control与7项回归。binding递归重验全部上游并精确绑定boot transport manifest `6a1ad09f…5140`的8项、原clean `8f59825`、全停inventory、probe/result均0及失败terminal。
- 控制在source/target、零process、target handles absent、唯一list证明21台全停和三轮静止后，才允许一次foreground start；启动后不再list/status，以最多60轮`lsof`发现唯一QEMULauncher PID并三次确认，再执行一次私有probe和result双回读。任何漂移失败关闭且不retry/stop/quit，不进入resume、业务guest或dpkg。
- clean `38b1bb8`对真实冻结链的纯离线binding返回8项、target/peers stopped、probe 0、result 0及原boot hash `18f1ba06…022a`；新host根`…-v4-Boot-Start-v1` absent。本批未调用`utmctl`、进入guest、启动或操作VM。
- 真实动作固定attempt `d75818f-v4-install-artifacts-staged-boot-start-20260825-v1`与新guest根`/var/tmp/radishlex-l6-v4-boot-start-…-v1`，但仓库闭合不构成授权。后续先单独授权一次list、三轮静止、一次foreground start、最多60轮runtime、PID三次确认、probe一次及result两次；original/new/indeterminate之后分别决策。

## 2026-08-25：真实boot start在guest agent门失败关闭

- 授权前clean `f972371`仍无工作区改动；纯离线binding逐项重验boot transport manifest `6a1ad09f…5140`的8项及全部上游，返回target/peers stopped、probe 0、result 0、原boot hash `18f1ba06…022a`和新host根absent。
- 唯一attempt `d75818f-v4-install-artifacts-staged-boot-start-20260825-v1`只调用一次plain `list`，证明target `50B75F88…8038`及其余20台registered stopped；三轮host process/target handles继续静止后，唯一foreground AppleScript start exit 0且双流为空。
- start后不再list/status。首轮runtime即发现PID `39591`的`QEMULauncher`与target EFI/qcow2双句柄；发现及三次定向确认均为378 bytes并绑定同一PID/command/双文件，同轮活动`utmctl`为0，target config hash `2de7280b…6195`在ready/started一致。
- 首次guest root创建是唯一guest exec：命令exit 0、未超时、stdout 0，但stderr 144 bytes为`OSStatus -2700`并报告QEMU guest agent未运行或不可用。控制以`guest-control-root-create-stderr-not-empty`闭合`state-indeterminate`；probe、file push/pull、result双回读均为0，observed boot hash为null且classification未生成。
- create-new host根`…-v4-Boot-Start-v1`包含29个manifest成员，SHA-256 `97959c55c05a3562b95412e57ec93249cf6df764700f3ebb49e484cfe3d6c57d`逐项通过；根/文件为`0700`/`0600`、single-link、零xattr。未执行resume、业务guest、dpkg、retry/cleanup/stop/quit，transaction仍为`artifacts-staged-preserved-no-resume`。
- attempt、host根及全部上游现冻结。该结果不证明boot身份、当前运行状态或guest agent永久缺失；不得沿用本授权补查、重跑list/status/start、复用guest根或直接resume。下一步只做repository-only guest-agent transport resolution设计，真实动作继续使用新attempt/absent根并另行授权。

## 2026-08-25：guest-agent transport resolution repository-only闭合

- 代码提交`c2d7113`新增独立binding/control与8项初始回归，并接入默认L6门禁。控制不调用list/status/start；先发现并三次确认唯一target QEMULauncher PID/双句柄，再以最多60次只读`test -r /proc/sys/kernel/random/boot_id`限定agent readiness，每次后复核同一PID和无活动`utmctl`。
- readiness只接受空输出成功或冻结现场逐字一致的144-byte `OSStatus -2700` agent unavailable；未知响应、预算耗尽、PID/target漂移均失败关闭。ready后才允许新私有guest根、唯一probe delivery/execution、marker与result双回读；original/new boot分流后仍复核PID/process/target，任何终态都不resume、业务guest、dpkg、retry/stop/quit。
- clean `c2d7113`首次真实冻结链纯离线binding在`host-process-preflight`暴露旧boot start process evidence实际复用runtime-v2格式；`78dcdb8`修正后第二次binding又发现共享handle helper只接受runtime-v1。两次均在新host根absence、零`utmctl`、零VM/guest动作下失败关闭。
- `5051234`改为本binding显式验证runtime-v2 handle语义并新增对应回归。9项专项测试、默认L6、package transaction、startup gate、文本门禁及全仓基线通过。
- clean `5051234`最终对真实manifest `97959c55…c57d`的29项和全部上游纯离线binding通过，返回PID `39591`、首次agent unavailable、probe 0、result 0及原boot hash `18f1ba06…022a`；计划host根`…-v4-Guest-Agent-Transport-v1`仍absent。本批未查询或操作VM。
- 未来真实动作固定attempt `d75818f-v4-install-artifacts-staged-guest-agent-20260825-v1`、全新host/guest根和新的逐项授权；范围不得包含list/status/start/resume、业务guest、retry/stop/quit或cleanup。original/new/indeterminate之后继续分别决策。

## 2026-08-25：guest-agent resolution在marker门失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-guest-agent-20260825-v1`、absent host/guest根、PID/双句柄三次确认、最多60次只读readiness，以及ready后一次私有probe与两次结果回读；明确禁止list/status/start/resume、业务guest、retry/stop/quit或cleanup。调用前HEAD `1d64916` clean，最终纯离线binding重验boot start 29项及全部上游，返回PID `39591`、原boot hash `18f1ba06…022a`和新host根absent。
- 真实控制没有调用list/status/start。全局target句柄发现与三次定向确认均固定同一`QEMULauncher` PID `39591`、EFI/qcow2各一次及378-byte observation，同轮无活动`utmctl`。首次readiness即exit 0、未超时且双流为空，随后同一PID/双句柄与host process复核仍通过。
- ready后创建唯一guest私有根，probe只push一次，经root-owned `0600`归一化并以7451 bytes、SHA-256 `27ea59b…b8ba`逐字回读。唯一probe调用同样exit 0、未超时且双流为空；但紧随其后的marker pull返回`OSStatus -2700`并明确`attempt.marker.json`不存在，故不能以exec返回码推断probe真正完成。
- control在`guest-marker-readback`闭合`state-indeterminate`：guest exec/push/pull/probe为4/1/2/1，result readback为0，observed boot hash为null，classification未生成；调用后的PID/process/target postflight未执行。未读取或生成operation ID，也未进入resume、业务guest、dpkg或任何自动补救。
- create-new host根`…-v4-Guest-Agent-Transport-v1`含26个manifest成员，SHA-256 `eb7c42f1b74701ce585188687b8cb18be6ecd2cddba8c4340fb09f775038d053`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr，raw operation ID扫描由控制闭合。attempt、host根与guest根均冻结，不补拉、不重跑probe或复用。
- 下一步只在仓库内绑定这26项，并先修正共享probe scope/root合同、补真实CLI组合回归；旧attempt和host/guest根均不复用。任何真实动作须新attempt与单独授权，当前不得resume。

## 2026-08-25：日终提交与文档交叉审阅

- 今日收尾提交前共有11个提交：3个实现提交`dbce3a5`/`38b1bb8`/`c2d7113`依次新增boot transport、boot start和guest-agent resolution，2个修复提交`78dcdb8`/`5051234`对齐真实boot start process/handle evidence，6个文档提交记录三轮repository closure与三次真实失败关闭。相对`b85c35c`共涉及15个文件、净增6837行、删除13行；新增源码均位于`scripts/linux-product/`且单文件未越过1500行，专项测试已接入`check-linux-l6-contract.sh`。
- 逐项对照控制器、共享probe、fake runner、真实26项terminal与三份活跃文档后，发现共享probe CLI默认只接受`…-boot-transport-<attempt>`根，而boot start/guest-agent分别传入`…-boot-start-<attempt>`与`…-guest-agent-<attempt>`。现有probe单测通过直接注入`expected_control_root`，host fake runner又只合成marker/result，因而没有执行真实CLI组合。精确合成guest-agent argv返回exit 12/`control-root-invalid`，且发生在marker写入前；这是本次marker absent的确定性仓库侧解释，但不能把不可信guest exec返回倒写成真实guest错误正文。
- 本问题只在L6验收控制与测试边界，不改变production transaction、startup、FFI ABI、Engine、隐私/同步、依赖、平台顺序或目录职责；因此README、技术方案、仓库结构、Linux稳定Boundary、FFI与隐私专题无需更新。需要变化的当前顺位、现场事实与明日动作已同步到`current`、L6 runbook和本周周志。
- 日终首次plain `utmctl list`显示21台中仅v4 `50B75F88…8038`为started。按用户授权只发送一次`utmctl stop UUID --request`正常关机，没有force/kill；终态list为21台全stopped，相关QEMU/`utmctl`进程为0，target EFI/qcow2零句柄。未进入guest、读取marker/result、resume、运行dpkg或清理任何冻结根。

## 明日事项（2026-08-26）

1. 只在仓库内新增guest-agent result binding，逐项固定manifest `eb7c42f1…d053`的26个成员、原clean `1d64916`、一次readiness、一次probe、marker absent、result 0、postflight未执行与全部禁止动作。
2. 修正共享probe的scope/root合同：让committed CLI对boot transport、boot start、guest-agent三类固定根均显式且可验证，拒绝任意调用方路径；新增真实CLI组合测试，不能再由`expected_control_root`注入或fake runner掩盖argv漂移。
3. 在21台全停且旧attempt/root冻结的前提下，repository-only设计新的启动与boot分类控制；不补拉旧guest根、不重跑旧probe、不resume。未来真实start、boot回读、exact resume分别使用新attempt、absent根和独立授权。

## 2026-08-26：probe scope、冻结结果与boot classification仓库闭合

- `862b003`把共享boot probe改为显式封闭scope；boot transport、boot start与guest-agent三类host生成的真实argv均通过committed parser和固定根派生回归，未知scope或scope/root错配在写marker前拒绝，测试不再以完整`expected_control_root`注入绕过CLI合同。
- `5b8d074`新增guest-agent result binding，精确固定manifest `eb7c42f1…d053`的26项、原clean `1d64916`、PID/handle/readiness/probe transport、marker absent、result readback 0、postflight未执行与全部禁止动作。clean committed HEAD对真实冻结链纯离线复核返回26项、`absent-after-probe-exec`和旧`state-indeterminate`；没有调用`utmctl`或创建新证据根。
- `65c7699`新增stopped-inventory boot classification binding/control：只有唯一list证明21台全停和三轮静止后才允许一次foreground start；启动后不再list/status，以有限runtime找到唯一QEMULauncher双句柄并三次确认，再进行最多60次只读agent readiness。ready后才执行一次固定`boot-start` scope probe、marker与result双回读；任一失败都不自动resume/retry/stop/quit，也不进入业务guest或dpkg。
- 首次clean-chain复核前发现新请求未显式携带冻结boot-start根、manifest和旧attempt id；`25e1608`补齐三项不可变输入、重叠/漂移拒绝及回归。最终clean `25e1608`递归绑定真实全部冻结根成功，返回guest-agent 26项、marker absent、旧`state-indeterminate`，计划输出根`…-v4-Boot-Classification-v1`仍absent。
- `./scripts/check-linux-l6-contract.sh`、package transaction、startup gate、文本检查与`./scripts/check-repo.sh`通过；全仓只保留既有L6 runbook和sync文档行数预算提醒。本批没有查询、启动或停止VM，没有进入guest、resume、运行dpkg或修改冻结证据，也没有改变production transaction、startup、FFI ABI、隐私/同步、依赖或平台边界。

## 当前下一步（2026-08-26）

1. 继续冻结旧boot-start/guest-agent attempt、host/guest根及manifest；仓库scope修复不能倒写成旧probe已在guest成功运行。
2. 若继续真实boot classification，必须另行授权固定新attempt、absent host根、一次全停inventory/foreground start、有限runtime/readiness、一次probe与双回读；本次仓库实现不构成该授权。
3. 只有稳定交付original/new boot分类并闭合终态identity后，才分别设计exact resume或新boot恢复；`state-indeterminate`立即停止，受控停止继续独立授权。

## 2026-08-26：真实boot classification闭合`new-boot-started`

- 用户授权只覆盖attempt `d75818f-v4-install-artifacts-staged-boot-classification-20260826-v1`、absent host根、一次全停inventory/foreground start、有限runtime/readiness、一次固定scope probe及marker/result双回读；不含resume、业务guest、dpkg、retry/cleanup/stop/quit。调用前HEAD `8a04d02` clean，纯离线preflight返回旧guest-agent 26项、旧`state-indeterminate`且新根absent。
- 唯一plain `list`证明21台registered VM全停，三轮静止后只启动target一次。PID `92422`与EFI/qcow2双句柄从runtime发现到terminal稳定，前9次readiness为固定agent unavailable，第10次ready；同轮相关host process为0。
- probe使用固定`boot-start` scope，唯一push/normalize/8404-byte readback、一次exec、marker回读及两次result回读全部通过。两份result observation SHA-256均为`b8a5cc65…1b5`，boot evidence `4f0bec04…9763`固定原boot `18f1ba06…022a`和新boot `b757c8fc…4758`，最终分类为`new-boot-started`。
- create-new host根`…-v4-Boot-Classification-v1`包含72个manifest成员，SHA-256 `a17920bd9a38c455f1151e5873f0b4261aa25237c64cf56cc8ef73e554830e17`逐项通过；terminal保持`artifacts_staged-preserved-no-resume`，自动resume/retry/cleanup/stop/quit与业务guest/dpkg均未执行。terminal后未再次查询或停止UTM，因此不把后续registered/running状态写成已知。

## 2026-08-26：72项结果binding在真实冻结链闭合

- `93f81d8`新增boot classification result binding与5项初始回归，精确固定72项名称/manifest、原clean request、全停inventory与单次start、9次unavailable+第10次ready、固定scope probe、marker/双result、新boot分类及禁止动作，并接入默认L6门禁。
- 第一次clean真实链调用在任何UTM动作前暴露binding上游少取一层；`f78a253`把链显式拆为`BootClassification -> BootTransport -> RuntimeResolution -> Reactivation`并增加拓扑回归。第二次调用发现PID discovery之前的首份global handle按原控制合同不含`backend_pid`；`3e31540`将其与后续PID-bound handle分开验证并补负向回归。
- 最终clean `3e31540`对真实72项及全部递归上游纯离线验证通过，返回`new-boot-started`、旧/new boot hash、PID `92422`与binding SHA-256 `0400774c…19e0`；没有调用`utmctl`、进入guest、创建新证据根或改写冻结材料。
- 审阅既有exact-resume driver确认它显式绑定原boot `18f1ba06…022a`并要求同boot遗留guard，本次新boot不能直接复用。下一批先只在仓库内设计新boot只读恢复资格预检，重验持久receipt/staging/checkpoint、current boot、package/dpkg、process/network与`/run/lock`跨重启语义；真实预检、canonical resume和受控停止分别另行授权。

## 2026-08-26：new-boot只读恢复资格控制仓库闭合

- 生产语义复核确认`artifacts_staged` receipt在guard缺失时由startup gate返回`MaintenanceRequired / OperationInProgress / state=2`，wire为`0:1:3:14:2|error-absent`；有效guard仍优先返回`ActiveGuard`，wire为`0:1:3:10:0|error-absent`。因此跨重启资格不能继续强制guard存在，也不能把guard缺失误判为事务消失。
- `4ba55cb`新增冻结结果binding、一次性host control与guest只读probe。probe从持久receipt重新绑定operation hash/chain与target-only staging，不依赖已随重启消失的`/run`测试secret；guard只分类为absent或root-owned `0600`/regular/single-link/zero-size且未锁，两类必须分别与Manager/Fcitx startup wire严格耦合。它不创建、删除或写产品guard，不调用旧resume driver的case派生、resume、postflight或结果验证路径。
- host控制不做inventory/list/status/start；未来只有固定同一QEMULauncher PID与EFI/qcow2双句柄、source/target、无活动`utmctl`和有界agent readiness全部稳定后，才向create-new私有guest根各交付一次冻结旧driver和只读probe。两文件均root-owned `0600`、逐字回读，probe至多执行一次，marker回读和result独立双回读后再做PID/process/target终态复核。任何未知输出、双读漂移或身份漂移失败关闭，不自动retry/resume/stop/quit，也不运行dpkg mutation。
- 新增6项host与5项guest测试并接入`./scripts/check-linux-l6-contract.sh`；专项与L6全门禁通过。clean `4ba55cb`随后递归重验真实boot classification 72项及全部上游，返回`new-boot-started`、旧/new boot `18f1ba06…022a`/`b757c8fc…4758`、PID `92422`、旧driver SHA-256 `0b649ecd…d05b`、probe SHA-256 `59cfa67a…3136`和binding evidence SHA-256 `18cc48a2…40d6`。
- 本批只修改仓库并读取冻结host evidence；没有调用`utmctl`、进入guest、创建真实预检根、查询/启动/停止VM、resume或运行dpkg，也未改写任何冻结证据。真实预检固定新attempt `d75818f-v4-install-artifacts-staged-new-boot-recovery-preflight-20260826-v1`，但代码闭合不构成系统授权；真实预检、后续resume与受控停止继续分别授权。

## 2026-08-26：真实恢复预检拒绝、取证绑定与合同修复

- 用户授权只覆盖UUID `50B75F88…8038`、固定新attempt/absent host与guest根、有限readiness、两份committed文件唯一交付、一次只读probe及marker/result/phase回读；不含inventory/list/status/start、resume、dpkg mutation、retry/cleanup/stop/quit。调用前HEAD `eba4bae` clean，递归binding重验boot classification 72项、PID `92422`与旧/new boot hash。
- 唯一真实控制一次readiness即ready，固定PID/EFI/qcow2双句柄并确认无活动`utmctl`；旧driver与probe各push/readback一次，probe唯一执行后marker与两份875-byte result及phase均稳定回读。guest terminal在`persistent-transaction`以`ResumeDriverError:directory-identity-invalid:state-root`返回`recovery-rejected`，guard未观察，maintenance resume与dpkg mutation均为0；exec transport却报告exit 0，旧host的exit比对把确定拒绝记为`state-indeterminate`并停在terminal identity postflight之前。
- host create-new根`…-v4-New-Boot-Recovery-Preflight-v1`含38项，manifest `1d593a8f7a9ce725320954257e763862703d43e89a3e82c7fa5cd94391f233c7`逐项通过；terminal计数为readiness/probe/result readback `1/1/2`、push/pull/guest exec `2/6/8`，未运行resume、dpkg、业务guest、inventory/list/status/start或自动补救。调用后没有查询、停止或清理UTM，VM状态未知，attempt及host/guest根冻结。
- 根因是production持久目录合同为state root与`operations/` root-owned `0755`、具体operation目录`0700`，旧probe却对三层都调用旧driver的`0700` helper；真实拒绝发生在receipt/staging与guard/live-system检查之前。`c523fb3`改为`0755/0755/0700`分层验证，不改冻结旧driver；host改以marker、双份相同terminal及phase为权威事实，只记录exec transport exit。
- 新增真实结果binding与5项测试，并补host transport-exit-0/rejected-result及guest目录mode回归，默认L6门禁通过。clean committed `c523fb3`随后对真实38项和全部递归上游纯离线验证通过，返回guest `recovery-rejected`、transport exit 0、旧host `state-indeterminate`与PID `92422`；没有调用`utmctl`或改写证据。第二次真实预检须新attempt、absent host/guest根及单独授权，resume和受控停止仍分别授权。

## 2026-08-26：日终提交审阅、文档校准与全停

- 今日13个提交从`862b003`至`4e3a9fd`，合计修改26个文件、增加6711行并删除28行：依次闭合共享boot probe scope、guest-agent 26项结果binding、全停启动/boot classification控制、72项实机结果binding、new-boot只读恢复资格控制，以及38项真实拒绝binding与两处合同修复。逐项对照committed control/probe/result binding、测试、默认L6入口和真实terminal后，未发现未记录的resume、dpkg、业务guest、retry/cleanup/stop/quit或隐私/FFI/依赖边界变化。
- 文档复核确认`current`、L6 runbook与周志已覆盖三次repository closure、两次真实guest链结果和冻结停止线；`docs/README.md`导航、技术方案、仓库结构、隐私/同步及FFI专题无需变化。稳定安装边界原先只写state root `0755`和operation私有目录`0700`，本次补出中间`operations/`固定`0755`；roadmap把已失效的“尚待exact resume”校准为“尚待新boot恢复资格与resume”。
- 收尾首次plain list只见v4 UUID `50B75F88…8038`为started、其余20台stopped；只对该VM发送一次正常`--request`关机。终态list为21台全stopped，宿主QEMULauncher/`utmctl`进程为0，target EFI/qcow2句柄为空；未force、kill、进入guest、读取冻结根、resume、运行dpkg或清理证据。
- 正常关机结束了`b757c8fc…4758`对应的live boot，故明日不能直接把`c523fb3`修复后的旧固定boot attempt重跑。下一步先repository-only设计“全停inventory→单次启动→fresh boot重新分类→只读恢复资格”的新attempt/absent-root链；门禁闭合后，真实启动/分类、资格预检、resume与受控停止仍分别授权。

## 2026-08-27：fresh-boot恢复资格两阶段控制仓库闭合

- `e9fdabc`新增5个control/binding模块、2个测试模块并扩展既有只读probe与host同PID门，共11个文件、增加2274行并删除21行。第一阶段递归消费昨日38项拒绝结果，以已结束boot `b757c8fc…4758`为基线，只允许全停inventory后的单次foreground start、有限runtime/readiness与fixed-scope boot分类；不复用旧attempt/root，也不进入恢复资格、resume或dpkg。
- 第二阶段使用动态但严格的结果binding：先逐文件验证第一阶段create-new evidence树、manifest顺序/hash、request/control/binding/probe身份、全停/单次start、动态poll/readiness、marker/双result、new boot和禁止动作，再要求资格控制发现的backend PID与分类terminal一致。旧/原boot、manifest漂移、PID变化或任何不确定状态均在guest mutation前失败关闭。
- guest只读probe保留2026-08-26固定attempt的精确合同，同时只为新fresh-boot attempt开放由host结果binding提供的canonical current boot；原transaction boot仍固定`18f1ba06…022a`，state root/operations/operation目录继续按`0755/0755/0700`验证。probe不调用resume、case派生、结果验证或dpkg mutation，guard absent/合法未锁仍与两类startup wire严格耦合。
- `./scripts/check-linux-l6-contract.sh`、`./scripts/check-linux-package-transaction.sh`、`./scripts/check-linux-startup-gate.sh`、`./scripts/check-text-files.sh`与完整`./scripts/check-repo.sh`通过；合成测试不能替代真实系统。该批未调用`utmctl`、进入guest、创建真实attempt/root、查询/启动/停止VM、resume、dpkg、retry或cleanup，也未改变production transaction、startup、FFI ABI、隐私/同步、依赖、平台或目录职责。
- 用户随后只授权第一阶段。clean `0be88ee`的唯一调用先离线重验38项旧拒绝结果与全部上游，新根absent；唯一plain `list`观察21台全停，三轮静止后一次foreground start。PID `42349`从首轮runtime、三次confirmation、10轮readiness至terminal保持EFI/qcow2双句柄；第10轮agent ready后只执行一次fixed-scope boot probe，marker与双result回读闭合。
- 新boot `d64b962e…b50`不同于已结束boot `b757c8fc…4758`，72项manifest `2211af7e…e60`闭合`new-boot-started`；双result SHA-256均为`34d1f032…c4e6`。未执行资格、resume、dpkg、业务guest、retry/cleanup/stop/quit，terminal后未再查询或停止UTM，transaction仍为`artifacts-staged-preserved-no-resume`。
- 实机记录提交前，clean `0be88ee`上的第二阶段纯离线binding已逐项验证72项动态manifest、新boot与同一PID `42349`，计划资格根仍absent；提交记录为`89639fd`后按新HEAD复核则在`prior-fresh-boot-classification-request-invalid`失败关闭。根因是新result binding把历史classification HEAD与当前资格控制clean HEAD合并比较；未checkout、回退、绕过或执行资格。
- clean `ef5d17f`分别验证当前上游`repository_clean`/HEAD和manifest内历史request HEAD，再以历史HEAD重建冻结request/binding，禁止两者交叉漂移；资格binding同时导出两项HEAD。successor HEAD通过、当前HEAD错误、历史交叉篡改与manifest漂移均有回归；fresh recovery 6项、classification 3项、旧recovery 7项、guest probe 7项、默认L6、package transaction/startup gate及完整`./scripts/check-repo.sh`通过。
- clean提交后的真实72项冻结链纯离线复核通过：历史HEAD `0be88ee`、当前HEAD `ef5d17f`、boot `d64b962e…b50`、PID `42349`，计划资格根仍absent。本批未调用UTM、进入guest、资格、resume、dpkg、retry/stop/cleanup；下一步只能另行授权一次只读资格，resume与停止继续分别授权。

## 2026-08-27：真实fresh-boot恢复资格预检失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、固定attempt `d75818f-v4-install-artifacts-staged-fresh-boot-recovery-preflight-20260827-v1`、absent host/guest根、同PID门、有限readiness、两份committed文件唯一交付、一次只读probe及marker/result回读；明确不含inventory/list/status/start、resume、dpkg mutation、retry/cleanup/stop/quit。调用前HEAD `5cb8160` clean，动态binding逐项验证fresh classification 72项、历史HEAD `0be88ee`、当前HEAD、boot `d64b962e…b50`与PID `42349`。
- 控制一次readiness即ready，三轮PID/双句柄确认和probe前复核通过；旧driver与只读probe各push、归一化及逐字回读一次，probe唯一调用exit 0且双流为空。marker回读存在且绑定attempt、driver/probe hash，但紧随其后的第一次result pull报告`OSStatus -2700`及guest `preflight.evidence.json`不存在；控制没有执行第二次result读回，按合同以`guest-recovery-result-readback-1-stderr-not-empty`闭合`state-indeterminate`，guest资格结果为`not-observed`。
- create-new host根`…-v4-Fresh-Boot-Recovery-Preflight-v1`含35个manifest成员，SHA-256 `39586a3bb662d9a28a7a2a6e5721b49c4b5395baf47f4ad03bef3da1a20e1bed`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link且零xattr。terminal记录probe/result readback为`1/1`，transaction仍是`artifacts_staged-preserved-no-resume`，未执行inventory/list/status/start、resume、dpkg mutation、retry/cleanup/stop/quit。
- 该现场不能区分guest probe尚未发布结果、结果发布较晚或已在其他阶段失败，也不能把transport exit 0解释成资格通过；attempt与host/guest根全部冻结，不补拉、不重跑。调用后没有追加UTM查询，实际VM/guest状态未知。后续先repository-only绑定35项并设计只消费冻结attempt的deferred-result resolution；任何真实补拉、fresh-boot resume及受控停止继续分别授权。
