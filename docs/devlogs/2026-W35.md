# 2026-W35 开发周志

本文记录 2026-W35 的阶段推进、真实验证证据和停止线，供维护者复核 M5-P05B 决策。本文不替代当前状态、Linux 安装维护边界或 L6 runbook，不保存 raw operation ID、用户输入、密钥或可直接执行系统 mutation 的授权。

## 2026-08-24：v4 `install_artifacts_staged` exact resume v1 repository-only控制闭合

- 本批从clean `dev`的`f95865a`开始，只修改和验证仓库；没有调用真实`utmctl`、进入guest、读取raw operation ID、运行maintenance/dpkg、执行resume、启停VM或触碰冻结guest/host证据。用户此前提出的候选词中英释义点子只保留在未来规划语义，不进入本批，也未改变当前M5-P05B顺位。
- 新binding逐项固定checkpoint manifest `3aca0576…0a4f7`的49个成员、原checkpoint clean `7bf6e04`、request/terminal/guest terminal与12类双回读artifact，并递归绑定network `40be3f3f…38383`、transfer `d1090e8d…dc09`、resolution `7bab8f20…4e4a`、negative preflight `aba59811…0d7a`、canonical source 92,825,600 bytes/`7bbeb291…403c`、attempt链和v4 target。最终clean committed HEAD的只读复验确认49项、operation ID hash `21041a89…111a`、checkpoint `9cb4acb8…25e0`、crash-state `55f70579…af1a`、receipt `c759b5c6…5d34`、同一boot/guest/preflight/dpkg身份全部吻合。
- host one-shot要求exact resume、既有secret只传hash、一次resume+一次postflight、禁止retry/cleanup/stop/quit/next checkpoint四项独立授权。通过clean committed HEAD、source/target/process/QEMU句柄、冻结network/checkpoint live artifact双回读后，才可建立固定create-new guest根，将单一driver从incoming归一化为root-owned `0600`并逐字回读，然后只调用一次；marker、terminal、phase及成功artifact均双回读并与guest terminal hash/size互证。host所有证据扫描raw 32-hex token，始终不接收operation ID正文。
- guest driver把attempt和control root固定为`d75818f-v4-install-artifacts-staged-resume-20260824-v1`，未验证目录不写terminal。它只在guest内读取32-byte secret并核对hash，复验receipt、operation chain、唯一target staging、合法未锁guard、package absent、dpkg status/config/log/audit/updates、startup input/FFI、`ActiveGuard`双startup、XDG、product/dpkg进程与映射、仅`lo`和双main route为空。通过后从冻结case派生只暴露`resume`与`postflight`的terminal dispatch，最多各调用一次；成功再验证maintenance输出、resume result、terminal postflight、receipt/dpkg delta/log身份。
- 失败分类固定为resume前`resume-rejected/artifacts-staged-preserved`、resume开始后`state-indeterminate`，均无自动retry/cleanup/stop/quit。7项host与9项driver测试、默认L6合同、package transaction、startup gate、L6 controller、release pair、maintenance refresh、文档/文本和repository门禁用于验证仓库合同；它们不代表真实resume或完整case已通过。

## 真实调用前停止线

1. 保持checkpoint及全部前序manifest冻结；真实授权前仍从clean committed HEAD重新执行同一只读binding，不访问guest、查询或操作VM。
2. 若只读绑定仍一致，真实调用也必须另行精确授权，且只允许attempt `d75818f-v4-install-artifacts-staged-resume-20260824-v1`、UUID `50B75F88…8038`和absent host根`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-v4-Exact-Resume-v1`。任一前置漂移不resume；resume开始后的缺证不重试。
3. 真实exact resume不包含受控停止、cleanup、quit、next checkpoint或下一case。只有resume终态独立闭合后，才为停止与磁盘冻结另行请求授权；其余crash、连续L6、P05C、发布、推送和其他平台继续关闭。

## 2026-08-24：v4 exact resume在host handle门失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-resume-20260824-v1`、absent host根`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-v4-Exact-Resume-v1`及guest内部至多一次resume和一次postflight；不含retry、cleanup、stop、quit、next checkpoint或其他VM动作。调用前HEAD `7309313` clean、49项checkpoint binding通过且新根absent。
- committed control先闭合binding、source、target文件和host process门；process观察exit 0且相关角色为0。随后固定target EFI/qcow2的`lsof`观察exit 1，`parse_target_handles`在任何guest observation/write之前拒绝，terminal为`precondition-rejected`、reason `target-handles-preflight-exit-1`、transaction `artifacts-staged-preserved`。
- terminal固定file pull/push、guest exec、maintenance resume和postflight全部为0；plain list/status/start、automatic retry/cleanup/stop/quit/next checkpoint均未执行。没有进入guest、读取raw operation ID、运行maintenance/dpkg或形成新的guest evidence，故不能把case记为resume，也不能推断package或receipt发生变化。
- create-new host根包含7个manifest成员，SHA-256 `a436677c28a65e6abb995f0d038086e09ccec6edb3a0c5f510d5ff192638adc7`逐项复核通过；根/8文件为`0700`/`0600`、single-link、零xattr，raw 32-hex operation ID token为0。该根与原checkpoint现场现冻结，attempt和输出根均已消费，不覆盖、补写或重试。
- 相关进程为0且target handles缺失只证明host观察时backend资格不成立，不证明VM为stopped、saved或不可恢复。此前plain list/status后backend可能再次出现，因此本批不以“只读诊断”为名调用它们，也不擅自激活或停止VM。

## 2026-08-24：v4 backend状态消歧repository-only控制闭合

- 本批只修改并验证仓库，递归绑定exact resume失败manifest `a436677c…adc7`的7项固定成员、原clean `7309313`语义及checkpoint/source/target/attempt链；没有调用真实`utmctl`、查询VM、进入guest、执行resume/dpkg、启停backend或改写冻结证据。
- 新控制固定attempt `d75818f-v4-install-artifacts-staged-backend-resolution-20260824-v1`和未来create-new根`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-v4-Backend-Resolution-v1`。三个授权开关分别覆盖本case的backend消歧、唯一一次可能重新拉起backend的plain `status UUID`、以及禁止list/start/resume/guest/retry/stop/quit；不能沿用此前exact resume授权。
- `status`前须再次闭合clean committed binding、source/target、零相关进程和target句柄精确absent。调用后只做有界`ps`/target `lsof`：`started`加QEMU backend和EFI/qcow2双句柄才为`runtime-reactivated`；`stopped`加全窗口静止才为`registered-stopped`；`started`但无backend为`registered-started-runtime-unavailable`，明确不把它猜成saved。partial、漂移或status异常均`state-indeterminate`，且不自动resume或停止。
- 7项合成回归覆盖runtime延迟出现、registered stopped、started但backend缺失、status失败后置观察、调用前句柄拒绝、partial backend及授权/路径隔离；默认L6合同通过。所有测试均为repository-only，不构成真实backend状态或系统动作证据。

## 2026-08-24：v4 backend状态消歧闭合registered-stopped

- 用户新授权只覆盖commit `92e82e2`、固定UUID/attempt、absent `…-Backend-Resolution-v1`根及一次可能激活backend的plain `status`；不含list/start、guest、resume、retry、stop、quit或后续动作。调用前HEAD clean、全部冻结binding通过且输出根absent。
- control复验source/target、零相关进程和target句柄absent后只调用一次`utmctl status UUID`，exit 0、空stderr并返回`stopped`。随后不再调用UTM命令；10轮host process均为0，同轮target句柄均为absent，target身份前后不变，故terminal为`registered-stopped`。该结论只固定registered status与有界静止，不推断saved状态或下次start会恢复原boot。
- create-new根含30个manifest成员，SHA-256 `0193b4350fa97ba1d00f159fd2384e2cdbe4f790b61d6a8cd98f3740b80363e9`逐项复核通过；根/31文件为`0700`/`0600`、single-link、零xattr且raw ID token为0。file pull/push、guest exec、maintenance resume、plain list/start、retry/cleanup/stop/quit全部为0，transaction保持`artifacts-staged-preserved-no-resume`。
- attempt和输出根均已消费并冻结，不循环status或原地补证。当前backend不具备exact resume资格，旧resume attempt也已消费；下一批先repository-only绑定新证据并设计registered-stopped后的重新激活/boot分流控制，真实激活、全新resume与停止继续拆分授权。

## 2026-08-24：v4 reactivation与boot分流repository-only控制闭合

- 本批commit `924118c`只修改仓库，没有调用真实`utmctl`、查询/启动VM、进入guest、读取raw boot ID或operation ID、运行maintenance/dpkg、resume、stop、retry、cleanup或改写冻结证据。新binding逐项固定backend resolution manifest `0193b435…63e9`的30个成员与原clean `92e82e2`完整语义，递归重验全部上游，并绑定v7二十台全停基线、v4 prepared、UTM 4.7.5 build 118及foreground AppleScript transport。
- clean committed HEAD纯读取复核确认30项、`registered-stopped`、v7 baseline 20/expected 21和原boot hash `18f1ba06…022a`一致。该复核只读取Git、冻结证据、target文件和UTM应用定义，不调用UTM CLI或观察VM运行状态。
- 未来one-shot固定attempt `d75818f-v4-install-artifacts-staged-reactivation-20260824-v1`与absent根`…-v4-Reactivation-v1`。前置source/target、零相关进程和target句柄absent后只允许一次可能带起backend的plain `list`；清单须为v7二十台stopped加唯一v4。list若已使target started且QEMU/双句柄闭合则禁止重复start；target仍stopped时只有整个settle和ready门持续静止才允许一次foreground AppleScript start。任何其他VM active、partial backend、漂移或异常均失败关闭，不再status/list或自动retry/stop。
- host runtime闭合后，唯一guest动作是隔离Python校验canonical boot UUID并只输出SHA-256，不向host暴露raw boot ID；同冻结hash闭合`original-boot-restored`，不同canonical hash闭合`new-boot-started`。它发生在任何业务guest、secret、resume或dpkg动作之前；两种结果都不自动resume或停止。10项回归与L6/全仓门禁通过，完整门禁仅保留既有runbook和sync文档行数预算提醒。

## 2026-08-24：v4 reactivation真实调用失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-reactivation-20260824-v1`、absent `…-v4-Reactivation-v1`根、一次潜在副作用`list`、必要时一次foreground start和一次guest boot hash；不含第二次list、status、resume、业务guest、retry、stop、quit或cleanup。调用前HEAD `e6305e8` clean、全部冻结binding通过且新根absent。
- control复验source/target、零相关进程和target句柄absent；唯一`utmctl list`证明其余20台all-stopped且v4 canonical stopped。10轮settle与ready持续静止后只调用一次foreground AppleScript，exit 0、stdout/stderr空；随后没有再调用UTM CLI。
- 30轮target `lsof`均稳定返回一个`QEMULauncher`记录并精确持有EFI/qcow2各一次，payload hash一致；但同轮固定`ps ... ucomm=`解析的相关进程始终为0。启动返回码、target句柄和全局进程观察不能共同闭合bounded target runtime，故terminal失败关闭为`state-indeterminate/post-start-runtime:foreground-start-without-bounded-target-runtime`，guest boot hash调用0次。VM/UTM可能仍在运行，不能从本证据推断终态。
- create-new根含94个manifest成员，SHA-256 `2a477730bc3f10167add9afee125c7aed8e8dc1df3d4aad1c8a1953f131c5c4c`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr，raw operation ID token为0。attempt与根已消费并冻结；terminal后未补查、重试、进入guest或停止VM。

## 2026-08-24：v4 runtime resolution repository-only控制闭合

- clean `b056402`的新binding逐项固定reactivation manifest `2a477730…c5c4c`的94个成员、原clean `e6305e8` request/binding/source/target/list/settle/ready/foreground transport、30轮矛盾观察及terminal语义，并递归重验全部上游、v7/prepared、attempt链与原boot hash `18f1ba06…022a`。clean HEAD离线复核确认94项、旧`state-indeterminate`和原boot hash一致；该复核没有调用UTM CLI、观察进程/句柄、进入guest或操作VM。
- 新控制固定attempt `d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v1`。它先重验source/target并排除活动`utmctl`，只调用一次可能激活backend的plain `list`；只有v4 canonical started且其他20台stopped才继续。随后从精确target EFI/qcow2发现唯一`QEMULauncher` PID，连续3次用`lsof -a -p PID`绑定同一PID、command和双文件，每次再排除活动`utmctl`，并复验live target descriptor。
- 全部身份稳定后才允许一次guest boot hash，之后再做终态PID、进程和target文件复核；hash相同/不同分别闭合`original-boot-restored`/`new-boot-started`。任何list、成员、PID、command、handle、process、target或hash异常在list后均为`state-indeterminate`，不执行start/status/resume/业务guest/file transfer/retry/stop/quit，也不读取或生成operation ID。
- 11项合成回归覆盖原/新boot、target stopped、其他VM active、句柄缺失、PID漂移、错误command、活动utmctl、hash异常、终态漂移及授权/路径隔离，并进入默认L6门禁；全仓基线通过，只有既有runbook和sync文档行数预算提醒。本批未执行新的真实runtime resolution。

## 2026-08-24：v4 runtime resolution在boot hash格式门失败关闭

- 用户新授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v1`、absent `…-v4-Runtime-Resolution-v1`根、一次潜在副作用`list`、固定PID观察、一次guest boot hash及终态复核；不含start/status/resume、业务guest、file transfer、retry、stop或quit。调用前HEAD `ab151a9` clean、94项reactivation binding通过且新根absent。
- 唯一`utmctl list` exit 0、空stderr，证明v4 canonical started且其他20台stopped。target双句柄发现得到唯一`QEMULauncher` PID `61666`；连续3次`lsof -a -p 61666`的argv、PID、command、EFI/qcow2各一次、378-byte stdout hash `bdd4dcc3…52ec`完全一致，同轮host进程观察均无活动`utmctl`。target config/EFI/qcow2 descriptor在boot调用前稳定。
- 唯一guest boot hash调用已发生，但输出没有满足精确64个小写hex加换行的合同；control在保存成功boot observation/classification前以`guest-boot-id-hash-output-invalid`失败关闭。terminal为`state-indeterminate`、observed hash null、inventory 1、identity observation 3、guest hash 1；终态targeted PID、host process与target postflight未执行。invalid output不能推断原boot、新boot或guest状态，也不授权重跑。
- create-new根含17个manifest成员，SHA-256 `bc83a806e635cb21b945524a2733263fde55dc4c53849f3be614fc606816c766`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr，raw operation ID token为0。source postflight保持92,825,600 bytes/`7bbeb291…403c`；foreground start、plain status/start、resume、业务guest、file pull/push、retry/cleanup/stop/quit均为0。attempt与根已消费并冻结，terminal后未再查询或操作VM，VM仍可能运行。

## 2026-08-24：runtime resolution v2先落盘boot observation

- 从clean `32475d6`只修改repository。v2 binding固定旧runtime resolution manifest `bc83a806…c766`的17个成员、原clean `ab151a9`、request/binding/source/target/list、PID `61666`发现/3次确认、host process、invalid boot hash terminal与全部禁止动作，并递归重验reactivation 94项及更早上游；新attempt固定为`d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v2`。
- 控制在唯一boot hash命令返回后、调用严格parser前，先独立持久化`guest-boot-id-hash-observation.json`；文件只含argv、exit/timeout、stdout/stderr字节数、SHA-256与截断标志，明确`raw_output_persisted=false`。解析成功后才另写classification；格式错误仍保留脱敏observation、无classification且不重试。terminal分别记录observation/classification是否落盘。
- 12项回归覆盖原boot、新boot、stopped/其他VM active、句柄缺失、PID/command/utmctl/终态漂移、无效hash、授权/路径隔离及“parser执行前observation已存在”；默认L6合同通过。代码提交为`e21c518`。随后在clean HEAD使用现有Python 3.14对真实冻结根做纯离线binding，闭合17项、旧`state-indeterminate`、旧observation未落盘及原boot hash `18f1ba06…022a`；没有创建新证据根、调用`utmctl`、观察进程/句柄、进入guest或操作VM。

## 2026-08-24：v2确证guest exec成功空输出并失败关闭

- 新授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-runtime-resolution-20260824-v2`、absent `…-v4-Runtime-Resolution-v2`根、一次潜在副作用`list`、固定PID/3次确认、一次guest boot hash及终态复核；不含start/status/resume、业务guest、file transfer、retry、stop或quit。调用前HEAD `61b24a9` clean，17项v1 binding、原boot hash、source三元组、target config与新根absence全部通过。
- 唯一`list`再次闭合v4 started且其他20台stopped；target发现及3次targeted观察均为同一`QEMULauncher` PID `61666`、EFI/qcow2各一次、378-byte stdout SHA-256 `bdd4dcc3…52ec`，四轮host相关进程数均0，target descriptor在boot前稳定。
- 唯一guest boot hash调用exit 0、未超时，stdout/stderr均为0 bytes与空串SHA-256。raw-free observation已在parser前落盘且`raw_output_persisted=false`；没有64-hex可解析，故无classification，并以`guest-boot-id-hash-output-invalid`闭合`state-indeterminate`。终态PID/process/target postflight未执行，空输出不能证明原boot、新boot或guest状态。
- create-new根含18个manifest成员，SHA-256 `e2e4123578bf51d28b4dad6257773fcb9cb2d7158c34ad9253ba55db4e4ec49e`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr且raw operation token为0。source postflight保持92,825,600 bytes/`7bbeb291…403c`；foreground start、plain status/start、resume、业务guest、file transfer、retry/cleanup/stop/quit均为0。attempt与根冻结，terminal后未再查询或操作VM，VM仍可能运行。

## 当前下一步

1. 保持v2 manifest `e2e41235…c49e`及全部前序现场冻结；不得把成功空输出改写为原boot、新boot或guest已进入，也不得沿用授权补查或重跑。
2. 下一批先repository-only绑定18项，并为exec成功空输出设计独立boot transport resolution；任何未来真实动作继续使用新attempt、absent根和精确授权。
3. 只有新控制闭合`original-boot-restored`才另行设计和授权exact resume；`new-boot-started`进入独立恢复决策，`state-indeterminate`不得继续。受控停止继续独立，其余crash、连续L6、P05C、发布、推送和其他平台继续关闭。

## 2026-08-24：日终提交与文档复核

- 今日从`847cb0e`推进至`c7dd6c8`，共13个提交、21个文件净变更、9,639行新增与15行删除。路线图提交`f95865a`只记录未排期的候选词双语释义构想；五个实现提交`7309313`、`92e82e2`、`924118c`、`b056402`、`e21c518`依次建立exact resume、backend resolution、reactivation、runtime resolution及parser前raw-free boot observation；其余七个文档提交逐次冻结真实失败关闭证据和当前顺位。
- 代码—文档交叉复核确认：新增实现全部位于既有`scripts/linux-product/`验收控制边界，对应回归均接入`check-linux-l6-contract.sh`，仓库总门禁继续调用该入口；L6 runbook已记录各控制合同、授权边界、真实结果、manifest和停止线，`current`保持唯一阶段入口。今日没有改变产品runtime、安装事务语义、FFI ABI、隐私/同步、依赖、平台顺序或目录职责，因此无需改写README、技术方案、仓库结构、Linux稳定Boundary、FFI或隐私专题。
- 今日真实链路从exact resume的target handle前置拒绝，经registered-stopped消歧与一次foreground reactivation，最终两次runtime resolution均在boot identity前失败关闭。v2进一步证明guest exec exit 0、未超时但stdout/stderr均为0 bytes，且raw-free observation先于严格parser持久化；这仍不能证明guest已执行命令或判断原/new boot，transaction保持`artifacts_staged`，VM可能运行，全部attempt和证据根冻结。

## 明日事项（2026-08-25）

1. 只在仓库内实现runtime resolution v3 binding，闭合v2的18项manifest并递归重验v1、reactivation与全部上游；加入成功空输出、observation/classification顺序及禁止重试的精确回归。
2. 设计并评审独立boot transport resolution：guest侧只向私有create-new证据写入boot ID SHA-256，host侧以独立双回读和严格parser交付分类，并在成功后复核同一PID/双句柄/target identity；不保存raw boot ID，不让stdout空值触发fallback或猜测。
3. 明日先完成实现、合成测试、默认L6与仓库门禁、文档提交，不调用`utmctl`、不进入guest、不查询、启动、恢复或停止VM。任何未来实机动作重新固定新attempt、absent host/guest根、动作次数和单独授权；exact resume与受控停止仍分别关闭。

## 2026-08-25：v3 binding与独立boot transport resolution闭合

- 本批从clean `b85c35c`开始，只修改和验证仓库。代码提交`dbce3a5`新增独立binding、host control与guest probe，并将8项host和3项probe回归接入默认L6门禁；没有修改production transaction、startup、FFI、隐私/同步、依赖或平台边界。
- v3 binding先复用既有v2 binding递归重验runtime resolution v1的17项、reactivation的94项与全部上游，再精确绑定v2 manifest `e2e41235…c49e`的18项。新语义明确要求原exec exit 0、未超时、stdout/stderr均0 bytes、`raw_output_persisted=false` observation先落盘、classification文件不存在、terminal保持`state-indeterminate`且retry/start/status/resume/stop/quit均未执行。
- guest probe只接受canonical attempt/target/probe identity与精确私有`0700`控制根，以`O_EXCL|O_NOFOLLOW`创建root-owned `0600` marker/result；它读取canonical boot ID后只持久化SHA-256，不保存或输出raw boot ID。第二次attempt不能覆盖既有文件，非法boot只保留marker且不产生result。
- host control在未来clean binding、source/target、一次可能激活backend的plain `list`、同一QEMULauncher PID三次双句柄确认后，才允许一次probe delivery/execution。probe经root-owned `0600`归一化和逐字回读，result必须两次独立pull完全一致、canonical且无未知字段；分类后再次复核同一PID、双句柄、无活动`utmctl`及target identity。任一缺失、漂移、格式或终态异常均失败关闭，不fallback、不自动retry，也不进入resume。
- 代码提交后在clean `dbce3a5`上直接调用binding读取真实冻结证据，返回`18 / exit 0 / stdout 0 / classification not-generated`；该调用没有创建host输出根、调用`utmctl`、观察live process/handle、进入guest或操作VM。`./scripts/check-linux-l6-contract.sh`、package transaction、startup gate、`./scripts/check-repo.sh`、文本检查与diff检查通过；全仓只保留既有L6 runbook和sync文档行数预算提醒。

## 后续停止线

1. v1/v2 runtime resolution attempt、manifest和全部上游继续冻结；repository-only binding不能表述为boot已识别或真实transport已通过。
2. 真实boot transport必须使用新attempt `d75818f-v4-install-artifacts-staged-boot-transport-20260825-v1`、absent host/guest根和新的逐项授权；不包含start/status/resume、业务guest、retry/stop/quit或cleanup。
3. 只有`original-boot-restored`及终态target稳定同时闭合后才另行设计exact resume；`new-boot-started`进入独立恢复决策，`state-indeterminate`不得继续。受控停止仍须单独授权。

## 2026-08-25：真实boot transport在stopped inventory失败关闭

- 授权前在最终clean `8f59825`复验v2冻结根及全部上游，binding返回18项、exec exit 0、stdout 0、classification not-generated；canonical source仍为92,825,600 bytes与SHA-256 `7bbeb291…403c`，新host根不存在。
- 唯一attempt `d75818f-v4-install-artifacts-staged-boot-transport-20260825-v1`只执行一次可能激活backend的plain `list`。调用exit 0且观察21台注册VM全部stopped，target `50B75F88…8038`未满足started门，因此以`target-not-registered-started-for-boot-transport`闭合`state-indeterminate`。
- 控制在inventory gate停止：PID发现/三次确认均未开始，guest exec/probe、file push/pull、result双回读均为0；没有生成boot classification，没有创建guest控制根，也未执行start/status/resume、业务guest、retry/stop/quit或cleanup。transaction仍为`artifacts-staged-preserved-no-resume`。
- 新host证据根`…-v4-Boot-Transport-v1`包含8个manifest成员，SHA-256 `6a1ad09f1db4b5e0b5d9edb9c4a9d1102fa8dfbf333a819f9961e759352e5140`逐项通过，权限为`0700`/`0600`、uid 501/gid 20且single-link。attempt与根冻结；单次registered stopped不证明boot、saved state或连续终态，不能补查、重试或直接resume。
- 下一步先repository-only评审保持stopped或授权一次新boot的恢复分流。任何新boot、后续transport、exact resume与受控停止分别使用新attempt、absent根和独立授权。

## 2026-08-25：boot start恢复分流repository-only闭合

- 恢复分流选择把一次启动与boot transport合并为单一失败关闭控制；保持stopped只冻结现场，不能识别boot或推进首个crash。新控制不修改production transaction、startup、FFI、隐私/同步、依赖或平台运行时。
- 代码提交`38b1bb8`新增boot start binding/control与7项回归。binding递归重验全部上游并精确绑定boot transport manifest `6a1ad09f…5140`的8项、原clean `8f59825`、全停inventory、probe/result均0及失败terminal。
- 控制在source/target、零process、target handles absent、唯一list证明21台全停和三轮静止后，才允许一次foreground start；启动后不再list/status，以最多60轮`lsof`发现唯一QEMULauncher PID并三次确认，再执行一次私有probe和result双回读。任何漂移失败关闭且不retry/stop/quit，不进入resume、业务guest或dpkg。
- clean `38b1bb8`对真实冻结链的纯离线binding返回8项、target/peers stopped、probe 0、result 0及原boot hash `18f1ba06…022a`；新host根`…-v4-Boot-Start-v1` absent。本批未调用`utmctl`、进入guest、启动或操作VM。
- 真实动作固定attempt `d75818f-v4-install-artifacts-staged-boot-start-20260825-v1`与新guest根`/var/tmp/radishlex-l6-v4-boot-start-…-v1`，但仓库闭合不构成授权。后续先单独授权一次list、三轮静止、一次foreground start、最多60轮runtime、PID三次确认、probe一次及result两次；original/new/indeterminate之后分别决策。

## 2026-08-25：真实boot start在guest agent门失败关闭

- 授权前clean `f972371`仍无工作区改动；纯离线binding逐项重验boot transport manifest `6a1ad09f…5140`的8项及全部上游，返回target/peers stopped、probe 0、result 0、原boot hash `18f1ba06…022a`和新host根absent。
- 唯一attempt `d75818f-v4-install-artifacts-staged-boot-start-20260825-v1`只调用一次plain `list`，证明target `50B75F88…8038`及其余20台registered stopped；三轮host process/target handles继续静止后，唯一foreground AppleScript start exit 0且双流为空。
- start后不再list/status。首轮runtime即发现PID `39591`的`QEMULauncher`与target EFI/qcow2双句柄；发现及三次定向确认均为378 bytes并绑定同一PID/command/双文件，同轮活动`utmctl`为0，target config hash `2de7280b…6195`在ready/started一致。
- 首次guest root创建是唯一guest exec：命令exit 0、未超时、stdout 0，但stderr 144 bytes为`OSStatus -2700`并报告QEMU guest agent未运行或不可用。控制以`guest-control-root-create-stderr-not-empty`闭合`state-indeterminate`；probe、file push/pull、result双回读均为0，observed boot hash为null且classification未生成。
- create-new host根`…-v4-Boot-Start-v1`包含29个manifest成员，SHA-256 `97959c55c05a3562b95412e57ec93249cf6df764700f3ebb49e484cfe3d6c57d`逐项通过；根/文件为`0700`/`0600`、single-link、零xattr。未执行resume、业务guest、dpkg、retry/cleanup/stop/quit，transaction仍为`artifacts-staged-preserved-no-resume`。
- attempt、host根及全部上游现冻结。该结果不证明boot身份、当前运行状态或guest agent永久缺失；不得沿用本授权补查、重跑list/status/start、复用guest根或直接resume。下一步只做repository-only guest-agent transport resolution设计，真实动作继续使用新attempt/absent根并另行授权。

## 2026-08-25：guest-agent transport resolution repository-only闭合

- 代码提交`c2d7113`新增独立binding/control与8项初始回归，并接入默认L6门禁。控制不调用list/status/start；先发现并三次确认唯一target QEMULauncher PID/双句柄，再以最多60次只读`test -r /proc/sys/kernel/random/boot_id`限定agent readiness，每次后复核同一PID和无活动`utmctl`。
- readiness只接受空输出成功或冻结现场逐字一致的144-byte `OSStatus -2700` agent unavailable；未知响应、预算耗尽、PID/target漂移均失败关闭。ready后才允许新私有guest根、唯一probe delivery/execution、marker与result双回读；original/new boot分流后仍复核PID/process/target，任何终态都不resume、业务guest、dpkg、retry/stop/quit。
- clean `c2d7113`首次真实冻结链纯离线binding在`host-process-preflight`暴露旧boot start process evidence实际复用runtime-v2格式；`78dcdb8`修正后第二次binding又发现共享handle helper只接受runtime-v1。两次均在新host根absence、零`utmctl`、零VM/guest动作下失败关闭。
- `5051234`改为本binding显式验证runtime-v2 handle语义并新增对应回归。9项专项测试、默认L6、package transaction、startup gate、文本门禁及全仓基线通过。
- clean `5051234`最终对真实manifest `97959c55…c57d`的29项和全部上游纯离线binding通过，返回PID `39591`、首次agent unavailable、probe 0、result 0及原boot hash `18f1ba06…022a`；计划host根`…-v4-Guest-Agent-Transport-v1`仍absent。本批未查询或操作VM。
- 未来真实动作固定attempt `d75818f-v4-install-artifacts-staged-guest-agent-20260825-v1`、全新host/guest根和新的逐项授权；范围不得包含list/status/start/resume、业务guest、retry/stop/quit或cleanup。original/new/indeterminate之后继续分别决策。

## 2026-08-25：guest-agent resolution在marker门失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、attempt `d75818f-v4-install-artifacts-staged-guest-agent-20260825-v1`、absent host/guest根、PID/双句柄三次确认、最多60次只读readiness，以及ready后一次私有probe与两次结果回读；明确禁止list/status/start/resume、业务guest、retry/stop/quit或cleanup。调用前HEAD `1d64916` clean，最终纯离线binding重验boot start 29项及全部上游，返回PID `39591`、原boot hash `18f1ba06…022a`和新host根absent。
- 真实控制没有调用list/status/start。全局target句柄发现与三次定向确认均固定同一`QEMULauncher` PID `39591`、EFI/qcow2各一次及378-byte observation，同轮无活动`utmctl`。首次readiness即exit 0、未超时且双流为空，随后同一PID/双句柄与host process复核仍通过。
- ready后创建唯一guest私有根，probe只push一次，经root-owned `0600`归一化并以7451 bytes、SHA-256 `27ea59b…b8ba`逐字回读。唯一probe调用同样exit 0、未超时且双流为空；但紧随其后的marker pull返回`OSStatus -2700`并明确`attempt.marker.json`不存在，故不能以exec返回码推断probe真正完成。
- control在`guest-marker-readback`闭合`state-indeterminate`：guest exec/push/pull/probe为4/1/2/1，result readback为0，observed boot hash为null，classification未生成；调用后的PID/process/target postflight未执行。未读取或生成operation ID，也未进入resume、业务guest、dpkg或任何自动补救。
- create-new host根`…-v4-Guest-Agent-Transport-v1`含26个manifest成员，SHA-256 `eb7c42f1b74701ce585188687b8cb18be6ecd2cddba8c4340fb09f775038d053`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link、零xattr，raw operation ID扫描由控制闭合。attempt、host根与guest根均冻结，不补拉、不重跑probe或复用。
- 下一步只在仓库内绑定这26项，并先修正共享probe scope/root合同、补真实CLI组合回归；旧attempt和host/guest根均不复用。任何真实动作须新attempt与单独授权，当前不得resume。

## 2026-08-25：日终提交与文档交叉审阅

- 今日收尾提交前共有11个提交：3个实现提交`dbce3a5`/`38b1bb8`/`c2d7113`依次新增boot transport、boot start和guest-agent resolution，2个修复提交`78dcdb8`/`5051234`对齐真实boot start process/handle evidence，6个文档提交记录三轮repository closure与三次真实失败关闭。相对`b85c35c`共涉及15个文件、净增6837行、删除13行；新增源码均位于`scripts/linux-product/`且单文件未越过1500行，专项测试已接入`check-linux-l6-contract.sh`。
- 逐项对照控制器、共享probe、fake runner、真实26项terminal与三份活跃文档后，发现共享probe CLI默认只接受`…-boot-transport-<attempt>`根，而boot start/guest-agent分别传入`…-boot-start-<attempt>`与`…-guest-agent-<attempt>`。现有probe单测通过直接注入`expected_control_root`，host fake runner又只合成marker/result，因而没有执行真实CLI组合。精确合成guest-agent argv返回exit 12/`control-root-invalid`，且发生在marker写入前；这是本次marker absent的确定性仓库侧解释，但不能把不可信guest exec返回倒写成真实guest错误正文。
- 本问题只在L6验收控制与测试边界，不改变production transaction、startup、FFI ABI、Engine、隐私/同步、依赖、平台顺序或目录职责；因此README、技术方案、仓库结构、Linux稳定Boundary、FFI与隐私专题无需更新。需要变化的当前顺位、现场事实与明日动作已同步到`current`、L6 runbook和本周周志。
- 日终首次plain `utmctl list`显示21台中仅v4 `50B75F88…8038`为started。按用户授权只发送一次`utmctl stop UUID --request`正常关机，没有force/kill；终态list为21台全stopped，相关QEMU/`utmctl`进程为0，target EFI/qcow2零句柄。未进入guest、读取marker/result、resume、运行dpkg或清理任何冻结根。

## 明日事项（2026-08-26）

1. 只在仓库内新增guest-agent result binding，逐项固定manifest `eb7c42f1…d053`的26个成员、原clean `1d64916`、一次readiness、一次probe、marker absent、result 0、postflight未执行与全部禁止动作。
2. 修正共享probe的scope/root合同：让committed CLI对boot transport、boot start、guest-agent三类固定根均显式且可验证，拒绝任意调用方路径；新增真实CLI组合测试，不能再由`expected_control_root`注入或fake runner掩盖argv漂移。
3. 在21台全停且旧attempt/root冻结的前提下，repository-only设计新的启动与boot分类控制；不补拉旧guest根、不重跑旧probe、不resume。未来真实start、boot回读、exact resume分别使用新attempt、absent根和独立授权。

## 2026-08-26：probe scope、冻结结果与boot classification仓库闭合

- `862b003`把共享boot probe改为显式封闭scope；boot transport、boot start与guest-agent三类host生成的真实argv均通过committed parser和固定根派生回归，未知scope或scope/root错配在写marker前拒绝，测试不再以完整`expected_control_root`注入绕过CLI合同。
- `5b8d074`新增guest-agent result binding，精确固定manifest `eb7c42f1…d053`的26项、原clean `1d64916`、PID/handle/readiness/probe transport、marker absent、result readback 0、postflight未执行与全部禁止动作。clean committed HEAD对真实冻结链纯离线复核返回26项、`absent-after-probe-exec`和旧`state-indeterminate`；没有调用`utmctl`或创建新证据根。
- `65c7699`新增stopped-inventory boot classification binding/control：只有唯一list证明21台全停和三轮静止后才允许一次foreground start；启动后不再list/status，以有限runtime找到唯一QEMULauncher双句柄并三次确认，再进行最多60次只读agent readiness。ready后才执行一次固定`boot-start` scope probe、marker与result双回读；任一失败都不自动resume/retry/stop/quit，也不进入业务guest或dpkg。
- 首次clean-chain复核前发现新请求未显式携带冻结boot-start根、manifest和旧attempt id；`25e1608`补齐三项不可变输入、重叠/漂移拒绝及回归。最终clean `25e1608`递归绑定真实全部冻结根成功，返回guest-agent 26项、marker absent、旧`state-indeterminate`，计划输出根`…-v4-Boot-Classification-v1`仍absent。
- `./scripts/check-linux-l6-contract.sh`、package transaction、startup gate、文本检查与`./scripts/check-repo.sh`通过；全仓只保留既有L6 runbook和sync文档行数预算提醒。本批没有查询、启动或停止VM，没有进入guest、resume、运行dpkg或修改冻结证据，也没有改变production transaction、startup、FFI ABI、隐私/同步、依赖或平台边界。

## 当前下一步（2026-08-26）

1. 继续冻结旧boot-start/guest-agent attempt、host/guest根及manifest；仓库scope修复不能倒写成旧probe已在guest成功运行。
2. 若继续真实boot classification，必须另行授权固定新attempt、absent host根、一次全停inventory/foreground start、有限runtime/readiness、一次probe与双回读；本次仓库实现不构成该授权。
3. 只有稳定交付original/new boot分类并闭合终态identity后，才分别设计exact resume或新boot恢复；`state-indeterminate`立即停止，受控停止继续独立授权。

## 2026-08-26：真实boot classification闭合`new-boot-started`

- 用户授权只覆盖attempt `d75818f-v4-install-artifacts-staged-boot-classification-20260826-v1`、absent host根、一次全停inventory/foreground start、有限runtime/readiness、一次固定scope probe及marker/result双回读；不含resume、业务guest、dpkg、retry/cleanup/stop/quit。调用前HEAD `8a04d02` clean，纯离线preflight返回旧guest-agent 26项、旧`state-indeterminate`且新根absent。
- 唯一plain `list`证明21台registered VM全停，三轮静止后只启动target一次。PID `92422`与EFI/qcow2双句柄从runtime发现到terminal稳定，前9次readiness为固定agent unavailable，第10次ready；同轮相关host process为0。
- probe使用固定`boot-start` scope，唯一push/normalize/8404-byte readback、一次exec、marker回读及两次result回读全部通过。两份result observation SHA-256均为`b8a5cc65…1b5`，boot evidence `4f0bec04…9763`固定原boot `18f1ba06…022a`和新boot `b757c8fc…4758`，最终分类为`new-boot-started`。
- create-new host根`…-v4-Boot-Classification-v1`包含72个manifest成员，SHA-256 `a17920bd9a38c455f1151e5873f0b4261aa25237c64cf56cc8ef73e554830e17`逐项通过；terminal保持`artifacts_staged-preserved-no-resume`，自动resume/retry/cleanup/stop/quit与业务guest/dpkg均未执行。terminal后未再次查询或停止UTM，因此不把后续registered/running状态写成已知。

## 2026-08-26：72项结果binding在真实冻结链闭合

- `93f81d8`新增boot classification result binding与5项初始回归，精确固定72项名称/manifest、原clean request、全停inventory与单次start、9次unavailable+第10次ready、固定scope probe、marker/双result、新boot分类及禁止动作，并接入默认L6门禁。
- 第一次clean真实链调用在任何UTM动作前暴露binding上游少取一层；`f78a253`把链显式拆为`BootClassification -> BootTransport -> RuntimeResolution -> Reactivation`并增加拓扑回归。第二次调用发现PID discovery之前的首份global handle按原控制合同不含`backend_pid`；`3e31540`将其与后续PID-bound handle分开验证并补负向回归。
- 最终clean `3e31540`对真实72项及全部递归上游纯离线验证通过，返回`new-boot-started`、旧/new boot hash、PID `92422`与binding SHA-256 `0400774c…19e0`；没有调用`utmctl`、进入guest、创建新证据根或改写冻结材料。
- 审阅既有exact-resume driver确认它显式绑定原boot `18f1ba06…022a`并要求同boot遗留guard，本次新boot不能直接复用。下一批先只在仓库内设计新boot只读恢复资格预检，重验持久receipt/staging/checkpoint、current boot、package/dpkg、process/network与`/run/lock`跨重启语义；真实预检、canonical resume和受控停止分别另行授权。

## 2026-08-26：new-boot只读恢复资格控制仓库闭合

- 生产语义复核确认`artifacts_staged` receipt在guard缺失时由startup gate返回`MaintenanceRequired / OperationInProgress / state=2`，wire为`0:1:3:14:2|error-absent`；有效guard仍优先返回`ActiveGuard`，wire为`0:1:3:10:0|error-absent`。因此跨重启资格不能继续强制guard存在，也不能把guard缺失误判为事务消失。
- `4ba55cb`新增冻结结果binding、一次性host control与guest只读probe。probe从持久receipt重新绑定operation hash/chain与target-only staging，不依赖已随重启消失的`/run`测试secret；guard只分类为absent或root-owned `0600`/regular/single-link/zero-size且未锁，两类必须分别与Manager/Fcitx startup wire严格耦合。它不创建、删除或写产品guard，不调用旧resume driver的case派生、resume、postflight或结果验证路径。
- host控制不做inventory/list/status/start；未来只有固定同一QEMULauncher PID与EFI/qcow2双句柄、source/target、无活动`utmctl`和有界agent readiness全部稳定后，才向create-new私有guest根各交付一次冻结旧driver和只读probe。两文件均root-owned `0600`、逐字回读，probe至多执行一次，marker回读和result独立双回读后再做PID/process/target终态复核。任何未知输出、双读漂移或身份漂移失败关闭，不自动retry/resume/stop/quit，也不运行dpkg mutation。
- 新增6项host与5项guest测试并接入`./scripts/check-linux-l6-contract.sh`；专项与L6全门禁通过。clean `4ba55cb`随后递归重验真实boot classification 72项及全部上游，返回`new-boot-started`、旧/new boot `18f1ba06…022a`/`b757c8fc…4758`、PID `92422`、旧driver SHA-256 `0b649ecd…d05b`、probe SHA-256 `59cfa67a…3136`和binding evidence SHA-256 `18cc48a2…40d6`。
- 本批只修改仓库并读取冻结host evidence；没有调用`utmctl`、进入guest、创建真实预检根、查询/启动/停止VM、resume或运行dpkg，也未改写任何冻结证据。真实预检固定新attempt `d75818f-v4-install-artifacts-staged-new-boot-recovery-preflight-20260826-v1`，但代码闭合不构成系统授权；真实预检、后续resume与受控停止继续分别授权。

## 2026-08-26：真实恢复预检拒绝、取证绑定与合同修复

- 用户授权只覆盖UUID `50B75F88…8038`、固定新attempt/absent host与guest根、有限readiness、两份committed文件唯一交付、一次只读probe及marker/result/phase回读；不含inventory/list/status/start、resume、dpkg mutation、retry/cleanup/stop/quit。调用前HEAD `eba4bae` clean，递归binding重验boot classification 72项、PID `92422`与旧/new boot hash。
- 唯一真实控制一次readiness即ready，固定PID/EFI/qcow2双句柄并确认无活动`utmctl`；旧driver与probe各push/readback一次，probe唯一执行后marker与两份875-byte result及phase均稳定回读。guest terminal在`persistent-transaction`以`ResumeDriverError:directory-identity-invalid:state-root`返回`recovery-rejected`，guard未观察，maintenance resume与dpkg mutation均为0；exec transport却报告exit 0，旧host的exit比对把确定拒绝记为`state-indeterminate`并停在terminal identity postflight之前。
- host create-new根`…-v4-New-Boot-Recovery-Preflight-v1`含38项，manifest `1d593a8f7a9ce725320954257e763862703d43e89a3e82c7fa5cd94391f233c7`逐项通过；terminal计数为readiness/probe/result readback `1/1/2`、push/pull/guest exec `2/6/8`，未运行resume、dpkg、业务guest、inventory/list/status/start或自动补救。调用后没有查询、停止或清理UTM，VM状态未知，attempt及host/guest根冻结。
- 根因是production持久目录合同为state root与`operations/` root-owned `0755`、具体operation目录`0700`，旧probe却对三层都调用旧driver的`0700` helper；真实拒绝发生在receipt/staging与guard/live-system检查之前。`c523fb3`改为`0755/0755/0700`分层验证，不改冻结旧driver；host改以marker、双份相同terminal及phase为权威事实，只记录exec transport exit。
- 新增真实结果binding与5项测试，并补host transport-exit-0/rejected-result及guest目录mode回归，默认L6门禁通过。clean committed `c523fb3`随后对真实38项和全部递归上游纯离线验证通过，返回guest `recovery-rejected`、transport exit 0、旧host `state-indeterminate`与PID `92422`；没有调用`utmctl`或改写证据。第二次真实预检须新attempt、absent host/guest根及单独授权，resume和受控停止仍分别授权。

## 2026-08-26：日终提交审阅、文档校准与全停

- 今日13个提交从`862b003`至`4e3a9fd`，合计修改26个文件、增加6711行并删除28行：依次闭合共享boot probe scope、guest-agent 26项结果binding、全停启动/boot classification控制、72项实机结果binding、new-boot只读恢复资格控制，以及38项真实拒绝binding与两处合同修复。逐项对照committed control/probe/result binding、测试、默认L6入口和真实terminal后，未发现未记录的resume、dpkg、业务guest、retry/cleanup/stop/quit或隐私/FFI/依赖边界变化。
- 文档复核确认`current`、L6 runbook与周志已覆盖三次repository closure、两次真实guest链结果和冻结停止线；`docs/README.md`导航、技术方案、仓库结构、隐私/同步及FFI专题无需变化。稳定安装边界原先只写state root `0755`和operation私有目录`0700`，本次补出中间`operations/`固定`0755`；roadmap把已失效的“尚待exact resume”校准为“尚待新boot恢复资格与resume”。
- 收尾首次plain list只见v4 UUID `50B75F88…8038`为started、其余20台stopped；只对该VM发送一次正常`--request`关机。终态list为21台全stopped，宿主QEMULauncher/`utmctl`进程为0，target EFI/qcow2句柄为空；未force、kill、进入guest、读取冻结根、resume、运行dpkg或清理证据。
- 正常关机结束了`b757c8fc…4758`对应的live boot，故明日不能直接把`c523fb3`修复后的旧固定boot attempt重跑。下一步先repository-only设计“全停inventory→单次启动→fresh boot重新分类→只读恢复资格”的新attempt/absent-root链；门禁闭合后，真实启动/分类、资格预检、resume与受控停止仍分别授权。

## 2026-08-27：fresh-boot恢复资格两阶段控制仓库闭合

- `e9fdabc`新增5个control/binding模块、2个测试模块并扩展既有只读probe与host同PID门，共11个文件、增加2274行并删除21行。第一阶段递归消费昨日38项拒绝结果，以已结束boot `b757c8fc…4758`为基线，只允许全停inventory后的单次foreground start、有限runtime/readiness与fixed-scope boot分类；不复用旧attempt/root，也不进入恢复资格、resume或dpkg。
- 第二阶段使用动态但严格的结果binding：先逐文件验证第一阶段create-new evidence树、manifest顺序/hash、request/control/binding/probe身份、全停/单次start、动态poll/readiness、marker/双result、new boot和禁止动作，再要求资格控制发现的backend PID与分类terminal一致。旧/原boot、manifest漂移、PID变化或任何不确定状态均在guest mutation前失败关闭。
- guest只读probe保留2026-08-26固定attempt的精确合同，同时只为新fresh-boot attempt开放由host结果binding提供的canonical current boot；原transaction boot仍固定`18f1ba06…022a`，state root/operations/operation目录继续按`0755/0755/0700`验证。probe不调用resume、case派生、结果验证或dpkg mutation，guard absent/合法未锁仍与两类startup wire严格耦合。
- `./scripts/check-linux-l6-contract.sh`、`./scripts/check-linux-package-transaction.sh`、`./scripts/check-linux-startup-gate.sh`、`./scripts/check-text-files.sh`与完整`./scripts/check-repo.sh`通过；合成测试不能替代真实系统。该批未调用`utmctl`、进入guest、创建真实attempt/root、查询/启动/停止VM、resume、dpkg、retry或cleanup，也未改变production transaction、startup、FFI ABI、隐私/同步、依赖、平台或目录职责。
- 用户随后只授权第一阶段。clean `0be88ee`的唯一调用先离线重验38项旧拒绝结果与全部上游，新根absent；唯一plain `list`观察21台全停，三轮静止后一次foreground start。PID `42349`从首轮runtime、三次confirmation、10轮readiness至terminal保持EFI/qcow2双句柄；第10轮agent ready后只执行一次fixed-scope boot probe，marker与双result回读闭合。
- 新boot `d64b962e…b50`不同于已结束boot `b757c8fc…4758`，72项manifest `2211af7e…e60`闭合`new-boot-started`；双result SHA-256均为`34d1f032…c4e6`。未执行资格、resume、dpkg、业务guest、retry/cleanup/stop/quit，terminal后未再查询或停止UTM，transaction仍为`artifacts-staged-preserved-no-resume`。
- 实机记录提交前，clean `0be88ee`上的第二阶段纯离线binding已逐项验证72项动态manifest、新boot与同一PID `42349`，计划资格根仍absent；提交记录为`89639fd`后按新HEAD复核则在`prior-fresh-boot-classification-request-invalid`失败关闭。根因是新result binding把历史classification HEAD与当前资格控制clean HEAD合并比较；未checkout、回退、绕过或执行资格。
- clean `ef5d17f`分别验证当前上游`repository_clean`/HEAD和manifest内历史request HEAD，再以历史HEAD重建冻结request/binding，禁止两者交叉漂移；资格binding同时导出两项HEAD。successor HEAD通过、当前HEAD错误、历史交叉篡改与manifest漂移均有回归；fresh recovery 6项、classification 3项、旧recovery 7项、guest probe 7项、默认L6、package transaction/startup gate及完整`./scripts/check-repo.sh`通过。
- clean提交后的真实72项冻结链纯离线复核通过：历史HEAD `0be88ee`、当前HEAD `ef5d17f`、boot `d64b962e…b50`、PID `42349`，计划资格根仍absent。本批未调用UTM、进入guest、资格、resume、dpkg、retry/stop/cleanup；下一步只能另行授权一次只读资格，resume与停止继续分别授权。

## 2026-08-27：真实fresh-boot恢复资格预检失败关闭

- 用户授权只覆盖UUID `50B75F88…8038`、固定attempt `d75818f-v4-install-artifacts-staged-fresh-boot-recovery-preflight-20260827-v1`、absent host/guest根、同PID门、有限readiness、两份committed文件唯一交付、一次只读probe及marker/result回读；明确不含inventory/list/status/start、resume、dpkg mutation、retry/cleanup/stop/quit。调用前HEAD `5cb8160` clean，动态binding逐项验证fresh classification 72项、历史HEAD `0be88ee`、当前HEAD、boot `d64b962e…b50`与PID `42349`。
- 控制一次readiness即ready，三轮PID/双句柄确认和probe前复核通过；旧driver与只读probe各push、归一化及逐字回读一次，probe唯一调用exit 0且双流为空。marker回读存在且绑定attempt、driver/probe hash，但紧随其后的第一次result pull报告`OSStatus -2700`及guest `preflight.evidence.json`不存在；控制没有执行第二次result读回，按合同以`guest-recovery-result-readback-1-stderr-not-empty`闭合`state-indeterminate`，guest资格结果为`not-observed`。
- create-new host根`…-v4-Fresh-Boot-Recovery-Preflight-v1`含35个manifest成员，SHA-256 `39586a3bb662d9a28a7a2a6e5721b49c4b5395baf47f4ad03bef3da1a20e1bed`逐项通过；根/文件为`0700`/`0600`、uid 501/gid 20、single-link且零xattr。terminal记录probe/result readback为`1/1`，transaction仍是`artifacts_staged-preserved-no-resume`，未执行inventory/list/status/start、resume、dpkg mutation、retry/cleanup/stop/quit。
- 该现场不能区分guest probe尚未发布结果、结果发布较晚或已在其他阶段失败，也不能把transport exit 0解释成资格通过；attempt与host/guest根全部冻结，不补拉、不重跑。调用后没有追加UTM查询，实际VM/guest状态未知。后续先repository-only绑定35项并设计只消费冻结attempt的deferred-result resolution；任何真实补拉、fresh-boot resume及受控停止继续分别授权。

## 2026-08-27：延迟结果消歧控制仓库闭合

- `368d386`新增fresh-boot recovery preflight result binding与deferred-result resolution control，并各配4项、7项回归后接入默认L6门禁。binding固定35项manifest、私有树权限/链接/xattr、历史/current HEAD、source/target、boot/PID、文件交付与回读、唯一probe、marker、首次result missing terminal及全部禁止动作；不以marker或transport exit 0补写guest结论。
- 新attempt固定为`d75818f-v4-install-artifacts-staged-fresh-boot-recovery-result-resolution-20260827-v1`，计划host根为`…-v4-Fresh-Boot-Recovery-Result-Resolution-v1`。真实控制只允许同PID/双句柄及source/target前后门、两次既有`preflight.evidence.json`回读与一次既有`phase.json`回读；不inventory/list/status/start、push/exec/probe、resume/dpkg、retry/cleanup/stop/quit。缺失、漂移或身份不稳均失败关闭。
- `./scripts/check-linux-l6-contract.sh`、package transaction、startup gate、文本检查及完整`./scripts/check-repo.sh`通过；仅保留既有runbook与sync专题行数提醒。clean `368d386`随后对真实冻结链纯离线复核通过：35项、历史HEAD `5cb8160`、当前HEAD、旧/new boot `18f1ba06…022a`/`d64b962e…b50`及PID `42349`均匹配，新host根仍absent。
- 本批没有调用`utmctl`、查询或操作VM、进入guest、创建现场根、补拉真实result、resume、dpkg或停止。真实结果消歧仍须单独授权；只有稳定canonical双读与phase一致才能交付qualified/rejected，后续resume与受控停止继续分别实现、验证和授权。

## 2026-08-27：真实延迟结果闭合`recovery-qualified`

- 用户授权仅覆盖UUID `50B75F88…8038`、固定result-resolution attempt、absent host根、同PID/双句柄与source/target前后门、两次既有result和一次既有phase pull；明确不含inventory/list/status/start、push/exec/probe、resume/dpkg、retry/cleanup/stop/quit。调用前HEAD `b2abff7` clean，35项冻结链离线binding返回历史HEAD `5cb8160`、boot `d64b962e…b50`、PID `42349`且新根absent。
- 唯一控制三轮确认及终态复核均保持PID `42349`与EFI/qcow2双句柄。两份945-byte result均exit 0、stderr空、未超时，SHA-256同为`fb4961ab…263e`；116-byte phase同样稳定回读`complete`。guest canonical结果为`recovery-qualified`：receipt `artifacts_staged`、guard `absent-after-reboot`、startup wire `0:1:3:14:2|error-absent`，maintenance resume/dpkg mutation均为0。
- create-new host根`…-v4-Fresh-Boot-Recovery-Result-Resolution-v1`含21项，manifest `181147204f3104319ef44a7d742063a7b81c6310e51f40caaee12d8358f3989b`逐项通过；`0700`/`0600`、uid 501/gid 20、single-link且零xattr。terminal计数为result/phase/file pull `2/1/3`，push/exec/probe、inventory/list/status/start、业务guest、resume/dpkg及全部自动补救为0。
- 现场记录后L6 contract、package transaction、startup gate、文档/文本检查及完整`./scripts/check-repo.sh`通过；仅保留既有runbook与sync专题行数提醒。本批未修改production代码、协议、依赖、FFI、隐私或平台边界。
- result-resolution attempt、21项根与全部上游冻结，调用后未追加UTM查询或停止。结论仅证明当前fresh boot上的只读恢复资格，不代表transaction完成；下一批先repository-only绑定21项并设计fresh-boot resume，真实resume与受控停止继续分别使用新attempt、absent根和独立授权。

## 2026-08-27：qualified结果绑定与fresh-boot resume控制仓库闭合

- `adf8b0d`新增21项result-resolution binding、fresh-boot resume host/guest控制与三组回归并接入默认L6门禁；冻结旧resume driver字节保持`0b649ecd…d05b`不变。binding固定manifest成员顺序、私有树身份、历史/current HEAD分离、source/target前后门、同PID/双句柄、双result/phase与零mutation语义。
- 首次clean-HEAD实链离线复核在读取不存在的资格阶段`target-files-agent-ready.json`时失败关闭；这是新binding复用了错误拓扑，不是冻结证据漂移。`df4ef3c`改为21项结果根实际存在的`target-files-preflight/postflight`前后门并保留package path/hash稳定校验；第二次从全部真实冻结链复核通过，返回21项、历史HEAD `b2abff7`、当前HEAD `df4ef3c`、boot `d64b962e…b50`、原boot `18f1ba06…022a`、PID `42349`、`recovery-qualified`、双读+complete phase及maintenance resume 0。
- 新attempt固定为`d75818f-v4-install-artifacts-staged-fresh-boot-resume-20260827-v1`，计划host根为`…-v4-Fresh-Boot-Resume-v1`。host调用前必须重新绑定qualified结果、source/target、无活动`utmctl`、同PID/双句柄与有限readiness，只向create-new guest根各交付一次冻结resume driver、冻结recovery probe和当前fresh resume driver；driver唯一调用后以marker、双terminal和phase消歧transport，再复核终态身份。
- guest先重验persistent receipt/operation chain/staging、current boot、absent guard/MaintenanceRequired、package/dpkg、XDG/process/network；只有固定transient run root原先absent时，才从receipt在guest内create-new重建root-only secret。它在production mutation前再次重验全部身份，只允许一次canonical resume和一次postflight；调用前失败保留`artifacts_staged`，调用后证据缺失为`state-indeterminate`，不自动retry、cleanup、stop或quit，raw operation ID不进入host evidence。
- 新增4项result binding、6项host与7项guest测试；`./scripts/check-linux-l6-contract.sh`与文本门禁通过。该批没有调用UTM、查询或操作VM、进入guest、创建真实resume根、重建secret、resume、dpkg或stop。真实resume、后续结果绑定与受控停止仍须分别授权。

## 2026-08-27：真实fresh-boot resume冻结为`state-indeterminate`

- 用户单独授权UUID `50B75F88…8038`、固定attempt `d75818f-v4-install-artifacts-staged-fresh-boot-resume-20260827-v1`、absent host/guest根、三份文件唯一交付、一次production resume与一次postflight；明确排除inventory/list/status/start、stop/restart/quit、retry/cleanup、重复资格probe、Manager/Fcitx及用户XDG动作。首次命令仅在本机参数解析阶段发现缺少旧恢复attempt并退出，根仍absent、零UTM/guest动作；补齐冻结参数后才进入唯一真实调用。
- clean `fb53cc6`的上游21项qualified result、source/target、boot `d64b962e…b50`、原boot `18f1ba06…022a`、PID `42349`与双句柄全部通过。一次readiness和三轮confirmation稳定；guest根create-new后，旧resume driver `0b649ecd…d05b`、recovery probe `e63c37e6…87c0`与fresh driver `90022dc6…b2ba`各交付/readback一次，唯一driver transport exit 0且双流为空。
- 两份1097-byte terminal逐字一致，SHA-256同为`e27368b7…49a`，phase为118-byte `indeterminate`。guest明确记录transient secret已重建、maintenance resume/postflight为`1/1`，但以`ResumeDriverError:terminal-postflight-semantics-invalid`闭合；故dpkg mutation为unknown、transaction为`state-indeterminate`。transport exit 0、postflight已调用及终态同PID/双句柄不能提升为完成，也不得自动补拉、重试、清理或再次resume。
- host根`…-v4-Fresh-Boot-Resume-v1`含47项，manifest `ee1879e7bcb8950ac6bd83630de4102aa87522a47cc2c64119d3d88b03420798`逐项通过；`0700`/`0600`、uid 501/gid 20、single-link且零xattr。file push/pull、guest exec为`3/7/11`；inventory/list/status/start、retry/cleanup/stop/quit均为0，调用后未追加UTM查询或停止。
- `450cfa8`新增47项冻结结果binding与5项回归并接入默认L6门禁；clean successor HEAD对全部真实上游离线复核通过，返回历史HEAD `fb53cc6`、PID `42349`、boot/双句柄、稳定双terminal、1/1调用、`state-indeterminate`与dpkg unknown。本批后半只读host证据并修改仓库，未再进入guest或触碰现场；下一步先repository-only设计受控停止，真实stop继续单独授权。

## 2026-08-27：日终全停、提交回顾与明日入口

- 日终前工作区为clean `e18c3c0`、`dev`相对`origin/dev` ahead 15。今日15个提交从`e9fdabc`至`e18c3c0`，其中6个代码提交依次闭合fresh-boot分类/资格、历史与当前HEAD分离、延迟结果消歧、resume host/guest控制、结果根target拓扑和47项真实结果binding；9个文档提交同步每个实机停止线与结果。累计只涉及Linux L6脚本/回归、默认门禁及四份相关文档，没有修改production Rust、公共协议、隐私、FFI、依赖、许可证或其他平台边界。
- 按代码回顾复核`current`、Linux安装维护Boundary、L6 runbook与本周周志；跨重启secret重建、单次resume/postflight、调用后不确定状态和禁止自动补救的稳定合同已经在Boundary中，需补齐的是该Boundary的当前结论、今晚all-stopped终态及明日顺位。根协作入口、技术方案、仓库结构、隐私/同步和FFI文档无需更新。
- 用户独立授权日终关闭全部VM。首次plain `utmctl list`仅见v4 UUID `50B75F88…8038`为started、其余20台stopped；只发送一次正常`stop --request`，终态plain list为21台全部stopped，宿主无`QEMULauncher`/`qemu-system`进程。没有force、kill、进入guest、补拉、retry、cleanup或证据改写；boot `d64b962e…b50`与PID `42349`的live实例已结束，但47项resume结果仍唯一权威为`state-indeterminate`、dpkg/transaction unknown。
- 明日先repository-only逐字段审计稳定双terminal与冻结resume driver的completed postflight合同，为`terminal-postflight-semantics-invalid`定位精确谓词并补回归，不放宽验证或倒写现场；根因闭合后再设计从all-stopped出发的全新只读transaction-state resolution。任何真实list/start、guest读取与终态stop重新分别授权，且不携带resume、dpkg mutation、retry或cleanup。

## 2026-08-29：fresh-boot terminal postflight 跨重启语义闭合

- 从clean `8c1331c`逐字段对照冻结canonical `case.sh`、旧resume driver与稳定双terminal，精确定位`terminal-postflight-semantics-invalid`：case的postflight已完成package、receipt、dpkg、startup、XDG、process与network检查，但输出字段`boot_id_sha256`来自原mutation preflight，因此是prior boot `18f1ba06…022a`；fresh wrapper随后把冻结driver的同一期望临时改为current boot `d64b962e…b50`，把两个不同身份混成一个validator predicate。
- `202b64d`让冻结driver按原合同继续严格验证prior terminal字段；外层在completed artifacts验证前后分别从`/proc/sys/kernel/random/boot_id`复验current boot，且不再改写冻结模块全局常量。新增四项driver回归覆盖原失败、postflight字段漂移、current前门漂移和验证后boot漂移，主成功路径改为执行真实completed wrapper而非整体mock。
- 同一提交把47项历史结果binding与successor driver解耦：从manifest约束的冻结readback取出真实fresh driver并固定`90022dc6…b2ba`/17390-byte身份，再以当前clean committed文件形成successor binding；新增一项回归拒绝历史/当前driver混用。旧resume attempt、guest/host根、terminal和manifest均未改写。
- 定向11项/6项测试、`./scripts/check-linux-l6-contract.sh`、`./scripts/check-linux-package-transaction.sh`与`./scripts/check-linux-startup-gate.sh`通过。首次真实链离线复核在递归解析前因解释器不支持`zip(strict=True)`退出，没有形成证据结论；改用固定mise Python 3.14.5后，clean `202b64d`递归验证全部真实根并返回47项、历史HEAD `fb53cc6`、当前HEAD `202b64d`、guest/transaction仍为`state-indeterminate`。全程未调用`utmctl`、进入guest、创建输出根、补拉、resume、dpkg、retry、cleanup或stop。
- 下一批只先设计all-stopped起点的只读transaction-state resolution；真实inventory/list、单次start、guest读取与stop继续分别授权，且不携带resume、dpkg mutation、repair、retry或cleanup。仓库修复不能倒推旧现场completed，dpkg/receipt真实终态仍未知。

## 2026-08-29：all-stopped只读transaction-state resolution仓库闭合

- `43ae490`新增transaction-state host control、47项冻结结果递归binding、guest只读probe与两组回归，并把既有boot-start控制抽成可注入只读probe workflow。新attempt固定为`d75818f-v4-install-artifacts-staged-transaction-state-resolution-20260829-v1`；不复用旧driver/root，不修改production transaction、公共协议、依赖、FFI、隐私或平台职责。
- host合同从一次all-stopped inventory开始，只允许一次foreground start、有限PID/双句柄与agent readiness、一个committed probe的唯一交付/执行、marker与两份逐字结果及终态身份复核。guest只读观察receipt/staging/guard、package/dpkg、startup、XDG/process/network和prior/current boot；只有完整installed terminal或精确原`artifacts_staged`基线可分别交付互斥终态，其余全部失败关闭为`state-indeterminate`。unknown receipt不fallback，raw operation ID不离开guest。
- 9项host与5项guest测试、默认L6、package transaction、startup gate、文本检查及完整仓库基线通过。真实冻结链纯离线复核返回47项、20台旧基线、原`transaction=state-indeterminate`、`dpkg=unknown`与probe SHA-256 `62d846dd…298c`；离线复核还暴露并修复successor request属性遮蔽旧recovery probe路径的问题，增加专门回归。
- 本批未调用`utmctl`、查询或启动VM、进入guest、创建真实host/guest根、resume、dpkg、retry、repair、cleanup或stop。真实inventory/list、单次start与只读transaction观察须逐项重新授权；终态stop继续独立授权，仓库闭合不提升旧现场结论。

## 2026-08-29：v1实机前门误判与v2收口

- 用户授权v1的一次inventory/list、一次foreground start和一次guest只读观察，不含resume、dpkg、retry、repair、cleanup或stop。clean `6858ec6`离线前门通过，但正式控制在任何UTM调用前因PID `97570`/`qemu-system-aarc`被广义process parser计为相关进程而失败关闭；7项manifest `a3d049e3…15cd`固定`precondition-rejected`，inventory/start/guest/result均为0。
- 因list未消费，唯一manual `utmctl list`证明21台注册UTM全停。随后只读PID/lsof确认该进程只持有Android Emulator AVD qcow2，没有UTM target句柄；未kill、stop或修改Android/UTM会话。调用包装器在控制返回后读取不存在属性产生的本机`AttributeError`不改变已经持久化的terminal与manifest。
- `f05ebd7`让custom transaction workflow只阻断`UTM`、`utmctl`、`QEMUHelper`与`QEMULauncher`，excluded generic QEMU仍以PID/accounting/role/uid结构记录；默认boot-start路径保持原行为。新增外部QEMU回归后host为10项，guest保持5项；默认L6、package transaction、startup gate与文本门禁通过。
- consumed v1与host根永久冻结。全新v2 attempt `d75818f-v4-install-artifacts-staged-transaction-state-resolution-20260829-v2`、host根`…-v4-Transaction-State-Resolution-v2`及guest根均未创建；clean v2真实链离线复核返回47项、20台基线、旧transaction不确定/dpkg unknown与probe `e706d622…d0e3`。真实v2重试和terminal stop须分别重新授权。

## 2026-08-29：v2只读transaction观察失败关闭

- 用户重新授权固定v2的一次inventory、一次foreground start与一次guest只读观察，不含resume、dpkg、retry、repair、cleanup或自动stop。clean `a03edae`离线binding重验47项冻结resume结果、20台旧基线、21台期望inventory与probe `e706d622…d0e3`，新host root在调用前absent。
- 唯一plain list证明21台UTM全部stopped；三轮静止后唯一foreground launch成功，target PID `36343`的EFI/qcow2双句柄在三次confirmation和10轮readiness稳定，第10轮agent ready。Android Emulator PID `97570`持续作为excluded generic QEMU记录，没有干扰或修改该进程。
- probe只push/normalize/readback/exec各一次，exec exit 0且双流为空；marker成功读回，但10秒settle后的首次result pull为`OSStatus -2700`/文件不存在。未补拉、重跑probe或推断guest结果，host按`guest-result-readback-1-stderr-not-empty`闭合`state-indeterminate`；transaction、dpkg与receipt真实终态仍未知。
- create-new host根含67项manifest成员，SHA-256 `37058a86c830e8aed040d3ef24893c92fbd7b9ebf08820aa513546be7fe308e4`；逐项hash、私有权限、single-link、零xattr和raw operation ID absent通过。inventory/start/probe/result为`1/1/1/1`，push/pull/exec为`1/3/13`；resume、dpkg mutation、retry、repair、cleanup、stop、quit与plain status/start均为0。调用后未追加UTM查询或停止，v2 host/guest根冻结；后续先repository-only设计只读取既有result/phase的deferred resolution，真实读取与terminal stop分别授权。

## 2026-08-29：transaction延迟结果消歧仓库闭合

- `9555d46`新增67项v2结果递归binding、deferred transaction result host control及5项/7项回归并接入默认L6门禁。新attempt固定为`d75818f-v4-install-artifacts-staged-transaction-state-result-resolution-20260829-v1`，计划host根为`…-v4-Transaction-State-Result-Resolution-v1`；只允许前后source/target/process/PID/句柄门、两次既有result pull和一次既有phase pull，不inventory/list/status/start、push/exec/probe、resume/dpkg、retry/repair/cleanup或自动stop。
- 首次clean-HEAD真实链离线复核先后暴露两处successor装配语义：新transaction phase属性遮蔽历史recovery phase，以及runtime未定PID、runtime/discovery evidence format被误作同一形状。`104d6b3`分离`guest_transaction_phase_path`；`617cbae`/`5e981f2`按原v2证据固定runtime PID为空、之后14份PID均为`36343`，并区分`runtime-resolution-v2`与transaction格式。修复均增加回归，没有放宽同一378-byte handle签名、历史/current HEAD分离或失败关闭语义。
- clean `5e981f2`纯离线复核通过67项manifest `37058a86…08e4`，返回历史执行HEAD `a03edae`、当前HEAD、PID `36343`、handle SHA-256 `6ad7484b…4286`/378 bytes及probe `e706d622…d0e3`。默认L6、package transaction、startup gate和文本门禁通过；新host根仍absent。
- 本批没有调用`utmctl`、查询VM、进入guest、创建现场根、补拉result/phase、resume、dpkg、retry、repair、cleanup或stop。当前VM/guest状态未知；真实result resolution仍须按新attempt/absent root单独授权，terminal stop继续独立授权。

## 2026-08-29：真实transaction延迟结果闭合`completed`

- 用户授权只覆盖固定attempt `d75818f-v4-install-artifacts-staged-transaction-state-result-resolution-20260829-v1`、absent host根、source/target/process/PID/句柄前后门、两次既有result和一次既有phase pull；不含inventory/list/status/start、push/exec/probe、resume/dpkg、retry/repair/cleanup或自动stop。调用前clean `f66e32e`，67项冻结v2链离线binding返回历史HEAD `a03edae`、PID `36343`、handle `6ad7484b…4286`/378 bytes及probe `e706d622…d0e3`。
- 唯一控制的三次confirmation和terminal复核均保持PID `36343`与EFI/qcow2双句柄；relevant UTM process为0，Android Emulator仅作为excluded generic QEMU记录。两份1259-byte result均exit 0、stderr空、SHA-256 `a40e1079…0eec`且逐字一致；113-byte phase为`completed`。
- guest只读结果闭合`transaction=completed`、package `installed-verified`、startup `allowed`、guard `absent-after-reboot`、boot `26beda73…70e6`与receipt `fec8b23a…f73d`/3786 bytes。maintenance/resume均为0，dpkg只执行read-only audit/verify且mutation为0，product state write为0；raw operation ID未离开guest。
- create-new host根含21项manifest成员，SHA-256 `7fcef38e05b7fff0d2c0fcb661bd4957343848cd06b8f5222f9dec9ef4540e3d`；逐项hash、私有权限、single-link、零xattr及raw operation ID absent通过。terminal计数为result/phase/file pull `2/1/3`，其余UTM/guest/业务/自动补救动作均为0。调用后未追加UTM query或stop，当前状态未知；attempt/root冻结，后续先repository-only绑定21项，terminal stop另授权。

## 2026-08-29：transaction completed结果binding仓库闭合

- `7acbbde`新增21项completed result递归binding和6项回归并接入默认L6门禁。binding逐项固定manifest顺序、`0700`/`0600`、owner/link、历史/current HEAD、source/target、五份process/handle、PID `36343`与378-byte签名、双result、completed phase、parsed result、terminal及精确命令inventory；不把transport、单份result或terminal handles单独当作结果权威。
- clean `7acbbde`真实链纯离线binding返回21项、历史HEAD `f66e32e`、boot `26beda73…70e6`、result `a40e1079…0eec`/1259 bytes、receipt `fec8b23a…f73d`/3786 bytes及`transaction=completed`。6项定向测试、默认L6与文本门禁通过，未调用UTM、查询VM、进入guest、停止或改写冻结根。
- 21项结果根继续冻结；下一步只先repository-only设计terminal stop前门。任何真实query/stop、guest、resume/dpkg或补救动作仍需按新范围单独授权。

## 2026-08-29：completed terminal stop前门仓库闭合

- `177b53c`新增terminal stop请求、completed-result递归binding与host控制。固定attempt为`d75818f-v4-install-artifacts-staged-terminal-stop-20260829-v1`，计划create-new根为`…-v4-Terminal-Stop-v1`；当前根仍absent。控制只允许preflight/terminal各一次plain list、started分支当前PID/双句柄三次确认、至多一次`utmctl stop UUID --request`和最多60轮host-only静止观察；all-stopped分支不会重复stop。
- 9项fake-runner回归覆盖双授权与固定预算、真实completed/baseline绑定、single graceful stop、already-stopped零stop、其他VM started、句柄漂移、stop异常、句柄残留和backend残留。异常命令即使后置看到stopped也保持不确定；status/start、guest/file、resume/dpkg、retry/repair/cleanup、force/kill/quit均无调用。
- 默认L6、package transaction、startup gate、文本门禁通过。clean `177b53c`对全部冻结链纯离线复核返回21项manifest `7fcef38e…0e3d`、20台旧baseline、历史PID `36343`、handle `6ad7484b…4286`/378 bytes、`transaction=completed`且计划输出根absent。
- 本批没有调用`utmctl`、查询或停止VM、进入guest、创建真实输出根、resume/dpkg或补救。当前VM状态仍未知；真实terminal stop需按上述attempt、根和命令面单独授权。

## 2026-08-29：真实completed terminal stop与结果binding闭合

- 用户授权只覆盖UUID `50B75F88…8038`、固定attempt `d75818f-v4-install-artifacts-staged-terminal-stop-20260829-v1`、absent host根、preflight/terminal各一次plain list、至多一次正常`stop --request`与最多60轮host-only静止观察；明确不含status/start、guest/file、resume/dpkg、retry/repair/cleanup、force/kill/quit。调用前clean `9485778`，21项completed result、20台baseline、PID `36343`与handle `6ad7484b…4286`/378 bytes全部离线binding通过。
- 唯一preflight list观察target started、其余20台stopped；untargeted lsof、discovery及三次same-PID confirmation保持QEMULauncher与EFI/qcow2双句柄。唯一`utmctl stop UUID --request` exit 0、未超时且双流为空。第1轮quiescence相关宿主进程已为0但句柄仍present，第2轮process与target句柄均absent；唯一terminal list为21台all-stopped，terminal target句柄absent、相关宿主进程0。source descriptor/inventory与target config/EFI/qcow2 identity前后不漂移。
- host根含28项manifest成员，SHA-256 `096fe01f5f082a4f494ddf6219b14add91f178c37a6a12b8fb1df635a6d5e133`；逐项hash、`0700`/`0600`、uid 501/gid 20、single-link、零xattr及raw operation ID absent通过。terminal固定inventory `1/1`、graceful stop 1、quiescence 2、outcome `stopped-verified`；status/start、guest/file、resume/dpkg及所有自动补救计数为0。调用后没有追加UTM query、重试或现场改写。
- 外层临时适配器在控制返回并完成manifest后，尝试读取`TerminalStopResult`不存在的`manifest_path`属性而exit 1。该尾部打印缺陷发生在系统控制完成之后，不改变已持久化terminal/manifest，也没有触发第二次控制；项目结论取自逐项离线验证的冻结证据，而非包装器进程退出码。
- `ad77c22`新增28项结果递归binding与6项回归，并接入默认L6门禁。binding固定历史执行HEAD `9485778`/当前successor HEAD、completed上游、manifest顺序与私有树、source/target、7份process、PID/句柄present链、两轮quiescence到absent、started/stopped双inventory、唯一stop、terminal及18条精确argv；额外`utmctl`或任一漂移均拒绝。clean successor HEAD对全部真实冻结链纯离线复核返回28项、PID/handle、21台`started→stopped`和`stopped-verified`。
- 6项定向测试、默认L6、package transaction、startup gate、文本检查与`git diff --check`通过；文档门禁和完整仓库门禁在本批文档完成后复验。第二个`install_artifacts_staged` crash的checkpoint、只读transaction completed、正常停止及结果binding至此闭合并冻结。下一步先repository-only设计第三个`upgrade quiesced`的独立clone/relationship/checkpoint/resume合同；真实clone、start、crash mutation、resume与stop继续拆分授权。

## 2026-08-29：`upgrade quiesced`独立case合同仓库闭合

- 只读复核冻结`S2-source-data-512e8ab-0c2cefd6`的local evidence与config/EFI/qcow2身份，确认其仍是未注册、source `38-1` completed、公开合成XDG `f3df287f…b86b`且upgrade未开始的不可变起点；同时对照committed source chain anchor与冻结d75818f handoff，固定source package `09ed1228…bec`、target package `4dd00540…cec`及record `c74fac12…849`。没有调用UTM、注册/创建clone、进入guest或改写任何冻结资产。
- `9ec3218`新增case JSON、严格validator与6项回归，交叉绑定L6 matrix、source chain anchor、既有canonical input identity与typed guest checkpoint。合同拒绝复用旧crash clone，要求clone前全停/源盘零句柄/目标absent/单VM，并把真实动作拆为clone、start、input-preflight、crash、resume、terminal-stop六个独立授权阶段。
- `upgrade_quiesced` checkpoint固定upgrade/target_newer、operation chain 2、source/target双staging、source仍installed、dpkg status/log未变、合法未锁guard、双proof absent、`ActiveGuard`与XDG零漂移；resume必须重新验证staged relationship、取得fresh quiescence permit并仅apply target一次。新增compile-isolated Rust回归从source completed进入quiesced，模拟worker退出后用fresh port闭合completed。
- 11项guest合同、6项case合同、完整L6合同、126项checkpoint feature、package transaction、startup gate、L6 controller、格式、文本与diff检查通过；这些都是repository-only证据，不代表第三个case已经clone、启动、crash或resume。下一步只先实现不可变S2到独立stopped target的repository-only clone前门；任何真实clone及后续五段动作继续分别授权。

## 2026-08-29：`upgrade quiesced`独立clone前门仓库闭合

- `8e55189`新增专用clone bindings、host前门和10项fake-runner回归，并把case合同扩展为严格`clone_front_door`策略。binding要求clean committed HEAD、精确S2/case/control身份和仅含`registration-shell.json`的manifest；壳体必须已注册、全停、零句柄、registration-only、未复用guest或source terminal且`Network=[]`，冻结crash/transaction terminal明确不可复用。
- host前门先验证canonical全停inventory、target absent、S2与壳体位于同一APFS设备及三域零句柄，再只调用一次`utmctl clone`。clone后的config只能在Name/UUID上区别于专用壳，随后仅用两次`/bin/cp -c`从S2物化EFI/qcow2到target-local incoming、逐项hash并各一次`/bin/mv -f`替换；source config不复制，terminal再次绑定source/壳/target身份、双重qcow2 hash、零句柄和未变全停registry。
- clone前拒绝保持target absent；clone后任何部分替换、identity/config/inventory漂移或命令异常均形成`state-indeterminate`，不自动delete、retry、rollback或start。10项新前门、7项case、默认L6、126项checkpoint、10项controller、119项package transaction、25项startup及完整`check-repo`均通过；只出现既存runbook行数预算警告。
- 本批只读取冻结S2与历史控制用于设计，没有调用真实`utmctl`、创建/注册专用壳或target、进入guest、执行transaction或改写冻结资产。真实clone前必须先另行设计、授权并冻结当次专用registration shell证据；clone与后续start、input-preflight、crash、resume、terminal-stop仍按六段分别授权。

## 2026-08-29：日终24提交回顾与8月30日入口

- 今日从`202b64d`至`9905bba`共完成24个提交，其中12个代码/测试提交、12个文档/治理提交，涉及43个唯一文件。代码主线依次闭合跨重启prior/current boot分离、all-stopped只读transaction观察、非UTM QEMU排除、延迟结果双读、真实`completed`结果binding、正常terminal stop及结果binding，以及第三个`upgrade_quiesced` case与repository-only clone前门。
- 实机主线没有用旧resume的`state-indeterminate`倒推成功；独立只读双结果和completed phase才成为transaction权威，随后独立授权的一次正常stop与host交叉检查才成为VM终态权威。第二个case至此冻结闭合；第三个case只完成仓库合同，未创建专用注册壳、真实target或执行六段系统动作。
- 治理提交`5c728a5`把串行普通开发固定为直接在`dev`推进，仅在外部贡献、并行写入、风险隔离或明确评审需求时使用主题分支/PR/worktree；根入口、贡献指南、ADR与ruleset口径一致。日终复核补充`docs/agent-collaboration.md`的同一稳定细则。
- 按今日代码复核`current`、路线图、Linux安装维护Boundary、L6 runbook、仓库结构与Linux packaging入口：runbook与目录导航已覆盖全部控制；本次补齐专用registration-only壳、不可变S2和APFS双磁盘物化的稳定Boundary，并更新落后的阶段摘要。技术方案、根README、隐私/同步、FFI、Engine、依赖和许可证没有发生边界变化，无需更新。
- 8月30日先在repository-only范围固定专用registration shell的准备、单成员manifest与冻结证据合同，不调用UTM。合同和门禁闭合后，再提交精确壳体创建/注册方案请求独立系统授权；壳体证据闭合后才为真实clone另行授权，后续start、input-preflight、crash、resume与terminal-stop继续逐段授权。
- 日终文档门禁`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check`和完整`./scripts/check-repo.sh`全部通过；仅保留既存的L6 runbook 664行与sync-server API/storage文档508行预算警告。本次日终收口没有调用UTM、进入guest、执行系统动作、修改远端或推送。

## 2026-08-30：`upgrade quiesced`专用registration shell控制仓库闭合

- `2f91d2a`新增专用registration shell bindings、committed AppleScript、创建/冻结控制与10项fake-runner回归，并让clone前门复用同一单成员壳体证据schema。binding固定clean committed HEAD、精确case/S2、控制与AppleScript源码、UTM `4.7.5` build `118`、bundle/SDEF identity及create API；计划壳体固定为QEMU `aarch64`/`virt`、4096 MiB、UEFI/Hypervisor、单个1024 MiB VirtIO空盘、VirtIO GPU、`Network=[]`、registration-only且never started。
- 控制把完整control root与独立shell evidence root分离，要求两者及壳体package均absent、S2不变/同device/零句柄，并由请求绑定precreate全停inventory数量与hash。授权命令面只包含precreate/postcreate/terminal三次plain `utmctl list`与一次committed AppleScript `make new virtual machine`；创建后只接受新增一个stopped注册项、精确bundle/UUID、S2与壳体零句柄和terminal inventory不漂移，成功manifest只含`registration-shell.json`。
- create前拒绝保持absent；create后package仍absent固定为`failed-closed-absent`，任何部分创建、identity/config/句柄/inventory漂移或命令异常均保持`state-indeterminate`，不自动delete、retry、clone、start、进入guest或执行transaction。10项定向测试、默认L6、package transaction、startup gate、controller、文本检查与完整`check-repo`通过，仅保留既存文档行数预算警告。
- 本批没有调用真实创建控制、`utmctl`、`osascript`或UTM，没有创建/注册壳体或target，也没有进入guest、改写冻结资产、执行系统动作、修改远端或推送。下一步先以当次clean HEAD、精确absent路径、实际全停inventory及冻结S2请求壳体阶段独立系统授权；壳体单成员manifest闭合后才为真实clone另行授权，后续start、input-preflight、crash、resume与terminal-stop继续逐段授权。

## 2026-08-30：registration shell v1真实创建失败关闭与类型修复

- 用户独立授权后，从clean `346191d`执行唯一attempt `d75818f-upgrade-quiesced-registration-shell-20260830-v1`。控制先绑定UTM `4.7.5` build `118`、不可变S2、三个absent路径、source零句柄/同device及21台全停inventory `d328e2cd…4f19`；随后只调用一次committed AppleScript create。
- create未超时但exit 1，stderr为AppleScript `-1700`：先赋值给变量的普通record不能转换为UTM `qemu configuration`。唯一postcreate list仍为相同21台全停，目标package与shell evidence均absent；控制据此闭合`failed-closed-absent`。14项manifest SHA-256为`3e21aa0265e02f009c8637a6b1867e653bf266c47bcecf71770c167bd87e3c34`，逐项复核通过；没有delete、retry、clone、start、guest、operation或transaction。
- `10cc4e1`依据本机SDEF和UTM官方create示例把configuration record直接内联到`make ... with properties`，并在静态回归中拒绝中间`set shellConfiguration`。10项定向、默认L6、package transaction、startup gate、controller、文本与diff门禁通过；`osacompile`在当前沙盒环境仍无法解析UTM application id，因此不作为成功证据。
- v1 control root立即冻结，不覆盖或复用；修复后尚未执行第二次create或追加UTM查询。下一次只可使用新attempt `d75818f-upgrade-quiesced-registration-shell-20260830-v2`、absent `…-Control-v2`/`…-Evidence-v2`并重新取得一次性创建授权；真实clone及后续五段仍分别授权。

## 2026-08-30：registration shell v2再次失败关闭与显式record class修复

- 用户独立授权v2后，从clean `d0696b4`执行唯一attempt `d75818f-upgrade-quiesced-registration-shell-20260830-v2`。binding重新固定UTM `4.7.5` build `118`、不可变S2、21台全停inventory `d328e2cd…4f19`、三个absent路径及内联AppleScript；source零句柄与同device门通过。
- 唯一create未超时但exit 1，stderr仍为`-1700`：内联configuration仍作为普通record，不能转换为`qemu configuration`或`apple configuration`。唯一postcreate list不变，目标package与shell evidence均absent；14项manifest SHA-256为`3c9c7ee8d7e14bc7d6b92c629982963ef5e8d19f077b0387e3a266d117e3f604`并逐项通过，控制闭合`failed-closed-absent`。没有clone、start、delete、retry、guest、operation、transaction或追加UTM查询。
- 离线对照UTM 4.7.5 SDEF、官方create/configuration示例及对应Swift实现后，确认create在进入字段更新逻辑前要求configuration为专用record type。`1b94b5f`保留一次`make`和既有禁止面，在record中加入`class:qemu configuration`；完整`osacompile`成功并反编译为SDEF `QeCf`，10项定向、默认L6、controller 126项、package 119项与startup 25项门禁通过。
- v1/v2现场均冻结且有效壳体仍不存在。修正后的真实create未执行；下一次只可使用新attempt `d75818f-upgrade-quiesced-registration-shell-20260830-v3`、absent `…-Control-v3`/`…-Evidence-v3`与同一absent package，并重新取得独立授权。真实clone及后续五段继续关闭。

## 2026-08-30：registration shell v3显式class仍失败关闭

- 用户独立授权后，从clean `9603ab6`执行唯一attempt `d75818f-upgrade-quiesced-registration-shell-20260830-v3`。binding固定S2、UTM `4.7.5` build `118`/SDEF、显式class AppleScript SHA-256 `4aad56cb…04dd`、21台全停inventory `d328e2cd…4f19`及control/evidence/package三个absent路径；source零句柄和同device门通过。
- 唯一create未超时、exit 1，stderr仍为`-1700`且错误record明确含`class:qemu configuration`。唯一postcreate list与precreate相同，目标package/evidence absent，故闭合`failed-closed-absent`；14项manifest SHA-256为`ae3fb81bfc4fd0b790f1d7254da8f48c191c575544d4466b9fe1619addb4a511`，逐项hash、`0700`/`0600`、owner `501:20`、single-link与零xattr通过。没有clone、start、delete、retry、guest、operation、transaction或追加UTM查询。
- v3证明显式record class仍不足，不再用真实attempt逐字段试探。下一设计若采用UTM官方“最小create后读取typed configuration并在stopped状态update”的路径，会新增一次configuration update，且update失败可能留下带默认shared network的stopped部分壳体；因此v4尚未定义或授权，须先批准动作面与失败风险，再实现repository-only控制和回归。真实v4、clone及后续五段继续关闭。

## 2026-08-30：registration shell两阶段控制仓库闭合

- 用户批准repository-only实现后，`17d10a6`把control evidence升为v2，并将committed AppleScript拆为显式`create`/`update`两模式：先按官方最小QEMU形状create，controller确认唯一新增stopped壳体、package和默认配置后，才以UUID读取typed configuration并执行最多一次stopped update。最终合同仍固定4096 MiB、UEFI/Hypervisor、单个1024 MiB VirtIO盘、VirtIO GPU、`Network=[]`、零串口与never-started语义。
- 控制新增独立update授权、timeout/调用计数、postcreate默认配置与postupdate inventory/package/bundle证据；create后任何异常均为`state-indeterminate`。update失败会保留并记录可能带默认shared network的stopped部分壳体，禁止自动delete/retry/start/clone；update还必须保持EFI/qcow2身份，网络、磁盘、package、handle或inventory漂移均拒绝冻结shell evidence。
- 13项定向回归、L6与controller门禁通过；完整`osacompile`/`osadecompile`固定一次`make`、typed `CoFg`、`NtIf=[]`/SDEF `SrPt=[]`与一次`UTMcUpDt`，无start/delete/duplicate。transport SHA-256为`4753be7e…6b02f`；clean `17d10a6`只读binding固定UTM `4.7.5` build `118`、SDEF `b4fd52c…928b3`及S2。本批没有调用AppleScript、`utmctl`或创建v4。
- v4候选attempt为`d75818f-upgrade-quiesced-registration-shell-20260830-v4`，新control/evidence根及既定`.utm` package均位于`/Users/luobo/VirtualMachines/`且只读确认absent。真实调用仍须重绑最终clean HEAD与实际全停inventory，并另行取得覆盖一次minimal create、一次条件式stopped update及四次plain list的系统授权；真实clone及后续五段继续关闭。

## 2026-08-30：registration shell v4创建到默认目录并失败关闭

- 用户授权只覆盖固定v4 attempt、`/Users/luobo/VirtualMachines/`下三个absent路径、一次minimal create、一次条件式stopped update、至多四次plain list及禁止自动补救；不含start、clone、delete、retry、guest、transaction或修改UTM设置。调用前HEAD `7ab855b` clean，UTM `4.7.5` build `118`、SDEF、S2零句柄/同device和21台全停inventory `d328e2cd…4f19`均重新binding通过。
- 唯一create exit 0、未超时、stderr为空并返回UUID `0BAA7355-A55A-463E-97FE-82A785E252A7`；唯一postcreate list观察22台全stopped、inventory `1074717c…714f`。但授权目录内预期`.utm` package仍absent，控制没有执行update，以`registration-shell-package-postcreate:registration-shell-package-not-created`闭合`state-indeterminate`。14项control manifest SHA-256为`70251467e0dc514347426e95f7374272e47da91edc038b10267d97d187a0fb04`并逐项通过；create/update/list为`1/0/2`，shell evidence、clone/start/delete/retry、guest/file、operation/transaction均为0，调用后没有追加UTM query。
- 后续只用文件系统离线定位到UTM应用默认`Documents`中的同名bundle。`config.plist`绑定同一名称/UUID，但仍是512 MiB、shared network、PTY串口、空display和1 GiB稀疏VirtIO盘的最小默认配置；config/qcow2/EFI为`711db7f1…6479`/`ae44c4d0…b94b`/`7b0a7f26…00d5`，owner `501:20`、regular文件`0644`/single-link且三文件均无打开句柄。该观察不属于原14项manifest，也没有补写冻结根。
- 项目所有者要求新UTM虚拟机位于`/Users/luobo/VirtualMachines/`，因此默认目录部分壳不满足位置合同，不能作为registration shell成功证据。v4 control、UUID及部分壳现冻结，不update、move、delete、start、clone、retry、补写evidence或追加query。下一步只做repository-only位置路径设计；任何迁移、清理、重新create、UTM全局设置修改或系统查询都需重新说明边界并独立授权。
- 只读对照UTM `v4.7.5`官方源码和本机SDEF确认：create/clone固定落默认`Documents`，受支持的`UTMData.move(vm:to:)`会复制到新位置、更新registry并删除旧bundle，但没有AppleScript/`utmctl`入口；scripted export只复制，scripted import又复制回默认目录。下一设计因此固定UTM UI原生Move为唯一候选迁移主路径，并以repository-only控制绑定move前后UUID、路径互斥、三文件身份、registry和零句柄；本批没有执行GUI、move、update或新query。

## 2026-08-30：registration shell原生Move恢复控制仓库闭合

- `9300ed7`新增原生Move恢复bindings、prepare/complete控制与8项fake-runner回归，并把case位置合同固定为operator asset root、默认storage部分壳不可作为clone source、UTM原生UI Move先于configuration update。控制递归绑定v4 14项manifest、历史HEAD/UUID、22台全停inventory `1074717c…714f`和默认部分壳精确config/EFI/qcow2身份。
- `prepare-move-once`要求目标package、新shell evidence和输出根absent，重验S2/default/目标父目录同device与零句柄，仅允许一次plain list；通过后形成15项`move-ready` manifest，但自身不调用GUI、Move或update。UTM UI Move保持独立外部系统动作，不用手工搬bundle、export/import、delete/recreate或全局storage修改替代。
- `complete-move-once`只接管已由UI迁至授权路径的同一部分壳：默认路径必须absent，外部config/EFI/qcow2必须与v4完全一致，prepare manifest、全停inventory和零句柄均重验后才执行一次typed update。第二次list、最终`Network=[]`、磁盘身份不变与terminal检查通过后，才写新`Evidence-v5`单成员manifest和20项complete manifest；任何异常都保留`state-indeterminate`且无delete/retry/rollback/start/clone/guest/transaction。
- 8项新回归、既有13项registration shell、10项clone前门和完整`./scripts/check-linux-l6-contract.sh`通过；case validator新增默认storage拒绝测试。本批只实施和提交仓库控制，没有调用真实`utmctl`、AppleScript、GUI、UTM Move或update，没有改写v4部分壳、冻结证据、真实target或guest。
- 下一步先以新prepare attempt/root单独授权一次plain list和host只读检查；其闭合`move-ready`后，再分别授权一次UTM UI Move与complete的两次list/一次stopped update。壳体manifest形成前，真实clone及后续start、input-preflight、crash、resume、terminal-stop继续关闭。
- 首次真实prepare调用因操作参数中的完整expected HEAD误写，在`binding-preflight`以`repository-head-drift`失败关闭；实际clean HEAD为`fe8b4b6ea3cd…fe93`。v1根只形成request/terminal两项manifest `4d32f219…2750`，list/Move/update均为0，权限与hash离线复核通过并冻结。下一次必须使用新v2 attempt/root、重新取得授权并复制届时精确HEAD，不覆盖或重试v1。
- 用户重新授权后，从clean `f7ac982`执行唯一v2 prepare。冻结v4/S2/UTM/default部分壳、三个absent路径、同device和两轮零句柄均通过；唯一plain list闭合22台全stopped及inventory `1074717c…714f`，source/default终态不漂移。15项manifest `dc6b0d01…738e`通过递归语义、逐项hash和`0700`/`0600`权限复核；terminal为`move-ready`，list/Move/update为`1/0/0`，无start/clone/delete/retry/guest/transaction或追加query。v2根冻结，下一步必须另行授权一次UTM原生UI Move，complete仍独立授权。

## 2026-08-30：registration shell唯一UTM UI Move完成

- 项目所有者独立授权精确stopped壳体的唯一一次UTM UI原生Move后，通过Computer Use只操作UTM界面：选择同名VM、点击`Move`，确认UTM的复制新位置/删除原位置/创建快捷方式提示，并在原生位置选择器中保存到`/Users/luobo/VirtualMachines/RadishLex-L6-Registration-Shell-d75818f-Upgrade-Quiesced-v1.utm`。没有出现覆盖提示；返回主界面后，同名VM仍为stopped且显示该外部路径。
- 该UI批次未调用`utmctl`、typed update、start、clone、额外delete、retry、guest或transaction。随后只用文件系统离线复核：默认`Documents` package已absent，外部package存在；config/EFI/qcow2尺寸为2631/329216/196624 bytes，SHA-256仍为`711db7f1…6479`/`7b0a7f26…00d5`/`ae44c4d0…b94b`，与prepare冻结身份一致。
- Move只闭合位置迁移，不生成或补写`Evidence-v5`，也不证明最终4096 MiB/`Network=[]`壳体合同。下一步仍须单独授权`complete-move-once`的两次list与一次条件式stopped typed update；真实clone及后续五段继续关闭，异常不自动重试、回滚或清理。

## 2026-08-30：registration shell complete v1首门失败与修复

- 项目所有者授权complete后，从clean `cbba328`执行唯一v1 attempt。控制在`binding-preflight:prepare-evidence-semantic-drift`闭合`state-indeterminate`，原因是实现错误地把历史prepare HEAD `f7ac982`与当前complete HEAD直接比较；尚未进入路径、清单或update阶段，inventory query/update均为0，也未start、clone、delete/retry、进入guest或transaction。
- create-new v1根只含request/terminal两项，manifest `1fd1dc94…ceb7`逐项hash通过；根/文件为`0700`/`0600`、owner `501:20`、single-link。该根冻结且不重试；外部package未被本调用修改，`Evidence-v5`和新v2根均absent。
- `9da8d0f`改为prepare请求HEAD与其冻结binding HEAD内部一致，complete仍绑定自身clean HEAD；跨提交回归、8项定向和完整L6合同通过。本批没有追加UTM调用。下一真实候选固定新v2 attempt/root，须绑定最终clean HEAD并重新授权，不能沿用v1授权。

## 2026-08-30：registration shell complete v2闭合`frozen`

- 项目所有者重新授权后，从clean `e6968bf`执行唯一v2 attempt。v4/prepare/S2/UTM、默认路径absent、外部部分壳身份、同device和零句柄均通过；pre/post两次list均为22台全stopped及inventory `1074717c…714f`。唯一typed update exit 0且只返回精确UUID，最终4096 MiB、VirtIO GPU、`Network=[]`、零串口与单VirtIO disk合同通过，EFI/qcow2不变。
- terminal为`frozen`，list/update为`2/1`，无start、clone、delete/retry/rollback、guest或transaction。20项control manifest `91778f48…c438`与单成员壳体manifest `c37b552e…f715`逐项hash、`0700`/`0600`、owner `501:20`、single-link通过；默认路径absent，外部package和`Evidence-v5`现冻结。
- registration shell阶段至此闭合。下一步只能另行授权clone前门：从专用壳生成新stopped target，并仅物化不可变S2的EFI/qcow2；clone失败不自动delete/retry/rollback/start，后续start、input-preflight、crash、resume与terminal-stop继续关闭。

## 2026-08-30：P05B收敛与本地UTM资产生命周期冻结

- 项目所有者明确要求停止长期扩张的Linux实机测试，并授权调整P05B退出口径、编写23个VM bundle/7个snapshot账本和安全清理设计；该授权不含真实UTM query、clone、start、delete、注销、snapshot隔离/purge或guest动作。
- 基于complete v2已冻结清单离线审计：22台注册VM全部stopped，另有1个故意未注册的DependencyFrozen bundle；23个bundle账面`du`合计220.08 GiB，7个snapshot合计65.25 GiB。APFS clone共享块使账面和可回收空间不同，Data volume仍约有527 GiB可用，当前问题是资产生命周期和退出口径而非容量事故。
- 八个crash scenario继续保留完整matrix、compile-isolated controller、checkpoint/resume、fake-runner与canonical evidence合同；真实系统阻塞样本收敛为已完成的`install_prepared`、`install_artifacts_staged`和待完成的`upgrade_quiesced`。其余五个真实crash转为后续hardening，不阻塞P05B/M5，但不得写成真实通过。
- P05B剩余主门禁固定为：资产先收敛；随后只建立一个`upgrade_quiesced` disposable target并在闭合后退休；最后用一台新guest、同一release pair与连续session跑完六类operation、dependency/font、startup、XDG、断网、process/package-manager lifecycle和reboot。P05C仍用另一台独立guest。
- 新增`docs/runbooks/linux-l6-asset-lifecycle.md`。17个旧注册VM列为“证据归档后删除候选”，账面157.60 GiB；P04、CleanBase、通用builder、pair builder和registration shell为5个注册锚点，DependencyFrozen继续未注册保留。5个旧snapshot列为隔离后删除候选，账面46.39 GiB；S2/S3保留。
- 未来控制器拆为repository-only实现、只读prepare、VM mutation、snapshot quarantine和snapshot purge五段。allowlist固定精确UUID/绝对路径/manifest/hash；VM每批最多4台、snapshot每批最多2个；每台仅一次plain`utmctl delete <exact-uuid>`并逐台验证精确inventory delta，任何矛盾停止且不retry/rollback。当前没有可靠unregister-only路径，不能把delete表述为归档。
- 本批只读取冻结inventory、bundle config、`du`和`df`并修改仓库文档；没有调用UTM、改写虚拟机/快照/证据、实现清理控制器、执行系统动作、修改远端或推送。实际repository-only控制实现、只读prepare及任何删除仍分别需要后续范围或系统授权。

## 2026-08-30：首批资产退休只读prepare控制仓库闭合

- 项目所有者授权先实现repository-only控制，再对精确首批4台执行独立只读prepare；删除继续关闭。committed allowlist `first-four-v1`固定两处绝对storage root、4个UUID/name/path、config/EFI/qcow2身份、7个既存证据锚点与37.71 GiB账面范围，不使用glob、名称前缀或目录扫描扩面。
- 新控制绑定clean committed HEAD、allowlist digest与absent输出根；逐项验证证据hash/权限/语义、bundle owner/mode/link/plist和两轮完整磁盘hash，只允许一次plain`utmctl list`与两轮精确`lsof`。所有注册项必须为stopped、22成员canonical inventory及4台注册身份必须一致；任一漂移闭合`precondition-rejected`。
- 控制没有delete/start/clone/move/guest命令面，terminal固定mutation count为0。7项fake-runner覆盖prepared、运行中inventory、打开句柄、轮间磁盘漂移、证据错绑、四个授权缺失和allowlist拒绝，并接入`check-linux-l6-contract.sh`；默认L6门禁通过。
- 离线验证4台的7个历史锚点均满足固定hash、`0600`/`0644`和语义合同，没有补拉raw operation ID、receipt或dpkg log，也没有倒造首套/第二套不存在的terminal。repository-only实现提交为`4a34ae3`，完整L6与仓库基线、文档、文本及diff门禁通过，只保留既有两项长文档预算提醒。
- 随后从clean `4a34ae3`执行唯一attempt `l6-asset-retirement-prepare-20260830-v1`。唯一plain list返回22台全stopped、名称唯一和canonical inventory `1074717c…714f`；4台UUID/name与allowlist一致。两轮精确`lsof`均exit 1且stdout/stderr为空，表示12个关键文件零打开句柄；两轮完整config/EFI/qcow2 hash与size逐项一致。
- terminal闭合`prepared`，inventory/handle/asset-hash轮数为`1/2/2`，delete/start/clone/move/guest为`0/0/0/0/0`。create-new根`RadishLex-L6-Asset-Retirement-Prepare-20260830-v1`为`0700`，9个证据成员为`0600`/single-link，manifest SHA-256 `68ce05b012fa5f821e9d5c47835830123777a08c0ba05c1ea2a70bd6e0d4f44e`逐项复核通过。
- 真实prepare没有启动、移动、克隆、注销、删除或进入任何VM，没有追加UTM query。该根现冻结且不能跨HEAD、inventory或asset identity复用；下一步须向项目所有者列出4台精确路径/UUID/账面影响、每台唯一delete调用和不可恢复影响并取得另一份授权，不能从prepare授权推导删除权限。

## 2026-08-30：首批资产退休删除控制仓库闭合与独立授权

- 项目所有者在看到4台精确UUID/路径、37.71 GiB账面影响、不可恢复bundle移除和APFS实际回收不确定性后，明确回复“授权执行 first-four-v1 VM 删除”。该授权只覆盖此批每台至多一次plain`utmctl delete <exact-uuid>`、必要的pre/post inventory与host只读检查；不覆盖其他VM/snapshot、retry、rollback、start、clone、move、guest或补救清理。
- 新删除控制递归绑定prepare的9项manifest和内部HEAD/allowlist/inventory/identity/handle语义，并在mutation前以当前clean committed HEAD重新验证7个锚点、两轮完整config/EFI/qcow2 hash、一次22台全停list和两轮零句柄。逐台delete后只允许一次list，只有精确移除该UUID、其余成员不变且全stopped、对应package absent和命令clean success同时成立才继续。
- 7项fake-runner覆盖完整删除、命令失败、registry/package单边消失、运行中inventory、四个破坏性授权缺失和旧prepare递归绑定/额外成员拒绝；任一delete调用后的异常均为`state-indeterminate`并停止，自动retry/rollback为0。真实prepare的9项manifest再次离线验签通过；本仓库阶段尚未调用新的`utmctl`、`lsof`或delete，也未改写任何VM。
- 删除控制提交为`93abcb0`；定向回归、默认L6、文档/文本/diff与完整repository基线通过，只保留既有两项长文档预算提醒。随后从clean `93abcb0`执行唯一attempt `l6-asset-retirement-delete-20260830-v1`，mutation前重新闭合7个锚点、两轮完整config/EFI/qcow2 hash、22台全停inventory `1074717c…714f`与两轮零句柄。
- 4次命令依次精确为`utmctl delete 6F73F6DC…1195`、`EBF12F50…AE2A`、`A3F757B1…0AA1`、`A3022255…3107`，均exit 0、未超时且stdout/stderr为空；每次唯一post-list分别为21/20/19/18台全stopped，除当次UUID外其余成员逐项不变。对应4个bundle均由`lstat`确认absent。
- terminal闭合`deleted`，inventory/delete/handle/hash轮数为`5/4/2/2`，deleted asset顺序与allowlist一致，retry/rollback/start/clone/move/guest均为0。create-new根`RadishLex-L6-Asset-Retirement-Delete-20260830-v1`为`0700`，21个证据成员为`0600`/single-link，manifest SHA-256 `d6369fb297a53213537947b5c168aa68c8af4d97e588f4ffaed09b62e36d0449`逐项复核通过。
- 首批账面37.71 GiB已从注册表与bundle路径移除，但APFS clone共享块使实际回收量不能由账面值承诺。旧prepare/delete、S0/handoff/repair证据继续保留；当前注册项为18台、物理bundle为19个（含1个故意未注册源），其余13个VM候选和全部snapshot仍未获本批授权。

## 2026-08-30：日终24提交回顾、文档审阅与8月31日入口

日终提交前逐笔回顾了当日24项推进提交：

1. `2f91d2a` `feat(linux): 固定 upgrade quiesced 注册壳控制`
2. `346191d` `docs(linux): 记录 upgrade quiesced 注册壳控制`
3. `10cc4e1` `fix(linux): 内联 UTM 注册壳配置`
4. `d0696b4` `docs(linux): 冻结注册壳首次失败现场`
5. `1b94b5f` `fix(linux): 显式绑定 UTM 注册壳配置类型`
6. `9603ab6` `docs(linux): 冻结注册壳 v2 失败现场`
7. `8104a2b` `docs(linux): 冻结注册壳 v3 失败现场`
8. `17d10a6` `fix(linux): 分离注册壳创建与配置`
9. `7ab855b` `docs(linux): 固定注册壳 v4 授权边界`
10. `f1281fc` `docs(linux): 冻结注册壳 v4 位置偏差`
11. `9300ed7` `feat(linux): add registration shell move recovery`
12. `03e3f57` `docs(linux): define registration shell move recovery`
13. `fe8b4b6` `fix(linux): keep move authorization external`
14. `f7ac982` `docs(linux): freeze rejected shell move prepare`
15. `0a5733e` `docs(linux): freeze move-ready shell prepare`
16. `cbba328` `docs(linux): record registration shell UI move`
17. `9da8d0f` `fix(linux): bind move stages to their own heads`
18. `e6968bf` `docs(linux): freeze failed move completion`
19. `b674c0f` `docs(linux): freeze registration shell completion`
20. `055e844` `docs(linux): 收敛 P05B 实机范围与资产生命周期`
21. `4a34ae3` `feat(linux): 添加 L6 资产退休只读准备控制`
22. `3430b9e` `docs(linux): 冻结首批资产退休准备结果`
23. `93abcb0` `feat(linux): 添加 L6 资产退休精确删除控制`
24. `1a9ae97` `docs(linux): 冻结首批 VM 退休结果`

- 代码—文档交叉审阅确认两条实现链均有对应真相源：registration shell的创建、typed update、原生Move恢复及跨阶段HEAD binding进入Linux安装维护Boundary与L6 runbook；资产allowlist、只读prepare、精确delete、失败关闭和授权边界进入资产生命周期账本、L6 runbook与`current`。`docs/README.md`已提供资产账本入口，路线图已固定三项代表crash与连续L6退出口径，无需再改。
- 审阅同时发现并修正四类滞后：`packaging/linux/README.md`仍写壳体尚未重试，Linux Fcitx5 Boundary和仓库成熟度仍写“其余七个crash”，L6主runbook仍把`first-four-v1`四台写成已注册，资产收敛文字仍停在22台。现统一为专用壳`frozen`、前两个代表crash闭合、其余五个转hardening、注册项22→18且剩余13个候选。
- 今日没有改变Engine、FFI ABI、安装事务、隐私/同步、许可证、依赖、目录职责或平台接入原则，因此根README、技术方案、FFI、crypto/sync、Agent协作入口和稳定安装事务语义无需更新。日终批次不调用UTM、不查询或改写VM/快照，不创建target，也不沿用任何实机授权。

## 明日事项（2026-08-31）

1. 先在repository-only范围复核剩余13个旧注册VM候选，固定不超过4台的`second-batch-v1`allowlist及对应既存证据；不调用UTM。
2. allowlist与fake-runner门禁闭合后，以新attempt、absent根和届时18成员inventory执行独立只读prepare；`first-four-v1`的HEAD、inventory、根和授权均不复用。
3. prepare通过后再列出精确UUID、绝对路径、账面影响和不可恢复风险，请求新的删除授权；逐台清理UTM注册列表并核对单成员delta，分批向5个注册锚点收敛，不使用GUI批量删除、名称匹配或自动重试。
4. 注册列表收敛前暂停`upgrade_quiesced` clone；snapshot继续冻结，quarantine与purge分别设计、prepare和授权。收敛后只创建一个第三场景target，闭合并退休后再建立一台连续L6 guest；P05C保持独立guest。

## 2026-08-31：第二批 VM 退休候选仓库闭合

- `a2c7f07`在repository-only范围新增`second-batch-v1`，精确固定`FD24ADFF-B160-46F0-B100-10BAAF17C056`、`B0B826F6-D7D3-433C-8987-E2D6993A87B3`、`5B19AEF1-0F29-40B6-8F24-1B1929117DAB`和`BE3579E0-B150-438D-ABE0-53A8D46137F8`，账面37.62 GiB。选择避开P04、CleanBase、DependencyFrozen、builder、registration shell、S2/S3和当前completion anchors；目标仍只是候选，不授权系统动作。
- allowlist固定4个bundle的UUID/name/绝对path、config/EFI/qcow2身份及7个既存证据锚点；新增语义分别验证旧`install_prepared`失败关闭、UTM单次/双次start失败关闭、冻结磁盘身份和maintenance repair completed-noop终态。加载器现要求asset ID/path/UUID/name全局唯一、每个asset恰好属于一个batch，并拒绝跨batch复用。
- 13项prepare回归、7项delete回归、默认`check-linux-l6-contract.sh`和完整`check-repo.sh`通过；JSON及`git diff --check`通过。另以纯文件系统离线方式验证7个真实锚点和4个真实bundle身份，未创建输出根、未读取真实inventory或执行句柄控制。
- 本批未调用UTM、未启动guest、未执行prepare或delete。删除控制器继续硬锁已完成的`first-four-v1`；下一系统动作必须重新列出新attempt、absent根、18成员inventory预期与只读影响后取得独立prepare授权，prepare通过后删除仍需再次授权。
