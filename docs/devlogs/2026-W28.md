# 2026-W28 开发日志

本文档记录 2026-W28 周内 RadishLex 的阶段性推进事实、验证证据和后续建议，读者是项目维护者和后续 AI / 人工协作者。本文不承载长期架构设计、接口字段完整参考或历史推演；长期边界应回写到对应专题文档。

## 本周目标

- 继续推进 Phase 4 Flutter manager 同步入口的非上传产品化链路。
- 以本地 Docker / 本地 HTTPS、readiness 安全摘要和现有 fixture 为联调输入，固定恢复码 / 设备授权真实交互前的状态解释、错误分类、诊断脱敏和测试证据。
- 保持真实远端同步、恢复码生成 / 输入、join request 创建、授权成功和设备撤销路径关闭，直到平台私钥 backend、恢复 / 授权实现测试和发布级部署证据齐备。

## 2026-07-06

完成情况：

- 补齐 `manager_sync_readiness.v1` bridge 样本 fixture，覆盖 ready、部分阻塞和不安全输入。
- settings 页支持导入安全 readiness 摘要到当前 manager 内存态，并驱动设置页、同步页和诊断报告派生字段。
- 把 readiness 摘要导入校验收敛到 Dart mapper helper，固定合法样本、不支持格式、不安全 redaction 和未知字段降级语义。
- 补 action helper 覆盖，确认 settings draft 保存后不会丢失已导入 readiness，导入态诊断导出使用当前 snapshot。
- 补 `manager_sync_readiness.v1` 场景目录，覆盖 ready 但当前阶段关闭、恢复记录缺失、join request 过期、平台 backend 阻塞、部署证据不足、未知 native 状态降级、unsupported format 和 unsafe redaction 拒绝，并由 model 测试遍历复验派生结果。
- 将同一份 readiness 场景目录接入诊断报告字段回归和同步页代表场景可见层回归，确认 settings / sync / diagnostics 使用一致的 source、blocked flows、issue codes、next evidence、interaction plan、user sync enabled 和脱敏边界。
- 补 readiness envelope schema / hygiene 回归和 settings gate preview 代表场景回归，把 `manager_sync_readiness.v1` 固化为 bridge 接线前验收包；该验收包仍只校验非敏感摘要、降级规则和可见层一致性，不新增 `ManagerBridge` contract 或 C ABI。
- 补 `manager_sync_evidence_bundle.v1` 开发期预演 fixture，组合 readiness 摘要、`sync_connection_health.v1`、部署证据来源和设备 production gate，覆盖全部 ready 但当前阶段关闭、本地 HTTPS 可达但 readiness 阻塞、连接不可达但 readiness ready、不安全 connection summary 拒绝、不安全 readiness redaction 拒绝和未知 readiness 字段降级。
- 将 evidence bundle 场景接入 entry gate / diagnostics 完整目录、settings gate preview 代表场景和同步页代表场景，确认连接健康只是解释性证据，不会绕过恢复码、设备授权、发布级证据或当前阶段关闭门禁。
- 补 `manager_sync_action_command_preview.v1` 非执行命令预演层，从现有 `SyncInteractionEntryPlan` 派生四条未来操作的 action id、execution status、data policy 和 stop line，并接入 settings gate preview、同步页恢复码 / 设备授权 section 与诊断报告。
- 扩展 readiness / evidence bundle 场景回归，确认 ready 但阶段关闭映射为 `not_executable_current_phase`，readiness 阻塞映射为 `blocked_by_readiness`，不生成 bridge 命令或真实操作入口。
- 补 `docs/manager-sync-action-protocol-preview.md`，固定四条 action 的 request boundary、result boundary、data policy、stop line、错误分类、allowed fields 和 forbidden material policy；该文档只定义非执行预演，不定义真实 bridge DTO。
- 补 `SyncActionRequestPreview` / `SyncActionResultPreview` 和 `manager_sync_action_command_preview.v1` request / result 可见层，把 request status、result status、allowed fields 和 forbidden material policy 接入 settings gate preview、同步页与诊断报告。
- 将 action command preview 拆入职责单一的 `manager_sync_action_preview_models.dart`，并新增 `manager_sync_action_preview_test.dart`，避免 `manager_sync_entry_models.dart` 和 entry gate 测试越过仓库行数门禁。
- 补 `sync_action_protocol_fixtures.dart` 和 `docs/manager-sync-action-acceptance-matrix.md`，覆盖四条 action 在当前阶段关闭、readiness 阻塞、用户确认、future ready shape、missing intent 和 unknown intent status 下的 execution / request / result 状态矩阵。
- 将 model、settings、sync、diagnostics 测试中重复的 action 状态推导收敛为共享 `syncActionExpected*` helper，减少后续状态码或协议字段调整时的漂移风险。
- 更新 `docs/status/current.md`、`docs/manager-sync-entry-boundary.md`、`docs/manager-settings-diagnostics.md`、`docs/manager-readiness-scenarios.md`、`docs/manager-recovery-device-auth-flow.md` 和 action protocol 相关文档，明确这些能力不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实同步或恢复 / 授权成功路径。

今日提交回顾：

- `b4d2b59 test(manager): 补同步 readiness bridge 样本`
- `311de61 feat(manager): 支持同步 readiness 摘要导入`
- `1649c74 feat(manager): 收口同步 readiness 摘要导入证据`
- `f355f19 test(manager): 固定同步 readiness 场景目录`
- `aee544e test(manager): 补 readiness 场景可见层回归`
- `4d8b83f test(manager): 固定 readiness bridge 验收包`
- `40b6264 test(manager): 补同步 evidence bundle 预演回归`
- `c9aaab8 test(manager): 补同步 action command 预演`
- `ac556a1 feat(manager): add sync action protocol preview`
- `e08113f feat(manager): add sync action request result previews`
- `aa60c08 test(manager): add sync action protocol matrix`

验证记录：

- `flutter test test/models/manager_sync_entry_gate_test.dart test/screens/manager_home_actions_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart test/screens/sync_test.dart`
- `./scripts/check-manager.sh`
- `git diff --check`
- `./scripts/check-repo.sh`
- `flutter test test/models/manager_sync_action_preview_test.dart test/models/manager_sync_entry_gate_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart test/screens/sync_test.dart`

风险与未完成项：

- readiness 摘要导入仍是开发期本地联调入口，不是 bridge contract 或持久化 settings draft 字段。
- readiness 场景目录已经覆盖模型、settings gate preview、诊断报告和同步页代表场景；后续新增场景时应继续同步更新 fixture 预期与可见层断言，避免只扩展单侧测试。
- evidence bundle 仍是测试预演层，不通过 UI 导入，不写 settings JSON；后续若要扩展真实 bridge 输出，应继续复用 readiness mapper 和 connection health summary 的现有 envelope / redaction 校验。
- action command preview 仍是非执行摘要层，不创建 bridge 请求、不定义 C ABI、不携带恢复码、短码、签名、wrapped material、payload bytes 或请求 / 响应体；后续真实 bridge 命令前必须另补协议文档和测试。
- action protocol matrix 已覆盖 future ready shape，但该状态只用于未来 contract 形状验收；当前 UI 仍必须保持 `closed_current_phase` / `not_executable_current_phase`，不能把矩阵测试解释为真实入口可打开。
- 平台私钥 backend、恢复码真实交互、设备授权成功路径和发布级部署证据仍是用户可用同步入口的停止线。
- 后续如果新增真实 bridge readiness 返回，需要继续复用当前 mapper allowlist、错误分类和诊断脱敏测试，不应把原始 native 错误、路径、token、恢复码、签名或 payload bytes 传入 UI。

明日事项（2026-07-07）：

- 继续推进真实 bridge 命令前的非敏感设计验收准备：先补“future ManagerBridge command contract 设计草案 / 检查清单”专题文档，明确哪些内容可以从 preview 矩阵继承，哪些内容必须等真实 contract 另行定义；仍不修改 `ManagerBridge` contract，不新增 C ABI。
- 围绕 `recovery_setup`、`recovery_restore`、`join_request_authorization` 和 `device_revocation` 梳理 future request / result 的验收前置条件、错误语义、幂等性要求和脱敏诊断要求，保持只写状态码、策略码和摘要字段，不写 payload 字段或真实请求体。
- 评估当前 settings / sync / diagnostics 可见层是否还存在重复 action protocol 文案或测试断言；若有，应继续收敛到 `sync_action_protocol_fixtures.dart` 或对应专题文档，避免下一次接线时多处漂移。
- 保持真实远端同步、恢复码生成 / 输入、join request 创建、授权成功和设备撤销路径关闭；任何下一步代码改动都应先证明没有新增按钮、点击回调、bridge 调用或 settings draft action payload。
- 若触及 manager UI 可见行为，优先补精准 widget / helper 覆盖，再跑相关 `flutter test`、`./scripts/check-manager.sh`、`git diff --check`；触及文档入口或仓库门禁时补跑 `./scripts/check-repo.sh`。

## 2026-07-07

完成情况：

- 补 `docs/manager-sync-bridge-command-contract-checklist.md`，把真实 `ManagerBridge` 同步命令前的 DTO、transient secret 生命周期、C ABI / FFI ownership、错误 envelope、幂等性、并发、fixture 和诊断脱敏审阅项整理为检查清单。
- 明确 `manager_sync_action_command_preview.v1`、`SyncActionRequestPreview` 和 `SyncActionResultPreview` 仍是非执行预演层，不能直接改名后当作 bridge DTO。
- 围绕 `recovery_setup`、`recovery_restore`、`join_request_authorization` 和 `device_revocation` 固定进入条件、真实 contract 必须另行定义的内容、当前禁止事项、request / result contract 检查、错误语义、幂等性和并发检查。
- 同步更新 `README.md`、`docs/status/current.md`、`docs/manager-sync-action-protocol-preview.md` 和 `docs/manager-sync-entry-boundary.md`，把真实 bridge 命令前检查清单纳入当前入口和边界引用。
- 本次仍不修改 `ManagerBridge` contract、不新增 C ABI、不新增按钮 / 点击回调 / settings draft action payload，不打开真实远端同步、恢复码生成 / 输入、join request 创建、授权成功或设备撤销路径。
- 补 `sync_bridge_command_contract_fixtures.dart` 和 `manager_sync_bridge_command_contract_test.dart`，把检查清单转成 design fixture 与 model 测试证据，覆盖四条 action 的安全 request / result 样本、transient / opaque / transport 字段隔离、allowlist 错误 envelope、幂等性状态、forbidden material 拒绝样本和代表 diagnostics 不泄漏。
- 补 `docs/manager-sync-bridge-command-contract.md`，固定 future `ManagerBridge` 四条同步命令的候选 Dart interface、common DTO、action DTO 分类、错误 envelope、幂等性 / 并发、FFI / C ABI 问题清单、诊断边界和测试映射；该文档仍不定义当前可执行接口。
- 补 `docs/manager-sync-ffi-command-boundary.md`，把真实同步 command 进入 `ime-ffi` 前的推荐 ABI 策略、C ABI 候选边界、request / result ownership、error ownership、transient secret 生命周期、线程 / 并发 / 取消、错误分层、Rust host smoke 与 Dart FFI smoke 设计固定为实现前评审输入；当前仍不新增 native symbol。
- 补 `sync_ffi_command_boundary_fixtures.dart` 和 `manager_sync_ffi_command_boundary_test.dart`，把 FFI command boundary 的推荐 ABI 策略、当前 native symbol 关闭态、request / result field policy、ownership rule、transient secret rule、ABI status 分层、Rust host smoke 项、Dart FFI smoke 项和 forbidden material 拒绝样本转成 design fixture / model test 证据。
- 补 `docs/manager-sync-ffi-command-contract-test-plan.md`，把当前 design fixture 之后的 L2 Rust host contract test、L3 Dart FFI binding contract test、L4 manager 可见层回归和 L5 gated platform / deployment smoke 分层固定下来，明确真实 symbol 前的测试输入、错误、ownership、copy / free、forbidden material 和停止线。
- 补 `ffi_manager_bridge_test.dart` fake native capability missing 回归，确认当前 `FfiManagerBridge` 即便有本地 userdb / sync preflight fake native，也只输出非执行 action preview，settings JSON 不写 action payload，diagnostics 不出现 future command request / result 或 forbidden material。
- 补 `manager_sync_ffi_binding_contract_test.dart` 测试侧 future binding harness，覆盖 summary copy -> free 顺序、unknown native status 安全降级、`ffi_library_load_failed` / `invalid_argument` / `sync_error` 到既有 bridge 分类的映射，以及 command error allowlist 下 forbidden material 不进入 diagnostics 摘要。
- 补 Rust host contract 合成输入样本 fixture，覆盖 current-phase capability closed、unknown schema version、unknown action、invalid bool、unenveloped sync error 和 panic boundary 的预期 ABI status；同步扩展 `ffi_bridge_smoke.dart`，确认真实 `radishlex-ime-ffi` dynamic library 当前未导出 future sync command symbol。
- 补 transient secret 交互生命周期 fixture 和 model test，覆盖恢复码一次性展示、恢复码输入、短码核对、授权确认和撤销确认的当前阶段关闭态、允许状态字段、完成证据码、禁止持久化目标和 bridge contract transient fields 映射。
- 补 recovery setup / restore 可见层文案与确认占位 fixture，把一次性展示状态、保存确认、恢复码输入占位、恢复记录查询、失败限速和设备登记状态固定为 UI / diagnostics 同源的非敏感状态码。
- 同步页、设置页 gate preview 和诊断报告新增 `sync.recovery_setup_display_status`、`sync.recovery_setup_save_confirmation`、`sync.recovery_restore_lookup_status`、`sync.recovery_restore_attempt_limit` 和 `sync.recovery_restore_device_registration`，仍不新增按钮、点击回调、bridge 调用或 settings action payload。
- 扩展 Rust host contract 合成输入样本，补 null pointer、invalid UTF-8 和 same-domain concurrent command；新增 `syncFfiCommandBoundaryRustHostContractCases`，把目标测试文件、建议 test name、sample id、ABI input case、expected status、required evidence 和 `planned_no_native_symbol` 状态固定为 host-side fixture 评审证据。
- 扩展 `manager_sync_ffi_command_boundary_test.dart`，确认 host catalog 覆盖全部 Rust host smoke 项和全部输入样本，ABI status 映射一致，forbidden material 只作为拒绝类别存在，不进入 fixture 输出。
- 扩展 `manager_sync_ffi_binding_contract_test.dart`，把 Dart fake native binding 接入 host catalog replay：逐个重放 host input sample 与 contract case，验证 copy -> free 顺序、ABI status / input case / required evidence 摘要、command error allowlist 和 forbidden material 脱敏边界。
- 新增 recovery setup / restore future confirmation detail fixture，固定 setup 保存确认、首次上传 gate、restore 输入关闭、恢复记录查询、失败限速和设备登记的状态码证据；model test 反查 visible layer、transient interaction 和当前 diagnostics，一并证明不创建 settings action、不生成 bridge request、不渲染或复制 secret。
- 新增 Rust host contract review fixture，把 7 个 host contract case 逐项绑定到既有 `ime-ffi` 模式、评审主题、非敏感证据码和停止线，覆盖 FFI contract 版本检查、输入校验、panic boundary、释放路径、error handle、summary 输出、copy-before-release 和 owner-thread 状态策略；当前状态仍是 `review_ready_no_native_symbol`，不新增真实 Rust host test、C ABI 或 `ManagerBridge` 可执行入口。

今日提交回顾：

- `17b6b81 docs(manager): add sync bridge command contract checklist`
- `701e4da test(manager): add sync bridge command contract fixtures`
- `7ee8c9b docs: define manager sync bridge command contract`
- `d10add8 docs: define manager sync ffi command boundary`
- `ec6d110 test(manager): add sync ffi command boundary fixtures`
- `4815d8b docs: add manager sync ffi contract test plan`
- `5e243fc test(manager): cover sync command capability absence`
- `14d8775 test(manager): expand sync ffi binding contract coverage`
- `34cf38c test(manager): add sync ffi capability smoke evidence`
- `2d13a11 test(manager): 固定 transient secret 交互生命周期`
- `d3bd889 test(manager): 补恢复入口可见层状态证据`
- `466e5d2 test(manager): 固定同步 host contract catalog 证据`
- `667a3cd test(manager): 补同步 host catalog binding replay`
- `4c2a37b test(manager): 补恢复确认细节证据`
- `36f269d test(manager): 补同步 Rust host 评审证据`

验证记录：

- `git diff --check`
- `./scripts/check-docs.sh`
- `./scripts/check-repo.sh`
- `flutter test test/models/manager_sync_bridge_command_contract_test.dart test/models/manager_sync_action_preview_test.dart test/models/manager_sync_entry_gate_test.dart`
- `flutter test test/models/manager_sync_ffi_command_boundary_test.dart`
- `flutter test test/ffi_manager_bridge_test.dart test/models/manager_sync_ffi_command_boundary_test.dart`
- `flutter test test/models/manager_sync_ffi_binding_contract_test.dart`
- `flutter test test/models/manager_sync_ffi_command_boundary_test.dart test/models/manager_sync_ffi_binding_contract_test.dart test/ffi_manager_bridge_test.dart`
- `flutter test test/models/manager_sync_transient_secret_interaction_test.dart`
- `flutter test test/models/manager_sync_transient_secret_interaction_test.dart test/models/manager_sync_entry_gate_test.dart`
- `flutter test test/screens/sync_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart`
- `flutter test test/models/manager_sync_ffi_command_boundary_test.dart`
- `./scripts/check-manager-ffi-smoke.sh`
- `./scripts/check-manager.sh`
- `flutter test test/models/manager_sync_ffi_binding_contract_test.dart`
- `flutter test test/models/manager_sync_transient_secret_interaction_test.dart`

风险与未完成项：

- 检查清单只是 future command contract 的设计前置，不是可执行 contract；后续真实命令仍需另补 Dart / Rust / C ABI contract、实现测试、平台私钥 backend 证据和发布级部署证据。
- 如果后续真实 contract 必须承载临时恢复码输入或一次性恢复码展示材料，必须在真实 contract 文档中单独定义 transient secret 生命周期，并继续禁止进入 settings、日志、诊断、fixture golden 和截图。
- design fixture 当前不进入 UI，也不写 settings draft；后续若要扩展 settings / sync 可见层，必须继续证明没有新增真实操作按钮、点击回调、bridge 调用或 action payload 持久化。
- recovery visible-layer 当前只补状态码、文案边界和诊断字段；`display_not_available_current_phase`、`input_not_available_current_phase`、`not_started` 和 `blocked_until_recovery_success` 不能被解释为真实恢复码展示、输入、限速或设备登记实现已经打开。
- future confirmation detail 当前只描述 setup / restore 的状态码、前置条件和允许证据码；`confirmation_not_available_current_phase`、`settings_action_absent` 和 `clipboard_auto_copy_blocked` 不能被解释为真实确认控件、剪贴板操作或 settings action 已存在。
- Rust host catalog 当前仍是 design fixture，不是 `crates/ime-ffi/tests/manager_sync_command_boundary.rs` 的真实 host test；Dart fake binding replay 只证明测试侧 mapper 能消费 catalog 摘要，不代表真实 native command 已实现。后续进入 Rust 测试前仍需评审真实 C ABI request / result struct、释放函数、panic boundary 和 command context 策略。
- Rust host review catalog 只把既有 `ime-ffi` 模式整理为真实 Rust test 前的评审材料，不代表已批准新增 native symbol；后续若进入 source-level checklist 或真实 Rust host test，应继续保持当前停止线并补精确验证。
- 下一步若继续推进，可继续细化 join / revocation confirmation detail，或把 Rust host review catalog 整理为真实 Rust test 前的 source-level checklist；不要直接新增 `ManagerBridge` 方法或 Rust symbol。

明日事项（2026-07-08）：

- 优先把 `join_request_authorization` / `device_revocation` 的 future confirmation detail 补到与 recovery setup / restore 同等可复验层级：新增或扩展现有 fixture / model test，固定 join request 状态、短码核对占位、授权显式确认、授权包状态、撤销显式确认、丢失设备风险提示和 key epoch 状态的 UI / diagnostics 非敏感状态码。仍不创建 join request、不输入短码、不签名授权包、不撤销设备、不写 settings action payload。
- 同步审阅 `docs/manager-recovery-device-auth-flow.md`、`docs/manager-sync-bridge-command-contract.md`、`docs/manager-sync-action-protocol-preview.md` 和 settings / diagnostics 文档，确认 join / revocation 的文案边界、状态码、错误分类和 forbidden material policy 与现有 fixture 一致；如出现重复字段表述，优先收敛到 fixture 或专题文档，不把历史过程复制进入口文档。
- 作为第二顺位，把 `syncFfiCommandBoundaryRustHostReviewItems` 整理成真实 Rust host test 前的 source-level checklist：只记录已有 `ime-ffi` 源码 / 测试模式、待评审结构和停止线，不创建 `crates/ime-ffi/tests/manager_sync_command_boundary.rs`，不新增 native symbol，不修改 Dart native binding。
- 每个子步骤都先补可复验 fixture / model test，再同步专题文档、`docs/status/current.md` 和本周周志；验证优先跑相关 `flutter test`、`./scripts/check-manager-ffi-smoke.sh`、`./scripts/check-manager.sh`、`./scripts/check-docs.sh`、`git diff --check`，触及入口文档或验证基线时补跑 `./scripts/check-repo.sh`。

## 2026-07-08

完成情况：

- 补 `syncDeviceAuthorizationFutureConfirmationDetailFixtures` 和对应 shape / id helper，把 `join_request_authorization` 与 `device_revocation` 的 future confirmation detail 固定为可复验 design fixture。
- 新增 model test，确认 join request 状态、短码核对占位、授权包前置条件、授权包状态、撤销状态、活跃设备要求、丢失设备风险提示和 key epoch 状态与当前 UI / diagnostics 字段同源。
- 测试确认 device authorization confirmation detail 仍只输出状态码、确认码、前置条件码和允许证据码；不包含短码值、signature bytes、wrapped material 值、settings action payload、bridge request payload、请求体或响应体。
- 同步更新 `docs/manager-recovery-device-auth-flow.md`、`docs/manager-settings-diagnostics.md`、`docs/manager-sync-bridge-command-contract.md`、`docs/manager-sync-entry-boundary.md` 和 `docs/status/current.md`，把 join / revocation confirmation detail 从“后续补齐”更新为“已在 fixture / model test 证据层固定”。
- 本次仍不修改 `ManagerBridge` contract、不新增 C ABI、不新增按钮 / 点击回调 / settings draft action payload，不打开真实远端同步、恢复码生成 / 输入、join request 创建、授权成功或设备撤销路径。
- 补 `syncFfiCommandBoundaryRustHostSourceChecklistItems` 和对应 model test，把 Rust host review catalog 的每个既有模式绑定到具体源码 / 测试引用，例如 `RadishLexFfiContract::current`、`ffi_status`、`ffi_ptr`、`ffi_release`、`read_utf8`、`read_ffi_bool`、summary output guard、error handle、owner-thread `InvalidState` 测试和 future sync command symbol 缺席 smoke。
- 补 `syncFfiCommandBoundaryRustHostTestDesignItems` 和对应 model test，把每个 host contract case 绑定到 source checklist、合成样本、expected status、断言组、forbidden output category 和 implementation guard，状态为 `test_design_ready_no_native_symbol`。
- 测试确认 Rust host test design package 覆盖全部 host contract case，且继续不输出候选 native symbol、真实请求 / 响应体、settings action payload、bridge request payload、token、恢复码、短码、signature、wrapped material 或真实路径。
- 同步更新 `docs/manager-sync-ffi-command-boundary.md`、`docs/manager-sync-ffi-command-contract-test-plan.md` 和 `docs/status/current.md`，明确 source checklist 是真实 Rust host test 前的源码审阅索引，test design package 是后续真实 Rust host test 文件前的转写设计包。
- 补 `sync_ffi_rust_host_contract_review_fixtures.dart`，新增 `syncFfiRustHostContractReviewItems`，把真实 Rust host test 前必须评审的 request struct layout、result struct layout、release / error lifecycle、panic / status boundary、command context serialization 和 forbidden material contract 固定为 C ABI contract review matrix。
- 扩展 `manager_sync_ffi_command_boundary_test.dart`，确认 C ABI review matrix 覆盖全部 test design item、绑定 source checklist、保留 implementation guard，并继续不输出候选 native symbol、真实请求 / 响应体、settings action payload、bridge request payload 或敏感材料片段。
- 补 `docs/adr/0006-manager-sync-c-abi-contract-governance.md`，把 manager 同步命令进入真实 Rust host contract test、C ABI symbol 和 Dart native binding 前的 request struct、result struct、release / error lifecycle、panic / status boundary、command context 和 forbidden material contract 治理固定为正式 ADR。
- 同步更新 `sync_ffi_rust_host_contract_review_fixtures.dart` 和 model test，把 C ABI review matrix 绑定到 ADR 0006，确保后续测试设计、source checklist 和决策记录可追踪。
- 同步更新 `docs/manager-sync-ffi-command-boundary.md`、`docs/manager-sync-ffi-command-contract-test-plan.md` 和 `docs/status/current.md`，明确 C ABI review matrix 与 ADR 0006 是是否进入真实 Rust host contract test 文件前的评审输入。
- 补 `syncFfiRustHostImplementationReviewItems` 和对应 model test，把每个 C ABI review item 绑定到后续 Rust `manager_sync_command.rs::*Draft` 草案、当前可复用 `ime-ffi` 源码模式、required implementation notes 和 unresolved implementation questions；状态为 `rust_host_implementation_review_ready_no_native_symbol`。
- 同步更新 ADR 0006、FFI boundary、contract test plan 和 `docs/status/current.md`，明确 implementation review package 是进入内部 Rust draft module 和真实 host test 前的结构索引。
- 新增 `crates/ime-ffi/src/manager_sync_command.rs` 内部非导出草案模块，并只在 `crates/ime-ffi/src/lib.rs` 挂载内部 module；当前不在 `abi.rs` 新增 `extern "C"`，不导出 native symbol，不修改 Dart native binding 或 `ManagerBridge`。
- 内部草案模块覆盖 versioned raw request draft、action id allowlist、borrowed UTF-8 view 读取、`u8` bool 校验、forbidden material 拒绝、当前阶段 capability closed 的 `InvalidState` 结果摘要，以及 Debug / error / result summary 不回显输入摘要或 secret-shaped 片段。
- 同步更新 ADR 0006、FFI boundary、contract test plan 和 `docs/status/current.md`，明确内部 draft module 不包含 Rust-owned result handle、result accessor、release function、error handle lifecycle、panic boundary C ABI 包装、action-specific section layout、sync domain guard、Rust sync / crypto 接线或远端 transport。
- 本次仍不创建 `crates/ime-ffi/tests/manager_sync_command_boundary.rs`，不新增 native symbol，不修改 Dart native binding，不连接 Go server，不触碰平台 key backend。

验证记录：

- `flutter test test/models/manager_sync_transient_secret_interaction_test.dart`
- `flutter test test/models/manager_sync_transient_secret_interaction_test.dart test/screens/sync_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart`
- `./scripts/check-manager.sh`
- `./scripts/check-docs.sh`
- `git diff --check`
- `flutter test test/models/manager_sync_ffi_command_boundary_test.dart`
- `flutter test test/models/manager_sync_ffi_command_boundary_test.dart test/models/manager_sync_ffi_binding_contract_test.dart`
- `cargo test -p radishlex-ime-ffi manager_sync_command`

风险与未完成项：

- join / revocation confirmation detail 仍是 design fixture 和状态码回归，不代表真实短码输入、授权签名、撤销记录或 key epoch 推进已经实现。
- 诊断报告允许出现 `wrapped_material` 这类 forbidden material policy 码；测试禁止的是实际值形态、payload、request / response body 和 secret-shaped 字段。
- Rust host source checklist、test design package、C ABI review matrix、ADR 0006、implementation review package 和内部 Rust draft module 仍是实现前治理证据，不代表已批准真实 Rust host test、native symbol 或 Dart binding；下一步应先继续评审真实 C ABI request / result struct、释放函数、panic boundary 和 command context 策略，再决定是否创建真实 Rust host contract test 文件。

## 当前入口

- 当前阶段、停止线和验证基线见 [`docs/status/current.md`](../status/current.md)。
- 阶段路线和退出标准见 [`docs/roadmap.md`](../roadmap.md)。
- 管理端本地验收口径见 [`docs/manager-local-acceptance.md`](../manager-local-acceptance.md)。
- 真实同步入口前置边界见 [`docs/manager-sync-entry-boundary.md`](../manager-sync-entry-boundary.md)。
- settings 与诊断字段边界见 [`docs/manager-settings-diagnostics.md`](../manager-settings-diagnostics.md)。
