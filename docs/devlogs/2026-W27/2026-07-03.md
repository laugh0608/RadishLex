# 2026-07-03 开发日志

本文档记录 2026-W27 周内该日期的推进事实、验证证据和后续建议。长期架构边界应回写到对应专题文档，当前状态短入口见 `../../status/current.md`。

## 2026-07-03 明天事项

- 首要推进 Android Keystore 真实设备 / API / provider 矩阵：优先选择一台真机或不同 OEM / system image / security patch 的 Android 环境，复用现有 diagnostics harness，记录新的 `device-matrix-YYYY-MM-DD-*.md`。如果 diagnostics 仍返回 `unsupported_signature_algorithm`，继续保持 production gate 关闭，并把设备差异、provider 差异和日志脱敏边界写清楚。
- 如果有可用真机，先跑 provider diagnostics，再判断是否需要 gated smoke；只有 diagnostics 证明 `AndroidKeyStore` 真实产出 Ed25519 public key 时，才推进 create / load / sign / delete smoke 的签名成功链路。
- 如果没有新的 Android 环境，转向 Phase 3 的另一条停止线：补目标部署运行证据，围绕真实证书 / 域名 / 外部反代 / 目标数据目录备份恢复 / 升级回滚做可复验记录，不进入 Flutter manager 或用户可用同步主线。
- 若后续真机矩阵仍无法证明 AndroidKeyStore 的非导出 Ed25519 signing key 支持，应准备一份平台私钥 backend 策略梳理，明确 `android-keystore-v1` 的证据边界、继续调查条件和可选后续 backend 的决策条件；该工作不直接修改签名协议或 Go / Rust verifier。
- 明天验证基线继续按改动范围执行：文档 / harness 变更至少跑 `git diff --check`、`./scripts/check-android-target.sh`、`cargo test -p radishlex-ime-crypto --features android-keystore` 和 `./scripts/check-repo.sh`；真实设备 diagnostics 仍必须显式获得触碰测试设备 Android Keystore 的授权。
