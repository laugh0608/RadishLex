# 2026-07-05 开发日志

本文档记录 2026-W27 周内该日期的推进事实、验证证据和后续建议。长期架构边界应回写到对应专题文档，当前状态短入口见 `../../status/current.md`。

## 2026-07-05 明日事项

- 先处理文档篇幅治理：补一个短入口或索引页，例如 `docs/status/current.md` 或 `docs/README.md`，把当前阶段、Phase 4 manager 已落地能力、验证基线、停止线和近期入口从长周志中抽出，降低新会话默认阅读成本。
- 继续 Phase 4 manager 本地质量治理：审视 `learning_view.dart`、`sync_view.dart` 是否需要按职责拆出浅层子组件，并只在行为风险明确时补页面级或 helper 级回归测试。
- 复查 `manager_home_actions.dart` 的分支复杂度：当前 widget tests 已覆盖跨页行为和失败提示；若后续 action 编排继续增长，再补更直接的 helper 级测试。
- 保持停止线：不改变 `ManagerBridge` contract，不新增 C ABI，不开启真实远端同步、恢复码或设备授权 UI；这些能力必须等待可用平台私钥 backend、目标部署运行证据和恢复 / 授权交互边界文档齐备。

## 2026-07-05 当前状态短入口治理记录

- 已新增 `docs/status/current.md`，作为新会话默认短入口，集中记录当前阶段、已落地能力、验证基线、停止线和近期推进顺位。
- 已同步 README、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，让入口索引优先指向当前状态短入口。
- 已将 `docs/devlogs/2026-W27.md` 拆为周索引，并把历史流水按天迁入 `docs/devlogs/2026-W27/`，消除新会话默认读取超长周志的问题。
- 本轮只做文档入口治理，不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实远端同步、恢复码或设备授权 UI。

验证记录：

```text
git diff --check
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests；拆分前仍保留 `docs/devlogs/2026-W27.md` 超过 1200 行的既有 warning。

## 2026-07-05 周志拆分验证记录

```text
git diff --check
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests；`docs/devlogs/2026-W27.md` 已降为 27 行索引，不再触发 devlog 篇幅 warning。

## 2026-07-05 Flutter manager 学习页结构拆分记录

- 已新增 `apps/radishlex-manager/lib/src/screens/learning/`，拆出学习页空态、学习摘要审计和 rank explain 筛选 / 列表 / 详情组件。
- `learning_view.dart` 现在只保留页面状态、筛选输入控制器、选中项状态和页面布局编排，继续不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实远端同步、恢复码或设备授权 UI。
- 已同步 manager README、`docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将学习子组件拆分纳入 Phase 4 manager 本地质量治理证据。

验证记录：

```text
dart format apps/radishlex-manager/lib/src/screens/learning_view.dart apps/radishlex-manager/lib/src/screens/learning/learning_empty_state.dart apps/radishlex-manager/lib/src/screens/learning/learning_summary_section.dart apps/radishlex-manager/lib/src/screens/learning/rank_explain_section.dart
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖学习页和新增学习子组件，格式化后无额外问题。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test，确认学习页页面级 widget tests 仍覆盖聚合摘要、rank explain 筛选、候选详情和空态。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge smoke 链路未受学习页结构拆分影响。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 Flutter manager 同步页结构拆分记录

- 已新增 `apps/radishlex-manager/lib/src/screens/sync/`，拆出同步预检 section、设备签名 section 和同步页空态组件。
- `sync_view.dart` 现在只保留同步页整体布局，继续保持真实同步按钮禁用，不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实远端同步、恢复码或设备授权 UI。
- 已同步 manager README、`docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将同步子组件拆分纳入 Phase 4 manager 本地质量治理证据。

验证记录：

```text
dart format apps/radishlex-manager/lib/src/screens/sync_view.dart apps/radishlex-manager/lib/src/screens/sync/sync_empty_state.dart apps/radishlex-manager/lib/src/screens/sync/device_signature_section.dart apps/radishlex-manager/lib/src/screens/sync/sync_preflight_section.dart
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖同步页和新增同步子组件，格式化后无额外问题。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test，确认同步页页面级 widget tests 仍覆盖 gate 禁用、本地 P2 对象分类、设备 backend 阻断和 local-only 空态。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge smoke 链路未受同步页结构拆分影响。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 Flutter manager home action helper 回归测试记录

- 已新增 `apps/radishlex-manager/test/screens/manager_home_actions_test.dart`，直接覆盖跨页 action helper 的导入 / 导出结果文案、跳过计数合成、文件路径不进入导出成功提示、native library failure、settings draft failure 和非 bridge exception 分类。
- 已复查 `manager_home_actions.dart` 当前职责：仍集中承载跨页对话框调用、bridge 调用和用户提示编排；本轮不为拆分而新增 action delegate，后续只有 action 编排继续增长时再按词库、诊断 / 设置职责拆分。
- 已同步 manager README、`docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将 action helper 级回归测试纳入 Phase 4 manager 本地质量治理证据。

验证记录：

```text
dart format apps/radishlex-manager/test/screens/manager_home_actions_test.dart
flutter test test/screens/manager_home_actions_test.dart
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖新增 action helper 测试，格式化后无额外问题。
- `flutter test test/screens/manager_home_actions_test.dart` 通过 7 个 helper 级断言，覆盖导入 / 导出成功文案、跳过计数合成、文件路径不进入导出成功提示，以及 native library、settings draft、非 bridge exception 的结构化失败分类。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test；manager 默认测试集扩展到 34 个测试。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge smoke 链路未受 action helper 测试补强影响。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 Flutter manager 本地验收口径记录

- 已新增 `docs/manager-local-acceptance.md`，集中记录 Phase 4 manager 本地验收范围、退出标准映射、验证入口、隐私检查、当前缺口和后续推进顺位。
- 已同步 README、manager README、`docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/repository-layout.md`、`docs/manager-ui-boundary.md` 和周索引，将近期推进口径从继续拆分现有 manager UI 调整为按验收标准复查证据缺口。
- 本轮只做验收文档和入口口径收口，不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实远端同步、恢复码或设备授权 UI。

验证记录：

```text
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test；manager 默认测试集保持 34 个测试。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge 本地管理 smoke 链路与验收文档口径一致。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 Flutter manager 本地验收复查记录

- 已按 `docs/manager-local-acceptance.md` 逐项复查 Phase 4 退出标准：本地词库管理、自部署后端配置草案、同步预检 / 生产不可用原因和同步开关关闭均有直接测试或 smoke 证据。
- 已在 `docs/manager-local-acceptance.md` 补充复查记录表，标明本地词库管理、settings draft、同步预检、同步按钮禁用、诊断脱敏和真实 Dart FFI bridge smoke 均通过。
- 已同步 `docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和周索引，将当前推进顺位调整为保持本地验收证据稳定，并把真实同步入口前置证据作为后续重点。
- 本轮不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实远端同步、恢复码或设备授权 UI。

验证记录：

```text
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test；manager 默认测试集保持 34 个测试。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge smoke 链路仍覆盖本地管理验收证据。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 真实同步入口前置边界记录

- 已新增 `docs/manager-sync-entry-boundary.md`，固定真实同步入口进入 UI / bridge 前的恢复码、设备授权、设备撤销、状态门禁、bridge 错误分类、诊断脱敏和测试计划。
- 已同步 README、`docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/repository-layout.md`、`docs/manager-ui-boundary.md`、`docs/manager-local-acceptance.md` 和周索引，让真实同步入口后续实现必须先遵守该边界。
- 本轮只做文档和入口口径治理，不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实远端同步、恢复码或设备授权 UI。

验证记录：

```text
git diff --check
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 目标部署证据口径记录

- 已在 `docs/runbooks/sync-server-production-deployment.md` 补充目标部署证据包模板，明确外部 TLS、访问控制失败响应、备份恢复、升级回滚和日志脱敏的非敏感记录口径。
- 已同步 `docs/manager-settings-diagnostics.md`，说明 settings draft 只保存 allowlist evidence source 标签，不保存证据包正文、运行日志、证书、token、路径、请求 / 响应体或 payload bytes。
- 已同步 `docs/manager-sync-entry-boundary.md`、`docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和周索引，将真实同步入口前置顺位调整为获取真实目标部署运行证据或平台私钥 backend 证据。
- 本轮只做文档和入口口径治理，不改变 `ManagerBridge` contract、不新增 C ABI、不打开真实远端同步、恢复码或设备授权 UI。

验证记录：

```text
git diff --check
./scripts/check-sync-server-deployment-rehearsal.sh --config-only
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过。
- `./scripts/check-sync-server-deployment-rehearsal.sh --config-only` 通过，确认部署态 Compose 临时 env、随机 bearer token 和配置解析仍可复验。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 目标部署证据包校验入口记录

- 已新增 `scripts/check-sync-deployment-evidence.py` 与 `scripts/check-sync-deployment-evidence.sh`，校验 `deployment_evidence.v1` 必填字段、状态枚举、固定 Compose 文件、时间戳、目标别名、commit、image tag 和敏感内容黑名单。
- 已新增 `tests/fixtures/sync-deployment-evidence-valid.txt` 合成 fixture，并把校验器 `--self-test` 接入 `scripts/check-repo.py`。
- 已同步 README、生产部署 runbook、manager 同步入口边界、settings / diagnostics 字段参考、当前状态、技术方案、路线图、仓库结构和周索引，保持真实同步 UI / bridge 关闭。
- 本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

验证记录：

```text
./scripts/check-sync-deployment-evidence.sh --self-test
./scripts/check-sync-deployment-evidence.sh tests/fixtures/sync-deployment-evidence-valid.txt
git diff --check
./scripts/check-sync-server-deployment-rehearsal.sh --config-only
./scripts/check-repo.sh
```

结果：

- `./scripts/check-sync-deployment-evidence.sh --self-test` 通过，确认合成 fixture 和反例覆盖校验器字段、状态和脱敏规则。
- `./scripts/check-sync-deployment-evidence.sh tests/fixtures/sync-deployment-evidence-valid.txt` 通过，确认合成证据包符合 `deployment_evidence.v1`。
- `git diff --check` 通过。
- `./scripts/check-sync-server-deployment-rehearsal.sh --config-only` 通过，确认部署态 Compose 临时 env、随机 bearer token 和配置解析仍可复验。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、目标部署证据包自测、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 目标部署证据包摘要导出记录

- 已扩展 `scripts/check-sync-deployment-evidence.py`，新增 `--summary-json` 和 `--summary-text`，只在证据包校验通过后输出 `deployment_evidence_summary.v1` 非敏感摘要。
- 摘要只包含目标别名、复验时间、commit、image tag、固定 Compose 文件、目标证据完整性、可映射到 manager 的 evidence source 标签和状态枚举；不输出证据文件路径、`notes`、真实 URL、token、证书、日志正文、请求 / 响应体、payload bytes、signature bytes、wrapped material bytes、恢复材料或平台私钥材料。
- 已同步 README、生产部署 runbook、manager 同步入口边界、settings / diagnostics 字段参考、当前状态、技术方案、路线图、仓库结构和周索引。
- 本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

验证记录：

```text
./scripts/check-sync-deployment-evidence.sh --self-test
./scripts/check-sync-deployment-evidence.sh tests/fixtures/sync-deployment-evidence-valid.txt
./scripts/check-sync-deployment-evidence.sh --summary-json tests/fixtures/sync-deployment-evidence-valid.txt
./scripts/check-sync-deployment-evidence.sh --summary-text tests/fixtures/sync-deployment-evidence-valid.txt
git diff --check
./scripts/check-repo.sh
```

结果：

- `./scripts/check-sync-deployment-evidence.sh --self-test` 通过，覆盖证据包校验、摘要输出、摘要不包含 `notes` / 敏感 marker、失败证据拒绝输出摘要和不完整证据状态派生。
- `./scripts/check-sync-deployment-evidence.sh tests/fixtures/sync-deployment-evidence-valid.txt` 通过，确认合成证据包仍符合 `deployment_evidence.v1`。
- `./scripts/check-sync-deployment-evidence.sh --summary-json tests/fixtures/sync-deployment-evidence-valid.txt` 通过，输出 `deployment_evidence_summary.v1` 非敏感 JSON 摘要。
- `./scripts/check-sync-deployment-evidence.sh --summary-text tests/fixtures/sync-deployment-evidence-valid.txt` 通过，输出 `deployment_evidence_summary.v1` 非敏感文本摘要。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、目标部署证据包自测、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 真实目标部署运行证据采集阻塞记录

- 已按当前任务复查仓库状态和相关入口文档，仓库内只存在 `tests/fixtures/sync-deployment-evidence-valid.txt` 合成 fixture、校验脚本和摘要导出工具；没有用户提供或目标环境产生的真实 `deployment_evidence.v1`。
- 本轮未验证真实目标部署，没有执行真实证据包的 `<evidence-file>` 校验或 `--summary-json <evidence-file>` 导出；合成 fixture 只作为校验器自测材料，不作为真实部署证据。
- 已同步 `docs/status/current.md`、`docs/runbooks/sync-server-production-deployment.md` 和 `docs/manager-sync-entry-boundary.md`，明确无真实证据包时只记录 `deployment_unverified` 阻塞，不打开真实远端同步、恢复码 UI 或设备授权成功路径。
- 部署者后续需要提供的非敏感字段包括：`reviewed_at`、`target_alias`、`git_commit`、`image_tag`、固定 `compose_file`、`public_url_status`、`access_token_status`、`access_control`、`external_tls`、`backup_restore`、`upgrade_rollback`、`log_redaction` 和非敏感 `notes`；不得提供 token、证书、真实路径、请求 / 响应体、payload bytes、signature bytes、wrapped material、恢复材料或平台私钥材料。

验证记录：

```text
./scripts/check-sync-deployment-evidence.sh --self-test
git diff --check
./scripts/check-repo.sh
```

结果：

- `./scripts/check-sync-deployment-evidence.sh --self-test` 通过，确认校验器和摘要导出自测仍能拒绝敏感 marker、失败证据和不完整证据。
- 因没有真实目标环境证据包，未运行 `./scripts/check-sync-deployment-evidence.sh <evidence-file>` 或 `./scripts/check-sync-deployment-evidence.sh --summary-json <evidence-file>`。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、目标部署证据包自测、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 同步开发阶段口径调整记录

- 已按新的阶段判断调整文档口径：真实域名、正式证书、外部反代和目标环境备份 / 升级演练保留为正式发布 / 真实用户开放前门禁，不作为当前产品开发阻塞项。
- 当前同步开发和联调以本地 Docker、本地 HTTPS、短生命周期数据目录和现有 smoke 为依据；`local_smoke` 可以支撑 manager 同步入口的非上传状态派生、阻塞说明、诊断脱敏和 UI gate 测试。
- 用户可用远端同步、恢复码 UI、设备授权成功路径和真实用户 P2 数据上传仍保持关闭，等待可用平台私钥 backend、恢复 / 授权实现测试和发布级部署证据齐备。
- 已同步 README、`docs/status/current.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/manager-ui-boundary.md`、`docs/manager-sync-entry-boundary.md`、`docs/manager-settings-diagnostics.md`、`docs/manager-local-acceptance.md`、`docs/privacy-sync.md`、`docs/crypto-boundary.md`、`docs/sync-key-management.md`、`docs/sync-server-api-storage.md`、`docs/platform-private-key-backend-strategy.md` 和生产部署 runbook。

验证记录：

```text
git diff --check
./scripts/check-sync-server-deployment-rehearsal.sh --config-only
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过。
- `./scripts/check-sync-server-deployment-rehearsal.sh --config-only` 通过，确认部署态 Compose 临时 env、随机 bearer token 和配置解析仍可复验；本轮未启动长期运行 server。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、目标部署证据包自测、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 Manager sync entry gate 非上传实现记录

- 已在 Flutter manager 中新增 `SyncEntryState` / `ManagerSyncEntryGate`，从 settings draft、deployment evidence allowlist source、平台 backend gate、恢复码关闭状态和设备授权关闭状态派生非上传阻塞说明。
- 同步页和设置页草案预览已展示 `entry state`、`entry blocker`、`local evidence`、`production blockers` 和 `user sync enabled`；真实同步按钮继续禁用。
- 诊断报告新增 `sync.entry_state`、`sync.entry_blocker`、`sync.local_evidence_source`、`sync.production_blockers`、`sync.user_sync_enabled`、`sync.recovery_status` 和 `sync.device_authorization_status`，只输出状态码、allowlist 标签和聚合阻塞码。
- 已同步 `docs/status/current.md`、`docs/manager-sync-entry-boundary.md`、`docs/manager-settings-diagnostics.md` 和周索引，将下一步推进顺位调整为恢复码和设备授权的结构化状态模型与只读准备态 UI。
- 本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码生成或设备授权成功路径。

验证记录：

```text
dart format apps/radishlex-manager/lib/src/models/manager_sync_models.dart apps/radishlex-manager/lib/src/models/manager_diagnostics_models.dart apps/radishlex-manager/lib/src/screens/sync_view.dart apps/radishlex-manager/lib/src/screens/manager_home_screen.dart apps/radishlex-manager/lib/src/screens/sync/sync_preflight_section.dart apps/radishlex-manager/lib/src/screens/settings_view.dart apps/radishlex-manager/test/models/manager_sync_entry_gate_test.dart apps/radishlex-manager/test/screens/sync_test.dart apps/radishlex-manager/test/screens/settings_test.dart apps/radishlex-manager/test/screens/settings_diagnostics_test.dart apps/radishlex-manager/test/ffi_manager_bridge_test.dart
flutter test test/models/manager_sync_entry_gate_test.dart
flutter test test/screens/sync_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart test/ffi_manager_bridge_test.dart
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
./scripts/check-sync-server-deployment-rehearsal.sh --config-only
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 通过，格式化后无额外问题。
- `flutter test test/models/manager_sync_entry_gate_test.dart` 通过，覆盖隐私策略、backend gate、本地 `local_smoke` 和非本地 evidence source 的 entry gate 派生。
- `flutter test test/screens/sync_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart test/ffi_manager_bridge_test.dart` 通过，确认同步页、设置页、诊断预览 / 复制和 Dart FFI bridge 映射均输出新的非敏感 gate 摘要，真实同步按钮仍关闭。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test；manager 默认测试集扩展到 38 个测试。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge smoke 链路未受 entry gate 与诊断字段扩展影响。
- `./scripts/check-sync-server-deployment-rehearsal.sh --config-only` 通过，确认部署态 Compose 临时 env、随机 bearer token 和配置解析仍可复验；本轮未启动长期运行 server。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、目标部署证据包自测、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-05 恢复码与设备授权只读准备态记录

- 已在 Flutter manager 中新增恢复码、设备授权和 join request 的结构化状态模型，当前默认保持 `recovery_code_flow_closed`、`device_authorization_flow_closed` 和 `join_request_unavailable`。
- 同步页新增恢复码准备态和设备授权准备态两个只读 section，只展示状态码、阻塞码和停止线，不提供恢复码生成 / 输入、join request 创建、授权成功或设备撤销入口。
- 设置页草案预览和诊断报告已接入 `sync.recovery_blocker`、`sync.device_authorization_blocker` 和 `sync.join_request_status`，继续只输出状态码和非敏感摘要。
- 已同步 `docs/status/current.md`、`docs/manager-ui-boundary.md`、`docs/manager-sync-entry-boundary.md`、`docs/manager-settings-diagnostics.md`、`docs/roadmap.md` 和周索引，将下一步推进顺位调整为本地 Docker / 本地 HTTPS 同步服务连接健康和错误分类的只读联调。
- 本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码生成 / 输入、join request 创建、设备授权成功或设备撤销路径。

验证记录：

```text
dart format apps/radishlex-manager/lib/src/models/manager_sync_models.dart apps/radishlex-manager/lib/src/models/manager_diagnostics_models.dart apps/radishlex-manager/lib/src/screens/sync_view.dart apps/radishlex-manager/lib/src/screens/sync/sync_preflight_section.dart apps/radishlex-manager/lib/src/screens/sync/sync_recovery_section.dart apps/radishlex-manager/lib/src/screens/sync/sync_device_authorization_section.dart apps/radishlex-manager/lib/src/screens/settings_view.dart apps/radishlex-manager/test/models/manager_sync_entry_gate_test.dart apps/radishlex-manager/test/screens/sync_test.dart apps/radishlex-manager/test/screens/settings_test.dart apps/radishlex-manager/test/screens/settings_diagnostics_test.dart apps/radishlex-manager/test/ffi_manager_bridge_test.dart
flutter test test/models/manager_sync_entry_gate_test.dart
flutter test test/screens/sync_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart test/ffi_manager_bridge_test.dart
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
./scripts/check-sync-server-deployment-rehearsal.sh --config-only
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 通过，格式化后无额外问题。
- `flutter test test/models/manager_sync_entry_gate_test.dart` 通过，覆盖恢复码关闭、设备授权关闭和 join request 不可用状态。
- `flutter test test/screens/sync_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart test/ffi_manager_bridge_test.dart` 通过，确认同步页只读准备态、设置页草案预览、诊断报告和 Dart FFI bridge 映射均保持非敏感摘要，真实同步按钮仍关闭。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test；manager 默认测试集保持 38 个测试。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge smoke 链路未受只读准备态模型和诊断字段扩展影响。
- `./scripts/check-sync-server-deployment-rehearsal.sh --config-only` 通过，确认部署态 Compose 临时 env、随机 bearer token 和配置解析仍可复验；本轮未启动长期运行 server。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、文档预算检查、目标部署证据包自测、Go server 测试、Rust workspace 测试和 doc-tests。
