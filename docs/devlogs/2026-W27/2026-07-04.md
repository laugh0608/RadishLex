# 2026-07-04 开发日志

本文档记录 2026-W27 周内该日期的推进事实、验证证据和后续建议。长期架构边界应回写到对应专题文档，当前状态短入口见 `../../status/current.md`。

## 2026-07-04 平台私钥 backend 策略收口记录

- 当前只有 Mac 开发设备，暂无新的 Android 真机、OEM 设备或不同 system image 可补真实设备矩阵；本轮不把真机矩阵作为当日硬阻塞。
- 已新增 `docs/platform-private-key-backend-strategy.md`，整理 `test-memory-v1`、`unavailable`、`apple-keychain-v1`、`android-keystore-v1`、`windows-cng-v1` 和 `linux-secret-service-v1` 的当前证据、生产合格条件、禁止 fallback 规则和可选后续路径。
- 策略继续保留 `ed25519-v1`，不修改 Rust / Go verifier，不把 P-256 或其他算法塞进现有字段，不把 seed / 软件私钥 fallback 混入 `apple-keychain-v1` 或 `android-keystore-v1`。
- 策略明确：有新 Android 设备 / system image 时继续跑 provider diagnostics；无新设备时可转向目标部署运行证据、Apple 原生非导出 Ed25519 spike，或先准备新的签名算法 / 软件保护 backend ADR 输入。
- 已同步 README、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/repository-layout.md`、`docs/adr/0004-platform-private-key-storage-backend.md`、`docs/sync-key-management.md` 和 `docs/crypto-boundary.md`，把平台私钥 backend 策略纳入正式入口。
- 本轮只修改文档，不运行 Android Gradle、instrumented diagnostics / smoke、模拟器、虚拟机、Keychain smoke 或长期运行 sync server；未创建 Android Keystore item，未触碰系统输入法配置，未写入真实设备 ID、账号、token、证书、私钥或用户数据。

## 2026-07-04 验证记录

```text
git diff --check
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未运行 `./scripts/check-android-target.sh`、Android Gradle build、Android instrumented diagnostics / smoke 或 Apple Keychain smoke；本次变更只收口文档策略，不触碰平台 keystore / keychain 或真实设备。

## 2026-07-04 Sync server 部署预演入口推进记录

- 已提交平台私钥策略收口，提交为 `6ab6bb3 docs: 收口平台私钥 backend 策略`。
- 已新增 `scripts/check-sync-server-deployment-rehearsal.sh` 和 `scripts/check-sync-server-deployment-rehearsal.py`，作为部署态 Compose 的短生命周期预演入口。
- 脚本会生成仓库外临时 env、随机 bearer token、临时数据目录和随机本机端口；完整模式会启动部署态 `docker-compose.yaml`，验证无 token `401 unauthenticated`、带 token 结构化业务响应、SQLite metadata / encrypted blob dir 创建、runtime log 脱敏，并执行冷备份恢复到隔离目录后复验。
- 脚本默认不打印 token；`--print-config` 也会把生成 token 脱敏。首次 `--config-only` 曾暴露脚本默认输出 `docker compose config` 会打印临时 token，本轮已修正为默认捕获配置输出并只在显式 `--print-config` 时打印脱敏内容。
- 沙盒内端口探测 `127.0.0.1:0` 会被权限限制拒绝，脚本已改为在端口探测被拒时退回随机高位端口；`--config-only` 已通过。
- 完整容器预演最初在沙盒和申请真实环境后都阻塞于 Docker Desktop socket 不存在：`unix:///Users/luobo/.docker/run/docker.sock` 无法连接。`docker compose version` 返回 Docker Compose v5.1.3，`docker context ls` 显示当前 context 为 `desktop-linux`，`docker version --format '{{.Server.Version}}'` 返回同一 socket 缺失错误。
- 开发者启动 Docker 后，完整预演首次进入容器启动，但 auth gate 一直连接拒绝；本轮据此补脚本失败诊断和临时数据目录权限准备。脚本现在会预创建 `data/sync-server/objects` 并对合成临时目录放宽权限，避免容器内 `USER 10001` 无法写入 bind mount。
- 修正后完整预演通过：脚本构建 `radishlex-sync-server:dev`，启动部署态容器，验证无 token `401 unauthenticated`、带 token 结构化业务响应、SQLite metadata / encrypted blob dir 创建、runtime log 脱敏，随后执行冷备份复制、恢复到隔离数据目录、二次启动和复验。
- 已同步 README、`docs/runbooks/sync-server-compose.md`、`docs/runbooks/sync-server-production-deployment.md` 和 `docs/repository-layout.md`，明确脚本用途、`--config-only` 边界、完整预演需要 Docker daemon，以及不能把配置解析通过写成容器启动通过。
- 完整预演结束后已确认 `docker ps --filter name=radishlex-sync-rehearsal` 无遗留容器；脚本已清理临时 env / token / 数据目录。未写入真实 token、证书、私钥、同步主密钥、恢复码或用户数据。

## 2026-07-04 Sync server 部署预演验证记录

```text
./scripts/check-sync-server-deployment-rehearsal.sh --config-only
./scripts/check-sync-server-deployment-rehearsal.sh --config-only --print-config
./scripts/check-sync-server-deployment-rehearsal.sh
PYTHONPYCACHEPREFIX=/private/tmp/radishlex-pycache python3 -m py_compile scripts/check-sync-server-deployment-rehearsal.py
docker compose version
docker context ls
docker version --format '{{.Server.Version}}'
docker ps --filter name=radishlex-sync-rehearsal --format '{{.Names}} {{.Status}} {{.Ports}}'
git diff --check
./scripts/check-repo.sh
```

结果：

- `--config-only` 通过，临时 env、随机端口、随机 bearer token 和部署态 Compose 配置可解析；输出只显示 `<redacted>` 或 `<redacted-access-token>`。
- Docker 启动前，完整脚本在 `docker compose up --build -d` 阶段失败，原因是 Docker Desktop daemon socket 不存在：`unix:///Users/luobo/.docker/run/docker.sock`。
- Docker 启动后，完整脚本第一次进入容器启动但 auth gate 连接拒绝；补临时数据目录权限准备和失败诊断后，完整脚本通过。
- 完整预演通过后，`docker ps --filter name=radishlex-sync-rehearsal --format '{{.Names}} {{.Status}} {{.Ports}}'` 无输出，确认没有遗留 rehearsal 容器。
- `docker compose version` 返回 Docker Compose v5.1.3；`docker context ls` 显示当前 context 为 `desktop-linux`，endpoint 为 `unix:///Users/luobo/.docker/run/docker.sock`；Docker 启动前 `docker version --format '{{.Server.Version}}'` 返回 socket 缺失错误。
- 初次 `python3 -m py_compile` 因 macOS Python 尝试写入用户缓存目录被沙盒拒绝；设置 `PYTHONPYCACHEPREFIX=/private/tmp/radishlex-pycache` 后通过。
- `git diff --check` 通过。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 管理端边界推进记录

- 已新增 `docs/manager-ui-boundary.md`，固定 Phase 4 Flutter manager 的职责范围、非职责范围、数据可见性、FFI / bridge 边界、同步 UI 状态模型、恢复码 / 设备授权停止线、日志 / 截图 / fixture 脱敏要求和第一批功能顺序。
- 管理端第一批实现顺序调整为本地 userdb 词条管理、学习状态摘要、rank explain 摘要和 sync preflight 摘要；同步配置页可以显示不可用原因，但真实上传、恢复码和设备授权 UI 必须等待可用平台私钥 backend 与目标部署运行证据。
- 已同步 README、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/repository-layout.md` 和 `docs/privacy-sync.md`，将管理端边界纳入正式入口。
- 本轮验证通过 `git diff --check` 和 `./scripts/check-repo.sh`；仓库级检查覆盖文本卫生、文档预算、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮只修改文档，不创建 Flutter 工程，不启动桌面 UI、Go server、Docker、模拟器、Keychain / Keystore smoke 或平台输入法服务；未写入真实 token、恢复码、同步主密钥、私钥、证书或用户词库数据。

## 2026-07-04 Flutter manager 起步记录

- 已创建 `apps/radishlex-manager/` Flutter macOS 工程，当前通过受控 `ManagerBridge` contract 接入合成 fixture，不接真实远端同步、恢复码或设备授权。
- 管理台首页包含词库、学习、同步、设置四个区域；词库页展示本地用户词、删除 tombstone、导入 / 导出按钮和搜索框；学习页展示 learning status 聚合指标和 `rank explain` 非敏感摘要；同步页展示 `backend_unavailable`、sync preflight 计数、设备签名状态和禁用的“启用同步”按钮；设置页展示自部署服务端草案、隐私模式、诊断摘要导出和同步配置草案。
- 已抽出 `ManagerBridge` 和 `FixtureManagerBridge`，UI 通过 bridge 加载 snapshot，并已验证删除词条动作经由 bridge 更新本地 snapshot、增加删除 tombstone 和同步摘要计数；导入检查、导入和导出按钮已接入 bridge 调用链，当前只使用合成数据返回非敏感结果反馈，后续等待真实 Dart FFI bridge 接线。
- 已移除生成模板中的 counter 示例和未使用的 `cupertino_icons` 依赖，新增 widget tests 覆盖词库初始页、学习页摘要、同步门禁禁用状态、删除词条 bridge 链路和导入 / 导出 bridge 链路。
- 已新增 `scripts/check-manager.sh`，统一执行 `dart format --set-exit-if-changed .`、`flutter analyze` 和 `flutter test`，并将管理端关键入口文件加入仓库必备文件检查。
- 已同步 README、`docs/manager-ui-boundary.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`，将 Flutter manager 起步状态纳入正式入口。

## 2026-07-04 Flutter manager 验证记录

```text
dart format --set-exit-if-changed .
flutter analyze
flutter test
./scripts/check-manager.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format --set-exit-if-changed .` 在 `apps/radishlex-manager` 通过。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 5 个 widget tests，覆盖词库初始页、学习页摘要、同步门禁禁用状态、删除词条 bridge 链路和导入 / 导出 bridge 链路。
- `./scripts/check-manager.sh` 在沙盒内因 Flutter SDK cache 写入权限受限失败；提权到真实环境后通过。

## 2026-07-04 Sync server admin console 规划记录

- 已新增 `docs/sync-server-admin-console.md`，把 Docker 部署后端 WebUI / 管理控制台记录为远期可选专题。
- 该专题定位为自部署运维控制台，只允许展示服务健康、migration、存储、认证模式、备份恢复、升级回滚和脱敏 audit 摘要；不得展示明文用户词库、明文输入历史、候选偏好、P1 原始事件、token、恢复码、私钥、wrapped material、signature bytes 或 encrypted payload bytes。
- 已同步 README、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`；该专题不纳入当前 Phase 3 / Phase 4 退出标准，不改变近期 Flutter manager 真实 Dart FFI bridge 推进顺序。

## 2026-07-04 Flutter manager FFI bridge 接线记录

- 已阅读 `docs/manager-ui-boundary.md`、`docs/ffi-boundary.md` 和 `crates/ime-ffi/src/abi.rs`，确认当前 C ABI 已覆盖 userdb list / delete、dictionary inspect / import / export、learning status 和 sync preflight；Rust ABI 尚未导出专用 rank explain 管理函数。
- 已新增 `FfiManagerBridge`、`DynamicRadishLexManagerNativeBinding`、native DTO contract 和 Dart FFI ABI struct 绑定；默认启动仍使用 `FixtureManagerBridge`，只有显式配置 `RADISHLEX_MANAGER_DB` 与可加载 `RADISHLEX_MANAGER_FFI_LIBRARY` 时才切到真实 FFI bridge。
- FFI bridge 会复制 Rust string view 后释放 list handle / error handle，不把 Rust 内部指针、SQLite handle、P1 原始事件、明文同步 payload、token、恢复码、signature bytes 或 wrapped material 透传给 widget 层。
- 本轮接线覆盖本地 userdb 词条 list / delete、用户词库 inspect / import / export、learning status 摘要和 sync preflight 摘要；`rank explain` 区域暂以 userdb term 和摘要计数派生非敏感接线摘要，后续真实 explain 需要先补专用 ABI。
- 同步 UI 继续只显示 `local_only` / `backend_unavailable` 类状态和不可用原因，不提供真实上传、恢复码、设备授权或用户可用同步成功路径；未启动长期服务、真实输入法、真实同步后端、Docker 或模拟器。
- 已新增 FFI bridge 单测，覆盖默认 factory 回退 fixture、FFI snapshot 映射、delete / import / export 调用委派；Flutter test 当前共 8 个测试通过。

## 2026-07-04 Flutter manager FFI bridge 验证记录

```text
flutter pub get
dart format .
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-repo.sh
```

结果：

- `flutter pub get` 通过，新增 `ffi 2.2.0` 直接依赖并更新 `pubspec.lock`。
- `dart format .` 在 `apps/radishlex-manager` 通过。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 8 个测试，包含新增 FFI bridge 单测和既有 widget tests。
- `./scripts/check-manager.sh` 通过，覆盖 `dart format --set-exit-if-changed .`、`flutter analyze` 和 `flutter test`。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager FFI smoke 推进记录

- 已新增 `apps/radishlex-manager/tool/ffi_bridge_smoke.dart` 和 `scripts/check-manager-ffi-smoke.sh`。
- smoke 脚本会构建 `radishlex-ime-ffi` 动态库，在仓库外临时目录创建 SQLite userdb、合成 TSV 导入文件和导出文件，并通过真实 Dart `FfiManagerBridge` 复验 inspect、dry-run import、实际 import、snapshot list、delete、export、learning status 和 sync preflight 摘要。
- smoke 只使用合成词条，不打印导入 / 导出文件内容，不连接真实同步后端，不读取真实输入法目录，不启动长期服务、Docker、模拟器或系统输入法。
- 已将新脚本加入仓库必备文件检查，并同步 README、manager README、`docs/repository-layout.md`、`docs/manager-ui-boundary.md` 和 `docs/technical-plan.md`。

## 2026-07-04 Flutter manager FFI smoke 验证记录

```text
./scripts/check-manager-ffi-smoke.sh
dart format apps/radishlex-manager/tool/ffi_bridge_smoke.dart
```

结果：

- `./scripts/check-manager-ffi-smoke.sh` 首次在沙盒内完成 Rust cdylib 构建，但 `dart run` 因 Flutter SDK cache 写入权限受限失败；提权到真实环境后进入 Dart 编译。
- 第一次真实环境运行发现 smoke 工具缺少 `dart:convert` 的 `utf8` import；补齐后复跑通过。
- smoke 通过后确认真实动态库路径、Dart FFI string view 复制、临时 SQLite userdb、导入 / 删除 tombstone / 导出和摘要映射均可执行。

## 2026-07-04 Flutter manager rank explain FFI 接线记录

- 已在 `ime-ffi` 新增 `RadishLexRankExplain*` opaque handle、`RadishLexRankExplainView` 和 `radishlex_userdb_rank_explain_new/view/free`，按显式 SQLite userdb 路径、输入码、候选文本、可选 reading 与 context kind 返回单候选 ranker explain 摘要。
- Rust ABI 测试覆盖活跃用户词贡献、ranker weight 的 frequency / recency / context 贡献、删除 tombstone penalty、非法参数和 null handle 释放；view 中字符串继续借用自 explain handle，平台绑定层必须复制后释放。
- Dart FFI bridge 已接入 rank explain 动态符号、native DTO 和 view copy；`FfiManagerBridge` 的 `rank explain` 区域不再用 userdb term 计数派生标签，而是展示 native `final_score` 与贡献项摘要。
- `scripts/check-manager-ffi-smoke.sh` 的真实 Dart FFI smoke 已扩展到导入后读取 rank explain，继续只使用临时 SQLite userdb 和合成词条，不连接真实同步后端，不读取真实输入法目录，不启动长期服务。
- 已同步 README、manager README、`docs/ffi-boundary.md`、`docs/manager-ui-boundary.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`；真实远端同步、恢复码和设备授权 UI 继续关闭。
- 验证中发现 `crates/ime-ffi/src/abi.rs` 被新增 ABI 测试推过 1500 行硬限制，已将 rank explain C ABI 测试迁入 `crates/ime-ffi/tests/rank_explain.rs`，并将 `docs/ffi-boundary.md` 压回 500 行目标内。

## 2026-07-04 Flutter manager rank explain FFI 验证记录

```text
cargo fmt
cargo fmt --check
cargo test -p radishlex-ime-ffi
cargo test -p radishlex-ime-ranker
dart format .
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `cargo test -p radishlex-ime-ffi` 通过 12 个 ABI 单元测试、5 个既有 integration tests、3 个 rank explain integration tests 和 doc-tests。
- `cargo test -p radishlex-ime-ranker` 通过 6 个 ranker 单元测试。
- `flutter analyze` 与 `flutter test` 在 `apps/radishlex-manager` 通过；当前 Flutter test 共 8 个测试，覆盖 FFI bridge fake 映射和既有 widget flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、导入后 rank explain、删除 tombstone、导出和摘要映射可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager 本地诊断收敛记录

- Dart FFI bridge 已接入 `radishlex_userdb_import_batches_*`，snapshot 现在包含导入批次摘要；学习页新增 import batches 区域，展示 source、导入量、插入 / 更新计数、跳过 deleted / duplicate 计数和创建时间。
- `ManagerSettings` 已补运行时诊断模型，设置页展示 bridge 模式、本地 userdb 配置来源、native library 来源、sync endpoint 来源和最近错误码；fixture fallback 会把 native 加载失败折叠成结构化错误码，不把动态库加载明细透传给 widget 层。
- `ManagerBridgeFailure` 固定 widget 层可见错误边界；导入 / 导出操作和首页加载失败只展示结构化 code 与通用说明，不显示 native 原始错误字符串、SQLite 细节、token、恢复码、signature bytes 或 wrapped material。
- `scripts/check-manager-ffi-smoke.sh` 的真实 Dart FFI smoke 已扩展到导入后读取 import batches；继续只使用临时 SQLite userdb 和合成 TSV，不连接真实同步后端，不读取真实输入法目录，不启动长期服务。
- 已同步 README、manager README、`docs/manager-ui-boundary.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`；真实远端同步、恢复码和设备授权 UI 继续关闭。

## 2026-07-04 Flutter manager 本地诊断验证记录

```text
dart format .
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format .` 在 `apps/radishlex-manager` 通过。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 10 个测试，覆盖 FFI bridge fake 映射、import batches 展示、配置来源诊断和结构化 bridge 错误展示。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、导入批次读取、rank explain、删除 tombstone、导出和摘要映射可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager 诊断报告与错误分类记录

- 已新增 `ManagerDiagnosticsReport`、诊断 section / item 和导出结果模型；报告只汇总 runtime 配置来源、聚合计数、import batch 计数、sync gate、device gate 和 redaction policy，不包含用户词条、导入 / 导出文件内容、本地真实路径、token、恢复码、signature bytes、wrapped material 或 payload bytes。
- `ManagerBridge` 已新增诊断报告预览和导出接口，`FixtureManagerBridge` 与 `FfiManagerBridge` 共用同一份脱敏报告生成逻辑；设置页可预览 / 导出诊断摘要，真实 Dart FFI smoke 已复验导出文件不包含临时 SQLite 路径、导入文件路径或合成用户词。
- 已新增 `ManagerBridgeFailurePresentation`，按操作和分类展示稳定错误提示，当前覆盖本地 userdb、dictionary import、dictionary export、native library、rank explain、sync preflight、invalid input 和 internal 等分类；widget 层不读取 native 原始错误明细。
- 已同步 README、manager README、`docs/manager-ui-boundary.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`；真实远端同步、恢复码和设备授权 UI 继续关闭。

## 2026-07-04 Flutter manager 诊断报告验证记录

```text
dart format .
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format .` 在 `apps/radishlex-manager` 通过。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 11 个测试，覆盖 FFI bridge 诊断报告脱敏、设置页诊断预览 / 导出和结构化错误分类展示。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager 设置草案与 sync gate 接线记录

- 已新增 `ManagerSettingsDraft` 与 `ManagerSettingsStore`，支持无路径时内存草案和显式 `RADISHLEX_MANAGER_SETTINGS_FILE` settings JSON 持久化；settings 文件只保存 server endpoint 草案、保留同步配置、隐私模式、诊断导出开关和部署证据草案，不保存 token、恢复码、wrapped material、signature bytes、payload bytes 或用户词条。
- `ManagerBridge` 已新增设置草案保存接口；`FfiManagerBridge` 会用 `RADISHLEX_MANAGER_SYNC_SERVER` 作为首次草案 fallback，并在显式 settings file 存在时读取 / 写入草案。`FixtureManagerBridge` 使用内存草案验证 UI 行为。
- sync gate 状态已由设置草案、隐私模式、平台私钥 backend gate 和部署证据草案派生；当前可显示 `local_only`、`sync_disabled_by_policy`、`backend_unavailable` 或 `deployment_unverified`，不会进入 `ready_for_user_sync`。
- 设置页已接入服务端草案、隐私模式、诊断导出、保留同步配置和部署证据草案的保存链路；同步页保存后立即反映新的 gate 状态，真实上传按钮继续禁用。
- 已同步 README、manager README、`docs/manager-ui-boundary.md`、`docs/technical-plan.md`、`docs/roadmap.md` 和 `docs/repository-layout.md`；真实远端同步、恢复码和设备授权 UI 继续关闭。

## 2026-07-04 Flutter manager 设置草案验证记录

```text
dart format .
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format .` 在 `apps/radishlex-manager` 通过。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 13 个测试，覆盖设置草案保存、settings JSON 持久化、sync gate 来源派生和真实 FFI bridge 重新加载草案。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、临时 settings JSON、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager 页面拆分记录

- 已将 `manager_home_screen.dart` 从单文件页面收缩为 manager shell，保留 snapshot 加载、导航、bridge 操作调度、加载失败和统一错误提示。
- 已新增 `dictionary_view.dart`、`learning_view.dart`、`sync_view.dart` 和 `settings_view.dart`，分别承载词库、学习、同步和设置页面，保留原有 widget key、文案和交互入口。
- 已新增 `manager_widgets.dart`，集中放置页面共享的 section、metric tile、key-value row 和 status badge，后续页面继续复用这一组基础组件。
- 已同步 manager README 与 `docs/repository-layout.md`，记录当前页面结构；本轮不改变 `ManagerBridge` contract，不新增远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager 页面拆分验证记录

```text
dart format .
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format .` 在 `apps/radishlex-manager` 通过，未产生额外格式化改动。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 13 个测试，覆盖 FFI bridge fake 映射、设置草案、sync gate、诊断预览 / 导出和既有 widget flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、临时 settings JSON、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出仍可执行。
- `git diff --check` 与 `./scripts/check-repo.sh` 通过，确认文本卫生、文档预算、Go server 测试、Rust workspace 测试和 doc-tests 未回归。

## 2026-07-04 Flutter manager 模型拆分记录

- 已将 `manager_models.dart` 收缩为模型 barrel 入口，保持现有 screen、bridge 和 test 的 import 兼容。
- 已按职责新增 `manager_dictionary_models.dart`、`manager_learning_models.dart`、`manager_sync_models.dart`、`manager_settings_models.dart`、`manager_diagnostics_models.dart` 和 `manager_snapshot.dart`。
- 词库、学习、同步 gate、设置草案、运行诊断、脱敏诊断报告和聚合 snapshot 的模型边界已分离；本轮不改变 `ManagerBridge` contract，不新增远端同步、恢复码或设备授权 UI。
- 已同步 manager README 与 `docs/repository-layout.md`，将 Flutter manager 当前实际结构对齐为 `lib/src/bridge`、`lib/src/models` 和 `lib/src/screens`。

## 2026-07-04 Flutter manager 模型拆分验证记录

```text
dart format apps/radishlex-manager/lib/src/models
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format apps/radishlex-manager/lib/src/models` 通过，7 个模型文件无需额外格式化。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 13 个测试，覆盖 FFI bridge fake 映射、设置草案、sync gate、诊断预览 / 导出和既有 widget flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、临时 settings JSON、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出仍可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager 同步预检审计记录

- 已将同步页从单纯状态摘要扩展为本地审计视图，展示 sync gate 状态来源、本地 P2 对象分类、syncable 对象数、local-only 事件计数、最近上传 / 下载摘要和设备 backend / production gate 阻断原因。
- 同步页 `启用同步` 主按钮继续禁用，即使本地状态进入 `ready_for_user_sync` 也只显示后续开放提示，不触发真实远端上传、恢复码或设备授权路径。
- 已补 local-only 空分类状态、策略禁用来源、backend unavailable 来源和设备 gate badge 的 widget tests；设置草案保存后同步页会刷新 gate 来源说明。
- 已同步 manager README 与 `docs/manager-ui-boundary.md`，记录同步页本地 P2 分类、设备门禁审计和 Phase 4 停止线；本轮不改变 `ManagerBridge` contract，不新增 C ABI。

## 2026-07-04 Flutter manager 同步预检审计验证记录

```text
dart format apps/radishlex-manager/lib/src/screens/sync_view.dart apps/radishlex-manager/test/widget_test.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
```

结果：

- `dart format` 覆盖同步页和 widget tests，格式化后无额外改动。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 24 个测试，覆盖新增同步页本地审计展示和既有 manager flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、导入批次读取、rank explain、删除 tombstone、导出和 sync preflight 摘要仍可执行。
- `git diff --check` 通过；本轮范围为 manager UI、widget tests 和文档同步，未运行 `./scripts/check-repo.sh`。

## 2026-07-04 Flutter manager native DTO 分组记录

- 已将 `ffi_manager_native_models.dart` 收缩为 native DTO barrel 入口，保持现有 bridge、mapper 和测试 import 兼容。
- 已新增 `ffi_manager_native_contract.dart`、`ffi_manager_native_dictionary.dart`、`ffi_manager_native_learning.dart`、`ffi_manager_native_sync.dart` 和 `ffi_manager_native_rank.dart`，分别承载 native bridge contract / exception、词库与导入导出 DTO、学习摘要 DTO、同步预检 DTO 和 rank explain DTO。
- 本轮只调整 Dart 侧 native DTO 组织方式，不改变 `RadishLexManagerNativeBinding` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager native DTO 分组验证记录

```text
dart format apps/radishlex-manager/lib/src/bridge
flutter analyze
flutter test
./scripts/check-manager-ffi-smoke.sh
./scripts/check-manager.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format apps/radishlex-manager/lib/src/bridge` 通过，23 个 bridge 文件无需额外格式化。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 14 个测试，确认 barrel export 拆分后 FFI bridge fake 映射、mapper 测试和既有 widget flows 仍通过。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、临时 settings JSON、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出仍可执行。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager 动态 FFI bridge 拆分记录

- 已将 `ffi_dynamic_native_binding.dart` 收缩为 `RadishLexManagerNativeBinding` 主实现，继续负责 list/delete/import/export、learning status、rank explain 和 sync preflight 的方法编排。
- 已将动态符号加载拆为 `ffi_dynamic_native_api.dart` 和 `ffi_dynamic_native_symbols.dart`，按 user terms、dictionary、import batches、learning、sync、rank 和 errors 分组映射 `ime-ffi` ABI。
- 已新增 `ffi_dynamic_native_calls.dart`，集中管理 status/error object、UTF-8 string view、native string 参数和 error free 规则。
- 已新增 `ffi_dynamic_native_views.dart`，集中把 Rust view 和 summary struct 复制成 Dart native model，确保 widget 层不持有 Rust handle 或 borrowed view。
- 本轮不改变 `RadishLexManagerNativeBinding` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager 动态 FFI bridge 拆分验证记录

```text
dart format apps/radishlex-manager/lib/src/bridge
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format apps/radishlex-manager/lib/src/bridge` 通过，13 个 bridge 文件格式化完成。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 13 个测试，覆盖 FFI bridge fake 映射、设置草案、sync gate、诊断预览 / 导出和既有 widget flows。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、临时 settings JSON、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出仍可执行。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager FFI manager bridge 映射拆分记录

- 已将 `ffi_manager_bridge.dart` 收缩为 `ManagerBridge` 编排层，保留 native binding 调用、settings store 读取 / 保存、诊断报告导出和 rank explain 加载入口。
- 已新增 `ffi_manager_dictionary_mapper.dart`、`ffi_manager_learning_mapper.dart`、`ffi_manager_sync_mapper.dart`、`ffi_manager_runtime_diagnostics.dart` 和 `ffi_manager_snapshot_mapper.dart`，分别承载 native DTO 到 manager model 的词库、学习、同步 gate、运行诊断和聚合 snapshot 映射。
- 已补 mapper 级测试，覆盖 term source label、learning 无活动摘要、sync preflight categories、rank explain signals、settings runtime diagnostics 和 settings fallback trim。
- 本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager FFI manager bridge 映射验证记录

```text
dart format apps/radishlex-manager/test/ffi_manager_bridge_test.dart apps/radishlex-manager/lib/src/bridge
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖 bridge 与 `ffi_manager_bridge_test.dart`，格式化后无额外问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 14 个测试，新增 mapper 级测试随 FFI bridge fake 映射和既有 widget flows 一起通过。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、临时 settings JSON、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出仍可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager 词库操作体验记录

- 已将词库页搜索框接入本地 snapshot 过滤，覆盖 input code、text、reading 和 source，并补空 userdb 与无匹配结果两类空态。
- 已将删除词条改为确认后再调用 `ManagerBridge.deleteUserTerm`；删除失败只显示结构化错误分类提示，保留当前词库页，不把页面切到全局加载失败态。
- 已补导入检查 / 导入结果和导出结果的用户可见摘要，包含记录数、新增 / 更新 / 跳过计数、导出格式和 sync class。
- 已补 widget tests 覆盖搜索过滤、空态、删除确认、删除失败、导入 / 导出结果反馈和导入检查失败分类；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager 词库操作体验验证记录

```text
dart format apps/radishlex-manager/lib/src/screens/dictionary_view.dart apps/radishlex-manager/lib/src/screens/manager_home_screen.dart apps/radishlex-manager/test/widget_test.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖词库页、home screen 和 widget tests，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 18 个测试，覆盖新增词库操作体验和既有 FFI bridge / widget flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、临时 settings JSON、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出和脱敏诊断报告导出仍可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go server 测试、Rust workspace 测试和 doc-tests。

## 2026-07-04 Flutter manager sync gate 口径对齐记录

- 已在 manager sync model 层新增统一 gate audit 表达，集中生成状态标签、状态来源、停止线、设备 production gate 标签和部署证据标签。
- 同步页改为复用统一 gate audit，不再在页面内维护独立状态来源和停止线文案。
- 设置页新增同步门禁草案预览，用户保存设置前即可看到当前草案会派生出的 gate 状态、状态来源、部署证据、设备 backend 和停止线。
- 脱敏诊断报告新增 `sync.state_label`、`sync.state_source`、`sync.action_stop_line`、`sync.deployment_evidence` 和 `device.production_gate_label`，继续不包含用户词、文件路径、token、恢复码、signature bytes、wrapped material 或 payload bytes。
- 已同步 manager README、`docs/manager-ui-boundary.md`、`docs/technical-plan.md` 和 `docs/roadmap.md`；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager sync gate 口径对齐验证记录

```text
dart format apps/radishlex-manager/lib/src/models/manager_sync_models.dart apps/radishlex-manager/lib/src/models/manager_diagnostics_models.dart apps/radishlex-manager/lib/src/screens/sync_view.dart apps/radishlex-manager/lib/src/screens/settings_view.dart apps/radishlex-manager/lib/src/screens/manager_home_screen.dart apps/radishlex-manager/test/widget_test.dart apps/radishlex-manager/test/ffi_manager_bridge_test.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖 manager sync model、诊断报告、同步页、设置页、home screen 和相关测试，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 24 个测试，覆盖设置页 gate 草案预览、诊断报告 gate source、FFI bridge fake 映射和既有 manager flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出、sync preflight 摘要和脱敏诊断报告导出仍可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 Flutter manager settings draft 格式与证据来源记录

- 已在 `ManagerSettingsDraft` 增加非敏感 `deploymentEvidenceSource`，只允许 `local_smoke`、`external_tls`、`backup_restore` 和 `upgrade_rollback` 这类来源标签；不保存日志正文、证书、token、恢复码、请求 / 响应体、payload bytes 或运行输出。
- settings JSON 继续使用 `format_version: 1`，新增 `deployment_evidence_source` 字段；旧 v1 文件如果只有 `deployment_evidence_recorded: true` 但没有来源字段，会降级为未记录证据，避免旧布尔值直接影响 gate。
- settings store 已补格式演进测试，覆盖 v1 写入 / 读取、旧 v1 缺字段降级、未知格式拒绝、带 userinfo 的 server endpoint 拒绝和非 allowlist evidence source 拒绝。
- 设置页新增部署证据来源下拉；在合成 `productionGate=ready` 的 fixture 下，只有 server 草案、有效 evidence source 和非隐私模式同时满足时才进入 `preflight_ready`，真实同步按钮仍保持禁用。
- 诊断报告新增 settings draft 的 evidence label / source 摘要，继续只输出脱敏标签和聚合信息。
- 已同步 manager README、`docs/manager-ui-boundary.md`、`docs/technical-plan.md` 和 `docs/roadmap.md`；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager settings draft 格式与证据来源验证记录

```text
dart format apps/radishlex-manager/lib/src/models/manager_settings_models.dart apps/radishlex-manager/lib/src/models/manager_sync_models.dart apps/radishlex-manager/lib/src/models/manager_diagnostics_models.dart apps/radishlex-manager/lib/src/bridge/manager_settings_store.dart apps/radishlex-manager/lib/src/screens/settings_view.dart apps/radishlex-manager/tool/ffi_bridge_smoke.dart apps/radishlex-manager/test/ffi_manager_bridge_test.dart apps/radishlex-manager/test/widget_test.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
```

结果：

- `dart format` 覆盖 settings model、sync model、诊断报告、settings store、设置页和相关测试，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 27 个测试，覆盖 settings draft 格式演进、非法输入拒绝、部署证据来源下拉、`preflight_ready` 草案派生和既有 manager flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、设置草案 evidence source 持久化、导入批次读取、rank explain、删除 tombstone、导出、sync preflight 摘要和脱敏诊断报告导出仍可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 Flutter manager settings 与诊断字段参考记录

- 已新增 `docs/manager-settings-diagnostics.md`，集中记录 Phase 4 manager settings draft JSON schema、部署证据来源 allowlist、sync gate 派生、诊断报告字段索引、脱敏禁止项和验证入口。
- settings draft 字段参考明确 `format_version: 1`、`server_endpoint` URL 校验、`deployment_evidence_source` allowlist 和旧 v1 缺字段降级语义；诊断报告字段索引覆盖 `runtime`、`settings_draft`、`local_data`、`latest_import_batch`、`sync_gate` 和 `redaction` sections。
- 已同步根 README、manager README、`docs/manager-ui-boundary.md`、`docs/technical-plan.md` 和 `docs/roadmap.md`，将 settings / diagnostics 字段参考纳入正式入口。
- 本轮只补文档和索引，不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager settings 与诊断字段参考验证记录

```text
git diff --check
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 Flutter manager 诊断预览审计视图记录

- 已将设置页诊断报告预览从整段文本展示扩展为字段审计视图，显示报告格式、分组数、字段数和脱敏策略摘要。
- 预览支持按 `runtime`、`settings_draft`、`local_data`、`latest_import_batch`、`sync_gate` 和 `redaction` 分组切换，并可按字段 key、value、kind 或分组名筛选。
- 新增复制脱敏文本入口，复制内容与 `manager.diagnostics.v1` 导出文本一致，继续不包含用户词、文件路径、token、恢复码、signature bytes、wrapped material 或 payload bytes。
- 已补 widget test 覆盖字段分组、section 筛选、关键字筛选、脱敏文本检查、clipboard 调用和既有诊断导出路径；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。
- 已同步 manager README、`docs/manager-ui-boundary.md` 和 `docs/manager-settings-diagnostics.md`。

## 2026-07-04 Flutter manager 诊断预览审计视图验证记录

```text
dart format apps/radishlex-manager/lib/src/screens/settings_view.dart apps/radishlex-manager/test/widget_test.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖设置页和 widget tests，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 27 个测试，覆盖诊断预览审计视图和既有 manager flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出、sync preflight 摘要和脱敏诊断报告导出仍可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 Flutter manager 设置诊断 UI 结构拆分记录

- 已将 `DiagnosticsReportDialog`、诊断报告摘要、section 视图和字段 row 从 `settings_view.dart` 拆到 `lib/src/screens/settings/diagnostics_report_dialog.dart`。
- 已将 `DiagnosticsExportDialog` 拆到 `lib/src/screens/settings/diagnostics_export_dialog.dart`，设置页本体继续只负责 settings draft 表单、sync gate 草案预览和配置来源入口。
- 已新增 `test/screens/settings_diagnostics_test.dart`，单独覆盖诊断报告预览、section 筛选、关键字筛选、脱敏文本复制和诊断导出调用；`test/widget_test.dart` 保留跨页主流程、词库、学习、同步和 settings draft 测试。
- 已同步 manager README 的代码结构说明；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager 设置诊断 UI 结构拆分验证记录

```text
dart format lib/src/screens/manager_home_screen.dart lib/src/screens/settings_view.dart lib/src/screens/settings/diagnostics_report_dialog.dart lib/src/screens/settings/diagnostics_export_dialog.dart test/widget_test.dart test/screens/settings_diagnostics_test.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖设置页、诊断对话框和相关测试，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 27 个测试，新增 `test/screens/settings_diagnostics_test.dart` 覆盖原诊断预览与导出路径。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实动态库、临时 SQLite userdb、设置草案保存、导入批次读取、rank explain、删除 tombstone、导出、sync preflight 摘要和脱敏诊断报告导出仍可执行。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 Flutter manager 页面 widget test 拆分记录

- 已将词库页测试迁入 `test/screens/dictionary_test.dart`，覆盖词库首页、搜索、空态、词条审计、导入历史、删除、导入 / 导出和失败分类。
- 已将学习页测试迁入 `test/screens/learning_test.dart`，覆盖聚合学习摘要、rank explain 筛选、候选详情和空态。
- 已将设置草案测试迁入 `test/screens/settings_test.dart`，覆盖配置来源、隐私模式 gate 降级和部署证据来源 `preflight_ready` 派生。
- 已将同步页测试迁入 `test/screens/sync_test.dart`，覆盖真实同步按钮禁用、本地 P2 对象分类和 local-only 空态。
- `test/widget_test.dart` 只保留 app shell 级加载失败结构化错误展示；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager 页面 widget test 拆分验证记录

```text
dart format test/widget_test.dart test/screens/dictionary_test.dart test/screens/learning_test.dart test/screens/settings_test.dart test/screens/settings_diagnostics_test.dart test/screens/sync_test.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖拆分后的 manager widget tests，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 27 个测试，确认拆分后页面级覆盖仍被默认发现。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认测试结构拆分未影响真实 Dart FFI bridge smoke 链路。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 Flutter manager 词库页 UI 结构拆分记录

- 已将词库页删除确认、导入请求、导入检查和导出对话框从 `dictionary_view.dart` 拆到 `lib/src/screens/dictionary/dictionary_dialogs.dart`。
- 已将导入历史区域、筛选 / 排序控件、本地 sync preflight 影响摘要和导入批次表格拆到 `lib/src/screens/dictionary/import_history_section.dart`。
- `dictionary_view.dart` 继续保留词库页状态、词条筛选、词条表格、审计详情和 tombstone 摘要；`ManagerHomeScreen` 仍统一调度 bridge 操作。
- 已同步 manager README 的代码结构说明；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager 词库页 UI 结构拆分验证记录

```text
dart format lib/src/screens/manager_home_screen.dart lib/src/screens/dictionary_view.dart lib/src/screens/dictionary/dictionary_dialogs.dart lib/src/screens/dictionary/import_history_section.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖词库页、迁出的词库组件和 manager home screen，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 27 个测试，覆盖词库页搜索、空态、审计详情、导入历史筛选 / 排序、删除、导入 / 导出和失败分类。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge 本地词库管理 smoke 链路未受 UI 拆分影响。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 Flutter manager home action 编排拆分记录

- 已新增 `lib/src/screens/manager/manager_home_actions.dart`，集中承载词条删除、词库导入 / 导出、诊断预览 / 导出和设置草案保存的对话框调用、bridge 调用、结果文案和结构化失败文案。
- `manager_home_screen.dart` 继续保留 snapshot future、页面选择、导航壳层、加载失败展示和 snackbar 入口；bridge 操作仍全部通过既有 `ManagerBridge` contract。
- 已同步 manager README 的代码结构说明；本轮不改变 `ManagerBridge` contract，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

## 2026-07-04 Flutter manager home action 编排拆分验证记录

```text
dart format lib/src/screens/manager_home_screen.dart lib/src/screens/manager/manager_home_actions.dart
flutter analyze
flutter test
./scripts/check-manager.sh
./scripts/check-manager-ffi-smoke.sh
git diff --check
./scripts/check-repo.sh
```

结果：

- `dart format` 覆盖 manager home screen 和 action helper，格式化后无额外问题。
- `flutter analyze` 在 `apps/radishlex-manager` 通过，无静态分析问题。
- `flutter test` 在 `apps/radishlex-manager` 通过 27 个测试，覆盖跨页 bridge 操作、结构化失败提示和既有 manager flows。
- `./scripts/check-manager.sh` 通过 Dart format、Flutter analyze 和 Flutter test。
- `./scripts/check-manager-ffi-smoke.sh` 通过，确认真实 Dart FFI bridge smoke 链路未受 action 编排拆分影响。
- `git diff --check` 通过；`./scripts/check-repo.sh` 通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests，仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning。

## 2026-07-04 今日提交回顾

本日提交覆盖平台私钥 backend 策略收口、同步部署演练检查、Phase 4 manager 边界固定、Flutter manager 起步、真实 Dart FFI bridge 接线、本地管理能力扩展、settings / diagnostics 审计能力、页面结构拆分和 widget test 拆分。所有 manager 变更均保持 `ManagerBridge` contract 不变，不新增 C ABI，不打开真实远端同步、恢复码或设备授权 UI。

提交清单：

- `6ab6bb3 docs: 收口平台私钥 backend 策略`
- `2f786b2 test(sync): add deployment rehearsal check`
- `43c54cd docs: 固定管理端边界`
- `4a0392c feat(manager): bootstrap Flutter management app`
- `5cbcd5c feat(manager): connect first Dart FFI bridge`
- `9efb855 test(manager): add FFI bridge smoke`
- `3b9ef55 feat(manager): extend local Dart FFI bridge`
- `b5c4eee feat(manager): persist local settings draft`
- `ee24e0d refactor(manager): split manager screen pages`
- `2002111 refactor(manager): split manager models`
- `c1c0aa2 refactor(manager): split dynamic ffi bridge`
- `ff0ae91 refactor(manager): split ffi manager mapping`
- `b48fffd refactor(manager): split native ffi models`
- `d71a917 feat(manager): improve dictionary operations`
- `201de9e feat(manager): add dictionary import history view`
- `a0cfc7d feat(manager): enrich dictionary import audit view`
- `1902ed3 feat(manager): add dictionary term audit detail`
- `550c5e9 feat(manager): improve learning explain audit`
- `a7fd34e feat(manager): expand sync preflight audit view`
- `d668ee7 feat(manager): align sync gate audit surfaces`
- `941ae58 feat(manager): gate sync draft evidence source`
- `2414711 docs(manager): document settings diagnostics schema`
- `6630bba feat(manager): add diagnostics audit preview`
- `7e8bcb0 refactor(manager): split settings diagnostics and widget tests`
- `4cecce4 refactor(manager): split dictionary view widgets`
- `c18dec9 refactor(manager): split home action orchestration`

代码验证回顾：

- manager 诊断预览、settings diagnostics 拆分、widget test 拆分、词库页拆分和 home action 编排拆分后，均已运行 `dart format`、`flutter analyze`、`flutter test`、`./scripts/check-manager.sh`、`./scripts/check-manager-ffi-smoke.sh`、`git diff --check` 和 `./scripts/check-repo.sh`。
- 最新一次代码提交前，`flutter test` 在 `apps/radishlex-manager` 通过 27 个测试；`./scripts/check-manager-ffi-smoke.sh` 复验真实 Dart FFI bridge、临时 SQLite userdb、settings JSON、导入批次、rank explain、词条删除、导出、sync preflight 和脱敏诊断报告导出。
- `./scripts/check-repo.sh` 持续通过 text hygiene、Go server 测试、Rust workspace 测试和 doc-tests；当前仅保留 `docs/devlogs/2026-W27.md` 篇幅 warning，明日应优先治理默认阅读成本。

相关文档审阅结果：

- `apps/radishlex-manager/README.md` 已在代码提交中记录 manager home action、词库子组件、设置诊断子组件、bridge 分层和页面级 widget tests 的当前结构。
- `docs/manager-ui-boundary.md` 与 `docs/manager-settings-diagnostics.md` 的边界、停止线和字段参考仍与实现一致，本轮不需要修改。
- `docs/repository-layout.md`、`docs/technical-plan.md` 和 `docs/roadmap.md` 需要同步今天形成的 manager UI / test 结构、诊断预览能力和下一步推进顺序，已在文档收尾中更新。
