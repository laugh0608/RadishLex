# 2026-07-01 开发日志

本文档记录 2026-W27 周内该日期的推进事实、验证证据和后续建议。长期架构边界应回写到对应专题文档，当前状态短入口见 `../../status/current.md`。

## 2026-07-01 推进记录

- 已补 `server/sync-server/internal/runtime/backup_restore_smoke_test.go`，新增短生命周期备份恢复 smoke。
- 测试先通过真实 HTTP handler 创建 `domain-backup`、第二设备 join / authorization、`dictionary.user_terms` / `ranker.weights` / `dictionary.deleted_terms` 三类 P2 encrypted object，并触发一次 deleted terms stale conflict 审计事件。
- HTTP server 停止并关闭 SQLite store 后，测试通过 storage 边界写入 signed active recovery record 和 encrypted wrapped material；随后复制完整数据目录到备份目录，再恢复到隔离目录并重启 Go HTTP handler。
- 恢复后复验 domain state、device-b active 状态、recovery latest metadata + wrapped material、三类 P2 object metadata / binary payload、deleted terms stale conflict latest metadata，以及 SQLite `audit_events` 中的上传与 stale conflict 记录。
- 测试断言 metadata 响应不包含 encrypted payload，recovery latest 不泄漏内部 `blob_ref`，runtime log 不包含 payload、wrapped key、wrapped material、恢复码或同步主密钥等敏感字段。
- 已同步 `docs/technical-plan.md`、`docs/roadmap.md`、`docs/sync-server-api-storage.md` 和 `docs/runbooks/sync-server-production-deployment.md`，将备份恢复 smoke 调整为已补实现级证据，同时保留真实部署必须在目标数据目录上做冷备份 / 恢复演练的停止线。

## 2026-07-01 验证记录

```text
go test ./internal/runtime -run TestLocalServerBackupRestorePreservesEncryptedSyncState -count=1
go test ./...
git diff --check
./scripts/check-repo.sh
```

结果：

- `go test ./internal/runtime -run TestLocalServerBackupRestorePreservesEncryptedSyncState -count=1` 通过，覆盖新增备份恢复 smoke。
- `go test ./...` 在 `server/sync-server` 通过，覆盖 Go sync server 全部 package 测试。
- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未运行 Docker `up` / `curl` 容器启动 smoke，未启动长期服务；未运行 Apple Keychain smoke，未触碰本机 Keychain、证书、输入法或系统环境；未提交真实 secret、本机绝对路径、容器生成数据或真实用户数据。

## 后续事项

- 当时下一步主线转为外部 TLS 真实验证证据；继续保持部署态 HTTP upstream、本地测试态 Caddy internal TLS、唯一 env 文件和唯一对外端口。
- 目标部署开放真实用户同步前，仍必须由部署者对目标 `RADISHLEX_SYNC_DATA_PATH`、真实 `.env`、镜像 tag、反向代理和备份保留策略执行冷备份 / 恢复到隔离目录演练。
- Apple Keychain 真实 smoke、可用平台私钥 backend、OIDC / Radish 产品账号体系、Flutter manager 同步 UI 和平台壳仍不作为当前主线。

## 2026-07-01 外部 TLS 推进记录

- 已补 `server/sync-server/internal/runtime/external_tls_smoke_test.go`，新增短生命周期外部 TLS 反代 smoke。
- 测试使用 HTTPS client 访问 TLS reverse proxy，proxy 再转发到 Go sync server HTTP upstream，验证 Go server 仍保持部署态 HTTP 上游边界，不在服务端内置 TLS。
- 测试覆盖 `Authorization: Bearer` 透传到 upstream、`X-Forwarded-Proto=https`、TLS 1.2+、缺失 token 的 `401 unauthenticated`、有效 token 下 domain 创建、encrypted object 上传 / payload 读取，以及超过 Go `MaxObjectBytes` 的 `payload_too_large`。
- 测试断言 runtime log 不包含 access token、encrypted payload bytes 或 signature bytes。
- 已同步 `docs/technical-plan.md`、`docs/roadmap.md`、`docs/sync-server-api-storage.md` 和 `docs/runbooks/sync-server-production-deployment.md`，将外部 TLS 反代 smoke 调整为已有实现级证据；目标部署真实证书、域名、反代配置和反代日志策略仍需部署者人工复验。

## 2026-07-01 外部 TLS 验证记录

```text
go test ./internal/runtime -run TestExternalTLSProxySmokePreservesAuthAndEncryptedObjectFlow -count=1
go test ./...
git diff --check
./scripts/check-repo.sh
```

结果：

- `go test ./internal/runtime -run TestExternalTLSProxySmokePreservesAuthAndEncryptedObjectFlow -count=1` 通过，覆盖新增外部 TLS 反代 smoke。
- `go test ./...` 在 `server/sync-server` 通过，覆盖 Go sync server 全部 package 测试。
- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未启动 Docker、Nginx、Caddy 或长期运行服务；未生成真实证书、未修改系统信任链、未提交真实域名、真实 token、TLS 私钥或用户数据。

## 后续事项（更新）

- 下一步代码主线转为 sync server 升级回滚演练运行证据，继续围绕生产部署停止线补可复验证据。
- 目标部署开放真实用户同步前，仍必须人工复验真实证书 / 域名 / 外部反代、目标数据目录备份恢复、升级回滚、平台私钥 backend 和用户可用同步 UI。

## 2026-07-01 升级回滚推进记录

- 已补 `server/sync-server/internal/runtime/upgrade_rollback_smoke_test.go`，新增短生命周期升级回滚 smoke。
- 测试先创建 domain、完成第二设备授权、上传 `dictionary.user_terms` v1，并在关闭 HTTP server / SQLite store 后写入 signed active recovery record；随后复制完整数据目录作为升级前冷备份。
- 测试使用同一数据目录重启 Go HTTP handler，触发 SQLite migration idempotent path，并复验 domain state、device state、recovery latest、v1 metadata / payload；随后用第二设备上传 v2，证明升级后写入路径仍可用。
- 测试再把升级前冷备份恢复到隔离目录，以该目录启动 rollback server，确认 v2 不存在、v1 payload 仍可读、stale upload 仍按 latest v1 metadata 返回 409，并断言 runtime log 不泄漏 payload、signature、wrapped material、恢复码或同步主密钥字段。
- 已同步 `docs/technical-plan.md`、`docs/roadmap.md`、`docs/sync-server-api-storage.md`、`docs/runbooks/sync-server-production-deployment.md`、`docs/privacy-sync.md` 和 `docs/crypto-boundary.md`，将升级回滚 smoke 调整为已有实现级证据，同时保留目标部署真实镜像 tag、数据目录、`.env`、外部反代和备份保留策略的人工演练停止线。

## 2026-07-01 升级回滚验证记录

```text
go test ./internal/runtime -run TestLocalServerUpgradeRollbackPreservesPreUpgradeBackup -count=1
go test ./...
git diff --check
./scripts/check-repo.sh
```

结果：

- `go test ./internal/runtime -run TestLocalServerUpgradeRollbackPreservesPreUpgradeBackup -count=1` 通过，覆盖新增升级回滚 smoke。
- `go test ./...` 在 `server/sync-server` 通过，覆盖 Go sync server 全部 package 测试。
- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未启动 Docker、Nginx、Caddy、长期 Go server 或平台输入法服务；未写入真实 `.env`、真实 token、TLS 私钥、证书、用户数据或仓库外目标部署数据目录。

## 后续事项（再次更新）

- 下一步主线转为目标部署运行证据和可用平台私钥 backend 验证边界，继续保持本地优先、端到端加密和服务端默认不可信。
- 目标部署开放真实用户同步前，仍必须人工复验真实证书 / 域名 / 外部反代、目标数据目录备份恢复、升级回滚、平台私钥 backend 和用户可用同步 UI。

## 2026-07-01 Android Keystore backend 验证边界推进记录

- 已提交上一批升级回滚 smoke，提交为 `af5bb2e test(server): cover sync upgrade rollback smoke`。
- 已新增 `docs/runbooks/android-keystore-signing-backend.md`，固定 `android-keystore-v1` 的 Android Keystore Ed25519 创建 / 加载 / 签名 / 删除、锁屏 / 权限、备份迁移、IME 生命周期和日志脱敏验证边界。
- runbook 明确 Android Keystore 的目标优势是 key material 不进入 app 进程并在设备支持时绑定到 TEE / Secure Element，但 RadishLex 不能只凭 API 文档推断 Ed25519 可用；必须通过 API / 设备矩阵 smoke 证明后才能接 Kotlin / JNI。
- runbook 明确不引入 P-256、不改变 Go / Rust Ed25519 verifier、不把 seed 存入普通 app storage、SharedPreferences、SQLite、文件或可导出的 backup，也不让 InputMethodService 输入热路径参与同步签名。
- 已同步 `docs/adr/0004-platform-private-key-storage-backend.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/crypto-boundary.md`、`docs/sync-key-management.md`、`docs/privacy-sync.md`、`docs/production-recovery-flow.md`、`docs/sync-payload.md` 和 `docs/repository-layout.md`，将 Android Keystore 调整为已有验证边界；后续 feature 门禁推进记录见下文。

## 2026-07-01 Android Keystore backend 验证记录

```text
git diff --check
./scripts/check-repo.sh
```

结果：

- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未接 Kotlin / JNI、未启动 Android 模拟器、未触碰真实 Android Keystore、未修改系统输入法配置，也未新增真实设备 ID、账号、token、证书、私钥或用户数据。

## 后续事项（平台 backend）

- 下一步可补 `android-keystore-v1` 的 feature / smoke 骨架或 Android API / 设备矩阵记录；若 Ed25519 仍不可用，必须先补签名算法 ADR 或独立软件保护 backend ADR，不能在 `android-keystore-v1` 内静默降级。
- 目标部署运行证据仍需要真实域名、证书、外部反代、目标数据目录和部署者人工演练。

## 2026-07-01 Android Keystore feature 门禁推进记录

- 已提交 Android Keystore runbook 与入口文档，提交为 `291148f docs: add Android Keystore signing backend boundary`。
- 已新增 `android-keystore` feature、`AndroidKeystoreDeviceKeyStore`、`DeviceSigningBackendCapabilities::android_keystore_v1()`、`DevicePrivateKeyStoreStatus::android_keystore_v1()` 和 ignored `android_keystore_smoke` 入口。
- 该批次的 `android-keystore-v1` store 只表达平台桥接前的不可用状态：不访问 Android Keystore，不创建 key，不声明 `available = true`，不回退到 `test-memory-v1`、软件 seed 或普通 app storage。
- 单元测试覆盖 Android Keystore capability metadata、生产签名阻断、feature-gated store status、Debug 脱敏和不可用错误语义；ignored smoke 当前只验证平台 bridge 完成前不能声明生产可用。
- 已同步 `docs/runbooks/android-keystore-signing-backend.md`、`docs/adr/0004-platform-private-key-storage-backend.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/crypto-boundary.md`、`docs/sync-key-management.md`、`docs/privacy-sync.md`、`docs/production-recovery-flow.md`、`docs/sync-payload.md` 和 `docs/repository-layout.md`，将状态调整为 feature-gated Rust 门禁已落地、Kotlin / JNI 与真实 Android Keystore smoke 未完成。

## 2026-07-01 Android Keystore feature 门禁验证记录

```text
cargo fmt --check
cargo test -p radishlex-ime-crypto
cargo test -p radishlex-ime-crypto --features android-keystore
git diff --check
./scripts/check-repo.sh
```

结果：

- `cargo fmt --check` 通过。
- `cargo test -p radishlex-ime-crypto` 通过，默认 feature 下不访问 Android Keystore。
- `cargo test -p radishlex-ime-crypto --features android-keystore` 通过，`android_keystore_smoke` 保持 ignored，普通测试只验证门禁和脱敏。
- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未接 Kotlin / JNI、未启动 Android 模拟器、未触碰真实 Android Keystore、未修改系统输入法配置，也未新增真实设备 ID、账号、token、证书、私钥或用户数据。

## 2026-07-01 Android Keystore bridge wrapper 推进记录

- 在 `android-keystore` feature 下把 `AndroidKeystoreDeviceKeyStore` 从纯不可用 stub 推进为 Rust 侧 bridge wrapper：store 统一负责 opaque handle、alias 映射、revoked key 阻断、Debug 脱敏和 production status 门禁，平台能力通过内部 bridge 调用。
- 默认 bridge 仍返回 unavailable / unsupported，不访问 Android Keystore，不创建 key，不声明 `available = true`，也不回退到 `test-memory-v1`、软件 seed 或普通 app storage。
- 新增合成 bridge 单元测试，复验 `create_signing_key`、`handle`、`public_key`、`sign`、`DeviceSignature::verify_at`、tamper reject、`delete_or_revoke` 后签名阻断和 Debug 不泄漏完整 alias / canonical bytes。
- `docs/runbooks/android-keystore-signing-backend.md` 已补 Rust bridge wrapper 当前状态，并把 Android API 候选路径继续写成需设备矩阵验证；当前官方 `KeyProperties` API 未提供 `KEY_ALGORITHM_ED25519` 常量，后续 Kotlin / JNI 必须用真实 API / provider / 设备结果证明可用。
- 已同步 `docs/technical-plan.md`、`docs/roadmap.md`、`docs/crypto-boundary.md`、`docs/sync-key-management.md`、`docs/privacy-sync.md`、`docs/production-recovery-flow.md`、`docs/sync-payload.md`、`docs/repository-layout.md` 和 `docs/adr/0004-platform-private-key-storage-backend.md`，将状态调整为 Rust bridge wrapper 已落地、Kotlin / JNI 与真实 Android Keystore smoke 未完成。

## 2026-07-01 Android Keystore bridge wrapper 验证记录

```text
cargo fmt --check
cargo test -p radishlex-ime-crypto
cargo test -p radishlex-ime-crypto --features android-keystore
git diff --check
./scripts/check-repo.sh
```

结果：

- `cargo fmt --check` 通过。
- `cargo test -p radishlex-ime-crypto` 通过，默认 feature 下不访问 Android Keystore。
- `cargo test -p radishlex-ime-crypto --features android-keystore` 通过，覆盖合成 bridge 单测；`android_keystore_smoke` 保持 ignored。
- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未接 Kotlin / JNI、未启动 Android 模拟器或虚拟机、未触碰真实 Android Keystore、未修改系统输入法配置，也未新增真实设备 ID、账号、token、证书、私钥或用户数据。

## 2026-07-01 Android Keystore bridge contract 推进记录

- 已提交 Android Keystore Rust bridge wrapper，提交为 `0440af4 feat(crypto): add Android Keystore bridge wrapper`。
- 在 `ime-crypto` 的 `android-keystore` feature 下新增 bridge contract 常量和类型：`ANDROID_KEYSTORE_BRIDGE_CONTRACT_VERSION = 1`、`ANDROID_KEYSTORE_PROVIDER = AndroidKeyStore`、`ANDROID_KEYSTORE_SIGNATURE_ALGORITHM = Ed25519`、bridge operation、bridge request 和 bridge error code。
- 已公开 alias 构造、public key 校验和 signature 校验 helper，后续 Kotlin / JNI 层返回的 public key 必须为 32 bytes，signature 必须为 64 bytes；格式不符映射为 `private_key_corrupted`，不能继续进入 Go server 验签路径。
- Bridge request Debug 只打印 alias 长度和 canonical bytes 长度；错误码白名单只映射到既有 `CryptoError`，不允许把完整 alias、canonical bytes、signature bytes、KeyInfo dump 或 provider exception 原文写入错误对象。
- 已同步 `docs/runbooks/android-keystore-signing-backend.md`、`docs/technical-plan.md`、`docs/roadmap.md`、`docs/crypto-boundary.md`、`docs/sync-key-management.md`、`docs/privacy-sync.md`、`docs/production-recovery-flow.md`、`docs/sync-payload.md`、`docs/repository-layout.md` 和 `docs/adr/0004-platform-private-key-storage-backend.md`，将状态调整为 Rust bridge contract 已落地、Kotlin / JNI 与真实 Android Keystore smoke 未完成。

## 2026-07-01 Android Keystore bridge contract 验证记录

```text
cargo fmt
cargo fmt --check
cargo test -p radishlex-ime-crypto
cargo test -p radishlex-ime-crypto --features android-keystore
git diff --check
./scripts/check-repo.sh
```

结果：

- `cargo test -p radishlex-ime-crypto --features android-keystore` 通过 41 个单元测试，覆盖 bridge request 校验 / Debug 脱敏、error code 映射、public key / signature 长度校验、默认 unavailable bridge 和合成 bridge 创建 / 签名 / 删除语义；`android_keystore_smoke` 保持 ignored。
- `cargo test -p radishlex-ime-crypto` 通过，默认 feature 下不访问 Android Keystore。
- `cargo fmt --check` 通过。
- `git diff --check` 通过，无尾随空白或文本 diff hygiene 问题。
- `./scripts/check-repo.sh` 通过 text hygiene、documentation budget、Go sync server package 测试、Rust workspace 测试和 doc-tests。
- 本轮未接 Kotlin / JNI、未启动 Android 模拟器或虚拟机、未触碰真实 Android Keystore、未修改系统输入法配置，也未新增真实设备 ID、账号、token、证书、私钥或用户数据。

## 2026-07-01 提交回顾

今日 `dev` 相对 `origin/dev` 新增 6 个提交，均围绕 Phase 3 自部署同步证据链和平台私钥 backend 停止线推进：

- `0ea4b4e test(server): cover sync backup restore smoke`：补短生命周期备份恢复 smoke，复验 SQLite metadata 与 encrypted blob dir 成对恢复、recovery latest、对象 payload 和日志脱敏。
- `76a0dfd test(server): cover external TLS proxy smoke`：补外部 TLS 反代 smoke，复验 HTTPS client、TLS 1.2+、Authorization header 透传、HTTP upstream、对象上传下载和日志脱敏。
- `af5bb2e test(server): cover sync upgrade rollback smoke`：补升级回滚 smoke，复验 migration idempotent path、升级后 v2 写入、恢复升级前备份后 v2 不可见、v1 payload 与 stale conflict。
- `291148f docs: add Android Keystore signing backend boundary`：补 Android Keystore runbook 和入口文档，固定 Ed25519 可用性必须实测、不可降级到 seed / `test-memory-v1`、IME 热路径不参与同步签名。
- `0440af4 feat(crypto): add Android Keystore bridge wrapper`：补 `android-keystore` feature、Rust store、默认 unavailable bridge、合成 bridge 单测和 ignored smoke 入口。
- `f6f20cb feat(crypto): define Android Keystore bridge contract`：补 bridge contract version、provider / algorithm 常量、operation、request、error code、alias / public key / signature 校验 helper 和对应文档。

## 2026-07-01 文档审阅结论

- `docs/technical-plan.md` 与 `docs/roadmap.md` 已覆盖今日状态：备份恢复、外部 TLS、升级回滚为已有实现级证据；Android Keystore 已有 runbook、feature 门禁、Rust bridge wrapper、bridge contract 和 ignored smoke，但未接 Kotlin / JNI 或真实设备 smoke。
- `docs/runbooks/android-keystore-signing-backend.md` 已覆盖 Android bridge contract、错误码白名单、public key / signature 长度校验、日志脱敏和真实设备 smoke 停止线。
- `docs/sync-key-management.md`、`docs/crypto-boundary.md`、`docs/privacy-sync.md`、`docs/production-recovery-flow.md`、`docs/sync-payload.md`、`docs/repository-layout.md` 和 `docs/adr/0004-platform-private-key-storage-backend.md` 已同步 Android Keystore 当前状态，没有发现必须额外补写的设计主文档。
- `docs/runbooks/sync-server-production-deployment.md`、`docs/sync-server-api-storage.md` 和相关入口文档已覆盖今日 server smoke 证据；目标部署真实证书、真实域名、目标数据目录、真实 `.env`、镜像 tag、外部反代日志和备份保留策略仍保留为部署者人工复验项。
- 本周志当前承担 2026-W27 的历史流水和交接职责，行数仍低于普通活跃 Markdown 的拆分警戒线；后续若继续大幅追加，应优先在 `docs/status/current.md` 或周志摘要中提炼当前状态入口。

## 明天事项（2026-07-02）

- 主线建议继续推进 Android Keystore Kotlin / JNI bridge 的仓库内接线准备：先补 Android platform shell / bridge contract 文档或代码骨架，固定 Kotlin 调用 `AndroidKeyStore` provider 的创建、加载、公钥读取、签名、删除和错误码映射，不要求明天必须打开模拟器或虚拟机。
- Android 接线应继续沿用 `ime-crypto` 已公开的 bridge contract：`ANDROID_KEYSTORE_BRIDGE_CONTRACT_VERSION = 1`、`AndroidKeyStore`、`Ed25519`、固定 operation、固定 error code、opaque `signing_key_id` 和内部 alias；Kotlin / JNI 层不得返回 seed、私钥 bytes、完整 alias、canonical bytes 或 provider exception 原文。
- 明天如果只做仓库内准备，验证重点应覆盖 Rust contract 测试、文档一致性、ignored smoke 不误触发真实设备，以及 `cargo test -p radishlex-ime-crypto --features android-keystore`、`git diff --check` 和必要时 `./scripts/check-repo.sh`。
- 如明天要进入真实 Android API / 设备矩阵 smoke，必须先明确设备、API level、Android security patch、build tools、Keystore provider 行为、是否会创建临时测试 key、如何删除测试 key，并在运行前获得开发者明确批准。
- 若真实设备证明 Ed25519 + `AndroidKeyStore` 不可创建、不可加载或不可签名，应记录 `unsupported_signature_algorithm`，继续保持 `android-keystore-v1` production status 阻断；不要在该 backend 内切到 P-256、软件 seed、SharedPreferences、SQLite、文件存储或 `test-memory-v1`。
- 目标部署运行证据仍是备选方向：真实证书 / 域名 / 外部反代、目标数据目录冷备份恢复、升级回滚和日志策略需要部署者人工复验；若明天不具备目标环境，应继续优先推进 Android bridge contract 到平台接线的仓库内准备。
- Flutter manager、平台输入法 UI、OIDC / Radish 产品账号、全量 Android 键盘功能和用户可用同步入口继续后置；平台私钥 backend 可用前，不进入用户可用同步主线。
