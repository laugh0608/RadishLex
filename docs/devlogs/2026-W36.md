# RadishLex 2026-W36 开发周志

本文按 Asia/Shanghai 记录 2026-W36 的阶段推进与真实验证事实，不替代当前状态、Linux L6 runbook或资产生命周期账本，也不保存raw operation ID或用户数据。

## 2026-09-01：第三批 VM 退休 prepare 与删除控制

- 从clean `e2098bb`执行唯一只读attempt `l6-asset-retirement-prepare-20260901-third-batch-v1`。一次plain `utmctl list`确认14台全部stopped及canonical inventory `4005023b5f65b64074d0af9fa90d1995738d53a70cf1b0eb495e17a635d04bac`，rollback `EFD15599…BBDD`、remove `5EA2BAA2…27A2`与reinstall `E671DB9C…D465`唯一在册。
- 3个既存终态锚点、两轮9文件零句柄及两轮config/EFI/qcow2完整身份均通过。create-new根`RadishLex-L6-Asset-Retirement-Prepare-20260901-Third-Batch-v1`为`0700`，10个文件为`0600`/owner `501:20`/single-link且零xattr；9项manifest SHA-256为`7bcfa513debbe7e276a509509b44953abe0ecb9218aa78da41c7b533eb5f7f38`。
- terminal闭合`prepared`，inventory/handle/hash为`1/2/2`，delete/start/clone/move/guest/retry均为0；prepare授权已消费且证据立即冻结。一次后续离线复核的手写expected HEAD错误在repository首门被拒绝，未创建delete根、调用UTM或delete；改用Git返回的完整HEAD后纯离线绑定通过。
- `22dcf30`新增`--authorized-delete-third-batch-v1`，要求三个batch授权互斥并将第三批资产数精确固定为3。10项delete与14项prepare定向回归、默认L6门禁通过；真实prepare的9项成员被新控制器离线递归绑定，未来delete根保持absent。
- 项目所有者随后明确授权`third-batch-v1`唯一真实删除并确认不可恢复移除注册项与bundle。从clean `2e49353`执行attempt `l6-asset-retirement-delete-20260901-third-batch-v1`，mutation前重新通过3个锚点、两轮完整身份、14台全停inventory与两轮9文件零句柄。
- 依allowlist顺序对rollback `EFD15599…BBDD`、remove `5EA2BAA2…27A2`、reinstall `E671DB9C…D465`各执行一次精确delete；三次均exit 0、未超时且stdout/stderr为空。每次唯一post-list严格为14→13→12→11，恰好移除当次UUID，其余成员不变且全stopped，三个package均absent。
- terminal闭合`deleted/third-batch-v1-deleted-and-verified`，inventory/delete为`4/3`，无retry/rollback/start/clone/move/guest。create-new根为`0700`，19个文件为`0600`/owner `501:20`/single-link且零xattr；18项manifest SHA-256为`74e2ac3cbdced1c39a74beb7cff34163a2bcdec24fa4d0ac431d72aa2c35ebdc`，最终11台全停inventory hash为`be17001859eff1fbc4e7fa4d9e9764af662630668521d7a9e54b4f689603b1f3`。
- 授权已消费，不得复跑。三个退休batch累计删除11个raw VM、账面103.32 GiB；注册项现为11台，剩余6个候选继续按repository-only设计、独立prepare与独立mutation授权推进，snapshot仍冻结。
