# RadishLex 2026-W36 开发周志

本文按 Asia/Shanghai 记录 2026-W36 的阶段推进与真实验证事实，不替代当前状态、Linux L6 runbook或资产生命周期账本，也不保存raw operation ID或用户数据。

## 2026-09-01：第三批 VM 退休 prepare 与删除控制

- 从clean `e2098bb`执行唯一只读attempt `l6-asset-retirement-prepare-20260901-third-batch-v1`。一次plain `utmctl list`确认14台全部stopped及canonical inventory `4005023b5f65b64074d0af9fa90d1995738d53a70cf1b0eb495e17a635d04bac`，rollback `EFD15599…BBDD`、remove `5EA2BAA2…27A2`与reinstall `E671DB9C…D465`唯一在册。
- 3个既存终态锚点、两轮9文件零句柄及两轮config/EFI/qcow2完整身份均通过。create-new根`RadishLex-L6-Asset-Retirement-Prepare-20260901-Third-Batch-v1`为`0700`，10个文件为`0600`/owner `501:20`/single-link且零xattr；9项manifest SHA-256为`7bcfa513debbe7e276a509509b44953abe0ecb9218aa78da41c7b533eb5f7f38`。
- terminal闭合`prepared`，inventory/handle/hash为`1/2/2`，delete/start/clone/move/guest/retry均为0；prepare授权已消费且证据立即冻结。一次后续离线复核的手写expected HEAD错误在repository首门被拒绝，未创建delete根、调用UTM或delete；改用Git返回的完整HEAD后纯离线绑定通过。
- `22dcf30`新增`--authorized-delete-third-batch-v1`，要求三个batch授权互斥并将第三批资产数精确固定为3。10项delete与14项prepare定向回归、默认L6门禁通过；真实prepare的9项成员被新控制器离线递归绑定，未来delete根保持absent。
- 项目所有者随后明确授权`third-batch-v1`唯一真实删除并确认不可恢复移除注册项与bundle。从clean `2e49353`执行attempt `l6-asset-retirement-delete-20260901-third-batch-v1`，mutation前重新通过3个锚点、两轮完整身份、14台全停inventory与两轮9文件零句柄。
- 依allowlist顺序对rollback `EFD15599…BBDD`、remove `5EA2BAA2…27A2`、reinstall `E671DB9C…D465`各执行一次精确delete；三次均exit 0、未超时且stdout/stderr为空。每次唯一post-list严格为14→13→12→11，恰好移除当次UUID，其余成员不变且全stopped，三个package均absent。
- terminal闭合`deleted/third-batch-v1-deleted-and-verified`，inventory/delete为`4/3`，无retry/rollback/start/clone/move/guest。create-new根为`0700`，19个文件为`0600`/owner `501:20`/single-link且零xattr；18项manifest SHA-256为`74e2ac3cbdced1c39a74beb7cff34163a2bcdec24fa4d0ac431d72aa2c35ebdc`，最终11台全停inventory hash为`be17001859eff1fbc4e7fa4d9e9764af662630668521d7a9e54b4f689603b1f3`。
- 授权已消费，不得复跑。三个退休batch累计删除11个raw VM、账面103.32 GiB；注册项现为11台，剩余6个候选继续按repository-only设计、独立prepare与独立mutation授权推进，snapshot仍冻结。

## 2026-09-01：第四批 VM 退休候选

- 从clean `6571e0c`开始，只在repository-only范围核对剩余6个候选。第五套rolled-back与repair completed的历史终态证据树均为owner `501:0`，不满足prepare的`501:20`合同；第三套source terminal在S2之后继续启动，最终EFI/qcow2与S2不同。三者继续保留，没有修改证据、放宽合同或把S2写成当前bundle副本。
- `8172c49`固定`fourth-batch-v1`三台：第六套target/S3 `193179D5…EC05`、`install_prepared` terminal `3EC83EB9…593B9`与`install_artifacts_staged` terminal `50B75F88…8038`，精确账面27.09 GiB。三项资产均不跨batch复用，剩余困难候选留给后续第五批。
- 5个既存锚点均为`0600`/owner `501:20`，新增语义分别绑定S3/source、第一场景正常stop与第二场景completed transaction/唯一graceful stop；三套config/EFI/qcow2两轮纯文件系统复核一致。allowlist SHA-256为`850f31b13e64dfa4a8fb253b30dbb89212beee3e022540d455302cf07caf1a47`。
- prepare 15项、delete 10项与完整L6合同通过；delete控制器显式在输出前拒绝`fourth-batch-v1`。本批UTM query/start/clone/delete/move/guest/retry均为0，prepare根保持absent；下一步只能另行授权一次只读prepare，不含mutation。
- 项目所有者随后授权唯一只读attempt `l6-asset-retirement-prepare-20260901-fourth-batch-v1`。从clean `edd58b0`执行一次plain `utmctl list`，确认11台全部stopped、canonical inventory `be17001859eff1fbc4e7fa4d9e9764af662630668521d7a9e54b4f689603b1f3`且三个目标唯一在册。
- 5个既存锚点、两轮9文件零句柄和两轮config/EFI/qcow2身份均通过。create-new根`RadishLex-L6-Asset-Retirement-Prepare-20260901-Fourth-Batch-v1`为`0700`，10个文件均为`0600`/owner `501:20`/single-link且零xattr；9项manifest SHA-256为`9fca287626c95482c070a7c7a90834adc2eb295d264d2e83a18287e9ccc7b388`。
- terminal闭合`prepared`，inventory/handle/hash为`1/2/2`，delete/start/clone/move/guest/retry均为0。prepare授权已消费且不得复跑；delete控制器仍拒绝第四批，下一步仅可repository-only扩展授权面，不构成真实mutation授权。
- `0ad0fc2`新增`--authorized-delete-fourth-batch-v1`，四个batch授权必须互斥，第四批资产数精确固定为3；成功terminal、错授权、未知batch和四批prepare递归绑定均纳入10项delete回归，15项prepare、完整L6与全仓基线通过。
- clean `0ad0fc2`纯离线递归绑定真实prepare的9项成员，返回3个精确asset、27.09 GiB、allowlist `850f31b1…1a47`与manifest `9fca2876…b388`；计划delete根仍absent，UTM/delete/start/clone/move/guest/retry均为0。真实mutation继续等待独立授权。
- 项目所有者随后授权第四批唯一真实删除。从clean `de14d7c`执行attempt `l6-asset-retirement-delete-20260901-fourth-batch-v1`，preflight重新通过5个锚点、两轮完整身份、11台全停inventory与两轮9文件零句柄。
- 依allowlist顺序对`193179D5…EC05`、`3EC83EB9…593B9`、`50B75F88…8038`各执行一次精确delete；三次均exit 0、未超时且stdout/stderr为空，post-list严格为11→10→9→8，三个package均absent，其余成员不变且全stopped。
- terminal闭合`deleted/fourth-batch-v1-deleted-and-verified`，list/delete为`4/3`，无retry/rollback/start/clone/move/guest。18项manifest SHA-256为`d1e548388275a28412e531e85d01a460aedb5b8c81d5c0ae436d5fad1335ed94`，最终inventory为`667af90157c9aaf001439b7dc403b8f5d978ca79ecbb53a79ddcbb683a48d701`；授权已消费，不得复跑。
- 四批累计删除14个raw VM、账面130.41 GiB，注册项现为8台；剩余3个困难候选只可先repository-only设计第五批，snapshot仍冻结。

## 2026-09-02 至 2026-09-05：`upgrade_quiesced` clone foreign overlay

- clean `b3e8491`的唯一clone v1通过第五批18项manifest、5台冻结基线、三package absent、不可变S2、专用壳、target absent和零句柄；首次plain list另观察2台Windows VM，其中`Windows11-ARM64` started。旧合同因全局7台不等于5台在clone前拒绝，clone/replacement为0/0，target仍absent。
- create-new根`RadishLex-L6-Crash-Upgrade-Quiesced-d75818f-Clone-Once-v1`含8项manifest，SHA-256为`79901be8bb19c18b71eef775c4ef11267ec580e294a8fa64cb65146611f5265f`；terminal闭合`precondition-rejected/utmctl-list-preclone:preclone-vm-count-mismatch`，无start/delete/retry/rollback/guest/transaction。授权已消费，根冻结且不复用。
- 项目所有者暂停UTM后，2026-09-05明确恢复并授权严格foreign overlay模型。一次新的plain list只读观察相同7项现均stopped；该观察不冒充持久attempt成功。repository-only v3继续逐项固定第五批5台managed基线并递归绑定v1的8项失败证据，live request改为绑定完整count/hash。
- foreign overlay不进入RadishLex managed 5→6预算，但必须在preclone全部stopped，clone后逐UUID/name/status保持不变；target是唯一允许新增的stopped项。managed成员缺失、foreign started、live hash/数量或terminal overlay漂移均失败关闭，不自动delete/retry/start/rollback或进入guest。
- 10项case、11项clone定向测试、完整L6与全仓门禁通过；真实v1冻结根的纯离线递归复核返回8项、2台foreign和零clone语义一致。该repository-only批次没有再次查询或操作UTM，也没有创建v2输出根。
- clean `2811ef3`随后执行获授权的唯一v2。preclone一次list精确闭合7台全stopped、5 managed + 2 foreign及canonical `d9103084…9bb1`；唯一`utmctl clone` exit 0且postclone仅新增stopped UUID `2672A88A…224A`，原7项逐UUID/name/status不变。
- UTM将新包放到默认Documents，而请求绑定的operator-root target持续absent，故控制在任何物化前以`target-package-postclone:clone-postconditions-indeterminate`停止。replacement为0；默认包EFI/qcow2 `7b0a7f26…00d5`/`ae44c4d0…2b94b`仍与壳体相同并不同于S2 `365b5a17…61bc`/`0c2cefd6…3eea`。12项manifest为`0fa2cd297be9d0739485c3e9d0d4e47d3193ed5c9ef6f1527f6522c61f3084ff`，逐项hash、`0700`/`0600`、owner `501:20`、single-link与零xattr通过；无start/delete/retry/rollback/guest/transaction。v2与新增stopped壳体现场冻结，下一步先repository-only设计原生Move恢复与后续exact materialization，真实动作另行授权。
- repository-only partial recovery v1固定历史HEAD、12项v2、7→8 inventory、target UUID及当前壳体身份，分离`move-prepare`、外部UTM UI Move、只读`move-adopt`与`materialize-once`四个授权面。物化才允许2次clonefile/2次原子替换，并在5轮句柄门下保持config、S2和8台全停inventory不变；11项case合同、12项合成回归、完整L6与全仓门禁及真实v2纯离线binding通过。三个固定create-new根均absent，本批未调用UTM或改写现场。
- 项目所有者只授权partial `move-prepare`。首次直接执行0644脚本在控制器启动前exit 126，根仍absent且UTM query为0；随后用`/usr/bin/python3`从clean `7765f1c`执行唯一控制器attempt。v2、S2、registration shell、默认partial、目标absent和两轮9文件零句柄通过；唯一list闭合8台全stopped、6 managed + 2 foreign及`4fdf8692…12a0`。16项manifest `fa6e7335c59b7adc97d811a88048be795ebfc0f4df0f8fda4f03cbc434233ea5`经权限/hash/语义和离线递归binding闭合`move-ready`；move/copy/replace/start/clone/delete/retry、guest/transaction均为0。根冻结且授权消费，UI Move仍须独立授权。

## 2026-09-05：入口历史证据归档

以下条目从 `a5345b8` 的 `docs/status/current.md`“当前证据”原样迁入，包含此前多个日期的历史记录，不表示在本日重跑或复验。当前终态、停止线与下一授权仍以 current 和对应 runbook 为准。

- P05A carrier、production relationship、恢复事务、system port/CLI/startup gate、L6 format/controller/pair/refresh均已闭合；合成矩阵不替代真实现场。
- 六类真实operation均有分散证据：第六套形成target completed/S3，独立clone闭合repair、rollback、remove与reinstall；这些不能冒充同一连续session。精确receipt、package、guest/host manifest和磁盘身份进入L6 runbook/周志。
- 旧pair的`install_prepared`在dpkg前因startup未接受合法`01777 /run/lock`失败关闭；修复target `d75818f`的第三台clone只生成一次operation ID并闭合checkpoint、exact resume与关机冻结，细项见runbook。
- `install_artifacts_staged`的typed guest合同固定target-only staging、package/dpkg未变、合法guard、XDG/process/network零漂移与resume重验/单次apply；首次安装精确Rust回归通过，production语义无需修改。
- 第二个case的旧clone、v2和v3均在host失败关闭且未进入guest。v1-v7诊断最终只定位到UTM接收start后的AppKit主窗口断言，未证实更深根因；全部失败现场与manifest冻结，不再原地start/clone或试跑`--hide`。
- v4 clone/prepared/foreground launch固定UUID `50B75F88…8038`、`Network=[]`与载体身份；launch manifest `6dbbdf40…fc3c`保持`state-indeterminate`，后续QEMU双句柄与network v2独立证明当前boot运行且仅loopback。plain list/status曾使backend再次出现，故不循环查询，也不把活动磁盘hash当terminal。
- v4断网、canonical bundle/input transfer、延迟resolution与negative preflight已按manifest `40be3f3f…38383`/`ffc990a3…e0de`/`d1090e8d…dc09`/`7bab8f20…4e4a`/`aba59811…0d7a`依次冻结并闭合`input-ready`、`preflight-ready`；过程不进入operation、dpkg或自动补救，精确阶段与权限见runbook。
- repository-only checkpoint v1绑定25项preflight、前序manifest、source/target与exclusive marker，固定canonical三阶段、operation ID hash-only、`artifacts_staged`/零dpkg mutation及系统静止；14项测试进入L6门禁，未执行真实系统动作。
- clean `7bf6e04`的唯一checkpoint attempt `d75818f-v4-install-artifacts-staged-checkpoint-20260823-v1`通过全部冻结绑定与双回读；guest-local operation ID宿主只存hash `21041a89…111a`。acceptance调用1次并在`artifacts_staged`后SIGKILL完整进程组；checkpoint/receipt为`9cb4acb8…25e0`/`c759b5c6…5d34`，target-only staging、合法未锁guard、package absent、dpkg status/log未变、`ActiveGuard`双startup、XDG/process/network静止。create-new host根49项manifest `3aca0576…0a4f7`逐项通过且raw operation ID扫描为0；未resume、dpkg apply、retry、cleanup、stop、quit或plain list/status/start。
- repository-only exact resume v1逐项绑定49项checkpoint及transaction/boot/dpkg身份，只允许guest重验后单次resume/postflight、host ID hash-only和双回读，漂移即失败关闭；7项host、9项driver及相邻门禁通过，未执行真实系统动作。
- clean `7309313`的唯一exact resume attempt在host `target-handles-preflight`失败关闭：冻结绑定、source、target与process门通过，但相关进程为0且`lsof` exit 1。create-new根7项manifest `a436677c…adc7`逐项通过；file pull/push、guest exec、maintenance resume与postflight均为0，transaction仍为`artifacts-staged-preserved`。该观察只证明当时backend/句柄不满足资格，不证明VM已停止；未调用plain list/status/start、retry、stop或quit。
- backend resolution单次`status`证明registered stopped；reactivation单次`list`与foreground start后出现稳定QEMU句柄，但process合同矛盾，manifest `2a477730…c5c4c`闭合`state-indeterminate`且未进入guest/resume/retry/stop。
- runtime resolution v1/v2曾以唯一`list`观察v4 started、其余20台stopped及PID `61666`三次稳定确认，但两次boot hash均无有效输出；v2的18项manifest `e2e41235…c49e`闭合`state-indeterminate`，不证明boot或后续运行状态。
- clean `dbce3a5`的repository-only boot transport绑定上述18项和全部上游。clean `8f59825`的唯一真实attempt只执行一次`list`，观察21台均registered stopped；target未过inventory gate，PID/probe/guest/file/result均为0，8项manifest `6a1ad09f…5140`闭合`state-indeterminate`。
- clean `38b1bb8`新增boot start控制；clean `f972371`的唯一实机调用在21台全停后start，PID `39591`双句柄三次稳定，但首次guest root遇到`OSStatus -2700`/agent不可用；29项manifest `97959c55…c57d`闭合`state-indeterminate`。
- guest-agent attempt一次readiness即ready，但probe后marker不存在；26项manifest `eb7c42f1…d053`闭合`state-indeterminate`，根因为共享probe scope/root错配，现已修复且旧现场冻结。
- v4 fresh-boot分类、只读恢复资格和resume控制均已冻结；原resume仍为`state-indeterminate`。`202b64d`分开验证prior/current boot并解耦历史/successor driver，没有倒写旧attempt。
- `f05ebd7`排除非UTM QEMU误判；v2首次result缺失后，独立deferred resolution以双份1259-byte结果与completed phase闭合transaction `completed`，21项manifest `7fcef38e…0e3d`已由`7acbbde`递归绑定。
- clean `9485778`只发送一次正常stop，第2轮闭合21台all-stopped及目标句柄/进程absent；28项manifest `096fe01f…e133`已由`ad77c22`递归绑定。
- clone v1因foreign started零调用拒绝。clean `2811ef3`的v2以7→8全停新增stopped `2672A88A…224A`，包落默认Documents而以零物化闭合`0fa2cd29…084ff/state-indeterminate`。clean `7765f1c`的partial `move-prepare`以一次list闭合8台全停，16项`fa6e7335…33ea5/move-ready`冻结；未Move或物化。
- registration shell v1-v3三种configuration record均以`-1700`失败关闭且package/evidence absent；三个14项manifest `3e21aa02…e3c34`/`3c9c7ee8…3f604`/`ae3fb81b…4a511`冻结。`17d10a6`改为最小create后typed update并通过13项回归。
- clean `7ab855b`的唯一v4最小create返回UUID `0BAA7355…52A7`及22台全停inventory；授权路径absent使update为0，14项manifest `70251467…fb04`闭合`state-indeterminate`。默认`Documents`中的同名部分壳无句柄但位置不合规，shell evidence与真实target仍不存在。
- `9300ed7`闭合原生Move恢复；prepare v1首门拒绝，v2以一次list闭合22台全停与15项`move-ready` manifest `dc6b0d01…738e`；唯一UI Move将部分壳移至授权路径且三文件身份不变。
- complete v1因跨阶段HEAD误绑零调用失败；`9da8d0f`修复后，clean `e6968bf`的v2以两次全停list和一次update闭合`frozen`。20项manifest `91778f48…c438`、单成员壳体manifest `c37b552e…f715`通过，最终`Network=[]`且EFI/qcow2不变。
- clean `f99ab2d`的第二批prepare以`d802f331…8ac60`闭合；`3e8796a`新增独立授权位。项目所有者随后授权真实mutation，从clean `fd6d501`执行唯一attempt：4次精确delete与5次list闭合18→17→16→15→14，4个package均absent；21项manifest `d29ae273…bd88f`通过。
- clean `2e49353`的第三批唯一delete以3次精确调用和4次list闭合14→13→12→11，3个package均absent；18项manifest `74e2ac3c…5ebdc`通过，无retry/rollback/start/clone/move/guest。
- `8172c49`固定`fourth-batch-v1`三台/27.09 GiB；clean `edd58b0`的唯一prepare闭合`9fca2876…b388`，`0ad0fc2`新增delete授权位。clean `de14d7c`的唯一delete以3次调用、4次全停list闭合11→10→9→8，manifest `d1e54838…ed94`。
- 第五批prepare以8台全停、4个anchor闭合`ef4d45ea…ad3163`。clean `da15e97`的唯一delete以3次调用、4次全停list闭合8→5，package全absent；manifest `a65abab2…1b106`与最终inventory `dc91dd99…30fe8`通过。

## 2026-09-05：综合审阅与文档整理

- 从 clean `a5345b8` 开始综合审阅。关键路径抽查发现 Rime 自有学习控制范围、SQLite WAL-reset 依赖版本、输入回调同步数据库等待、新词召回与评测、删除与事件保留、MSRV/CI 覆盖等问题；源码事实、风险推断与尚未执行的实测分别记录。
- 审阅轮次的 offline/locked runtime、ranker 与 userdb lib 共 96 项测试通过；没有执行真实平台、用户数据、UTM、Keychain 或部署操作，也不构成全面安全审计。
- 项目所有者随后授权按审阅建议完善文档。新增 [产品审阅与改进跟踪](../remediation/product-review-2026-09.md)，固定 REV-01 至 REV-10 的证据、建议优先级、待决策项和关闭条件；依赖、架构、隐私方案、系统操作和发布尚未因此获准实施。
- current 增加能力与开放状态矩阵、审阅入口及当前证据摘要；原“当前证据”逐字迁入本周志上一节，原停止线逐字保留。入口字符数由 9,986 降至 6,177；迁移只改变文档归属，没有重验现场。
- 路线图移除 P05 历史流水，仓库结构移除重复成熟度快照；专题补清 Rime 隐私证据的观察范围、删词不等于全部记录擦除、当前新词召回和性能测试限制、ranker 传递依赖、CI 覆盖口径及首版同步关闭状态。
- 本轮保持 M5 阶段和系统操作顺位、统一发布政策、源代码可见许可证、根协作约定及冻结资产规则；尚未修复代码或更新依赖。
- 文档、文本、全量 Markdown 本地链接与 `git diff --check` 通过；机械比对确认原停止线逐字不变、历史证据完整迁移，改动仅涉及 10 份 Markdown。既有 L6 runbook 744 行、Sync API/Storage 508 行仍有预算警告，本批未扩展这些文档。
- 全仓检查先以 `CARGO_NET_OFFLINE=true GOPROXY=off GOSUMDB=off ./scripts/check-repo.sh` 在沙盒中执行，安装事务 guard 测试出现 I/O 失败；获准沙盒外按相同离线限制重跑后 exit 0，输出 `Repository baseline passed.`。这是既有 repository-only 检查，不构成真实安装、UTM 或产品用户数据验收；审阅风险没有因该门禁通过而关闭。
