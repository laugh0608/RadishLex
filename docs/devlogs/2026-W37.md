# RadishLex 2026-W37 开发周志

本文按 Asia/Shanghai 记录 2026-W37 的实际推进与验证，不替代当前状态、产品边界或实机 runbook；只使用合成测试数据与版本身份。

## 2026-09-08：REV-01 隔离复现与 REV-02 版本核验

- 起点为 clean `dev` / `1496901`，与本地 `origin/dev` 一致。项目所有者批准开始今日建议中的隔离隐私诊断、版本核验及修复方案准备；没有批准产品隐私合同调整、依赖更新或真实系统操作。
- 新增默认 ignored 的 `native_rime_privacy_probe`：产品数据离线装配，每场景独立临时数据，每阶段独立 OS 子进程，逐次释放与 shutdown 后才观察。LevelDB 导出仅打开合成副本，保留原库供后续阶段使用。正式记录轮次覆盖 12 场景 / 48 子进程；之前九场景探索根也原样保留。
- 本机 librime 1.17.0、Rust 1.96.0、Go 1.26.3。产品 schema 的 privacy、unknown、secure、sensitive、normal→privacy 与 normal→unknown 六场景均返回 skipped_by_policy、SQLite selection_events 为 0，但 Rime 保存一条合成选择；新进程在 unknown 策略下该候选仍升为首位。privacy→normal 与 unknown→normal 的既有 composition 提交还各生成一条 SQLite 事件。baseline/cancel 对照均零学习。
- 临时 schema 关闭 `enable_user_dict` 后，normal 仍产生一条 RadishLex 学习，privacy/unknown 为零；三项均未创建 Rime 用户词典。这不是产品修复，未验证旧库读取、完整输入质量或系统场景。指定日志普通文件未匹配合成选择 UTF-8，不扩展为所有日志无敏感数据。
- 12 场景结果位于系统临时目录 `radishlex-rime-privacy-34553-1788867658795085000/report.json`，SHA-256 为 `54299a9db8da3cd2393541503d640ae9f56515da41a814ee01417133e5c37be5`；目录由本轮新建，未复用用户或冻结实机数据。另存 `radishlex-rev01-identity-20260908.json` 绑定 source HEAD、测试源码/二进制、librime、导出工具与两份 schema 的 hash。测试源码与重跑说明已纳入仓库，详见 [隔离诊断 runbook](../runbooks/rime-privacy-probe.md)。
- 新增 Rust/Go opt-in SQLite runtime identity 入口：Rust 为 3.46.0，Go 为 3.53.2。只读扫描 build 38 双 FFI 均含 SQLite 3.46.0/source id；hash 与证据限制登记在 [审阅专题](../remediation/product-review-2026-09.md)。重核官方公告与 rusqlite v0.39.0/SQLite 3.51.3 候选，未安装依赖、改 lockfile、启动 bundle 或访问 guest。
- 方案建议为 RadishLex 独占学习并保留 composition 的禁学状态；已明确 Rime 自学词/新词召回收益、旧配置与旧数据处理的影响，待项目所有者批准。SQLite 建议单独处理 Rust 依赖链和 MSRV 评审；两项均保持开放。
- 验证：12 场景 native 诊断完成、Rust/Go runtime identity 查询通过，新增 Rust 测试格式/check/Clippy（`-D warnings`）通过，Go 已 gofmt；文档、文本和差异检查通过。完整 `./scripts/check-repo.sh` 首轮在沙盒 Unix socket guard 处出现 Io 失败，随后获准在沙盒外以离线依赖设置运行原完整入口，结果为 `Repository baseline passed`。沙盒与提权日志分别保留为系统临时目录下 `radishlex-rev01-check-repo-20260908.log` 与 `radishlex-rev01-check-repo-escalated-20260908.log`。
- 文档检查仍提示两个既有长文档预算警告；未执行真实平台隐私回归、Linux 制品 SQLite 查询、损坏复现或新依赖兼容测试。本轮通过表示诊断与仓库基线成立，不表示隐私缺口已修复或 M5 已退出。
- 诊断测试单独本地提交为 `4c3fe0f`；文档与观察结果随后独立提交，未推送远端。

## 2026-09-08：REV-01 单一学习存储修复

- 项目所有者要求“提交更改，然后继续推进下一批”，据已说明的推荐方案实施；诊断文档本地提交为 `b793187`，本批从该 clean 基线继续，没有推送、依赖更新或真实系统操作。
- runtime 增加 session 内 composition 策略收紧与实际空状态释放，覆盖受限→普通、普通→受限→普通、空闲切换、自动/分段 commit、reset/schema 失败、同 schema、余串、独立 session 及来源不明的 engine 输入。21 项 runtime 单元/集成测试通过。
- adapter 以公开 config API 检查并保活有效 default/所有 schema，拒绝底层启学和未审阅组件；部署只运行 installation/workspace 更新。拆出 learning_guard 与 runtime 测试文件，保持生产代码职责清晰。产品 schema 关闭 user_dict 并更新来源锁、Linux 产品 hash。
- 存储回归结果根为 `radishlex-rime-privacy-40007-1788869394558035000`：12 场景 / 48 独立进程通过；全程普通的 SQLite 各一条、受限/往返切换为零；空库不创建 Rime userdb，旧合成高频词库字节不变，重启基础候选及空库对照一致。配置回归根 `radishlex-learning-guard-40738-1788869584787540000`：六场景 / 12 进程通过，覆盖 stale/custom/未选中 schema 和真实缓存生命周期。
- 基础 native smoke 在新的 `radishlex-rev01-input-smoke-h_94pitd` 通过，验证分段、重排、删除恢复、翻页、退格/取消/Enter 与基础提交。前一次尾部断言失败暴露旧测试把 display index 当作 Rime highlight index；平台原本用 display-index selection，测试已把原生 highlight 段切到 engine-only，并保留产品映射断言。失败根 `radishlex-rev01-input-smoke-oj1ddtk0` 保留，未写成通过。
- Linux 冻结 L6 JSON 与制品不改写。静态入口新增冻结声明校验，实际构建仍执行原严格 source/target 比较；当前新 Rime hash 必须被拒绝。23 项 pair 与六项 source contract 测试、产品数据/metadata 检查通过。
- 完整 `check-repo.sh` 沙盒轮次在既有安装 Unix socket guard 处受限，获准在沙盒外以离线依赖设置重跑后为 `Repository baseline passed`；两份日志为 `radishlex-rev01-fix-check-repo-20260908.log` 与 `radishlex-rev01-fix-check-repo-escalated-20260908.log`。后续最终 native/文本检查记录于本节交付补记。
- 不关闭真实平台隐私回归、不量化新词召回损失、不替换 macOS build 38 或 Linux 载体；旧版进程与冻结 pair 不能套用新实现保证，REV-01/REV-04 与独立 SQLite 依赖批仍保持相应开放项。

交付补记：存储与配置回归分别在 `radishlex-rime-privacy-43940-1788870268466457000`、`radishlex-learning-guard-44152-1788870271667933000` 重跑通过；补充的 native 分段/自动 commit 与下一次普通学习在 `radishlex-rev01-input-smoke-fdyzy0bm` 通过，固定产品页大小为 5。FFI 无效 schema 与共享 runtime/peer release 在 `radishlex-rev01-ffi-lifecycle-v2mv3iq9` 通过。native feature 全量 Rust 测试在沙盒同步资格初始化受限后获准提权通过，日志为 `radishlex-rev01-fix-native-unit-escalated-20260908.log`；adapter 29 项、runtime 21 项与包含 CLI 的 Clippy `-D warnings` 通过。文档、文本与 `git diff --check` 通过，仅保留两个既有长文档预算警告。输入与报告身份快照保存为 `radishlex-rev01-fix-identity-20260908.json`；本轮未重建或替换冻结产品 bundle、未操作真实输入法，也未安装依赖。

- 代码与测试本地提交为 `40cd1bd`。在该提交上再次完成 48 进程存储与 12 进程配置回归，根分别为 `radishlex-rime-privacy-44873-1788870603674313000` 与 `radishlex-learning-guard-45085-1788870606819026000`；`radishlex-rev01-fix-identity-40cd1bd.json` 绑定实现 commit、测试二进制、Rime 数据锁、动态库 hash 与本次日志路径。文档随后独立提交，未推送。

## 2026-09-08：REV-02 Rust SQLite 升级与数据回归

- 项目所有者单独批准 `rusqlite 0.39.0` 候选评估/升级、WAL/旧库/备份恢复、native 隐私回归、新 FFI 身份与本地提交。本批从 clean `aea72ad` / `dev` ahead 4 开始，未推送或操作真实系统。
- `cargo update -p rusqlite --precise 0.39.0` 与当前 macOS target 的 locked fetch 在沙盒网络代理受限后分别获准提权执行。Cargo.lock 固定 `rusqlite 0.39.0` / `libsqlite3-sys 0.37.0`，bundled SQLite 3.51.3；保留 backup 与默认 cache，同步修改 userdb 和 runtime dev-dependency。新增原生传递项为 hashlink/hashbrown/foldhash 链；wasm 条件依赖仅进入 lockfile，未启用新平台。
- 核查上游 0.33–0.39 API/feature 变化，现有 SQL/backup 无需代码适配；没有修改 schema 9、WAL/NORMAL、busy timeout、产品 ABI、Go 依赖或全局工具链。变更前依赖图已要求 Rust 1.88，根仍声明 1.80；本机 1.96.0 通过不代表 MSRV 验收，具体限制见 [REV-02/REV-06](../remediation/product-review-2026-09.md)。
- 新增三个写者各 64 轮与 PASSIVE/RESTART/TRUNCATE checkpoint 并发回归；192 次学习全部保留，频次/事件/词数与完整 integrity_check 通过。另验证固定读事务阻止 WAL 重置、保留旧视图，释放后可 truncate 并继续写入。不宣称确定性复现上游罕见损坏。
- 从此前合成 privacy 正常场景原字节复制 SQLite 3.46.0/schema 9 fixture，来源、SHA-256 和纯合成内容登记在 [fixture 说明](../../crates/ime-userdb/tests/fixtures/README.md)。新版本打开、继续学习、删除后迟到选择不复活、备份/显式恢复/重开及原副本隔离通过。旧 schema migration、损坏/未来库拒绝和事务失败回滚仍由既有测试覆盖。本批 userdb 75 项单元测试及其集成测试、runtime 21 项单元/集成测试通过。
- 常规 SQLite 身份测试要求实际链接版本至少 3.51.3；实测 source id 为 `2026-03-13 10:38:09 737ae4a34738ffa0c3ff7f9bb18df914dd1cad163f28fd6b6e114a344fe6d618`。runbook 去掉 Rust 入口的 `--ignored`，避免以后误跑零项测试；Go 身份打印继续 opt-in。
- `cargo build --offline --locked --release -p radishlex-ime-ffi --features native-rime,apple-keychain` 使用本机已安装 librime 完成。新静态库与 dylib 单独保留在 `/private/tmp/radishlex-rev02-ffi-8xo3qblo`；C probe 从新静态库链接，实际打印 3.51.3/source id，并执行内存 SQL 与 mock FFI session 创建/释放。`identity.json` 绑定代码提交、源码/lockfile、双 FFI 与 probe hash、native 结果根和门禁日志。未调用真实 Keychain，也未构建或替换 app/DMG/deb。
- 升级后 48 进程存储回归根为 `radishlex-rime-privacy-49179-1788871575320429000`，12 进程配置回归根为 `radishlex-learning-guard-49523-1788871600367317000`，均通过。native 输入行为在 `/private/tmp/radishlex-rev02-input-di99tni1` 通过，覆盖分段、普通学习、删除恢复、隐私限制、基础按键与页大小 5；使用新的隔离数据目录。
- 工作区 `cargo check --offline --locked --workspace --all-targets`、包含 native FFI 的 workspace/all-targets Clippy `-D warnings` 与格式检查通过。完整 `check-repo.sh` 首轮因沙盒 Unix socket guard 失败，获准离线提权重跑后为 `Repository baseline passed`；日志为 `/private/tmp/radishlex-rev02-check-repo-20260908.log` 与 `/private/tmp/radishlex-rev02-check-repo-escalated-20260908.log`。native adapter/FFI 全量测试首轮同步资格初始化受沙盒限制，获准重跑后通过，日志为 `/private/tmp/radishlex-rev02-native-unit-escalated-20260908.log`。
- 依赖与回归测试提交为 `a03c69b`，文档随后独立提交。文档、文本与差异检查通过，只保留两个既有长文档预算警告。REV-01/REV-02 的新平台候选/实机回归和 REV-06 工具链声明仍开放；冻结 build 38、Linux L6 pair 与系统操作顺位保持原状。

## 2026-09-08：macOS build 39 联合候选准备

- 从 clean `32b5ad9` / `dev` ahead 6 开始，项目所有者批准新 macOS 双组件、固定身份与后续验收矩阵准备。版本提交 `5e9b0a8` 同步根产品 build 39 与平台投影；Linux package 元数据为 `26.7.1+39-2`，未构建新 `.deb`。L6 测试使用真实冻结版本 fixture，并单独断言当前 build 被冻结 target 资格拒绝。
- Manager 构建显式使用 `--no-pub`，阻止候选组装隐式解析依赖。以已有缓存及本机工具完成 `target/macos-product/26.7.1-39/`，再组装 `target/macos-release/26.7.1-39/` 的 Product、InstallPayload 和 ad-hoc Installer；未创建 DMG、未启动 app。历史升级源为空，不将 build 38 冒充已批准升级输入。
- 最终 ProductManifest SHA-256 为 `de1bf1ea937f742242975ad05a1d639e1d3899454e9acfb41eb28e089fe8362f`；双 FFI、新 Rime schema/source、ReleaseIdentity 与工具链详情统一记录在[本批验收入口](../runbooks/macos-rev01-rev02-acceptance.md)。独立校验最终产品、Installer 内外 payload、release identity、Rime/native manifest 和签名；双 FFI 静态内嵌 SQLite 3.51.3 新 source id，无旧 source id 或系统 SQLite 动态链接。
- 新增可重复包内 smoke：直接链接最终 InputMethod dylib，七个合成进程验证普通/受限/往返上下文，全部保持 commit、正常一条学习、受限零学习，无 Rime 自有 userdb，SQLite integrity_check 通过。正式结果根 `radishlex-bundled-privacy-jw_2a6y3` 绑定实际产品与 C 源码 hash。包内 Manager Dart FFI smoke 在新临时数据目录通过；其装配和最终载体 FFI hash 相同。
- 完整 `check-repo.sh` 在沙盒 Unix socket guard 受限后获准离线提权通过；日志 `/private/tmp/radishlex-build39-check-repo-escalated.log`。构建/Flutter 检查因现有 SDK 缓存写权限受限、Manager smoke 因本地同步资格初始化受限，均按必要验证提权后通过，保留初始失败日志。Flutter analyze 无问题，99 项测试通过；未下载新依赖或修改锁文件。
- 旧 build 38 的 104 项 mode/hash/link 快照逐项一致，Cargo.lock、pubspec.lock、Linux L6 JSON 与旧 ProductManifest hash 不变。新 C/Python smoke 提交为 `1ec8859`，它与后续文档不改变已封存的候选二进制；代码与文档分开本地提交，未推送。最终文档、文本与差异检查通过，只保留两个既有长文档预算警告。
- 本批仅完成候选准备。真实安装、控制器隐私路由、系统 secure input、人工输入质量和重启矩阵全部待执行；下阶段先明确验收用户与首次安装资格，再按具体动作取得系统授权。M5 原顺位和冻结现场保持原状。

## 2026-09-08：build 39 实机前只读盘点

- 项目所有者要求继续推进，从 clean `e98238e` / `dev` ahead 9 开始，只读核验当前 `luobo` console/uid、固定程序路径、安装 receipt、数据目录元信息、输入源和产品进程。未查询 SQLite、读取 P1 明文或运行产品 GUI。
- 双 bundle absent，沙盒外 TIS 为 `0/0/0`，精确产品进程查询为空；Application Support 保留 build 38 默认 remove 的 Rime、SQLite/WAL/SHM 与 completed receipt。初始沙盒进程表受限，另一次 TIS 查询出现 XPC 错误，均保存为受限观察；获准在真实会话只读复验后才确认上述 TIS/进程结果。
- receipt 记录的 data-root device id `16777230` 与当前 `16777234` 不同，inode/owner/mode 相同，沙盒外 stat 复验成立。没有将差异归因为已证实的系统机制，也没有修改 receipt 或套用其他 cleanup helper 的设备号兼容规则。
- 观察根为 `/private/tmp/radishlex-build39-host-inventory-lgqvz4r8/`，精确摘要见[联合验收入口](../runbooks/macos-rev01-rev02-acceptance.md)。本轮不写旧 M4 数据、不执行安装或修复；已请求项目所有者指定独立 macOS 测试账户，后续先核验该账户资格。

## 2026-09-08：当前用户旧记录保留式归档

- 项目所有者先讨论独立账户与 macOS 虚拟机，随后明确选择本机测试，并允许清理或整理旧记录。本批从 clean `a9c9f9c` / `dev` ahead 10 开始，不创建账户或虚拟机；修正当前入口，将独立账户从建议方案中撤下，不把它作为安装器必须条件。
- 只读盘点固定数据根及双程序父目录，识别 8 组配对的历史 operation，内容仅为空目录或 build 35/37/38 程序备份；数据根仍含 SQLite/WAL/SHM、Rime 和 build 38 completed-remove receipt。未打开数据库查询明文，也未把名称/版本识别冒充完整历史事务验证。
- 为本次系统整理准备精确 17 根、305857438 bytes 清单，绑定每个节点及父目录身份、源 hash 和脚本 hash。合成试验验证同盘 rename 保持内容/inode/mode/link、目标碰撞和源漂移零 mutation、外部链接拒绝；未增加产品 API、放宽身份检查或引入通用删除入口。
- 项目所有者批准精确归档命令后，把这些根移入 `~/Library/Application Support/RadishLex-Archives/20260908-before-build39/original-home/` 的原路径结构；父目录私有 `0700`，每步 journal 持久化，数据库和 WAL/SHM 随整个父目录一起移动。永久删除数为 0；旧 receipt 原字节保留，设备号差异未被“修正”。
- 实际执行前后 TIS 归零、产品进程停止、无打开句柄/guard；独立读回 545 个节点的元数据/hash/link 一致，17 项原路径 absent。最终 `--status` 为 bundle/data root/Rime/userdb/sidecars absent、InputMethod stopped，build 39 ProductManifest 再次验证通过。归档根保存 `plan.json`、`preflight.json`、`moves.jsonl` 与 `completed.json`，准备/执行材料位于 `/private/tmp/radishlex-macos-history-archive-20260908/`。
- 归档只整理历史现场，不证明生产目录设备身份问题已修复，也不属于本机安装验收。下一步可对空正式路径执行 build 39 Installer；GUI/实际安装仍需具体动作授权。build 38 候选制品、既有验证事实和 Linux 冻结资产保持原状。

## 2026-09-08：全天提交与文档收尾

项目所有者将本机测试留到次日，要求回顾当天全部提交、按修改代码复审相关文档并完成最后一次文档提交。收尾前工作区 clean，`dev` ahead 11；当天已有代码、测试和记录均已提交，无需补做空提交。

| 提交 | 变更与文档核对范围 |
| --- | --- |
| `4c3fe0f` | 隔离 Rime 学习、存储观察与 SQLite 身份诊断；核对诊断 runbook 的合成数据及 opt-in 边界 |
| `b793187` | 诊断结果、隐私复现和依赖候选记录；保留其修复前事实 |
| `40cd1bd` | RadishLex 独占学习、composition 最严格策略保留、有效配置检查与产品 schema 来源锁 |
| `aea72ad` | 单一学习合同和 native 回归证据；对照 adapter、个人化、隐私与数据准备文档 |
| `a03c69b` | SQLite 3.51.3 依赖链、WAL/旧合成库回归及冻结 L6 声明检查；核对 fixture 与版本取证口径 |
| `32b5ad9` | SQLite 验证、MSRV 限制和两平台待复验事项 |
| `5e9b0a8` | build 39 元数据投影、Manager `--no-pub` 及版本测试；核对装配与产品包边界 |
| `1ec8859` | 实际候选包内 FFI 七场景 smoke；核对 C 上下文注入与真实系统路由的证据边界 |
| `e98238e` | build 39 身份、工具链和联合实机矩阵 |
| `a9c9f9c` | 本机安装前只读盘点与 data-root device id 差异；保留当时观察及后续方向修正 |
| `a9769f3` | 本机旧记录保留式归档、空正式路径与安装条件 |

本轮复审发现并修正文档遗漏：

- README、CLI 与学习取证 CLI 的活跃示例仍使用 `luna_pinyin` / `pinyin_simp`，与当前配置 guard 不符；统一指向来源锁装配的 `radishlex_pinyin`，说明关闭 Rime 自有学习及隔离缓存要求，修正候选仍受 Rime 学习状态影响的旧描述。
- 社区 DMG 手册仍把 build 38 称为当前候选，并把 completed remove 一律写成失败关闭；将载体实测归位为 M4 历史事实，构建示例改为明确版本占位，并按 InstallerDriver 与稳定边界说明有效 completed remove 可再次 first install。build 39 尚无 DMG，未把旧验收覆盖到新修复。
- current、审阅专题与联合验收入口区分已完成的本机盘点/归档和未执行的新安装/输入验收，登记次日入口；补明冻结 L6 合同检查通过不代表当前 build 39 可作为旧 pair target。
- 单一学习、composition 生命周期、SQLite schema/WAL/backup、FFI ABI 和安装合同的稳定专题与本批实现一致，无需扩大改写。旧库归档不等于生产目录长期身份问题已解决，MSRV、真实平台路由与输入质量仍待闭合。

本轮 `./scripts/check-docs.sh`、`./scripts/check-text-files.sh` 与 `git diff --check` 通过，仅保留 Linux L6 runbook 和同步存储专题两个既有长文档预算警告。仅修改文档并做本地提交，未重跑代码构建或实机测试，也未启动 Installer/Manager/InputMethod、执行安装、制作 DMG、操作 guest 或推送远端。

## 2026-09-09：明日事项

1. **优先继续本机 build 39 联合验收。** 使用现有 `luobo` 账户，从[联合验收入口](../runbooks/macos-rev01-rev02-acceptance.md)重新核验候选 manifest、固定安装路径、receipt/guard、输入源和进程。09-08 归档完成不代表次日状态无需复核；不创建新账户或虚拟机，不自动恢复旧库。
2. 前置条件满足后，明确本地 `target/macos-release/26.7.1-39/RadishLex Installer.app` 的启动、首次安装及双组件启动范围，取得当次具体动作授权。此载体无历史升级源，安装目标为当前用户固定路径；旧资料归档继续保留。
3. 安装终态与实际双 bundle/进程身份一致后，按联合矩阵依次验证普通输入与学习、隐私模式及 composition 往返、secure/sensitive/unknown 路由、分段/自动 commit、Manager 同库删除恢复和正常重启。仅使用合成输入与新测试库，逐项记录实际结果；系统输入源和人工/自动按键操作按明确范围执行。
4. 再补独立旧合成 fixture/备份恢复与固定词集质量对照；遇到身份漂移、事务未知、越界学习或 commit 丢失即保留现场，不自动重试、修复或清理。旧 data-root device id 差异的长期处理单独评估，不靠修改 receipt 绕过。
5. 根据实测结果更新 REV-01/REV-02 的关闭条件。Linux 新产物/pair、MSRV、回调锁等待及输入收益继续列为后续事项；Linux M5 的原生 Move、第三个 crash 和连续 L6 顺位仍按 [current](../status/current.md)，不混用旧资产或自动切入系统操作。

09-08 完成文档收尾后停止工作；本清单是次日交接，不代表已执行测试、设定自动任务或取得新的系统操作授权。

## 2026-09-09：build 39 首次安装与 Manager 启动

- 开始基线 `dev` / `478ba01`，工作区干净，与本地 `origin/dev` 一致。先按联合 runbook 重新只读核验；产品、内外 payload、签名及冻结摘要通过，固定正式路径为空，历史升级源为空，输入源与产品进程均为零。沙盒内 TIS XPC / `ps` 限制由最小范围沙盒外只读查询补证。
- 项目所有者随后明确授权 Installer 启动、首次安装及静止确认、安装终态核验和 Manager 启动。通过 Installer 原生界面先持久化 `prepared`，再复核静止并确认；operation `514bd2af74b57ee131b66104b1333962` 到达 `first_install/completed`，无 failure/manual recovery。UI 的 `begin_repair` 是已安装后的后续动作投影，本批未执行修复或移除。
- 双程序完整 tree、manifest、release identity 和 `26.7.1 (39)` 一致；Manager 53 节点、InputMethod 48 节点无 quarantine。新 data root 的实际 device/inode/uid/mode 与 receipt 一致，state 仅有 receipt。只读 xattr 检查最初因 macOS Python 缺少 `os.listxattr` 中止，随后改用系统 `xattr -r -s` 完成核验；该工具问题没有触发安装重试或现场修复。
- Manager 从固定路径启动并显示空词库、`local_only`、零导入批次；真实进程不在 App Translocation。新建 userdb 为 uid 501 / `0600`，receipt 字节未变，未读取用户库正文或导入旧库。普通 Manager 界面成功进入证明其前置两层 startup gate 允许；没有独立成功 gate 数值日志。
- 本批证据分别保存在 `/private/tmp/radishlex-build39-preinstall-20260909-oyfvjeis/` 和 `/private/tmp/radishlex-build39-first-install-20260909-y8ikrpku/`；精确状态、矩阵和边界维护于[联合验收入口](../runbooks/macos-rev01-rev02-acceptance.md)。
- 文档验证：`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 通过；文档检查保留两份无关既有长文档的预算警告。本批未修改产品代码、依赖或候选二进制，未重跑昨日完整仓库门禁。
- 停止位置：Installer 与 Manager 运行，InputMethod 未运行。后续由项目所有者手动添加/选择输入源后开始公开合成输入矩阵；尚未验证输入、隐私路由、同库删除恢复、正常重启或旧合成库恢复。未制作 DMG、操作 Linux、恢复归档、开放同步或发布，REV-01/REV-02 保持开放。

## 2026-09-09：普通输入与同会话学习

- 项目所有者手动添加/选择 RadishLex 后，只读 TIS 为 `2/2/1`，InputMethod 固定路径 `running_verified`；两轮输入反馈后均为 `2/2/0`。未运行连续输入源监视器；候选编号、实际 commit 和卡顿体验以项目所有者人工反馈为准。
- 本轮固定 TextEdit `shi → 时`：第一次候选 2、数字键提交“时”；第二次候选 1、空格提交“时”，两轮均未感到卡顿。操作前全库 term/selection/ranker 为 `2/2/2`，没有读取两条既有记录正文，固定合成身份尚不存在；第一次后为 `3/3/3`、目标 `editor` frequency=1，第二次后为 `3/4/3`、frequency=2。无重复词条/排序行，删除/负反馈/导入计数均为零，未发现 Rime `*.userdb*`。
- 三次数据观察使用 `mode=ro`、`query_only=ON` 和同一读事务；只取聚合及固定合成身份，保持 data root/userdb identity 与 completed receipt 一致。证据在 `/private/tmp/radishlex-build39-input-20260909-_drd2sd7/`，分别为 `before-shi-time.json`、`after-first-shi-time.json` 和 `after-second-shi-time.json`。
- 本组补齐 InputMethod 启动和普通选词/学习实机证据；没有重启、fresh-engine 对照或量化延迟结果。隐私键只读为 absent，未修改；下一组通过 Manager 隐私设置继续测试，隐私模式、composition 往返与其余矩阵仍待执行。REV-01/REV-02 不关闭，未改变任何 Linux 或发布边界。

## 2026-09-09：全程隐私输入与普通恢复观察

- Manager 隐私开关首次处于未落地草案：平台只读为 absent、settings 文件不存在。执行者只滚动定位保存按钮，由项目所有者点击保存后，沙盒外平台状态和 Manager settings 均为 true；未在前置未通过时测试输入。相关快照与消歧保留，未覆盖初次观察。
- 新的 TextEdit `shi → 时` 全程隐私 composition：项目所有者反馈候选 1、准确提交且不卡顿；前后 term/selection/ranker 为 `3/4/3`，六类聚合均零变化，目标频次 2 与词条/排序时间戳不变，无 Rime 自有 userdb。全程隐私输入零学习通过，人工不卡顿不作为量化延迟。
- 按步骤关闭隐私并保存后再选择一次“时”，项目所有者反馈候选 1、提交正确且不卡顿。只读平台/settings 为 false、目标 `editor` frequency=3；全库却到 `5/7/5`，selection 增 3，额外两条事件未归属。仅询问是否另有 RadishLex 输入，不索取/读取文字，等待澄清或干净用例；不将该区间写成单次全库精确恢复通过。
- 固定合成身份 selection 总数为 3，数据根/userdb identity 与 completed receipt 不变；反馈后 TIS `2/2/0`、InputMethod `running_verified`。当前隐私键为用户 GUI 保存的显式 false，不是初始 absent。未清理数据、回写 receipt、停进程或执行 Linux/发布操作。
- 证据续存 `/private/tmp/radishlex-build39-input-20260909-_drd2sd7/`，文件职责及未提交 composition 的后续限制见[联合验收入口](../runbooks/macos-rev01-rev02-acceptance.md)。本批只修改验收记录，未修改产品代码或候选二进制。
- 随后项目所有者确认关闭隐私后还使用 RadishLex 输入了其他内容。追加 `normal-return-additional-input-confirmed.json`，不改写原先待核对快照；该区间按含其他输入处理，目标词恢复学习成立，全库不作为单次精确用例，额外增量不再列作未知缺陷。没有索取或读取其他输入文字。
- 已准备下一组保持 TextEdit 焦点的未提交 composition 隐私切换计划，覆盖隐私→普通、普通→隐私、普通→隐私→普通及每次之后的新普通学习对照。仅完成方案；后台设置控制、临时基线消费与合成按键需本次具体授权，尚未执行。
