# 2026-W30 开发周志

本文记录 2026-W30 的阶段事实、验证证据、风险和交接事项，读者是 RadishLex 维护者与后续协作者。本文不替代 `docs/status/current.md`、架构边界、协议参考或 runbook。

## 本周目标

- 完成 Manager 通过真实 Rust bridge 执行的本地 HTTPS 合成同步资格链，并以真实 Caddy HTTPS 证明双客户端同步、冲突恢复、第二轮收敛与资源清理。
- 完成 M4-P01 双 bundle 产品装配，固定产品元数据、RimeData、native manifests、传递依赖与产品 manifest。
- 推进 M4-P02 Application Support v1 数据升级协调器，按 receipt/guard、快照、候选 migration、只读启动门禁和双端产品验证的证据顺序落地。
- 固定下一阶段切换、崩溃恢复、最终路径复验与回滚边界；保持真实用户同步、App Group 迁移、真实系统安装和发布供应链关闭。

## 2026-07-20：Manager 同步资格执行批次启动

- PR #5 已把上一阶段成果合入 `master`，并以 `a73adab` 将最新 `master` 回同步到共享 `dev`；远端拓扑闭环、工作区干净。
- 复核确认 `ime-sync` 不能反向依赖 `ime-userdb`，本地输入 `ime-runtime` 明确不负责远端同步，`ime-ffi` 也不能直接组合网络和数据库。因此新增 `ime-sync-runtime` 作为 Manager 同步产品组合层，依赖稳定 sync/userdb/crypto 边界并向 FFI 提供 Rust-owned run。
- 本批只允许 loopback HTTPS、隔离合成 P2 与单次调用 transient 参数，不访问真实 userdb 或 Keychain/Secure Enclave，不改变产品资格和用户同步总 gate。

## 当前验证

- `cargo test -p radishlex-ime-sync --lib`：81 项通过；资格专用 crypto snapshot 不改变 production/test backend 判定，HTTP transient token/CA 使用 zeroize 容器。
- `cargo test -p radishlex-ime-sync-runtime --all-targets`：5 项单元测试与 1 项环境门控集成入口通过，覆盖 loopback HTTPS/版本/token/CA/timeout 校验、Debug 脱敏、跨进程单运行、并发 start 拒绝、幂等取消、异常退出后的 marker 限定重启清理、worker/临时文件/transient 清理。
- `cargo test -p radishlex-ime-ffi --all-targets`：Rust 单元、跨边界、C11 与 Objective-C header contract 全部通过；ABI v7 的 request/snapshot 布局与 start/poll/cancel/free 函数签名已固定。
- `./scripts/check-manager.sh`：格式化、静态分析与 95 项 Flutter 测试通过；新增 mapper 失败关闭、token buffer 覆写、非 loopback 禁用、运行取消与“启用同步”保持关闭的 widget 证据。
- `./scripts/check-manager-product.sh`：48.8 MB macOS Release bundle 构建通过，签名/架构/依赖/必需 symbol 通过；真实 bundle dylib 的不可达 loopback run 完成失败态、worker/临时文件/transient 清理和 handle 释放，未启动 GUI。
- `./scripts/check-sync-server-local-https.sh`：真实短生命周期 Caddy 链通过 TLS、bearer `401`/授权响应、Rust verified transport、Manager qualification、loopback-only、容器 hardening、日志脱敏和隔离清理。资格链真实执行 v1/v2 上传、`conflict_stale_base_version`、A 重发现与 v3、B 合并 v4，第二轮双方零上传。
- `cargo clippy -p radishlex-ime-sync -p radishlex-ime-sync-runtime -p radishlex-ime-ffi --all-targets -- -D warnings` 通过。
- `./scripts/check-repo.sh` 通过；文档仅保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。

## 批次结论与后续风险

- Manager 本地 HTTPS 合成资格批次已完成，M3 路线图退出项闭环，项目转入 M4 产品发布候选。
- 初次 Docker 镜像元数据拉取曾超时；重试后完成。首次真实 runner 暴露两处合成编排误设：同版本冲突不是 stale-base、第一轮 B 合并会产生必要 v4；最终改为预留 v3 stale-base 和两轮 A/B 配对、第二轮双零上传，未放宽服务语义。
- 用户同步、恢复、授权、撤销与轮换入口继续关闭；合成资格摘要不进入 readiness、deployment evidence、settings 或 diagnostics。
- M4 首批不得直接改安装目录或数据容器；应先固定 product package、App Support/App Group 迁移、升级回滚、签名公证与供应链门禁边界。

## 2026-07-20：M4-P01 macOS 产品元数据与装配基础

- 新增 `docs/macos-product-package-boundary.md`，固定双 bundle 独立、各自携带 Rust dylib、InputMethod 携带合法 `librime`/RimeData、manifest、版本兼容、数据所有权、签名公证和安装移除停止线。
- 首个候选继续使用已验证的非 App Sandbox `application-support-v1`；当前没有 App Group 必要性，未来变更必须经 ADR、原子迁移和双端复验，不能把容器迁移当作产品化装饰。
- 新增 `packaging/macos/product.json`，统一产品 `0.1.0 (35)`、macOS 13.0、FFI ABI v7、userdb v9、bundle ID 与两个 native manifest 版本；Manager pubspec/Xcode 与 InputMethod Info.plist 改由该 contract 约束。
- 新增 product manifest 工具、7 项单元测试、source contract 门禁和双 bundle 装配入口。manifest 绑定普通文件 hash 与安全内部 framework symlink，拒绝绝对/逃逸/broken symlink、版本错配和产物篡改，不写构建机绝对路径。
- `./scripts/check-manager-product.sh` 已用统一版本重新构建 48.8 MB Release bundle并通过签名、架构、FFI 与产品 smoke。`./scripts/check-macos-imk.sh` 已通过统一版本 contract bundle；期间重复暴露 AppKit 将半点窗口原点对齐到 backing pixel 的既有测试假设，修为最多一个 backing pixel 的精确平台对齐容差后，尺寸、屏幕夹取和定位 contract 继续通过。
- 完成官方 Rime 数据审计：`rime-pinyin-simp` 固定 commit `0c6861ef7420ee780270ca6d993d18d4101049d0`，词典 SHA-256 为 `e341598343a0f0f2035bb1aafc34a7f3bb7887deeecb3f60796262aaa2983e6b`，LICENSE/AUTHORS 与词典均按 Apache-2.0 保留。原 schema 依赖 LGPL `stroke` 并导入 LGPL `prelude`；`stroke` 又依赖 `luna_pinyin`，不能用主仓库许可证代表完整数据链。
- 隔离部署确认没有 `stroke` 时主拼音仍能返回 5 项真实候选，只复制 `stroke.dict.yaml` 不会启用反查；加入完整 stroke schema 会扩展部署并尝试读取 `essay`。因此首个候选改用 RadishLex 维护的 `radishlex_pinyin` schema/标点/按键配置，只引入固定 Apache 词典，明确延期笔画反查和扩展符号表。
- 新增 `packaging/rime/product-rime-data.json`、committed 产品数据、逐资产许可证与离线装配工具。锁门禁拒绝未知字段、额外文件、hash 漂移、非完整 commit、schema 延期能力和 symlink；7 项产品数据测试与 RimeData manifest v2 的 4 项测试通过。
- 使用固定 Homebrew `librime 1.17.0` 和仓库装配数据完成 CLI 非选择 snapshot、完整 native InputMethodKit 门禁、FFI smoke、递归 dylib/逐库许可证、ad-hoc 签名及双 bundle 产品装配。最终 `ProductManifest.json` 显式绑定 `rime_schema_id=radishlex_pinyin`，SHA-256 为 `12144d807cc5ee68359d77cd8da0f108b82750f287582771224b1264c5c78832`；产品 manifest 与 Rime source manifest 均无构建机绝对路径，未安装、启动、公证或上传产品。
- 收口验证通过 `./scripts/check-macos-product-metadata.sh`、`./scripts/check-macos-imk.sh`、`./scripts/check-macos-imk-native.sh`、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、完整 `./scripts/check-repo.sh`、最终产品 manifest/RimeData 独立复验和 `git diff --check`；文档只保留既有 `docs/sync-server-api-storage.md` 508 行提醒。

## 2026-07-20：今日提交回顾

- `688d8e7` 将本周已验证的阶段成果合入稳定主线，`a73adab` 随后把最新 `master` merge 回共享 `dev`，保持稳定主线是开发分支祖先。
- `d8f4830` 完成 Manager 本地 HTTPS 合成同步资格链并闭合 M3 退出证据；真实用户同步仍由独立产品 gate 关闭。
- `faa73a0` 完成 M4-P01 首个 macOS 产品装配批次，固定产品版本、FFI/schema contract、合法 RimeData、双 bundle native 依赖和完整性 manifest。
- 今日推进从本周成果进入稳定主线，经 M3 退出转入 M4，并完成 M4-P01。下一主批次为 M4-P02 Application Support v1 数据升级协调器；发布安装载体、Developer ID、公证和真实用户同步均不在下一批提前展开。

## 明天事项（2026-07-21）

主任务是推进 M4-P02 Application Support v1 数据升级协调器。先完成边界文档，再按文档拆出可验证的实现顺序；不修改或复制真实用户数据，不安装输入法，不迁入 App Group，也不提前进入安装载体、公证或发布工作。

1. 复核现有 `ime-userdb` schema v9 migration、Manager 与 InputMethod 的 Application Support 路径解析、SQLite WAL/SHM 并发规则、进程生命周期和 M4-P01 product manifest，明确哪些能力已有真相源，哪些必须由 macOS 产品协调层补齐。
2. 新增 M4-P02 升级协调器边界文档，固定参与者职责、受控数据范围和状态机。Rust userdb 继续拥有 schema migration 与数据库校验；协调器只负责编排 preflight、进程静止证明、安全快照、隔离副本 migration、双端打开验证、原子切换、receipt、失败回滚和中断恢复；平台壳不得接管业务迁移逻辑。
3. 在文档中明确 SQLite 安全语义：不能把活动 WAL 数据库当普通文件直接复制；必须处理 `-wal`/`-shm`、busy/locked、损坏库、未来 schema、权限错误、符号链接和磁盘空间不足。升级失败必须保留原数据与可诊断错误，不能以空库、默认配置或静默重建掩盖问题。
4. 固定升级 receipt 与恢复规则。receipt 只记录本地版本、schema、阶段、结果和必要的非敏感校验信息，不包含用户词、输入历史、路径正文或可逆数据摘要；状态机需支持重复启动和进程中断后的幂等恢复，并明确何时允许清理备份、何时必须保留人工恢复入口。
5. 定义验证矩阵和首批实现切面，至少覆盖空库、schema v9、可迁移旧 schema、未来 schema 拒绝、活动 WAL、并发占用、损坏/只读/空间不足、migration 中途失败、切换前后崩溃、Manager 与 InputMethod 新版本双端打开，以及回滚后旧数据仍可用。边界通过文档门禁后，再开始协调器状态模型、receipt schema 和隔离 fixture 测试；如 contract 影响 ABI、产品 manifest 或 runbook，必须同步更新对应正式文档。

明日停止线：没有进程静止证据、隔离 migration、切换验证和失败回滚时，不允许对原数据原地升级；没有完整测试矩阵时，不把单次迁移成功当作 M4-P02 完成证据。

## 2026-07-21：M4-P02 升级边界与第一段 contract

- 新增 `docs/macos-data-upgrade-coordinator.md`，固定 `application-support-v1` 内的参与者职责、受控对象、状态机、receipt、SQLite 一致快照、双端 validation、原子切换、启动门禁、失败回滚和验证矩阵。安装载体、App Group、真实用户目录操作、Developer ID 与公证继续后置。
- `ime-userdb` 新增只读 `inspect_file`，不配置 WAL、不改权限、不执行 migration；它返回旧版/当前/未来 schema compatibility，并对当前 schema 额外验证完整结构。新增 `migrate_and_validate` 明确只允许协调器传入隔离候选，返回源/目标 schema 与是否迁移，再以只读入口复验目标。
- 新增 `ime-product-upgrade` crate，固定 `radishlex-product-upgrade-receipt-v1`、`application-support-v1`、产品版本/build、schema、固定 artifact 身份、稳定 failure code 和单向状态。receipt 拒绝未知字段、非 canonical JSON、非私有 mode、hardlink、不稳定 operation ID、缺少阶段证据和非法跳转；不保存真实路径、数据正文或内容 hash。
- 状态门禁测试首次发现非法 `quiesced -> snapshot_ready` 虽返回错误却留下已修改内存状态；所有 receipt 变更已改为校验失败恢复原状态，避免调用方把失败转换持久化成有效证据。生产 contract 与单元测试拆到浅层 `src/lib.rs` / `src/tests.rs`，避免新增源码触及 1000 行治理线。
- `ime-userdb` 68 项单元测试通过；`ime-product-upgrade` 7 项状态、回滚、严格解析和身份测试通过；两 crate `clippy --all-targets -- -D warnings`、文档、文本、`git diff --check` 与 AGENTS/CLAUDE 同步检查通过。
- 沙盒内首次 `./scripts/check-repo.sh` 仅在既有同步资格 Unix domain socket run 启动处失败；同一精确测试在真实本机环境立即通过，确认是沙盒 socket 限制。随后真实环境完整 `./scripts/check-repo.sh` 通过，Go、Rust workspace、跨语言/HTTP 集成与文档基线全部闭合；仅保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本批没有访问或修改真实 Application Support，没有安装/启动输入法或 Manager，没有执行系统设置、Keychain、签名、公证、上传或真实用户同步。

下一切面实现临时隔离目录内的 receipt 原子持久化、固定数据根身份校验和跨进程 guard，再进入 SQLite backup API 一致快照、空间预算与故障注入。M4-P02 在双端产品 host、切换与崩溃恢复完成前保持未退出。

## 2026-07-21：M4-P02 receipt 文件系统切面

- `ime-product-upgrade` 新增 `VerifiedDataRoot` 与 `UpgradeReceiptStore`：只接受 canonical、同 owner、`0700` 的固定数据根，创建并锁定 `.radishlex-upgrade-v1` 状态目录；加载与持久化前都会重验 data root、状态目录和既有 receipt 身份，未知对象、symlink、hardlink、mode/owner/device/inode 漂移均失败关闭。
- receipt 固定使用 `receipt.json.tmp` 的 `create_new` 写入、`0600`、文件 `fsync`、原子 rename、状态目录 `fsync` 和写回复验。既有 operation 的 metadata 不可改写，artifact 只能追加，每次最多持久化一个合法状态；相同 bytes 可幂等重放，新 operation 不得覆盖既有 operation。中断临时文件当前保留现场并返回稳定错误，自动恢复决策留给后续 crash-recovery 切面。
- 跨进程 guard 使用 root-owned sticky 临时根中的短 Unix socket，名称绑定 uid 与 data root device/inode，不包含 home、真实路径或内容 hash。活动 socket 拒绝重入；只有身份、owner、mode、type 都符合且连接明确 `ConnectionRefused` 的精确 stale socket 可以恢复。symlink/普通文件保持原样并失败关闭，Drop 只删除取得时同一 inode 的 socket。
- 文件系统验证确认 APFS 目录 link count 会随目录项变化，因此目录 link count 只作 receipt 观察字段，稳定身份比较使用 type/device/inode/owner/mode/canonical path；普通文件仍严格要求 `link count = 1`。同时把 guard 从深层数据目录移到短临时路径，避免 macOS `sockaddr_un` 长度上限使合法 Application Support 路径无法绑定。
- 生产文件系统实现与测试拆为 `src/filesystem.rs` 和 `src/filesystem_tests.rs`，避免单文件触及 1000 行治理线。真实本机环境下 `ime-product-upgrade` 17 项测试与 `clippy --all-targets -- -D warnings` 通过；沙盒仅因 Unix socket 权限隔离产生 4 项 `Io`，同批非 socket 用例正常通过。
- 真实本机环境完整 `./scripts/check-repo.sh` 通过，包含 Go、Rust workspace、跨语言/HTTP 集成、68 项 `ime-userdb` 测试与新增 17 项产品升级测试；文档、文本、`git diff --check` 和 AGENTS/CLAUDE 同步门禁通过。仅保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本切面全部使用短生命周期合成临时目录；没有访问或修改真实 Application Support，没有启动、安装或停止 Manager/InputMethod，也没有执行系统设置、Keychain、签名、公证、上传或真实用户同步。

下一切面进入 SQLite backup API 一致快照、空间预算与文件系统故障注入。原固定数据库继续只读，任何 migration 仍只能发生在隔离候选上；在双端 validation、切换与崩溃恢复闭合前，M4-P02 保持未退出。

## 2026-07-21：M4-P02 SQLite 一致快照切面

- 上一批升级边界、userdb inspection/migration contract、receipt 原子持久化与跨进程 guard 已提交为 `9497dfe feat(upgrade): 建立产品数据升级协调基础`；未推送远端。
- `ime-userdb` 在既有 `rusqlite 0.32.1` 上启用 `backup` feature，新增 `estimate_snapshot` 与 `create_consistent_snapshot`。源连接保持只读事务，backup 纳入未 checkpoint WAL 的可见页；目标必须是调用方预建的独立空普通文件，完成后转为 standalone `DELETE` journal、通过 `quick_check(1)` 并与源事务 schema/page 元数据一致，不执行 migration、不留下 WAL/SHM。schema 0 零字节数据库按一个 materialized header page 估算并保持可迁移语义。
- `ime-product-upgrade` 只接受 fixed data root 下的 `userdb.sqlite3`，只写 `.radishlex-upgrade-v1/source-snapshot.sqlite3.tmp` 与最终 snapshot。源和目标校验 type/device/inode/owner/mode/link/byte length；成功顺序为临时文件、SQLite backup、文件 `fsync`、原子 rename、目录 `fsync`、先持久化 snapshot identity、再推进 `snapshot_ready`，原库字节不被改写。
- 空间预算固定为 `3 * logical_snapshot_bytes + 64 MiB`，覆盖 snapshot、migration candidate、切换/rollback 工作余量和最低 reserve；overflow 或 available bytes 不足会在创建临时文件前失败。真实 available bytes 仍由后续固定 macOS 文件系统 adapter 提供，核心不引入平台 `unsafe` 或接受 UI 自定义路径。
- 故障注入覆盖临时文件创建后、backup 后、rename 后和 receipt evidence 后。staged snapshot 或无 receipt identity 的最终 snapshot 会使启动加载失败关闭；已持久化 identity 但仍为 `quiesced` 的 receipt 可以读取证据，当前不会自动跨状态，留给后续 crash-recovery 编排。
- 为遵守源码篇幅治理，SQLite backup 生产逻辑与测试分别拆入 `store/snapshot.rs`、`store/tests/snapshot.rs`；产品 snapshot 编排与测试拆入 `snapshot.rs`、`snapshot_tests.rs`。当前 `ime-userdb` 72 项测试、`ime-product-upgrade` 24 项测试与两 crate Clippy 已通过。
- 真实本机环境完整 `./scripts/check-repo.sh` 通过，SQLite backup feature 没有破坏 FFI、runtime、sync、Go HTTP 或跨语言测试；文档、文本、格式、`git diff --check` 与 AGENTS/CLAUDE 同步门禁通过。仅保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本切面只使用合成临时目录，没有访问真实 Application Support，没有启动、安装或停止 Manager/InputMethod，也没有执行系统设置、Keychain、签名、公证、上传或真实用户同步。

下一切面从已固化 snapshot 创建独立 migration candidate，接入固定 macOS available-space/静止证明与 settings 副本，并实现 Manager/InputMethod 候选 validation host；在最终切换和崩溃恢复闭合前，M4-P02 保持未退出。

## 2026-07-21：M4-P02 隔离 migration candidate 切面

- 上一批 SQLite 一致快照已提交为 `b7b80d8 feat(upgrade): 增加 SQLite 一致快照`；未推送远端。
- `ime-product-upgrade` 新增固定 `migration-candidate.sqlite3.tmp` / `migration-candidate.sqlite3` 编排，只接受 receipt 已固化身份的 snapshot。候选以私有 `create_new` 文件承接已打开 snapshot 的完整复制，复验 snapshot 与 staged inode 后才调用 userdb migration；不接受 UI 自定义路径，也不接触固定原库。
- `ime-userdb::migrate_and_validate` 现在保证迁移后 candidate 收敛为 standalone `DELETE` journal SQLite，重新执行完整性与当前 schema 校验，并拒绝 WAL/SHM/journal 残留。正常运行时 `UserDb::open` 的 WAL 语义不变。
- candidate 成功顺序固定为 copy、隔离 migration、文件 `fsync`、原子 rename、目录 `fsync`、先持久化 candidate identity、再推进 `candidate_migrated`。当前 schema 返回未迁移，schema 0 返回 `0 -> 9`；原库与 snapshot 字节保持不变。
- 故障注入覆盖 staged 创建、copy、migration、rename 和 receipt evidence。staged candidate、无 identity 的最终 candidate、损坏 snapshot、snapshot 路径替换和 artifact identity 漂移均失败关闭；已有 identity 但仍为 `snapshot_ready` 的 receipt 可读取证据，不自动越过未持久化状态。
- 真实本机环境下 `ime-product-upgrade` 31 项与 `ime-userdb` 72 项测试通过，其中 7 项 candidate 专项覆盖当前/schema 0、三类中断证据、损坏内容与路径替换；两 crate Clippy 零 warning。完整 `./scripts/check-repo.sh` 通过，确认 FFI、runtime、sync、Go HTTP 与跨语言基线未被破坏；文档只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本切面只使用合成临时目录，没有访问真实 Application Support，没有启动、安装或停止 Manager/InputMethod，也没有执行系统设置、Keychain、签名、公证、上传或真实用户同步。

下一切面接入固定 macOS available-space/静止证明与 settings 副本，并分别实现 Manager/InputMethod 数据升级 validation host。现有 InputMethodKit `ValidationHost` 只验证 unknown/P0 上下文分类，不能作为数据升级双端打开证据；在双端 validation、切换与崩溃恢复闭合前，M4-P02 保持未退出。

## 2026-07-21：M4-P02 settings 保留副本切面

- 隔离 migration candidate 已提交为 `1643ac9 feat(upgrade): 增加隔离迁移候选`；未推送远端。
- `ime-product-upgrade` 新增固定 `source-settings.json.tmp` / `source-settings.json` 保留链，只接受 receipt 已记录身份的固定 `manager-settings.json`。复制使用已打开源文件与 `create_new` 私有目标，不解析或规范化 settings 正文；完成文件 `fsync`、源身份复验、原子 rename 和目录 `fsync` 后，再追加 `BackupSettings` identity。
- receipt 仍停留在 `quiesced`，只有 `SourceSettings` 与 `BackupSettings` 同时存在或同时不存在，SQLite snapshot 才能推进 `snapshot_ready`。这补齐了状态文档此前已要求、实现尚未强制的 settings 证据门禁；原 settings 字节不被改写。
- 故障注入覆盖 staged 创建、copy、rename 与 receipt evidence。staged settings、无 identity 的最终备份、源路径替换和身份漂移均失败关闭；identity 已持久化而调用方尚未更新内存 receipt 时，持久化证据仍可安全加载且不跨状态。
- 真实本机环境下 `ime-product-upgrade` 38 项测试与 Clippy 通过，其中 7 项 settings 专项覆盖原样保留、snapshot 状态衔接、三类中断证据与路径替换。完整 `./scripts/check-repo.sh` 通过，确认 candidate、FFI、runtime、sync、Go HTTP 与跨语言基线未被破坏；文档只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本切面只使用合成临时目录，没有访问真实 Application Support，没有启动、安装或停止 Manager/InputMethod，也没有执行系统设置、Keychain、签名、公证、上传或真实用户同步。

下一切面接入固定 macOS available-space 与进程静止证明，再分别实现 Manager/InputMethod 数据升级 validation host。现有授权 cleanup/stop 脚本是人工运维边界，不能直接变成产品协调器的静默停止接口；M4-P02 在双端 validation、切换与崩溃恢复完成前保持未退出。

## 2026-07-21：M4-P02 macOS 只读 preflight host 切面

- settings 保留副本已提交为 `a90704b feat(upgrade): 增加 settings 保留副本`；未推送远端。
- 新增 `platforms/macos-product/UpgradePreflightHost`，生产 executable 不接受任何参数，只通过 Foundation 解析固定用户域 `Application Support/RadishLex`。Manager/InputMethod bundle ID 和最低 macOS 从 `packaging/macos/product.json` 编译注入，不接受调用方进程名、bundle ID 或数据路径。
- available-space 同时查询 important-usage 与普通 volume available capacity，取可用正值中的保守值。data root 必须 canonical、无 symlink、同 uid、`0700`；已存在的 userdb family 与 settings 文件必须是同 uid、`0600`、单 link 普通文件。
- 静止探针通过 `NSRunningApplication` 检查固定双 bundle ID，并用固定 `/usr/sbin/lsof` 只读检查受控 SQLite/settings 文件。输出只有固定 JSON format/result/available bytes/quiescent/blocker，不回显路径、PID、命令行或 `lsof` 内容；host 不创建目录、不修改数据、不停止进程。
- contract 使用合成私有目录验证容量、无文件 ready、同进程打开数据库时 `data_handle_open`、关闭后恢复 ready、受控文件 symlink 与 data root symlink 拒绝，以及生产二进制拒绝所有参数。产品 host 只构建不在门禁中对真实 Application Support 执行。
- `scripts/check-macos-upgrade-preflight.sh` 已接入 macOS 仓库基线；专项检查与完整 `./scripts/check-repo.sh` 通过，确认产品元数据、Go、Rust、FFI、同步和跨语言基线未被破坏。点时探针尚不能阻止旧版本在检测后重新启动，只有双端 startup gate 与 upgrade guard 接线后才可形成持续静止证据；文档只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本切面没有访问真实 Application Support，没有启动、安装或停止 Manager/InputMethod，也没有执行系统设置、Keychain、签名、公证、上传或真实用户同步。

下一切面把非终态 upgrade receipt 检查接入 Manager/InputMethod startup gate，并分别实现数据升级 validation host。现有 unknown/P0 `ValidationHost` 继续只承担上下文分类证据；在持续静止、双端 validation、切换与崩溃恢复完成前，M4-P02 保持未退出。

## 2026-07-21：M4-P02 双端启动门禁与候选数据产品验证

- 只读 preflight host 已作为 `ae2c130 feat(upgrade): 增加 macOS 只读升级预检宿主` 独立提交；提交前完整 `./scripts/check-repo.sh` 再次通过。
- `ime-product-upgrade` 新增零写入 startup gate：data root/state absent 与终态 receipt 允许启动；active guard、非终态、损坏 receipt、中断 artifact、未知对象、unsafe root/state 和 identity drift 失败关闭。Manager 在 Flutter delegate 初始化前、InputMethod 在 `IMKServer`/Rime 前通过 ABI v8 调用。
- `ime-userdb::open_read_only_current` 以 SQLite read-only/query-only 打开 candidate，不配置 WAL、不迁移、不 chmod。Manager validation 执行真实管理查询并校验 settings v1；InputMethod validation 组合 native Rime、personalized runtime 与 privacy-mode 只读候选信号，两端 helper 均固定 candidate、拒绝参数并复验字节与 sidecar 零残留。
- 精准 Rust 测试在沙盒因 `/private/tmp` Unix socket 返回 `EPERM`，真实环境下 `ime-product-upgrade` 40 项、`ime-userdb` 72 项、FFI/header 与同步集成全部通过。`check-macos-imk.sh` 和 Manager 产品 build/FFI smoke 通过；未启动或安装任一真实产品，未访问真实 Application Support，真实用户同步仍关闭。

下一切面把双端 host 成功证据纳入 receipt 的 `candidate_verified` 推进，再实现原子切换、最终路径复验和崩溃恢复。App Group、安装载体、签名公证与真实用户目录演练继续后置。

## 2026-07-21：M4-P02 candidate validation receipt 切面

- 双端启动门禁与候选数据产品验证已提交为 `8c37cb4 feat(upgrade): 增加双端启动门禁与候选验证`；未推送远端。
- `ime-product-upgrade` 新增 validation evidence v1 与受控 report：Manager 必须给出目标 schema、管理查询和 settings 检查位，InputMethod 必须给出同一 schema、personalized runtime 和候选信号检查位。端点顺序与 failure precedence 固定，不能交换证据或用版本漂移摘要推进状态。
- 协调核心只在 guard、persisted receipt、candidate identity 与 sidecar 状态仍一致时处理结果。双端成功后再次以 read-only current-schema connection 复验 candidate，再原子推进 `candidate_migrated -> candidate_verified`；Manager/InputMethod 明确失败分别原子进入带稳定 failure code 的 `aborted_preserved`，原固定 `userdb.sqlite3` 不变。
- receipt 不新增 host 输出、路径、内容 hash 或布尔数组；`candidate_verified` 状态本身是双端成功证明。测试覆盖精确成功、双端失败、版本/check evidence 漂移、候选同 inode 损坏、未知 sidecar 和 stale receipt，candidate 成功路径字节保持不变。
- `ime-product-upgrade` 44 项测试已通过；本切面仍只使用合成临时目录，没有访问真实 Application Support，没有启动、安装或停止 Manager/InputMethod，也没有执行系统设置、Keychain、签名、公证、上传或真实用户同步。

下一切面固定 `switch_prepared` 的备份对象、同文件系统 rename/fsync 顺序及每个崩溃点的恢复语义，再实现最终固定路径复验与精确回滚。App Group、发布载体、签名公证和真实用户目录演练继续后置。

## 2026-07-21：当日提交回顾与文档复核

今日在 `dev` 依次形成七个 M4-P02 功能提交：

- `9497dfe feat(upgrade): 建立产品数据升级协调基础`
- `b7b80d8 feat(upgrade): 增加 SQLite 一致快照`
- `1643ac9 feat(upgrade): 增加隔离迁移候选`
- `a90704b feat(upgrade): 增加 settings 保留副本`
- `ae2c130 feat(upgrade): 增加 macOS 只读升级预检宿主`
- `8c37cb4 feat(upgrade): 增加双端启动门禁与候选验证`
- `1d5b9ce feat(upgrade): 持久化候选双端验证结果`

这些提交把 M4-P02 从文档边界推进到 `candidate_verified`：原固定库保持只读，SQLite 一致快照和 migration candidate 位于私有状态目录；Manager/InputMethod 在任何 userdb、settings、Rime runtime 或业务初始化前执行 ABI v8 startup gate；双端产品 validation host 复用真实 native library，并以 validation evidence v1 推进 receipt。所有实现批次均在提交前完成匹配的精准验证，影响 ABI、产品构建或启动路径的批次补跑完整 `./scripts/check-repo.sh`；全程仅使用合成临时数据，没有访问真实 Application Support、安装或启动真实产品、迁入 App Group、开启真实用户同步或执行签名公证。

按今日代码复核活跃文档后，补齐当前 ABI v8、startup/validation FFI 入口、`candidate_verified` 证据和仓库职责说明。历史周志中 ABI v7 首次引入同步资格 run 的记录保持原样；当前文档明确 v8 兼容保留 v7 能力并增加 M4-P02 产品升级门禁。

## 2026-07-22：明日事项

1. 先在 [macOS 数据升级协调器边界](../macos-data-upgrade-coordinator.md) 固定切换对象模型和持久化顺序：明确原库、migration candidate、备份对象的固定文件名与身份字段；`switch_prepared` 必须在任何源路径变化前持久化可恢复的备份身份，并再次确认三者位于同一文件系统。
2. 在 `ime-product-upgrade` 实现受控切换：保持原库不做原地 migration，按已固定顺序完成原库到备份、candidate 到 `userdb.sqlite3` 的 rename，并在每个文件和父目录边界执行必要 `fsync`；任何未知 WAL/SHM/journal、中断 artifact 或身份漂移都失败关闭。
3. 为每个持久化边界加入确定性故障注入与重启恢复测试，至少覆盖备份 rename 前后、candidate rename 前后、`switch_prepared` / `switched` receipt 前后、`fsync` 失败、对象缺失或替换、未知 sidecar。恢复逻辑只能依据已持久化 receipt 与精确对象身份判断，不能猜测或自动清理不明现场。
4. 切换后由 Manager 与 InputMethod validation host 重新打开固定最终路径。双端通过后依次推进 `post_switch_verified` 与 `completed`；任一端失败进入 `rollback_required`，按精确身份恢复旧库并完成旧版本兼容或只读验证后进入 `rolled_back`。恢复材料的删除继续作为带版本和明确目标的后续动作，不在本批自动执行。
5. 按风险执行验证：先运行 `ime-product-upgrade` 精准测试；若触及 userdb 或 FFI，同步扩大到相关 crate、header contract 与 Clippy；若触及产品 host 或启动/切换路径，运行 Manager/InputMethod 产品门禁；ABI、产品构建、启动或切换路径完成后再运行完整 `./scripts/check-repo.sh`。
6. 保持停止线：不访问真实 Application Support，不安装、启动或停止真实 Manager/InputMethod，不迁入 App Group，不开放真实用户同步，不提前实现安装载体、Developer ID、Hardened Runtime、公证或发布上传。

## 2026-07-24：M4-P02 原子切换与逐边界恢复

- 在正式边界与 `ime-product-upgrade` 组件说明中固定三个不可覆盖路径：最终 `userdb.sqlite3`、`migration-candidate.sqlite3` 和 `source-backup.sqlite3`。`SourceDatabase` 与 `BackupDatabase` 表示同一个旧库 inode，`CandidateDatabase` 在 candidate 与最终固定路径间保持同一 inode；receipt format 与 ABI 均未变化。
- `switch_userdb_candidate` 先在任何路径变化前追加 backup identity 并持久化 `switch_prepared`，再执行旧库到 backup、candidate 到最终路径的同文件系统 rename；每次 rename 后按目标目录、源目录顺序 `fsync`，精确复验两个对象及 sidecar 零残留后持久化 `switched`。
- 重启恢复只接受四类 receipt/identity 现场：尚未移动、只移动旧库、两次 rename 已完成、已持久化 switched。其他缺失、替换、提前出现的 backup、跨设备、未知 sidecar 或身份歧义均失败关闭且不自动清理；已 switched 调用幂等返回。
- 新增 13 个 receipt/rename/fsync 故障点的逐一恢复测试，并覆盖原库或 candidate 缺失/替换、未知 WAL 与无证据 backup。`cargo test -p radishlex-ime-product-upgrade --all-targets` 现有 48 项全部通过，`cargo clippy -p radishlex-ime-product-upgrade --all-targets -- -D warnings` 通过。
- 本切面仅使用隔离合成目录，没有访问真实 Application Support，没有安装、启动或停止 Manager/InputMethod，也没有迁入 App Group、开放真实用户同步或执行发布动作。

下一切面在最终固定路径重新调度 Manager/InputMethod validation host：双端通过后推进 `post_switch_verified -> completed`；任一端失败进入 `rollback_required`，精确恢复旧 inode 并完成旧版本兼容验证后进入 `rolled_back`。M4-P02 仍未退出。

## 2026-07-24：M4-P02 最终复验与精确数据回滚

- 原子切换切面已提交为 `02a94d4 feat(upgrade): 增加原子切换与崩溃恢复`；未推送远端。
- 双端 `RadishLexUpgradeValidationHost` 保持 ABI v8，不改历史 `candidate_path` 字段布局。无参数模式固定 candidate 与 settings backup；唯一参数 `--post-switch` 固定最终 `userdb.sqlite3` 与产品 settings。共享 Objective-C contract 覆盖两种合法模式、任意路径和多余参数拒绝、四个固定路径映射。
- 协调核心只在精确 switched 现场接受 post-switch evidence v1。双端成功先持久化 `post_switch_verified`，独立完成动作再次复验相同 inode 与 current schema 后进入 `completed`；端点失败、证据漂移、核心数据库复验失败或完成前重复复验失败都进入带稳定 failure code 的 `rollback_required`。
- 精确回滚把失败新库原 inode 移回固定 candidate，再把 `source-backup.sqlite3` 的旧库原 inode 恢复到 `userdb.sqlite3`；每次跨目录 rename 后按目标、源目录顺序 `fsync`。文件恢复不冒充旧版本兼容成功，receipt 保持 `rollback_required`，直到 source-release evidence v1 与核心 `inspect_file` schema/integrity 同时通过才进入 `rolled_back`。
- 升级协调器测试增至 56 项，覆盖 post-switch 成功、完成复验漂移、端点失败、三类 rollback 现场、全部回滚 rename/fsync 故障边界、source-release evidence 拒绝和终态 receipt 幂等恢复；Clippy 与 FFI 全套测试通过。InputMethod、Manager 产品门禁和完整 `./scripts/check-repo.sh` 均通过，仅保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本切面只使用合成临时目录与构建产物，没有访问真实 Application Support，没有安装、启动或停止 Manager/InputMethod，没有修改系统输入源、Keychain 或签名身份，也没有迁入 App Group、开放真实用户同步或执行发布上传。

下一切面实现完整平台协调入口：在同一 guard 内持续证明双端静止，按 receipt 状态调度现有 preflight/snapshot/candidate/validation/switch/rollback 原语，并由受控 source-release host 产生旧版本打开证据。完成端到端合成故障恢复门禁后再判断 M4-P02 是否退出；安装载体与程序版本回滚仍属于 M4-P03。

## 2026-07-24：M4-P02 guard-bound 核心协调驱动

- 最终复验与精确回滚切面已提交为 `6238cfd feat(upgrade): 完成最终复验与精确回滚`；未推送远端。
- 完整驱动复核发现三个更早的 evidence-only 崩溃窗口：settings backup、snapshot 或 candidate identity 已写入 receipt，而下一状态尚未落盘。三个原 API 现会复验源对象、固定目标 identity、sidecar 与 SQLite schema/integrity 后幂等补写状态；rename 已发生但无 identity、临时文件残留或对象漂移仍失败关闭。
- 新增 `resume_userdb_upgrade` 与 `UpgradeCoordinatorPort`。核心从已持久化 `preflighted` 或任一后续状态续跑，在 settings/snapshot/candidate、双端 validation、switch、completion、rollback restore 和 source-release validation 的固定 checkpoint 前后要求平台重新证明静止；不定位 executable、不接受路径，也不把 host 输出写入 receipt。
- 合成驱动覆盖成功收敛 `completed`、candidate 失败保留旧 inode 并进入 `aborted_preserved`、post-switch 失败恢复旧 inode 并进入 `rolled_back`、validation 后静止丢失停在 `candidate_migrated` 后重启续跑，以及旧版本 evidence 暂不可得时保持已恢复的 `rollback_required` 后续完成。
- `cargo test -p radishlex-ime-product-upgrade --all-targets` 增至 61 项，Clippy 通过。全部测试只使用私有合成临时目录，不访问真实 Application Support、不启动产品 host、不安装或停止真实进程，也不修改系统输入源、Keychain 或真实用户同步 gate。

下一切面实现 macOS 固定 host adapter：每个 checkpoint 复用只读 preflight，candidate/post-switch/source-release helper 必须绑定受 ProductManifest 证明的固定 source/target bundle；随后在隔离合成 Application Support 中运行完整产品协调 smoke。安装载体选择、真实程序 bundle 切换与授权系统动作仍属于 M4-P03。

## 2026-07-24：M4-P02 manifest-bound macOS 协调适配器

- guard-bound 核心协调驱动已提交为 `f775f53 feat(upgrade): 增加守卫内协调驱动`；未推送远端。
- 新增 `radishlex-macos-upgrade-coordinator` 平台 crate，实现 `UpgradeCoordinatorPort` 而不把 macOS 进程调用迁入核心。构造入口只接收 source/target 产品根，严格解析两份 `ProductManifest.json` 并固定双 bundle/helper 相对路径，不接受独立 executable、数据库、settings、bundle ID、release 或 schema 覆盖。
- 所有 quiescence checkpoint 绑定 target Manager preflight；candidate/final validation 绑定 target Manager/InputMethod；恢复后的 source-release evidence 要求 source Manager/InputMethod 都以最终路径模式成功。每次执行前复验 helper 为非 symlink、单 link、可执行普通文件，长度和 SHA-256 必须与所属 manifest 一致；release/schema 漂移、manifest 重复/缺项、helper 替换或端点失败均失败关闭。
- 产品 host runner 清除 `DYLD_*` 与 `CFFIXED_USER_HOME`，使用空 stdin、受控输出和 30 秒时限；stdout/stderr、绝对产品路径与 checkpoint 不进入 receipt。manifest hash 只证明内容绑定，不替代 M4-P03 的 Developer ID、notarization、固定安装来源和 code signature requirement。
- target Manager 产品构建现嵌入并 ad-hoc 签名 `RadishLexUpgradePreflightHost`；产品门禁验证双端 bundle ID 已编译进入、任意参数返回 2，临时双 bundle `ProductManifest.json` create/verify 已确认新 helper 进入 file records。
- adapter 10 项 contract 覆盖严格 ready JSON、逐 checkpoint 重跑、target candidate/final 顺序、source 双端恢复、Manager/InputMethod 失败、release 漂移、manifest 缺项、字节篡改、symlink parent 与 hardlink。专项 gate、核心 61 项测试、双 crate Clippy 和 Manager 产品构建/FFI smoke 均通过。
- 本切面没有无参数执行生产 preflight，没有访问真实 Application Support，没有启动 GUI、安装/停止真实产品、修改系统输入源或 Keychain，也没有迁入 App Group、开放真实用户同步或执行发布上传。

下一切面建立隔离产品协调资格 harness：通过仅测试进程使用的固定用户域重定向，让受 manifest 证明的真实 source/target helper 在合成 Application Support 上驱动完整成功、端点失败、checkpoint 静止丢失、切换后回滚和重启恢复。生产 adapter 必须继续清除测试覆盖；安装载体与发布身份验证仍属于 M4-P03。

## 2026-07-25：M4-P02 隔离真实产品协调资格与退出

- manifest-bound macOS 协调适配器已提交为 `0dff0a2 feat(upgrade): 绑定 macOS 产品协调宿主`；未推送远端。
- 新增仅在 `qualification-harness` Cargo feature 下编译的资格入口。它要求 canonical temp 根、精确 marker、私有合成 user home 和 source/target 产品根，拒绝根内 symlink、权限或 owner 漂移；production runner 继续主动清除 `CFFIXED_USER_HOME`。
- 新增独立 source `0.0.9 (34)` metadata、资格 marker 和产品协调门禁。脚本重新构建真实 Manager、native InputMethod 与 locked RimeData，形成独立 source/target 装配并复验 manifest，再让真实 preflight/validation helper 驱动合成 Application Support。
- 资格场景覆盖完整成功并保留原 inode 备份、Manager/InputMethod candidate 端点失败并保持原库、candidate validation 后静止丢失与重启续跑、post-switch 失败后精确恢复原 inode，以及 source evidence 一次不可得后的重启完成回滚。
- 真实门禁发现 InputMethod validation 在全新临时 Rime user data 上禁用 deployment 时无法加载 locked YAML。修正为只向该短生命周期目录部署；产品 `RimeData`、candidate 和 Application Support 均保持只读，helper 退出仍必须删除临时目录。
- adapter 默认 10 项、资格 feature 11 项、FFI 48 项相关测试与双 crate Clippy 通过；真实产品协调资格 1 项集成测试覆盖 5 组场景并确认资格临时根零残留。adapter 快速门禁已接入 macOS 仓库基线，完整 `./scripts/check-repo.sh` 通过；真实产品协调继续保留为专用门禁。全程未访问真实 Application Support，未安装/启动 GUI 产品，未修改系统输入源或 Keychain。
- M4-P02 依据既定退出标准完成。首发 source qualification 的版本、签名和 manifest 独立，但 native code/schema 与 target 同源，不冒充尚不存在的历史发布二进制；首个发布包形成后，后续升级必须保留实际 source release 并重跑跨发布资格。

下一主批次为 M4-P03。先在产品包边界内完成安装载体决策与退出标准，明确固定安装目标、权限/提权、preflight/协调器安置、程序 bundle 切换和回滚、Developer ID/Hardened Runtime、公证、Gatekeeper 以及默认保留 Application Support 的移除语义；真实系统安装和发布动作仍需单独授权。

## 2026-07-25：M4-P03 用户域安装载体与 InstallPayload contract

- M4-P02 隔离产品协调资格已提交为 `6f425b2 feat(upgrade): 完成隔离产品协调资格`；未推送远端，工作区从干净 `dev` 进入 M4-P03。
- 新增 ADR 0008，选择“Developer ID Application 签名的独立用户域 Installer app + 签名、公证、staple 的 UDIF DMG”。纯拖拽 DMG 不能完成双目标安装，ZIP 容器不可签名，标准 `.pkg` 不承担当前用户的输入源交互、数据协调和外层程序事务；未来系统域/企业分发需单独决策。
- 首个候选固定写入 current-user home 下的 `Applications/RadishLex Manager.app` 与 `Library/Input Methods/RadishLexInputMethod.app`，数据和外层状态继续位于 `Library/Application Support/RadishLex`。Installer 不请求管理员权限，不接受自定义目标，不程序化选择/停用输入源。
- 新增 `packaging/macos/install-layout.json` 与严格 Python contract。`InstallPayloadManifest.json` 绑定 committed layout、ProductManifest、版本/build、Installer bundle ID、两个 component-to-target 映射和默认程序移除保留数据语义；payload 拒绝产品根额外条目、内容变更、layout 替换、symlink 根和既有输出覆盖。
- 新增只写 `target/macos-install-payload/<version>-<build>/` 的稳定装配入口。7 项合成测试覆盖用户域决策、严格字段、确定性双次装配、产品 mutation、未绑定文件、bundle symlink、layout substitution 和输出覆盖；门禁不读取真实 Application Support，不安装/启动产品，也不访问系统设置、Keychain、签名凭据或网络。
- 安装载体依据 Apple 当前直接分发与 notarization 文档复核：发布顺序为嵌套 executable/产品 bundle、Installer app、DMG，使用 Hardened Runtime、trusted timestamp、`notarytool`/Notary API、notary log、stapling 与 Gatekeeper；本切面只固定 contract，不冒充发布证据。

下一切面先建立独立 `ime-product-install` 外层事务边界与 receipt/guard/startup decision，再实现合成用户域中的双 bundle 切换、部分完成恢复和 source 程序回滚。M4-P02 数据 receipt 保持单一职责；没有外层 startup gate 前不写真实安装路径。

## 2026-07-25：M4-P03 外层程序安装事务核心

- 新增 `docs/macos-installation-transaction.md`，把 Installer UI、macOS 路径/签名 adapter、程序事务核心和 M4-P02 数据协调器分层；数据 receipt 不扩张为程序事务真相源。
- 新增独立 `ime-product-install` crate。四种 operation 明确 source/target 关系；升级只接受兼容的更高 release，修复要求同 release，默认移除没有 target 且不删除数据。
- 产品身份只记录 ProductManifest、bundle tree 与 canonical code identity evidence 的 SHA-256，以及固定 component/bundle ID/release；receipt 不记录绝对路径、Team ID 明文、签名输出或用户数据。
- 外层状态覆盖 staging、source preserve、逐端 commit、数据协调、最终验证、提交前保留终止和提交后程序回滚。artifact evidence 只能按阶段追加；终态只有通过匹配 `previous_operation_id` 与实际结果 source/target 的新 operation 才能替换。
- `.radishlex-install-v1` receipt 使用 canonical JSON、`0600` 原子临时文件/rename/目录持久化和绑定 data-root identity 的 Unix socket guard。startup decision 完全只读，active/non-terminal/损坏现场阻止启动，终态还必须匹配当前运行 Manager/InputMethod 身份；completed remove 拒绝残留程序启动。
- 18 项合成测试与 Clippy 覆盖四类 operation、状态/证据顺序、并发 guard、中断/未知/损坏对象、终态 operation chain、root/运行程序漂移、回滚和只读缺失路径。未访问真实 Application Support、程序安装位置、系统设置、Keychain 或签名凭据。

下一切面实现程序切换文件系统原语：两个固定目标各自同文件系统 staging/backup，按 receipt 逐端 rename/fsync，覆盖每个持久化边界的中断恢复和 source 程序恢复。真实路径、签名 adapter、Installer UI 和系统动作继续后置。

## 2026-07-25：M4-P03 双 bundle 同文件系统切换与恢复

- 外层程序事务 receipt 的 artifact slot 扩展为 source/staged/backup/installed，并为每条 evidence 持久化程序根 `device/inode/owner/mode`。source/backup 与 staged/installed 必须保持精确 inode 连续性；拒绝 evidence 时不污染内存 receipt。
- 新增跨平台 Unix 程序切换原语。Manager 与 InputMethod 分别绑定平台已验证的 canonical 目标父目录和固定 bundle 名，在同一父文件系统创建 `0700`、只允许 `staged.app` / `source-backup.app` 的 operation 事务目录；未知对象、跨设备、symlink 根、owner/mode 或父目录 identity 漂移均失败关闭。
- upgrade/repair/remove 在路径变化前记录 source evidence，再把 source 原 inode 移到 backup；install/upgrade/repair 依次把 Manager、InputMethod staged 原 inode移到固定目标。每次 rename 后按目标目录、源目录顺序 `fsync`，receipt evidence 与状态分别持久化，重启可识别 rename 已完成但下一 receipt 尚未落盘的现场。
- rollback 对已提交 component 先把失败 target 原 inode移回 staged，再恢复 source backup；未提交 InputMethod 直接保留 target staged 并恢复 source。部分切换测试证明 Manager 已提交、InputMethod 未提交时两个 source 原 inode均可恢复，只有双端结果复验后才推进 `programs_restored`。
- 25 项合成测试覆盖四类 operation 的状态/证据、首次安装和移除恢复、升级部分提交恢复、receipt/guard/startup gate、目标与事务目录安全边界，以及 preserve、commit、target 返回 staging、source 恢复四类动作的 rename 前、rename 后、目标目录 fsync 后、源目录 fsync 后故障与幂等重试。Clippy 和精准 crate 门禁通过；全部目录位于短生命周期合成根，没有访问真实 Application Support、`~/Applications`、`~/Library/Input Methods`、系统设置、Keychain 或签名凭据。

下一切面实现 macOS 固定路径与产品身份 adapter：绑定 committed install layout、InstallPayloadManifest、ProductManifest 和 canonical code-signature evidence，使用保留 macOS bundle metadata 的复制方式填充核心 staging 槽位，并在 source、staged、installed、restore 各阶段复验逻辑身份。随后再映射 M4-P02 数据协调状态和接入双端 startup gate；真实安装、输入源修改、Developer ID、公证与发布上传继续后置。

## 2026-07-25：M4-P03 manifest-bound macOS 安装适配器

- 新增 `radishlex-macos-product-install` 平台 crate。adapter 内嵌 committed `install-layout.json` 字节，严格解析 InstallPayloadManifest/ProductManifest、双 component、许可证与完整文件/symlink records，只从 authoritative current-user home 形成 Manager、InputMethod 和 Application Support 固定路径。
- 新增 `radishlex-bundle-tree-v1`：按 UTF-8 相对路径排序，绑定单 link 普通文件的 size/SHA-256 与内部相对 symlink target，拒绝未知对象、hardlink、绝对/逃逸/broken symlink、路径重复和记录漂移。target payload 必须与 ProductManifest 完整一致；source、staged、installed 和 restored bundle 使用同一算法匹配 receipt identity。
- production code identity 先执行 `codesign --verify --deep --strict -R=<expected requirement>`，再精确匹配 Identifier、TeamIdentifier 和 designated requirement，并把 CDHash、Signature、CodeDirectory 等固定字段编码为 v1 SHA-256。receipt、错误和日志不保存 requirement、Team ID 或 codesign 原文；没有 Developer ID 要求时不提供 ad-hoc production fallback。
- production staging 使用 `/usr/bin/ditto` 保留 resource fork、extended attributes、ACL、quarantine 和 HFS compression。复制前复验 guard、operation、固定目标与 payload，复制后重复 tree/signature 验证并递归 fsync，才调用核心记录 staged inode；完整复制但 evidence 未落盘时可重启补记，部分或被替换对象保持现场且不覆盖。
- adapter 9 项与程序核心 25 项测试、双 crate Clippy 通过。合成链覆盖首次安装、完整升级/程序回滚、source/target/restored 复验、layout/payload/product/signature/path/hardlink 漂移、复制完成未记录和部分复制停止线；没有访问真实 `~/Applications`、`~/Library/Input Methods`、Application Support、系统设置、Keychain、Developer ID 或网络。

下一切面把 M4-P02 升级协调器纳入外层事务：同一 guard 内调度程序提交后的数据协调，映射 `data_coordinating` / `data_settled`，并让数据失败同时收敛程序与数据 rollback。随后接入双端外层 startup gate 和隔离产品资格；真实安装、Installer UI、输入源操作、Developer ID、公证与上传继续后置。

## 2026-07-25：M4-P03 程序与数据事务协调组合

- 新增 `radishlex-macos-product-install-coordinator`，保持 `ime-product-install` 与 `ime-product-upgrade` 不互相依赖、不解释对方 receipt。组合开始前精确绑定同一 operation ID、Application Support device/inode/uid/mode、source/target version/build、当前 persisted 外层 receipt、双 `ProgramSwitchStore` 与 component 顺序。
- 外层先在 target preflight 与 installed target 双 bundle 身份同时成立时持久化 `data_coordinating`。随后用 wrapper 把 `InstallProgramValidationPort` 注入 M4-P02 每个 quiescence checkpoint，target tree/code identity 暂不可证明时保留外层/data 最后状态，不继续 snapshot、migration 或切换。
- data `completed` 后再次证明静止与 target 双程序身份，外层才推进 `data_settled`。data `aborted_preserved` / `rolled_back` 后，组合层先持久化外层 `rollback_required`，再按核心精确 inode 恢复 source 双程序；只有 restored source tree/code identity 与核心 evidence 都成立，外层才经过 `programs_restored` 收敛为 `rolled_back`。
- 9 项合成测试在同一私有 data root 上组合真实两个事务核心、双 bundle rename 与 SQLite coordinator，覆盖成功、candidate abort、post-switch data rollback、静止丢失、installed target 漂移、restored source 暂不可得、重启续跑、未持久化双 receipt 状态和 operation/release/root 绑定拒绝。三个相关 crate 测试与 Clippy 通过；没有访问真实 Application Support、用户域程序、系统设置、签名身份、Keychain 或网络。

下一切面完成外层 `data_settled -> final_verified -> completed` 的产品终态动作，并把 Manager/InputMethod 启动接入外层只读 gate。随后以隔离双 bundle + 合成 Application Support 建立覆盖成功、失败、回滚、启动阻断和重启续跑的产品级门禁；真实安装与 Installer UI 继续后置。

## 2026-07-25：M4-P03 产品终态与双端启动门禁

- `ime-product-install` 新增四类 operation 共用的终态 port。first install、repair、remove 从 `programs_committed`，upgrade 从 `data_settled` 进入统一两段动作；`final_verified` 与 `completed` 前分别复验 persisted receipt/guard、双 `ProgramSwitchStore` 和最终程序结果，第二段中断后保留现场并重新取证续跑。
- install/data 组合层为 upgrade 在两段终态前精确绑定 data receipt/guard、Application Support device/inode/uid/mode、source/target release、data `completed` 与 installed Manager/InputMethod 身份。核心与组合测试覆盖四类 operation、成功、第二段中断、重启续跑、非终态/漂移 data receipt 和程序身份暂不可得。
- 公共 FFI 升级到 ABI v9，新增独立 `radishlex_product_install_startup_gate` request/result v1，并在 header、Rust、Dart expected contract、产品 metadata、source qualification fixture、manifest 测试和双产品 symbol 门禁中同步。既有 ABI v8 数据 gate/validation 布局兼容保留。
- 外层 gate 不接受 component、bundle path、release 或 hash identity。macOS 仅从当前 executable 反向绑定固定用户域 Manager/InputMethod bundle，读取 Info.plist、复算完整 tree，并以 strict `codesign` 的 Developer ID 字段形成 canonical code identity；UI、settings、`HOME` 和调用方自报身份均不进入契约。
- Manager 在 `super.applicationWillFinishLaunching` 前、InputMethod 在 `IMKServer`/`NSApplication` 前依次执行外层 install gate 与数据 upgrade gate。active guard、非终态/损坏 receipt、中断写、未知对象、identity drift、completed remove、FFI 错误和未知 result 全部失败关闭；日志只保留 decision/error/state。静态启动顺序门禁同时接入 Manager 与 InputMethod 产品检查。
- 27 项 install core、9 项 macOS adapter、12 项 install/data 组合测试及相关 Clippy 通过；Manager Release bundle、InputMethod bundle、FFI/header、ABI symbol、双门禁顺序、产品 metadata/install layout 与 `./scripts/check-repo.sh` 全部通过。验证只使用仓库构建产物和合成临时数据；本切面不安装、启动或停止真实产品，不修改系统输入源、用户 Application Support、Keychain 或签名身份，不清理 staging/backup，也不开放真实用户同步。

下一切面建立隔离双 bundle + 合成 Application Support 的端到端故障恢复门禁，组合程序切换、数据协调、两段终态、双端启动阻断和重启续跑；Installer UI、真实用户目录安装、Developer ID、公证与 DMG 发布继续后置。

## 2026-07-25：M4-P03 隔离产品终态与故障恢复资格

- InstallPayload contract 新增显式 product metadata 输入，使 source `0.0.9 (34)` 与 target `0.1.0 (35)` 能各自由自己的 ProductManifest 形成确定性 payload；默认生产入口仍绑定 committed `packaging/macos/product.json`，错误 metadata 无法验证另一代 payload。
- macOS install adapter 新增仅在 `qualification-harness` feature 下可用的隔离入口。它复用数据资格的固定 marker，要求根位于系统 temp 后代、root/home/payload 为同 owner、无 symlink、精确 `0700` 目录，并只在该边界接受 `Signature=adhoc`、`TeamIdentifier=not set`、ad-hoc CodeDirectory 与包含主 CDHash 的 bounded designated requirement。
- 真实 `codesign -d --verbose=4 -r-` 复验发现身份字段位于 stderr、designated requirement 位于 stdout。adapter 现分别限制两路长度、UTF-8/NUL/CR 和唯一 requirement 后再形成 canonical evidence；production Developer ID 与 qualification parser 都复用这一真实输出模型，production requirement 校验未放宽。
- `check-macos-upgrade-product-coordination.sh` 现复用一次真实 Manager/native InputMethod 构建，装配并验证 source/target ProductManifest 与 InstallPayload，再依次运行既有数据协调资格和新增 install/data 产品恢复资格。
- 新资格覆盖三组完整场景：`final_verified` 第二段失败后释放 guard、回读双 receipt 并完成；真实 candidate host 通过后注入失败，恢复 source 双 bundle 并拒绝 target 运行身份；Manager 已提交而 InputMethod 未提交时重启，非终态 gate 保持阻断，再续跑数据与两段终态。active 双 guard、data terminal/outer non-terminal 和 completed source/target 身份决策均有断言。
- Python payload 8 项、install adapter qualification 11 项和联合集成测试通过；真实产品门禁构建两端并完成既有 5 组数据场景与新增 3 组安装恢复场景，完整 `./scripts/check-repo.sh` 通过，仅保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。全程只使用带 marker 的临时根和合成 Application Support，没有安装产品、访问真实用户目录、停止进程、修改系统输入源、使用 Developer ID/公证凭据或开放真实用户同步；staging、backup 和历史 operation 材料仍不进入终态清理。

下一切面先固定独立 Installer app 的 UI/驱动 contract、operation 状态展示、稳定错误和显式用户授权交互，再决定真实用户目录安装与发布供应链的独立授权验收。

## 2026-07-25：M4-P03 独立 Installer UI/驱动契约

- 隔离产品终态与故障恢复资格已提交为 `b435012 feat(installer): 建立隔离产品恢复门禁`；未推送远端，工作区从干净 `dev` 进入 Installer 切面。
- `ime-product-install` 新增零写入 status projection。absent data root/state 不创建目录；active guard 隐藏 receipt，stale guard 保留现场但允许投影持久化 receipt；中断/损坏 receipt、未知对象、unsafe root/state、身份漂移和 IO 全部使用既有稳定错误失败关闭。
- 新增 `radishlex-macos-installer-driver`。snapshot v1 固定 verified product situation、operation、phase、primary/secondary action、receipt state、0–10 持久化进度、manual prompt、稳定 error 和 `retain_application_support`；无 receipt 时只允许平台已验证的 first install/upgrade/repair 分支，更新版本或身份不可得阻断。
- mutation action 先重新验证当前 snapshot，拒绝 stale action 和缺失显式确认；remove 还要求确认只移除两个程序、保留 Application Support，并确认已手动切换中立输入源、关闭 Manager。确认只形成 intent，不能替代公开平台 API 和 manifest-bound preflight。
- 新增独立 AppKit contract shell，以 `org.radishlex.installer.macos`、产品 version/build/minimum macOS 构建 ad-hoc bundle，嵌入 committed InstallLayout，显示固定目标、operation、重启继续、进度与只含 `phase/action/error/state` 的诊断摘要。未知 snapshot 只允许刷新；默认 bridge 固定 `driver_unavailable`，不执行真实安装。
- 原生 presentation contract、bundle metadata/layout/code-signature 与禁止 receipt/TIS/`HOME`/自定义路径边界已进入 `./scripts/check-macos-installer.sh` 和仓库基线。普通门禁不启动 GUI、不访问真实用户目录、不停止进程、不修改系统输入源，不使用 Developer ID/公证凭据，也不清理 staging、backup 或历史 operation 材料。

下一切面在隔离合成用户域内把 authorized intent 接入现有 manifest-bound adapter、外层事务和只读 preflight，打通 first install、upgrade、repair、remove 的 restartable executor。完成后再决定真实用户目录、进程停止/输入源交互和发布供应链的独立授权验收。

## 2026-07-25：M4-P03 隔离可续跑 Installer 执行器

- 独立 Installer UI/driver 契约已提交为 `52a13da feat(installer): 固定独立安装器 UI 与驱动契约`；未推送远端，工作区从干净 `dev` 进入执行器切面。
- 新增 `radishlex-macos-installer-executor`。所有 mutation intent 都在外层 guard 内重新读取 current receipt，并要求 target-only manifest-bound preflight 与 InstallPayload target version/build 精确匹配；begin/retry/remove 只生成系统随机 operation ID、持久化 `prepared` 并返回，不能越过 UI 的人工静止确认。
- `ConfirmQuiescence` 再次 preflight 后持久化 `quiesced`，随后按 receipt evidence 幂等重放 source 记录、staging、source preserve、Manager/InputMethod 逐端 commit 与四类终态。非 prepared 中断只接受重新投影并授权的 resume，不创建新 operation。
- first install、repair、remove 共用外层终态 port；upgrade 绑定同 operation ID、data root 与 release 的既有 M4-P02 receipt，复用原数据协调/回滚组合层。审计并修正 `final_verified` 重启分派：该状态直接重新证明 data `completed` 与双程序后完成第二终态，不重复进入 migration。
- driver 的 terminal receipt 投影改为与当前真实 installed-product situation 交叉判断：completed install/upgrade/repair 可继续提供 upgrade 或 repair/remove，completed remove 可重新 first install；receipt 与实际产品矛盾、身份不可得或已安装版本更新均失败关闭。upgrade、repair、retry、remove 与 confirm 都要求中立输入源及 Manager 已关闭确认。
- 核心 receipt store 增加 own-guard 内的 guarded load，避免 acquire 后再用无 guard 读取留下竞态；UpgradeCoordinator adapter 增加只绑定 target 的 preflight port，不接受 UI 自报路径或 available bytes。
- 执行器集成测试覆盖 first install、upgrade、repair、remove、preflight 阻断、active guard、stale intent、staging 中断同 operation 续跑和 upgrade `final_verified` 异常退出重启；Installer 专项门禁同步执行 driver/executor test 与 Clippy，并保留 AppKit contract/bundle 检查。相关核心、adapter、协调器专项门禁和完整 `./scripts/check-repo.sh` 均通过，只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本切面只使用私有合成用户域和既有 manifest-bound 测试 port，没有访问真实 `~/Applications`、`~/Library/Input Methods` 或 Application Support，没有启动/停止产品、修改输入源、使用 Developer ID/公证凭据、清理 staging/backup/历史 operation，也没有开放真实用户同步。AppKit 默认 bridge 仍为 `driver_unavailable`。

下一切面固定 AppKit 到 Rust driver/executor 的版本化 bridge 与 upgrade data-receipt bootstrap，并先在隔离合成用户域证明 prepared/confirm、稳定错误映射与异常退出重启；真实用户目录、进程停止/输入源交互和发布供应链仍需独立授权。

## 2026-07-25：M4-P03 Installer 原生 bridge 与 upgrade receipt bootstrap

- 隔离可续跑执行器已提交为 `6e029c3 feat(installer): 增加可续跑隔离执行器`；未推送远端，工作区从干净 `dev` 进入 bridge 切面。
- 新增 `radishlex-macos-installer-bridge` 与 ABI v1 header。契约固定整数 phase/action/error/operation/state/prompt、POD snapshot 和 contract/snapshot/perform 三个 symbol；AppKit 已静态链接并实际调用，Objective-C 对未知版本、枚举、进度或 action 统一投影 `unknown_driver_result`。
- Rust dispatch 在每次 action 前重新运行只读 status projection 和 authorization，再进入现有 executor；隔离测试证明 `BeginFirstInstall -> prepared`、重启重新投影 `ConfirmQuiescence`、stale begin 拒绝与 active guard 阻断。UI 不传入路径、receipt state、operation ID、产品身份或 available bytes。
- executor 新增 upgrade data receipt bootstrap：只从外层 upgrade receipt 的 source/target、固定 data-root identity、只读 `userdb.sqlite3` schema/identity，以及可选 `manager-settings.json` / `Rime` 身份创建 M4-P02 `preflighted` receipt。既有 progressed receipt 只按同 operation、root、release 与 target schema 重绑；data 已 `completed`、外层停在 `final_verified` 时重启可继续第二段终态，不重新检查已经移动的 source 数据 inode或重复 migration。
- `ime-product-upgrade` 增加 own-guard `load_guarded`，在 guard 活跃时仍按已知 artifact、snapshot/candidate/settings/switch 现场验证后读取 receipt，避免 bootstrap 为绕开 active guard 使用无保护读取。
- Installer 专项门禁执行 driver/executor/bridge tests 与 Clippy，Release 静态库、AppKit bundle、presentation contract、ABI symbol、未知 action、metadata/layout/ad-hoc signature 和 UI 禁止边界全部通过。验证只使用合成 `0700` data root 和构建产物，不访问真实用户目录、不启动/停止产品、不修改输入源、不使用 Developer ID/公证凭据、不清理 staging/backup/历史 operation，也不开放真实用户同步。

下一切面在单独授权前先固定真实用户域 bootstrap 与系统交互验收 runbook；生产 bridge 继续稳定返回 `driver_unavailable`。Developer ID/Hardened Runtime、公证、Gatekeeper 和 DMG 发布证据作为后续独立切面推进。

## 2026-07-25：M4-P03 真实用户域只读 bootstrap 与验收边界

- 原生 bridge 切面已提交为 `262876c feat(installer): 接入版本化原生桥接`；未推送远端，工作区从干净 `dev` 进入获授权的真实用户域 bootstrap 切面。
- Installer bridge 使用 `geteuid/getpwuid_r` 从系统用户数据库取得 effective uid 与 authoritative home，只从当前 executable 固定反推 `RadishLex Installer.app/Contents/Resources`。不读取 `HOME`、`~`、settings、UI/caller path 或 `CFFIXED_USER_HOME`。
- App 构建现嵌入完整 `InstallPayload`，门禁重新验证 layout、payload manifest 和 Product 目录。新增 sealed `ReleaseIdentity.json` format v1，严格绑定 Team ID 与双 component designated requirement；缺失、字段漂移、非普通单链接文件、owner 漂移或 ad-hoc requirement 统一投影 `product_identity_unavailable`。身份通过但 mutation port 尚未开放时保留 `driver_unavailable`。
- bridge 新增真实 bundle/user-domain 路径推导与 release identity 拒绝测试；安装 adapter 的 payload-only production verifier 被 bridge 实际调用，不增加未绑定 API。Installer stderr 只输出 `phase/action/error/state` 稳定摘要。
- 新增真实用户域验收 runbook，固定 ad-hoc 负向验收、Developer ID 正向前置、人工切换/添加/移除输入源、Manager/InputMethod 精确静止、首次安装、升级故障续跑、程序移除和回退停止线；禁止自动 TIS mutation、用户数据删除和历史 operation 材料清理。
- 实机基线确认 `0 valid identities found`，TIS `matches=0 enabled=0 selected=0`、双 bundle absent、Manager/InputMethod stopped、runtime/userdb absent。Application Support/RadishLex 是空 `0755` 目录，不满足事务 `0700` 要求，本轮没有静默 chmod 或删除。重建 ad-hoc Installer 为 `TeamIdentifier=not set`，嵌入完整 payload 且无 release identity；真实启动稳定输出 `phase=blocked action=refresh error=product_identity_unavailable state=none`。精确终止后复验 TIS、进程、双 bundle、外层 state、runtime/userdb 和目录 mode 全部保持基线，因此只记录负向 bootstrap 证据，不冒充成功安装。
- Rust bridge 4 项、安装 adapter 10 项测试和 Clippy 通过；Installer UI/driver/executor/AppKit/嵌入 payload 专项门禁与完整 `./scripts/check-repo.sh` 通过，只保留既有文档篇幅提醒。为重新生成与当前代码一致的产品 manifest，旧 target 装配保留为明确归档，重新构建当前双 bundle/产品装配；没有安装产品、修改输入源、写真实 Application Support、清理事务材料、开放真实同步或执行公证上传。唯一真实 GUI 动作是上述只读负向启动，已精确终止并复验零漂移。

下一切面需要可用 Developer ID Application 身份：生成并 seal release identity，按嵌套顺序启用 Hardened Runtime，并把通过身份验证的 bootstrap 接到真实 mutation port。随后再形成 DMG、公证、staple、Gatekeeper 与正向实机安装/升级/移除证据。
