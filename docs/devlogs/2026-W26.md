# 2026-W26 开发日志

本文档记录 2026-W26 周内 RadishLex 的阶段性推进事实、验证证据和后续建议，读者是项目维护者和后续 AI / 人工协作者。本文不承载长期架构设计、接口字段完整参考或历史推演；长期边界应回写到对应专题文档。

## 本周目标

- 从 Phase 0 方案冻结推进到 Phase 1 Rust Core 原型起步。
- 先固化 Rust core 与底层输入引擎之间的稳定边界。
- 建立可复验的 `ime-core` 最小 workspace 基线。

## 完成情况

- 新增 `docs/engine-boundary.md`，明确 engine trait、核心模型、adapter 职责、错误语义、生命周期和 clean-room 约束。
- 新增 Rust workspace 入口 `Cargo.toml` 与 `crates/ime-core/`。
- 在 `ime-core` 中落地 `InputSession`、`Engine`、`KeyEvent`、`Composition`、`Candidate`、`Commit`、`SchemaId` 和 `SessionState`。
- 新增 test-only stub engine，验证 `push_key -> candidates -> commit_candidate` 生命周期。
- 新增 `radishlex-ime-cli demo <input-code> [candidate-index]`，基于合成 demo adapter 复验 core 生命周期。
- 新增 `docs/engine-rime-adapter.md`，明确 `ime-engine-rime` 的 crate 边界、构建探测、FFI 生命周期、数据目录和验证分层。
- 新增 `crates/ime-engine-rime/` skeleton，默认不启用 native Rime 绑定。
- 补 `RimeEngineConfig`、`RimeEngineError`、key 分类、候选转换和 build script native 探测。
- 在 `native-rime` feature 下补最小 FFI session 管理，覆盖 `setup`、`initialize`、`create_session`、`select_schema` 和 drop 时 `destroy_session`。
- 更新 README 与仓库结构文档入口。
- 将 Rust fmt/test 纳入 `scripts/check-repo.py` 仓库级基线。

## 关键决策

- Phase 1 起步只定义平台无关核心模型，不接 `librime`、SQLite、Go server、Flutter 或平台 SDK。
- test-only stub engine 只用于生命周期验证，不作为真实中文输入引擎。
- CLI demo 使用合成候选，只验证 core 调用路径，不代表真实输入质量。
- `ime-engine-rime` 默认不 vendor `librime`，默认 CI / workspace 检查不依赖本机 native Rime 环境。
- `native-rime` feature 可做本机依赖探测与最小 session 生命周期；输入处理、候选读取和提交读取后续再实现。
- `ime-core` 不暴露底层 engine 私有对象 ID；后续 ranker 与学习只依赖 RadishLex candidate。

## 验证记录

```text
cargo fmt --check
cargo test -p radishlex-ime-core
cargo test --workspace
RIME_INCLUDE_DIR=/private/tmp/radishlex-rime-check/include RIME_LIB_DIR=/private/tmp/radishlex-rime-check/lib cargo check -p radishlex-ime-engine-rime --features native-rime
cargo run -p radishlex-ime-cli -- demo luobo
./scripts/check-repo.sh
git diff --check
```

验证结果：

- `cargo test -p radishlex-ime-core` 通过，3 个测试通过。
- `cargo test --workspace` 通过。
- `cargo check -p radishlex-ime-engine-rime --features native-rime` 在临时 fake include/lib 探测目录下通过，用于确认 Rust 侧 native feature 类型检查；该命令不链接或运行真实 `librime`。
- `cargo run -p radishlex-ime-cli -- demo luobo` 可展示 composition、候选和 commit。
- `./scripts/check-repo.sh` 通过。
- `ime-engine-rime` 默认测试覆盖配置、错误展示、key 分类和候选转换，不需要本机 `librime`。
- 当前仍存在既有文档预算警告：`docs/technical-plan.md` 超过活跃文档 500 行目标，但未超过硬限制。

## 风险与未完成项

- 尚未接入真实底层输入引擎，不能证明中文候选质量。
- 尚未落地 `ime-ranker`、`ime-userdb`、真实 Rime FFI session 或后续 FFI 边界。
- 尚未实现 `ime-engine-rime` 输入处理、候选读取、commit 读取或 native smoke。
- GitHub Actions runner 需要具备 Rust / cargo / rustfmt；本地 `./scripts/check-repo.sh` 已验证通过。

## 下周建议

- 实现 `ime-engine-rime` 最小输入与输出 FFI，先覆盖 `process_key`、`get_context`、`get_commit` 和对应释放。
- 补 CLI 与真实 adapter 的切换策略，明确没有 `librime` 环境时的错误提示和验证降级方式。
- 暂不推进平台壳、同步后端或 Flutter manager，直到 CLI 可复验链路稳定。
