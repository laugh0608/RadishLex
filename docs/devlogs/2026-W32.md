# 2026-W32 开发周志

本文记录 2026-W32 的阶段事实、验证证据、风险和交接事项，读者是 RadishLex 维护者与后续协作者。本文不替代 `docs/status/current.md`、架构边界、协议参考或 runbook。

## 本周目标

- 完成 M5-P04 Linux Manager product bootstrap、共享 XDG/userdb 与同库自动证据。
- 固定并实现 Linux privacy 配置真相源、受控应用粗分类和失败关闭策略。
- 在自动门禁稳定后完成 Linux 桌面学习、管理、并发与重启验收；不提前进入 M5-P05。
- M5-P04 冻结后只收口 M5-P05 安装维护设计、实现顺位和授权边界，不在同批写安装代码或修改系统。

## 2026-08-03：M5-P04 Linux Manager 设计门禁

- 工作从干净且与 `origin/dev` 对齐的 `c6d7b00` 开始；M5-P03 已完成，当前主批次为 M5-P04 Linux Manager、同库个人化与本地管理验收。
- 新增 `docs/linux-manager-local-acceptance.md`，固定 Linux Flutter host、staged bundle `.so`、共享 XDG resolver、product/demo 隔离、WAL/busy/migration、同库双端证据和桌面验收矩阵。现有 Flutter UI、ManagerBridge、ABI v9 与 Rust userdb/ranker 直接复用，不重写业务真相源。
- Linux privacy 真相源固定为独立 `${XDG_CONFIG_HOME}/radishlex/privacy-mode.json`，避免 Fcitx 输入热路径解析持续演进的 Manager settings。文件使用严格 format v1、私有权限、原子替换和读回；非法状态在 Manager 失败关闭，在 addon 投影为 privacy enabled。
- 普通应用学习不能从当前 `context_known=0` 直接放开。后续只使用 Fcitx 公开 program identity 做平台内固定粗分类，原始值不跨 FFI、不持久化、不记录；程序 identity 与 sensitive 传播须经 Wayland/X11 实机评审后才能进入 allowlist。unknown 和当前 GTK4 `PRIVATE` 缺口继续失败关闭。
- 首个代码批次固定为真实 Linux runner、engine 前 umask、共享 XDG、固定 bundle `.so`、平台中立 Dart bootstrap，以及 personalized runtime 写入—Manager bridge 刷新—另一 runtime 观察的产品双端自动证据；不以空 runner、fixture 或单连接 SQLite 测试作为完成。
- 生成与 Flutter 3.44.0 锁定模板一致的 Linux runner，并立即接入 `dev.radishlex.manager/runtime`：runner 在 engine 前设置 `umask(0077)`，从 executable 固定派生 `lib/libradishlex_ime_ffi.so`，直接复用共享 XDG resolver；Dart 路径模型只接受固定 `.dylib`/`.so` basename，错误语义改为平台中立并保留 macOS 回归。
- 新增严格 `privacy-mode.json` platform contract：缺省、true/false、非法格式/版本/类型/大小、宽权限、symlink/hardlink、原子临时文件、文件/目录 fsync、read-back 与 absent rollback 均由共享 C++ 实现。异常路径采用单次 descriptor 所有权关闭，避免解析或 rename 失败后重复关闭；Manager host 已接 read/write/restore/secure method channel，addon watcher 尚未进入本子批。
- 新增 `build-manager-linux-product.sh` 与 `check-manager-linux-product.sh`，固定 workspace native-rime 构建、Flutter staged bundle `.so` 装配、ELF dependency/RPATH/symbol 和无 GUI FFI smoke。当前 macOS 没有 Linux GTK/Flutter 编译环境，真实 bundle 命令尚未执行，不把源码或 Apple clang contract 写成 Linux 产品通过。
- 扩大 native-rime smoke：输入 runtime 选择非首候选后，Manager C ABI 读取同库聚合，新 runtime 观察提升；Manager delete 后新 session 去除旧提升，explicit restore 后新 session 恢复。该链在本机既有 librime、仓库锁定 RimeData 与 `/tmp` 合成 userdb 上通过，未读取真实用户数据。
- 本地门禁通过：`flutter analyze` 无问题，Flutter 全量 97 项测试通过；`cargo test -p radishlex-ime-ffi`、真实 librime ignored smoke、Linux platform contract、Manager runtime ASan/UBSan、文档门禁和 `./scripts/check-repo.sh` 均通过。文档门禁仅保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 既有 guest SSH 转发在本次收尾时不可达；未启动 UTM、未安装 Flutter Linux 工具链、未运行 GUI，也未写 guest 或系统目录。真实 Linux Release bundle 与 ELF/product smoke 仍是首个子批的唯一未闭合自动证据。

## 2026-08-03：M5-P04 Linux Manager ARM64 产品门禁

- 提交首个 host/同库代码子批为 `efd01d3 feat(linux): 建立 Manager 本地产品启动链`；宿主 `dev` 随后保持干净且只领先 `origin/dev` 一项，未推送远端。
- 在既有 UTM Debian 13 ARM64 中只补齐 Flutter/Linux 构建依赖；旧 `RadishLex` 工作副本保留 M5-P03 的 8 个未提交文件，不做 reset、clean 或覆盖。验证使用从其已提交基线克隆并导入 `efd01d3` 的独立工作副本。
- Flutter 工具链固定为官方 `3.44.0` 标签 `559ffa3f75e7402d65a8def9c28389a9b2e6fe42`、Dart 3.12.0 和原生 Linux ARM64 artifacts；`flutter doctor -v` 的 Linux toolchain 与 Linux ARM64 device 通过。Android、Chrome 和非 Linux 网络诊断不纳入本批退出证据。
- `check-manager-linux-product.sh` 首轮成功构建 ARM64 Release executable 与 bundled `libradishlex_ime_ffi.so`，随后按门禁停止：脚本检查了不存在的 `radishlex_userdb_list_terms` / `radishlex_userdb_rank_explain` 缩写名，而库实际正确导出 ABI v9 的 `radishlex_userdb_terms_new` / `radishlex_userdb_rank_explain_new`。该失败属于门禁契约漂移，不是链接裁剪或 Rust ABI 缺失。
- Linux 门禁已改用正式 ABI 名称，`check-repo.py` 同步增加 Linux required-symbol 静态口径，确保非 Linux 宿主也能发现同类误写。修复后真实 ARM64 Release bundle、`ldd` closure、`$ORIGIN/lib`、动态导出符号、native-rime ignored FFI smoke 与 Dart Manager FFI smoke 全部通过，全程未启动 GUI、未安装系统产品或修改输入法配置。
- 首个 M5-P04 host、真实 bridge、固定 bundle `.so` 与同库自动证据子批闭合；下一批转向 addon privacy 变更感知和受控应用粗分类。M5-P04 桌面双进程验收与 M5-P05 安装链仍未完成。

## 2026-08-03：M5-P04 privacy 变更感知与分类契约

- 新增共享 `PrivacyModeRuntime` 和 Linux-only `PrivacyModeMonitor`。监听顺序固定为先对 config 目录建立 inotify watch、再读取初始 snapshot，避免 read/watch 间漏掉 Manager 原子替换；Fcitx event loop 回调与每次 FFI 操作前的非阻塞 drain 串行消费事件，未发生目标变化时不读取或解析 JSON。
- privacy 文件替换、删除与权限变化触发严格重读；非法格式/身份/IO、目录 metadata 变化、watch 丢失、队列溢出或 IO source 异常均转为 enabled，并只记录 `invalid_format`、`unsafe_file`、`io_failure`、`monitor_unavailable` 等稳定类别。有效 privacy 位变化刷新所有现存 addon session 的 `LearningContext`，清除 press 配对与旧候选面板；Rust 既有 context-change contract 清除 deferred selection。
- 新增受控应用粗分类模块。Password、Sensitive、Terminal 在读取 `InputContext::program()` 前返回；Terminal 修正为 `context_kind=terminal, context_known=0`，不再因 category 已知而意外进入 ReadWrite。普通程序只允许精确、大小写敏感的固定表映射为 general/browser/chat/code/editor/office，输出不包含原始身份。
- 合成 classifier contract 覆盖六类允许项、privacy 保留已知粗分类、empty/unknown/大小写/包装器变体和安全 capability 优先级。生产 allowlist 当前保持空表，等待 Wayland/X11 frontend、普通字段和敏感字段传播实机证据；unknown 与 GTK4 `PRIVATE` 继续 EngineOnly，不以合成项冒充产品开放。
- 宿主 `./scripts/check-linux-fcitx5.sh` 通过。Debian 13 ARM64 最终版 `--require-fcitx` 门禁真实编译 `radishlex.so`，通过 staged install、ELF/`$ORIGIN`/`ldd`、headless loader 和 application context、FFI projection、XDG、Manager runtime、privacy monitor、runtime layout 六项 CTest。
- 同一 guest 随后再次通过 `check-manager-linux-product.sh`：Flutter 3.44.0 ARM64 Release bundle、正式 ABI symbol、native-rime ignored smoke 与 Dart FFI smoke 均通过。全程未启动 GUI、Fcitx daemon 或 Manager 窗口，未安装/启用输入法，也未修改系统配置。
- 自动子批已闭合；下一步需单独授权桌面会话，先完成程序身份/frontend/敏感传播评审并形成生产 allowlist，再进入学习排序、privacy 零增量、删除/恢复、导入导出、explain、双进程并发和重启验收。

## 2026-08-03：受控应用身份取证与 Wayland 证据

- 提交 `3a1bb02 feat(linux): 建立受控应用身份取证模式`。新增默认关闭的 `RADISHLEX_APPLICATION_EVIDENCE`，只对源码内固定候选做精确匹配并输出 opaque token；未命中只输出 `unmatched`。Password、Sensitive 与 Terminal 输出 `*_program_unread`，不调用 `InputContext::program()`；capability watcher 只记录 `password_on/off`、`sensitive_on/off` 和 `terminal_on/off`。
- 新增无脚本、无表单提交、无外部资源的 Firefox 离线普通/密码字段夹具。强门禁同时验证固定候选映射、夹具惰性和默认产品 addon 不含取证日志字符串；默认构建不改变生产 allowlist。
- 经既有桌面授权在 Wayland Firefox 完成两轮实体复验：`browser_candidate_01` 精确对应源码中的 `firefox-esr`，进程环境为 Wayland，GTK3 Fcitx frontend 与 Wayland client 映射存在，X11 client 列表无 Firefox。密码字段产生 `password_on` 后恢复 `password_off`；用户确认普通字段显示候选，密码字段只显示圆点且无候选。
- 复验前后 userdb v9 的 active、suppressed、ranker、tombstone、selection、negative 和 import 聚合均为零。该结果证明 Wayland 半侧身份/frontend/密码传播和失败关闭，不足以单独开放普通应用学习；生产 allowlist 保持空表，等待 X11 同序对照。
- guest 最终通过默认与 evidence 两种 ARM64 构建、六项 CTest，以及默认构建的 staged install、ELF/`$ORIGIN`/`ldd`、headless loader 和取证字符串排除。日终已停止 evidence 服务并恢复默认关闭取证的最新 addon 与生产临时服务；P03 backup、staging 和旧含未提交改动的工作副本均保留。
- 今日四个本地提交依次为 `efd01d3`、`46ca8de`、`a20230a`、`3a1bb02`；`dev` 领先 `origin/dev` 四项，未推送、未创建 tag 或 Release。

## 2026-08-04：Firefox X11 对照与生产分类

- 工作从干净且与 `origin/dev` 对齐的 `5148e9a` 开始。用户启动既有 UTM Debian 13 ARM64、保存现场并手动选择 GNOME on Xorg；guest 会话为 `Type=x11`，Xorg 使用 `-nolisten tcp`。旧 P03 副本的 8 个未提交文件、M5-P04 隔离副本、P03 staging/backup 均未 reset、覆盖或清理。
- M5-P04 隔离副本叠加的 22 个实现文件与宿主 `3a1bb02` 逐一对象哈希一致。既有 evidence build 六项 CTest 全过；直接 build 目录因宽权限被 runtime probe 正确拒绝，随后只在用户 state 下建立 `0700` 私有 stage，以 `0600` 放置 evidence addon、同版 FFI 与锁定 RimeData，runtime probe 通过。
- evidence Fcitx 以 user transient unit 运行，`UMask=0077`、`NoNewPrivileges=yes`，只允许 `AF_UNIX/AF_NETLINK`；production addon 未被替换。Firefox 普通字段得到 `browser_candidate_01`，精确对应 `firefox-esr`；进程为 `DISPLAY=:0`、`XDG_SESSION_TYPE=x11`，未设置 `WAYLAND_DISPLAY`/`MOZ_ENABLE_WAYLAND`，登记为 X11 client，并加载 GTK3 `im-fcitx5.so` 与 `libFcitx5GClient`。
- 用户在普通字段输入公开合成 code 后确认候选正常显示并以 Escape 清空；密码字段产生 `password_on`，输入法保持 `keyboard-us`，五个遮罩点期间没有 RadishLex preedit 或候选；返回普通字段后产生 `password_off`。AI 未合成实体操作，日志未记录输入正文、窗口标题或原始程序身份。
- 完整普通/密码往返前后，userdb v9 的 active、suppressed、ranker、tombstone、selection、negative 和 import 七项聚合保持全零。Wayland/X11 的精确身份、frontend、密码传播和失败关闭矩阵由此闭合。
- 生产分类只加入精确 `firefox-esr -> browser`。contract 同时固定 privacy 下保留只读粗类别、Password/Sensitive/Terminal 优先级，以及路径、大小写、wrapper、其他 Firefox 候选和 unknown 继续失败关闭。
- 宿主 `./scripts/check-linux-fcitx5.sh` 与 `git diff --check` 通过；Debian ARM64 `--require-fcitx` 通过真实 addon 构建、ELF/`$ORIGIN`、staged runtime probe、默认产品 evidence 字符串排除和六项 CTest。evidence unit 已停止，guest 恢复未改动的默认 production transient service；私有 evidence stage 继续保留。
- 同一 guest 使用既有 Flutter 3.44.0 与 Cargo 工具链重跑 `check-manager-linux-product.sh`，Release bundle、固定 `.so`、ELF、正式 ABI symbol、native-rime ignored FFI smoke 与 Dart FFI smoke 通过，未启动 Manager GUI。非交互 SSH 首次因 PATH 不含既有 Flutter 而在构建前停止，补入锁定路径后通过；未修改依赖清单或安装新的系统工具链。
- 宿主 `./scripts/check-docs.sh` 通过并仅保留既有 508 行预算提醒；原始 `./scripts/check-repo.sh` 以退出码 0 输出 `Repository baseline passed.`。两次为压缩输出临时增加管道的诊断运行因不同沙盒规则把 product-install 文件锁拒绝为 `Io`，不作为门禁结果；直接 `cargo test -p radishlex-ime-product-install --lib` 28/28 与原始仓库入口均通过。
- 下一步是生产分类后的 Firefox 学习、Manager 刷新与 privacy 零增量，再进入删除/恢复、导入导出、explain、双进程并发和重启。不进入 M5-P05，不清理旧资产，不推送远端、不创建 tag/Release，也不并行 Android、Windows 或 iOS。

## 2026-08-04：Firefox 同库学习与 Linux Manager 实机

- 精确 `firefox-esr -> browser` 规则以默认关闭取证的产品 addon 在私有用户级 stage 运行；addon、同版 FFI 和 RimeData 保持私有权限，Fcitx transient unit 继续使用 `UMask=0077`、`NoNewPrivileges=yes` 和仅 `AF_UNIX/AF_NETLINK` 的边界。默认二进制不包含受控 evidence 字符串，旧 P03 资产与脏工作副本未清理或覆盖。
- 使用公开合成 code `ba`，初始候选顺序为“吧、把、八、爸、巴”。用户实体选择第二候选“把”后，userdb v9 的 active user term、selection event 和 ranker weight 各为 1，其余 suppressed、deleted、negative 和 import 聚合保持 0；再次输入 `ba` 时“把”提升到首位，Escape 取消后计数未重复增加。
- 从 staged ARM64 Release bundle 启动 product Manager，fixture 标识未出现；页头刷新读取与 addon 相同的 XDG userdb，学习摘要显示 user term 1、selection event 1、suppressed 0、deleted 0。首次界面暴露 Dart bridge 固定按 `general` 查询导致 explain 只有 user=1.00，与输入侧 `browser` 排序不一致。
- Manager bridge 改为对最多六个可见词条查询 `general`、`browser`、`chat`、`code`、`editor`、`office`，只保留存在 frequency、recency、context 或 negative 信号的类别，无信号时回退 `general`。筛选、行 identity、详情和 chip 均显示 context；实机刷新后“把”的 `browser` explain 约为 1.79，包含 user=1.000、frequency≈0.243、recency≈0.249 和 context=0.300，与候选提升一致，同时未开放 context 分布或 P1 原始事件。
- Linux 设置页原先写死 macOS 隐私偏好的说明已改为“当前平台输入法隐私设置”。实机还暴露 Flutter 默认字体选择不稳定：无显式 fallback 时中文缺字，单独优先 CJK 字体时 Latin/数字出现方框；最终主题按 Latin 通用字体到简体中文字体的顺序声明 fallback，中文、英文状态码和数字同时恢复可读。该 staged product 仍依赖 guest 已有系统字体，M5-P05 必须固定随包字体或发行依赖。
- 宿主在最终字体顺序下通过 `flutter analyze` 及设置/学习相关 16 项测试；Debian ARM64 再次通过 `check-manager-linux-product.sh` 的 Release bundle、ELF、ABI symbol、native-rime ignored smoke 与 Dart FFI smoke。日终进一步通过 Manager 全量 analyze/99 项测试、macOS Release product bundle/真实 FFI smoke、文档与文本检查及 `./scripts/check-repo.sh`；只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。
- 本次只闭合学习、刷新、排序、context explain 和界面可读性。Manager privacy 开启保存后的 addon 零增量、关闭后单次恢复学习，以及删除/恢复、导入导出、双进程并发和重启均未执行，不宣称 M5-P04 完成；下次从 privacy 写入读回开始，不进入 M5-P05、不推送远端、不创建 tag/Release。

## 2026-08-05：privacy、Space 学习与删除恢复实机

- 工作从宿主 `dev` 的干净 `ea963ae` 开始，未推送远端。guest 使用独立 M5-P04 工作副本；构建源码与宿主逐项一致，旧工作副本、P03/P04 staging、backup 和既有 userdb 均未 reset、覆盖或清理。
- Manager 将 `privacy-mode.json` 写为严格 format v1、`0600`、owner 正确且无 hardlink 的 `true`；Firefox 以公开合成 `ba / 把` 完成一次实体选择后，selection 与 frequency 均保持 1。Manager 刷新仍显示 user terms 1、selection events 1、suppressed 0、deleted 0。写回 `false` 后文件身份与权限不变。
- privacy 关闭后的首次 Space 确实提交“把”，但 selection/frequency 没有增加。根因是 addon 在 Fcitx candidate list 前要求原始 `key.states().toInteger() == 0`；GTK/Fcitx 合法锁定或内部投递状态会绕过 display-index selection，按键随后走 engine commit，文本可提交但不产生选择学习。修复把候选键识别抽成内部 helper，使用 Fcitx `Key::check` / `digitSelection`，明确允许 CapsLock、NumLock、Handled/Ignored、Virtual、Repeat，拒绝 Shift/Ctrl/Alt/Hyper/Super/Meta，并增加真实 Fcitx candidate-key contract 和禁止恢复原始零状态门禁的静态检查。
- Debian ARM64 修复版通过 staged install、ELF/`$ORIGIN`、loader probe、默认 evidence 字符串排除和 7/7 CTest。最初从 SSH 重启 daemon 时漏带桌面用户管理器中的 `FCITX_ADDON_DIRS`，profile 仍能显示 `radishlex`，但 addon 未映射、按键直接透传；userdb 保持不变。随后改由继承完整桌面环境的 user transient service 运行，日志确认加载修复 addon，实际映射 SHA-256 为 `22d5486f0de473fe6ddd6dda28d28b58d05c907247aa54e3c3d540b20f68fbfb`。
- 修复后同一 Firefox Space 选择使 selection `1 -> 2`、frequency `1 -> 2`，其他聚合不变，`browser` explain 的 frequency/recency/context 恢复且 addon 无投影错误。Manager 刷新观察到同一聚合。
- Manager 删除使 active/ranker `1/1 -> 0/0`、tombstone `0 -> 1`、negative `0 -> 1`，selection 保持 2。既有 Fcitx session 以数字键 5 选择“把”后 selection `2 -> 3`，term/ranker/tombstone 和删除版本不变；重启 Fcitx 后新 session 再次选择使 selection `3 -> 4`，仍不能复活。
- Manager explicit restore 使 active `0 -> 1`、tombstone `1 -> 0`，ranker 保持 0；restore version 严格晚于 delete version，旧 frequency 未复活。再次重启 Fcitx 后“把”恢复首候选，Space 选择使 selection `4 -> 5`、ranker `0 -> 1`、frequency 从 1 重新开始。Manager 终态刷新为 user terms 1、selection events 5、suppressed 0、deleted 0。
- Manager 与 Fcitx 全程同时持有同一 XDG userdb，多次 Fcitx 重启后没有 `SQLITE_BUSY`、migration 漂移、重复学习或投影错误。该子批截至此处已闭合 privacy、删除、既有/新 session 防复活、explicit restore 与恢复后学习；真实导入导出和重启验收留给同日后续子批，尚不在此处宣称 M5-P04 完成。
- 最终源码在宿主与 guest 逐项对象哈希一致；guest 以该源码通过 `check-linux-fcitx5.sh --require-fcitx` 的 staged install、ARM64 ELF、`$ORIGIN`、loader probe、默认 evidence 排除和 7/7 CTest。宿主通过 Linux platform、文档、文本、diff 门禁及完整 `./scripts/check-repo.sh`，最终输出 `Repository baseline passed.`；文档检查只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。

## 2026-08-05：Manager 导入导出与重启实机

- 本子批从宿主 `dev` 的干净 `108855a` 开始，较 `origin/dev` ahead 3 且未推送。继续使用同一 Debian ARM64 guest、XDG userdb、私有 staging、backup 和临时服务；没有 reset、覆盖或清理既有资产。
- Manager 对两条公开合成 TSV 执行 dry run，界面显示 `2 / 2`、新增 2、更新 0、跳过 0；前后逻辑指纹均为 `eba6efe713a05ab708df83c69fe878a969819514360e616b314a397150df9894`，active 1、selection 5、import batches 0 等计数不变，证明检查不落库。
- 关闭 dry run 后普通导入显示 `2 / 2`、新增 2、更新 0、跳过 0，新增“巴”和“爸”。随后只删除“爸”，再以普通导入重放同一文件，结果为 `1 / 2`、新增 0、更新 1、跳过 1；第二批 `term_count=1`、`updated=1`、`skipped_deleted=1`，“爸”继续保持 tombstone、user weight 0 且无 ranker，普通导入不能复活删除。
- 显式导出生成 `dictionary.user_terms.v1` 的两条 P2 词条视图，只含 active 的“巴”和“把”；严格 UTF-8、固定 header、末尾换行，130 bytes，SHA-256 为 `5fcb8b0fb998723f3b5269d37bc2e0d207a0a35b424bc0e19b3bd61f45e00aa7`。文件不含“爸”、tombstone、selection/negative 明细、session、context 或时间字段，导出前后数据库计数不变。
- 仍在运行的 Fcitx `PID 15746` 无需重启即可把导入词“巴”排到第一、“把”排到第二；底层引擎仍可把“爸”列为第五候选，但数据库保持 deleted 且无 ranker。用户以 Escape 取消，selection 保持 5。Manager 随后从 `PID 2910` 重启为 `PID 23378`，Fcitx PID 未变；词库、tombstone、两批导入、学习摘要和 rank explain 全部刷新一致。
- 用户保存工作并手动注销 X11 session `2`，再登录同一账号形成新 session `154`。旧 Manager/Fcitx/Firefox transient units 均按 `Restart=no` 退出；新会话先自动启动默认 Fcitx，再从原 staging 建立 `UMask=0077`、`NoNewPrivileges=yes`、仅 `AF_UNIX/AF_NETLINK` 的验收单元。新 Fcitx `PID 25329` 实际映射 addon SHA-256 `22d5486f0de473fe6ddd6dda28d28b58d05c907247aa54e3c3d540b20f68fbfb` 与同版 FFI，新 Manager `PID 25410` 从固定 Release bundle 映射相同 FFI；离线 Firefox fixture 为 `PID 25411`。
- 桌面冷启动后，Fcitx 候选仍为“巴”第一、“把”第二，Escape 后 selection 仍为 5；Manager 刷新显示 user terms 2、selection events 5、suppressed 0、deleted 1、import batches 2、negative feedback 2、ranker weights 1。数据库保持 schema v9/WAL、`integrity_check=ok`，日志只有注销时预期的 XCB/DBus 断开、新 X11 会话的 Wayland 不可用与一次 XIM 重试，随后 addon 正常加载；没有 busy、migration、projection、FFI、崩溃、丢失或重复学习错误。
- M5-P04 的真实 Manager I/O、同库并发与进程/桌面会话重启矩阵据此闭合。guest staging、backup、userdb、公开合成输入/导出和临时服务终态全部保留；不进入 M5-P05，不清理旧资产，不推送远端、不创建 tag/Release。
- 收口文档完成后，`AGENTS.md` 与 `CLAUDE.md` 保持逐字一致；`./scripts/check-docs.sh`、`./scripts/check-text-files.sh` 与 `git diff --check` 通过，文档检查只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。完整 `./scripts/check-repo.sh` 以退出码 0 输出 `Repository baseline passed.`；本子批未改代码，guest 继续运行同一 `108855a` 已通过的 ARM64 addon/Manager 产品构建。

## 2026-08-06：M5-P05 安装维护前置设计

- 工作从干净 `dev` 的 `27c3bd2` 开始，较 `origin/dev` ahead 4 且未推送；未执行 reset、覆盖或远端动作。先完整复核协作入口、current、roadmap、Linux Fcitx/Manager 边界、repository layout 与本周证据，M5-P04 的实机矩阵没有重复运行。
- 新增 `docs/linux-installation-maintenance-boundary.md`，固定首个完整载体为 Debian 13 ARM64 的单一系统级本地 `.deb`，distribution identity 为 `debian-local-deb-v1`。完整用户级程序安装继续关闭；P02/P04 的 `FCITX_ADDON_DIRS`、用户 staging、autostart 副本和 transient service 保持开发/验收身份。
- CJK/Latin 文本字体选择发行版 hard dependency 而非随包资产：Debian profile 固定 `fonts-dejavu-core` 与 `fonts-noto-cjk`，既覆盖 Manager 的 Latin/数字/中文，也覆盖 Fcitx 原生 panel 的系统字体栈；后续实现审计确认 Flutter `Icons.*` 会生成固定 Material Icons 图标字形例外，文本字体仍不随包且不直接刷新 font cache。
- 系统 layout 绑定 multiarch Manager bundle、Fcitx addon、两份字节相同但独立 inode 的 FFI、完整 RimeData/source/license、desktop entry、icons、Linux product manifest 和 root-owned receipt/operation staging。系统文件固定 root owner、严格 mode、单 link/无 symlink；用户数据继续使用现有 XDG `0700`/`0600` 边界。
- install、upgrade、repair、remove 与 rollback 固定显式 source/target、本地 package artifact、quiescence、guard、幂等 receipt、dpkg 状态映射、完整复验和失败关闭。首批升降级只接受 ABI v9、schema v9、XDG/settings/privacy/Rime contract 完全相同的 release pair；所有 operation 默认对用户 XDG 零写入，remove/purge 不遍历 home。
- `packaging/linux/` 归属 P05A metadata/rootfs assembly；`platforms/linux-product/` 只在 P05B 承担 dpkg adapter、receipt/guard、恢复和 startup decision。现有 macOS `.app`/rename 型 `ime-product-install` 不直接冒充 Linux package transaction，只有去除路径假设且保持 M4 回归后才允许提取通用原则。
- P05A 首批只在临时 `DESTDIR` 生成 metadata/rootfs，并建立版本、字体依赖、layout、ELF、双 FFI、RimeData/license、权限和 link 负向门禁，不生成安装成功事实。P05C 必须使用 P04 guest 的独立 clone/snapshot 或另一台 guest；依赖安装、dpkg、服务/会话、输入法配置与故障注入仍逐项授权。
- current、roadmap、repository layout、Linux Fcitx5/Manager/Manager UI 边界、平台 README 与协作入口已同步。P04 guest staging、backup、userdb、导入导出文件和临时服务未读取正文、未清理、未改写；本批没有创建 `packaging/linux/`、安装代码、package、tag 或 Release。
- `./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与宿主 `./scripts/check-linux-fcitx5.sh` 均通过；Linux 门禁按预期只验证平台无关合同，没有在 macOS 上冒充真实 addon 构建。完整 `./scripts/check-repo.sh` 以退出码 0 输出 `Repository baseline passed.`；文档检查只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。

## 2026-08-06：M5-P05A metadata 与 rootfs 实现

- 工作从干净 `dev` 的 `facb6e2` 开始，较 `origin/dev` ahead 5 且未推送；本批只修改仓库源码和临时目录，没有运行 dpkg、写系统目录、启动服务/GUI、改动 Fcitx 配置或读取/清理 P04 guest 资产。
- 新增 `packaging/linux/` format v1 产品镜像与 `debian-system-v1` layout，固定 `26.7.1+38-1`、Debian 13 ARM64、`aarch64-linux-gnu`、`debian-local-deb-v1`、FFI ABI/userdb schema v9、settings/privacy v1、RimeData lock SHA-256、字体 hard dependencies、desktop/icon 和 programs-only remove 语义。control 仍只是确定性模板，没有生成 `.deb`。
- 元数据校验器交叉绑定根 `version.json`、Manager pubspec/application ID、Rust/C/Dart ABI、userdb schema、Linux privacy/settings、字体 fallback、Fcitx metadata、RimeData source/license 和仓库 LICENSE；未知字段、版本派生、multiarch、依赖、layout、desktop shell indirection 与外部 SVG 资源全部失败关闭。
- rootfs 装配器只接受 canonical、无 symlink/hardlink、无 group/other write 的显式 Manager bundle 与 addon stage，离线装配完整 RimeData/source/license，生成 canonical product manifest，并复验完整 inventory、mode、预期 root owner、禁止目标、双 FFI 同 hash/不同 inode、metadata、desktop 与字体资产。Flutter `MaterialIcons-Regular.otf` 只作为固定图标字形例外，必须由精确 `FontManifest.json` 和非空 `NOTICES.Z` 绑定；其他字体和 fontconfig payload 均拒绝。
- Fcitx CMake 保留默认 addon-relative `staged` profile，并新增产品 `system` profile；后者从 metadata 注入产品版本、固定 `/usr/share/radishlex/rime`，且 addon stage 不复制三份 sibling RimeData。`check-linux-fcitx5.sh` 同时覆盖 staged/system runtime contract，Docker 入口增加 system-profile addon stage，但本批未获授权启动 Docker 或真实 guest 构建。
- `check-linux-product-metadata.sh` 当前通过 9 项 Linux metadata 与 7 项既有 RimeData 正反向测试；`check-linux-product-layout.sh` 当前通过 13 项 rootfs 场景，其中 extended-attribute 负向测试因 macOS Python 不提供 `os.listxattr` 跳过，目标 Linux 会强制执行。真实载荷模式已实现 ARM64 ELF、RPATH/closure、ABI symbol、构建路径、双 FFI 与 Debian fontconfig 门禁，但尚未接收真实 Manager/addon 输入，因此 P05A 不宣称退出。
- current、roadmap、repository layout、Linux 安装/Fcitx/Manager/Manager UI 边界、平台 README 与协作入口已按实现同步。后续先取得隔离 Debian 13 ARM64 构建授权并通过真实载荷门禁，再进入 P05B 设计；package transaction、startup gate、系统安装、远端推送、tag/Release 和其他平台继续关闭。
- 最终 `./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check`、宿主 staged/system Fcitx contract 和完整 `./scripts/check-repo.sh` 均通过，仓库入口输出 `Repository baseline passed.`；只保留既有 508 行文档提醒和 macOS 宿主无法执行 Linux xattr 负向场景的单项 skip。一次嵌套快速调用因沙盒文件锁返回 `Io`，随后直达 `check-product-install-core.sh` 28/28 与完整原始仓库入口均通过，不是代码回归。
