# 2026-W34 开发周志

本文记录 2026-W34 的阶段推进、真实验证证据和停止线，供维护者复核 M5-P05B 决策。本文不替代当前状态、Linux 安装维护边界或 L6 runbook，不保存 raw operation ID、用户输入、密钥或可直接执行系统 mutation 的授权。

## 2026-08-17：真实 reinstall_target 闭合

- 工作从 clean `dev` 的 `c5b6631` 开始；HEAD 与上一轮一致，但 `origin/dev` 已从 ahead 2 变为完全对齐。确认没有内容差异后继续，未 pull、reset、覆盖、推送或修改远端。
- 启动前完整复验 reinstall preflight host manifest `a98dbec6…5006a`、clone关机盘 `db1e59ae…13b90`/`d9bd0aa3…97463`/`cdf0ee06…72dd`、`Network=[]`、十七台VM全停与reinstall/remove/rollback v3/S3四盘零句柄。默认sandbox内plain `utmctl start`返回0但VM仍stopped；获系统边界授权后用同一plain命令启动唯一clone `E671DB9C-5E2C-447B-9425-8D91D2CFD465`，其余十六台保持stopped。
- guest-agent stdin/file调用在默认sandbox内返回134且没有可用文件，未据此判定网络或脚本结果，也未重跑maintenance。受限授权下改用plain `utmctl file push/pull`，再以文件回读验证。运行态network evidence `11c00403…be2`固定boot `3e278249…623d`、仅`lo`、IPv4/IPv6 main route为空、package absent、remove receipt chain 4、guard/tmp和产品/dpkg进程静止。
- 本地控制脚本先通过`sh -n`、size/hash审查，再以单一transfer tar `a5b1ad35…a656`传入。transfer gate `db9b3c59…3e50`把mutation preflight、one-shot、postflight、startup probe与removed-payload inventory归一化为root-owned `0600` single-link regular files；此时未创建新operation ID，未执行maintenance/acceptance或dpkg mutation。
- 正式mutation preflight `fca225a7…3a01`的terminal为`passed/postflight/reason=none`，重新闭合canonical pair `cda70afa…659b`、target package/evidence `b211d940…d09c`/`2a1132c6…0e1b`、28项payload/product tree/dpkg info absent、remove receipt `770a27b7…b40e`、20项依赖、字体、`RemovedProgram` startup、XDG `f3df287f…b86b`、WAL/SHM/profile absence、进程/映射和断网。通过前operation count仍为4，没有新ID或mutation。
- one-shot在最后一次boot/断网/receipt/artifact复验后才生成唯一32 hex operation ID，宿主只保存SHA-256 `fafb05ad…79c8`。production maintenance `b060c240…7d81`只调用一次，参数为`kind=install`与target package/evidence，对应matrix `reinstall_target`；exit 0、stderr空、stdout精确为`maintenance_outcome=completed`，没有retry、resume、acceptance或手工dpkg。
- 新receipt `3eb44171…e274c`、3,926 bytes，为`install/not_applicable/completed`、chain 5、staged target only、initial package not-installed、target proof installed、failure null、manual recovery false。package为`26.7.1+38-2`，dpkg status `c09365b3…cece`；log为`906fba41…560c`/887,476 bytes，1,512-byte delta `b0338d25…116a`只记录absent→target install/configure/installed及发行版triggers。target MD5 inventory `bc36eff3…71f1`的28项全部通过。
- strict postflight `b4bf57ba…09f93`再次闭合target manifest `9f08c7fb…0196`、双FFI `f51dc0f1…0d3`同hash/不同inode、依赖/字体、Manager/Fcitx startup `0:1:2:2:6`、negative component、XDG零漂移、进程/映射与loopback-only。未启动Manager/Fcitx或桌面会话，未运行acceptance，未写用户XDG。
- deterministic guest archive `4ea296fa…fd3ba`为81,920 bytes、19项，内部18项manifest全过，并明确排除raw operation ID、raw receipt/status与完整dpkg log。host evidence目录`RadishLex-L6-Reinstall-80e49ce`从absent staging原子发布，`files.sha256`为`54f1e952…6685`并覆盖其余34项，逐项hash全过。
- 正常request关机后clone config/EFI/qcow2为`db1e59ae…13b90`/`76753750…a1c3`/`feb2ba7e…aaa5`，qcow2两次复算一致。remove、rollback v3与S3三项未漂移，四盘零句柄，plain `utmctl`枚举十七台全部stopped。host freeze工具的完成通知早于final目录可见；诊断重跑在第一条`final-exists`断言失败，未改写已发布目录、guest或transaction。

## 当前判断与停止线

- source install、upgrade、repair、rollback、remove、reinstall六类真实operation均已有独立证据，但repair位于独立S3 clone，不能把这些分散terminal冒充同一连续L6 session。
- reinstall clone现为冻结terminal；与全部failure/mismatch/rolled-back、repair/rollback/remove terminal、S3、handoff和host evidence一起原样保留，不resume、重试、再次调用、启动、恢复、清理或复用。
- 下一主线是八个crash/retry实机证据，先准备`install_prepared`的独立clone/snapshot、双授权、断网、脱敏operation identity与恢复终态。未获新的逐case授权前不创建clone、不启动VM、不运行acceptance/maintenance或dpkg。
- 连续完整L6、guest reboot、dynamic preload/错误sibling、外部package lifecycle、P05C、桌面启动/重启、清理、推送、tag/Release、公开发布、真实同步及其他平台继续关闭。

## 2026-08-17：首个 crash checkpoint 失败关闭与 startup 根因修复

- 从clean `3831628`、第六套pair与DependencyFrozen absent盘开始。第一台clone `C0D96C0A-EC5C-4809-86A0-733C4523A1F8`的config虽为`Network=[]`，UTM注册态仍继承网卡；在input与operation ID前停止，只保留失败资产。第二台clone `FD24ADFF-B160-46F0-B100-10BAAF17C056`从已验证无网卡的registered terminal取得注册身份，再原子替换absent EFI/qcow2；运行态仅`lo`、双main route为空，package/state/XDG与产品进程absent。
- v2输入tar `0550fc74…6ef`往返同hash；mutation preflight `0deb6537…66dc`通过。guest随后生成唯一operation ID，宿主只保存SHA-256 `9429b171…9145a`；acceptance controller只调用一次，形成`install_prepared` checkpoint并证明完整process group已`SIGKILL`、无dpkg child。此时receipt为`install/not_applicable/prepared`、guard与staged target pair存在，但未调用dpkg。
- 首次crash-state检查在startup probe处失败：`/run`挂载为`noexec`，同hash FFI无法映射。保留原始result/stderr后，只把root-owned `0600` single-link FFI复制到`/var/tmp`并以symlink供探针读取；relocation evidence `b373f7c0…f293`明确product state未改变、transaction未重试。第二次检查仍失败，实际Manager/Fcitx tuple均为`0:1:4:11:0|error-absent`。
- 独立诊断确认guard为`root:root 0600`、零长度、single-link regular file，父目录是canonical `root:root 01777 /run/lock`。store自`f0415ad`起已按合同接受该Debian共享目录，但startup只读validator仍拒绝world-write，因而误报`FailedClosed/GuardInvalid`而非预期`MaintenanceRequired/ActiveGuard`。该差异发生在resume前，故没有production resume、maintenance retry、dpkg mutation或postflight，也没有补写crash-state成功证据。
- 正常request关机后v2 config/EFI/qcow2恢复启动前`cfb5d343…1f2d`/`0b797641…1418`/`4967234b…4b18`且零句柄，plain `utmctl`枚举十九台全部stopped。host目录`RadishLex-L6-Crash-Install-Prepared-80e49ce-Failed-Closed`以`0700`原子冻结；manifest `5d8c914a…3e42a`逐项通过，summary `dab8cde5…1905`不含raw operation ID。
- `a6622d4`新增共享父目录权限纯策略，让store与startup同时消费；startup回归覆盖接受`0755`/`0775`/精确`01777`并拒绝`0777`/`01703`/`01733`/`01757`，保持只读且不触达package/component observer。Linux product默认119项、L6-feature 124项、acceptance 10项、clippy与controller合同门禁通过。
- 该case只证明checkpoint触发、证据脱敏和缺陷下的失败关闭，不计crash/retry通过。旧pair、两台clone与全部证据不得resume、热替换、清理或复用；下一步从新clean target重建相邻revision pair，再经独立授权用新clone从头执行`install_prepared`。

## 2026-08-17：冻结 startup-lock 修复后的 ARM64 release pair

- 从clean `d75818f`、十九台VM全停、旧handoff source package/evidence `09ed1228…bec`/`fe3d6297…cf94`和builder预启动盘`1f509c73…d4f`/`04a67b0f…02a`/`e903b56d…499`开始。完整dev bundle `a03ac739…b901`含source祖先与target HEAD；source按五个固定块传入并在guest重组，同hash、size、mode与single-link逐项通过。
- 只启动2223隔离builder。第一项有效guest动作关闭`enp0s1`，回读evidence `7824cd8e…8bd5`证明ARM64、无IPv4/IPv6地址且双main route为空；package/state absent、dpkg audit为空，新run路径absent。prepare evidence `64eb1869…b610`闭合clean target `d75818f`、source anchor与Cargo 8687/pub 11064/Flutter 1514项稳定cache。
- production build只执行一次；user/network namespace内uid/gid为`1000:1000`、loopback down、零路由且`CARGO_NET_OFFLINE=true`。Flutter `pub get`一度约九分钟处于futex sleep，只做`/proc`诊断且未启网、删锁、发信号或重跑，随后自然完成。build status为0，evidence `64eb5157…940e`；record `c74fac12…9849`，target package/evidence `4dd00540…dcec`/`9e646c86…3f17`，production/acceptance ELF `422a5080…7f3`/`5ca804e6…c7a6`。
- 第一次postverify包装因root读取luobo-owned Git而在verifier前失败；v2把直接clean检查切为luobo，但Python verifier内部Git仍被safe-directory拒绝，独立status 1/log `81ac7485…72b`保留，双Rust输出仍absent。v3让Python verifier整体以luobo只读运行，status 0、evidence `c8d8495b…cf2`，source/target production Rust均返回`artifact_verification=ok`；没有重建pair、安装package或执行transaction。
- canonical USTAR为85,401,600 bytes、SHA-256 `bbc96507…b4db`，11块逐hash回读宿主。临时解包、hidden incoming与正式目录均通过当前仓库Python verifier、8文件owner/mode/link/hash和零symlink/xattr检查；最终复用仓库Darwin `renameatx_np(RENAME_EXCL)`实现从absent incoming原子发布`RadishLex-L6-Handoff-d75818f/release-pair-d75818f`，旧`80e49ce` record/source未漂移。
- builder final postflight `88ee0383…3e81`再次证明record/archive、clean target、三套cache、package/state、dpkg audit、进程与断网不漂移。正常stop后config/EFI/qcow2为`1f509c73…d4f`/`ecaefd2a…0699`/`84314201…573d`，qcow2两次一致且零句柄，十九台VM全部stopped。未创建transaction clone、operation ID，未运行maintenance/acceptance/dpkg，未写用户XDG。

## 当前判断与停止线（新 pair 冻结后）

- 新handoff只作为后续独立clone的冻结输入，不热替换旧clone或跨pair混搭；旧pair、两台crash clone及全部terminal/evidence继续原样保留。
- 下一步须另行授权从未改写DependencyFrozen absent盘建立新clone；先闭合注册身份、运行态断网、input switch和只读/mutation preflight，通过前不生成operation ID或运行acceptance。随后再单独授权一次`install_prepared`，通过后才进入下一checkpoint。
- 连续完整L6、其余crash/retry、guest reboot、dynamic preload/错误sibling、外部package lifecycle、P05C、桌面启动/清理、推送、tag/Release、公开发布、真实同步与其他平台继续关闭。

## 2026-08-17：收尾提交回顾与文档复核

- 今天收尾前共有四个提交：`3831628`冻结真实reinstall终态，`a6622d4`统一startup/store共享锁父目录策略，`d75818f`记录首个crash checkpoint失败关闭，`700e982`冻结修复target的ARM64 release pair。唯一production代码变化是`a6622d4`；其接受精确`01777`且拒绝其他world-writable mode的权限矩阵，已经由Linux product默认/L6 feature测试、clippy与controller门禁覆盖。
- 复核阶段入口、路线图、安装维护边界、L6 runbook、仓库布局与三个Linux模块README后，确认`current`、`roadmap`、安装边界、runbook和治理文件已准确记录新pair与停止线；同时修正Fcitx5边界、仓库布局、package/product/acceptance README中仍停留在“真实reinstall未完成”或未记录首个crash失败关闭的活跃描述。根README继续只索引当前状态，technical plan、privacy/sync、license与其他平台架构没有因本次局部filesystem policy修复发生变化，无需制造重复阶段流水。
- 收尾时Git工作区以`dev` clean、相对`origin/dev` ahead 4开始；plain `utmctl list`确认十九台已注册VM全部stopped，因此没有再发停止、启动、clone、guest命令，也没有触碰任一冻结磁盘、handoff、evidence或系统状态。

## 2026-08-18：明日事项

1. 开始时先只读复核Git clean、十九台VM全停、旧pair/两台crash clone/全部terminal与evidence未漂移，以及新handoff record `c74fac12…9849`、source package/evidence `09ed1228…bec`/`fe3d6297…cf94`、target package/evidence `4dd00540…dcec`/`9e646c86…3f17`仍逐项匹配；不把host目录存在或guest-agent空输出替代identity证据。
2. 第一项系统动作另行授权后，只从未改写DependencyFrozen absent盘建立新的`install_prepared`重试clone，闭合注册UUID、config/EFI/qcow2与`Network=[]`；该步不启动VM、不传输入、不生成operation ID，也不运行acceptance/maintenance/dpkg。
3. 启动动作再次单独授权。启动前确认其余十八台stopped；启动后第一项有效guest动作关闭非loopback接口，并用文件回读证明仅`lo`、IPv4/IPv6 main route为空。断网证据通过前不传入pair或控制脚本。
4. 只使用`RadishLex-L6-Handoff-d75818f`冻结输入，逐项闭合input switch、package/state/XDG absent、依赖/字体、进程静止、read-only preflight与mutation preflight；任何identity、owner/mode/link/hash、startup或网络差异都失败关闭并保留现场，通过前不生成operation ID或运行controller。
5. mutation preflight通过后，再取得一次独立授权，只执行一次production acceptance controller的`install_prepared` checkpoint。预期checkpoint已终止完整process group、无`dpkg` child，receipt停在prepared，Manager/Fcitx只读startup语义为`MaintenanceRequired + ActiveGuard`；若仍出现`GuardInvalid`或其他差异，不resume、不热修、不调用dpkg，冻结失败证据后停止。
6. checkpoint符合预期时，只形成脱敏crash-state与host manifest，正常request关机；复算config/EFI/qcow2、qcow2两次一致、相关冻结盘零句柄并确认十九台VM全部stopped。当天不自动进入resume或下一checkpoint，后续恢复动作继续按单case另行授权。
7. 连续完整L6、其余七个crash/retry、guest reboot、linked startup动态负向、外部package lifecycle、P05C、清理、推送、tag/Release、公开发布、真实同步与其他平台继续关闭。

## 2026-08-18：新 pair 首台 retry clone 在 input switch 前失败关闭

- 从clean `a14709e`、十九台VM全停和已复验的`d75818f` handoff开始；record `c74fac12…9849`、source package/evidence `09ed1228…bec`/`fe3d6297…cf94`、target package/evidence `4dd00540…dcec`/`9e646c86…3f17`及双production verifier均未漂移。新clone `45D8205F-64B5-4E20-A129-895A1A4CF2B4`从DependencyFrozen absent盘建立，config/EFI/qcow2与`Network=[]`通过；启动后其余十九台保持stopped，guest evidence `10e43d43…d79`证明仅`lo`且IPv4/IPv6 main route为空。
- input bundle为92,827,648 bytes、SHA-256 `f6200a49…ec9f`，绑定新pair八文件、case/startup、snapshot identity与只读startup FFI。第一次transfer因UTM push把archive建为`root:root 0666`而被精确`0600`gate拒绝；未input switch、preflight或transaction。修正版只接受该精确root-owned/single-link/size的push结果，立即收敛至`0600`并fsync后再验hash，不放宽最终合同。
- host重试先受sandbox内`utmctl list` abort影响，仅产生一枚零字节占位文件；证明为空后移除。提权重试的guest-agent返回0且stdout/stderr为空，但transfer evidence无法回读，未据此判成功。只读probe `4ebeb11c…f227`证明archive已归一化、正式input/run/output均absent；第二份probe `ecdd5540…0a89`证明无残留transfer进程，incoming已完整解包12文件/12目录且无软硬链接，operation ID、state root、checkpoint与guard仍全部absent。
- 精确根因为host harness的expected inventory literal未按其自身`sort`结果排列：`target-startup/...`应先于`target/artifacts/...`，实际diff保留为`c3cfb684…fc39c`对`4358c08f…3f9d5`。同时离线审查发现case预期`build-environment.evidence.json`，而release-pair、transfer gate和其他L6资产的合同名称均为`build-environment.json`；即使修复排序，原case也会在preflight失败。
- 本轮没有运行phase wrapper、case preflight、acceptance、maintenance或dpkg，没有生成raw/hashed operation ID，没有写用户XDG。当前clone仍是二十台VM中唯一started且保持离线；须先正常停止并原样冻结，不得在guest内改名、补文件、热替换脚本或原地重试。
- 下一步经授权先修正host harness：expected inventory canonical sort、统一`build-environment.json`，并把guest-agent返回码/stdout降为观察量，只以回读的result/evidence与正负向状态证明成功。之后从未改写DependencyFrozen absent盘另建clone，仅重跑注册/断网/input switch/只读与mutation preflight；通过前仍不生成operation ID或进入transaction。

## 2026-08-18：第二台 retry 在 fresh-absent startup 预期处失败关闭

- 先正常停止首台retry `45D8205F…F2B4`，固定关机前config/EFI/qcow2为`dcf723b8…84b`/`f762ee52…e76`/`c80cb26c…15d`，其作为input harness失败资产不再启动或复用。第二台`0BB24317-5FC7-4DD3-8572-F0AC282B307C`从未改写DependencyFrozen盘建立；snapshot `7a5384ba…536d`绑定config/EFI/qcow2 `4f69bf98…6e2a`/`0b797641…1418`/`4967234b…4b18`与`Network=[]`。
- 启动后其余二十台均stopped，network evidence `3c15dae7…9775`证明仅`lo`且IPv4/IPv6 main route为空。修正canonical inventory sort与`build-environment.json`后的bundle为92,827,648 bytes、`b98210ac…fa9f`；transfer只以guest result/evidence文件回读判定，通过evidence `2e7a4272…5100`闭合12项input、pair `c74fac12…9849`、snapshot与startup FFI identity。
- preflight phase可靠回读为exit 1，stderr精确是`install_prepared_case_error=manager-preflight-startup`；外层先报`pull-empty`只是因为失败路径的success stdout为空。独立只读startup诊断 `04ccacf0…56c7`得到Manager/Fcitx均为`0:1:4:15:0|error-absent`，即`FailedClosed/ReceiptMissing/no receipt`。
- 根因是临时case同时要求`/var/lib/radishlex/install-v1` absent，却沿用了remove terminal的`0:1:4:24:6`即`RemovedProgram/Completed`预期。源码`inspect_without_receipt`与既有回归均要求system product在package/receipt absent时返回`ReceiptMissing`，因此这是harness场景建模错误，不是`d75818f` startup修复回归；不得为过门禁修改产品语义。
- host failure manifest `66212f2c…a8a3e`绑定phase result `c900f442…a16f`、stderr `31df7e44…16d`、startup诊断与guest state `434541c5…75ae`。后者直接证明mutation preflight evidence、operation ID、state root、checkpoint、guard均absent，网络仍loopback-only；没有运行acceptance、maintenance、dpkg或transaction，也未写用户XDG。
- 当前二十一台VM中仅该clone离线started。它不得热修case、补写preflight evidence或原地重跑；下一步须先取得授权正常停止并冻结磁盘/零句柄，再把fresh absent与remove terminal拆成独立startup预期，从第三台clean absent clone重新闭合input与preflight。通过前继续不生成operation ID，`install_prepared`仍须另一笔独立授权。

## 2026-08-18：第二台 retry 关机冻结与 UTM 清理盘点

- 获得授权后正常停止`0BB24317…307C`。freeze `65157b95…cb2b`绑定failure manifest `66212f2c…a8a3e`、二十一台all-stopped清单、`Network=[]`、config/EFI/qcow2 `4f69bf98…6e2a`/`f762ee52…e76`/`b0ffd920…bbfe`、qcow2两次一致与零打开句柄；本轮未重启guest、补写preflight或进入transaction。
- 注册VM名义占用约210.57 GiB，Data卷仍有约577 GiB可用；APFS COW共享块使逐package `du`不能等同于删除后真实回收量，因此当前清理主要解决UTM界面拥挤。六个候选package均stopped且零句柄：第一批四台约36.30 GiB为旧network失败`C0D96C0A…23A1F8`、rollback transfer失败`BFB3EF09…2720`及两台新pair harness失败；第二批两台约18.90 GiB为repair `A3022255…3107` aborted-preserved与`BE3579E0…37F8` completed-noop。
- 旧network failure与rollback failure的持久`files.sha256`逐项通过；两台repair的bundle/archive hash与local evidence一致，后继`394217A7…B6FB`已形成真实repair completed。两台新pair retry的证据仍在`/private/tmp`任务根，删除前必须先复制到新的持久host evidence目录、生成自校验manifest并复验；当前只形成候选清单，不授权或执行任何删除。
- P04、CleanBase、DependencyFrozen、通用builder、PairBuilder、前四套L6、第五套rolled-back、第六套/S3、真实repair/rollback/remove/reinstall terminal、旧pair真实checkpoint失败现场继续保留。若清理第一批，UTM注册项可从21降到17；连同第二批可降到15，但后者因包含真实operation历史应晚于无transaction失败clone。

## 2026-08-18：第一批四台失败 clone 持久归档并删除

- 获得精确清理授权后，先把两台新pair retry的完整临时证据、关机freeze与最终`config.plist`复制到持久host root；source清单和最终`files.sha256`均在发布前后逐项通过。input harness与preflight harness归档manifest分别为`ae4d1da2…ab8d`和`d99cbfad…324c`，目录/文件统一为`0700`/`0600`。
- 删除前再次复验21台全部stopped、四个package UUID/Name精确匹配、四套持久证据manifest `5d8c914a…3e42a`/`40dab9b5…680e`/`ae4d1da2…ab8d`/`d99cbfad…324c`逐项通过且package零打开句柄。随后只对`C0D96C0A…23A1F8`、`BFB3EF09…2720`、`45D8205F…F2B4`、`0BB24317…307C`调用plain `utmctl delete`。
- cleanup manifest `e1930d6c…41f7c`绑定before/after清单与逐台结果：四个注册项及四个`.utm` package均absent，未授权注册项集合无差异，四套host evidence在删除后再次通过。UTM现为17台且全部stopped；Data卷仍约577 GiB可用，APFS共享块下不把名义36.30 GiB当作真实回收量。
- 第二批repair历史clone `A3022255…3107`与`BE3579E0…37F8`未触及；P04、CleanBase、DependencyFrozen、builder、PairBuilder、前四套L6、第五套rolled-back、第六套/S3、真实repair/rollback/remove/reinstall terminal及旧pair真实checkpoint现场均保持原样。

## 2026-08-18：今日提交回顾与文档复核

- 今日共形成五笔提交。`a14709e`是唯一代码/CI治理提交：新增始终运行的聚合`Candidate Quality` job，严格汇总`Repo Hygiene`、`Repository Baseline`、`Rust Clippy`、`Flutter Manager`与`Go Quality`五个组件；ruleset模板只绑定该稳定context，单人维护阶段审批数为0但仍要求会话解决。workflow、ruleset、ADR、ruleset README与`check-repo.py`逐项一致，聚合job对任何非`success`结果失败，未发现需要补修的实现缺口。
- `80f6351`、`49069e5`、`954f015`、`9013047`依次记录两台新pair retry的input/preflight harness失败、第二台关机冻结、清理盘点及首批四台clone持久归档/删除；这四笔没有修改production代码。最终事实是四个package absent、四套host evidence逐项通过、未授权注册项无差异、十七台VM全部stopped。
- 对照`current`、`roadmap`、technical plan、Linux安装边界、L6 runbook、Fcitx5边界和三个Linux模块README后，发现technical plan仍把“创建reinstall clone”写作下一步、Linux product README仍停留在118项测试，现已按真实reinstall terminal、119项默认/124项L6-feature测试和新pair harness停止线补正。路线图仍只承载里程碑与退出标准，根README、privacy/sync、license、架构/目录职责及其他平台边界没有变化，不追加重复流水。
- 两次新pair retry都在transaction前被host/guest case harness错误消耗，说明下一步不能只继续修改临时脚本并创建VM。明日先把canonical inventory、`build-environment.json`与fresh-absent/remove-terminal两套startup预期收敛为committed离线回归，再进入第三台clean clone；不为通过case修改正确的production `ReceiptMissing`语义。

## 2026-08-19：明日事项

1. 开始时只读确认Git clean、plain `utmctl list`为十七台且全部stopped；复验`d75818f` handoff record `c74fac12…9849`、source/target package与evidence，以及两套retry归档manifest `ae4d1da2…ab8d`/`d99cbfad…324c`和cleanup manifest `e1930d6c…41f7c`。已删除VM package或临时任务根不得再作为运行身份。
2. 先做宿主离线开发：优先扩展既有L6 contract/controller测试，固定canonical sort后的input inventory、唯一`build-environment.json`命名、guest-agent返回码只作观察量以及result/evidence文件回读真相源；fresh absent必须精确期待`FailedClosed/ReceiptMissing/no receipt`，remove terminal继续独立期待`FailedClosed/RemovedProgram/completed`。职责不适合现有模块时才拆浅层guest-case contract，不增加空转wrapper。
3. 为上述harness缺陷先写失败回归，再做最小修正；运行定向Python/Rust测试、Linux startup/L6/controller/release-pair门禁、clippy与完整`check-repo`。代码和合同通过并单独提交前，不创建或启动VM。
4. 取得新的系统动作授权后，只从未改写DependencyFrozen absent盘创建第三台`install_prepared` retry clone，固定注册UUID、config/EFI/qcow2 identity与`Network=[]`；创建批次不启动、不传input、不生成operation ID。
5. 启动须再次授权。启动前确认其余十六台stopped；启动后先关闭非loopback接口，并用文件回读证明仅`lo`且IPv4/IPv6 main route为空，再切入冻结handoff并闭合input switch、package/state/XDG absent、依赖/字体、进程静止、只读与mutation preflight。任一predicate失败即保留现场并停止，通过前不生成operation ID或运行controller。
6. mutation preflight通过后再单独授权一次`install_prepared` checkpoint。预期完整process group已被`SIGKILL`、无`dpkg` child、receipt停在prepared，Manager/Fcitx只读startup为`MaintenanceRequired + ActiveGuard`；任何其他tuple都不得resume、热修或调用dpkg。
7. checkpoint符合预期时只形成脱敏crash-state/host manifest并正常关机，复算磁盘、双重qcow2与零句柄。当天目标只闭合首个有效checkpoint，不自动resume或进入其余七个case；第二批repair清理、连续完整L6、P05C、发布、推送、真实同步与其他平台继续关闭。

## 2026-08-19：L6 guest-case 离线合同闭合

- 工作从clean `8a0311e`与17台VM全部stopped开始，只进行repository离线开发；没有创建/启动VM、传入guest input、生成operation ID，也没有运行acceptance/maintenance/dpkg或触碰用户XDG。现有`l6_contract.py`负责矩阵、`l6_controller_contract.py`负责编译隔离，故新增浅层`l6_guest_case_contract.py`承载宿主/guest case共同规则，没有把临时harness逻辑塞入production startup。
- 先写失败回归，分别复现模块缺失与matrix未声明fresh-absent probe。最小实现随后固定12项input inventory按UTF-8 bytes排序，保证`target-startup/...`先于`target/artifacts/...`；环境文件唯一为`build-environment.json`，unsafe/非ARM64 package basename失败关闭。matrix同步登记inventory order、environment filename与`result-and-evidence-file-readback-v1`信任策略。
- `GuestAgentObservation`明确不以observer exit/stdout/stderr判成功：即使返回0且输出为空，result/evidence任一缺失仍拒绝；即使observer非0，只要独立回读的两个文件均为非空、有界、UTF-8/LF canonical文本，才交给上层继续验证。startup expectation把fresh absent固定为`FailedClosed/ReceiptMissing/no receipt`与`0:1:4:15:0|error-absent`，remove terminal固定为`FailedClosed/RemovedProgram/completed`与`0:1:4:24:6|error-absent`，未修改已正确的production语义。
- 新合同validator与7项回归已接入`check-linux-l6-contract.sh`及`check-repo.py` required files。L6 contract的8项matrix、7项guest-case、4项controller回归通过；startup gate、Linux product L6-feature 124项、acceptance 10项、controller clippy、release-pair 21+6项及maintenance-refresh 20+6项门禁通过，完整`check-repo.sh`也通过。下一步形成独立提交，再申请只创建第三台clean absent clone的系统动作授权；创建批次仍不得启动。

## 2026-08-19：第三台 L6 retry 只完成 clone 与身份冻结

- 离线guest-case合同以`c5d1116`提交后取得精确clone-only授权。开始时Git clean且ahead 1，handoff record `c74fac12…9849`、source/target package与evidence、两套harness失败归档和cleanup manifest均只读复验通过；plain `utmctl list`为17台全部stopped，DependencyFrozen与reinstall注册壳三项磁盘身份及`Network=[]`未漂移、零句柄。
- 首次控制脚本在第一条`utmctl`前失败：固定调用系统`/usr/bin/python3` 3.9.6，而仓库verifier使用`zip(strict=True)`。失败证据`25880257…8b54`确认clone目录absent、17台仍全停。随后把运行时绑定为Python 3.14.5及其hash，静态扫描确认脚本唯一UTM mutation为一次plain `utmctl clone`，没有start/delete/exec/file/dpkg入口。
- clone-only成功创建UUID `3EC83EB9-094B-492B-9756-D47E61C593B9`、名称`RadishLex-Debian13-ARM64-L6-d75818f-crash-install-prepared-v3`。config/EFI/qcow2为`62040cc9…eb9`/`0b797641…418`/`4967234b…b18`，配置精确为`Network=[]`，qcow2独立复算一致且source/registration/clone文件零句柄；18台注册VM全部stopped。
- 宿主冻结器前两次分别因误从plist根读取Name/UUID、绑定Python不提供`os.listxattr`而在正式目录发布前失败关闭；第二次self-consistent hidden incoming完整保留到任务根，两次失败由`aa4bf549…3201`记录。最终改为读取`Information`并使用系统`xattr`，从absent incoming以Darwin `renameatx_np(RENAME_EXCL)`发布`RadishLex-L6-Crash-Install-Prepared-d75818f-v3-Clone-Prepared`；manifest `13f2e3e9…954a2`覆盖其余11项，目录/文件为`0700`/`0600`、单link、零xattr且逐项复验通过。
- 本批从未启动VM、传input、生成operation ID或运行acceptance/maintenance/dpkg，也未触碰guest/XDG。下一步必须另行授权启动；启动前确认其余17台stopped，启动后的第一项guest动作是关闭非loopback接口并以文件回读证明仅`lo`且IPv4/IPv6 main route为空。input与只读/mutation preflight通过前不生成operation ID，`install_prepared`仍须下一笔独立授权。

## 2026-08-19：第三台 L6 retry 启动并固定运行态断网

- 从clean `39f660e`、clone-only manifest `13f2e3e9…954a2`和plain `utmctl`十八台全stopped开始，取得仅启动/断网/文件回读授权。控制脚本在`utmctl start`前完整复验manifest、Git、Name/UUID/`Network=[]`、source/clone三项磁盘hash与零句柄；静态入口仅含list/status/start、一次内联网络切断命令与file pull，没有stop/clone/delete/file push、input、preflight、acceptance、maintenance或dpkg。
- 只启动`3EC83EB9…593B9`，其余十七台保持stopped。启动后的第一项guest命令关闭全部非loopback接口并原子发布root-owned `0600`单link证据；observer exit为0但stdout/stderr均空，未用它单独判成功。首次文件回读与独立第二次pull SHA-256均为`711f850c…b9d15`，固定Debian 13 ARM64、仅`lo`、loopback地址`127.0.0.1/8|::1/128`且IPv4/IPv6 main route为空。
- host network summary `cc09ec24…01b49`绑定启动前后清单、clone/source身份、observer诊断、boot/machine identity hash及guest evidence。持久目录`RadishLex-L6-Crash-Install-Prepared-d75818f-v3-Network-Ready`从absent incoming原子发布，manifest `4b7081d5…7291`覆盖其余14项；目录/文件为`0700`/`0600`、单link、零xattr且逐项复验通过。
- 当前该clone是唯一started VM，保持断网；没有传入handoff/input、生成operation ID或运行preflight/acceptance/maintenance/dpkg，也未触碰产品state或用户XDG。下一步须另行授权在同一boot再次回读网络证据，再投影canonical input并闭合fresh-absent只读/mutation preflight；`install_prepared`与停止仍须后续独立授权。

## 2026-08-19：第三台 L6 retry 闭合 canonical input 与 fresh-absent preflight

- 从clean `4da267d`、network manifest `4b7081d5…7291`和18台注册VM中仅`3EC83EB9…593B9` started开始；input写入前两次再次直接回读guest network evidence，均与`711f850c…b9d15`逐字节一致，boot/machine identity未变，仍只有`lo`且IPv4/IPv6 main route为空。全程没有并发启动其他VM，也没有停止当前clone。
- 只从冻结handoff `c74fac12…9849`重建12项UTF-8 bytewise canonical inventory；target startup FFI从冻结`38-2` actual `.deb`重新提取为`19314535…f27d`，未复用旧retry临时字节。case相对第二台失败版本只把Manager/Fcitx预期改为`0:1:4:15:0|error-absent`，把原因改为`ReceiptMissing/no receipt`，SHA-256为`ddfa7fac…c7e`；USTAR bundle为92,822,016 bytes、`7e52f445…dd7a`，header顺序、owner/mode、size和内容hash均固定。
- input控制先运行fresh-absent只读资格并形成guest identity `c9f680c7…59328`，再验证archive header和12项解包字节后原子切换；transfer evidence `d808589e…3b969`绑定pair、source/target package/evidence、acceptance/maintenance ELF、startup FFI、snapshot `5f4c08cd…cbe8`与canonical inventory。guest-agent exit/stdout只保留为观察量，case/environment/snapshot及result/evidence均另行文件回读。
- mutation preflight `24724002…f9807`复验package not-installed、state/checkpoint/guard absent、direct dpkg status/config/log、20项依赖、两类字体、XDG与进程静止；Manager/Fcitx均返回`FailedClosed/ReceiptMissing/no receipt`。独立负向probe `0ee3d34c…2fb0`再次运行两端startup并确认operation ID仍未生成。没有调用acceptance controller、maintenance CLI或`/usr/bin/dpkg`，没有写产品state或用户XDG。
- 事后网络回读仍为`711f850c…b9d15`，四份UTM清单均为目标started、其余十七台stopped。65项控制、guest-copy、result/evidence与空stderr从absent incoming原子发布到`RadishLex-L6-Crash-Install-Prepared-d75818f-v3-Input-Preflight-Ready`，manifest `0b703d10…5027`逐项通过，目录/文件为`0700`/`0600`、单link、零xattr。下一步须再单独授权一次`install_prepared` checkpoint；resume、dpkg后续、停止和其他case仍不在本批范围。

## 2026-08-19：修复 pair 的 `install_prepared` checkpoint 通过

- 从clean `633dc14`、input/preflight manifest `0b703d10…5027`及唯一started clone `3EC83EB9…593B9`开始；先逐项重验65项冻结manifest，再从guest重新回读network、mutation/negative preflight与phase wrapper。新readiness `41f5dc1f…0ae1`在同一boot再次固定仅`lo`、双main route为空、package/state/checkpoint/guard/operation ID absent、dpkg status/log未漂移、fresh startup仍为`ReceiptMissing`，其余十七台stopped。
- guest只生成一次32 hex operation ID，宿主仅保存SHA-256 `55af5046…1828`；production acceptance controller只调用一次`install_prepared`。checkpoint `3fd5df67…4a70`绑定target commit `d75818f`、guest/snapshot identity、双显式授权与preserve policy，确认prepared通知来自继承pipe，完整worker process group经`SIGKILL`终止、成员数0、无dpkg child且proc检查完整。
- 独立`inspect-crash`形成crash-state `e18bde6c…8719`：receipt `a75b9985…9286c`为2,863 bytes、`install/not_applicable/prepared/chain-1`，只含冻结source `38-1` staged target；guard为root-owned `0600`零长度单link且已解锁，dpkg status/log与preflight逐字节相同，证明尚未执行package mutation。Manager/Fcitx均为`0:1:3:10:0|error-absent`，即`MaintenanceRequired/ActiveGuard`；XDG、产品进程和网络未漂移。
- 首轮并行sandbox postverify的四个plain `utmctl`只读调用因host并发退出134并产生空文件；随后单次sandbox file pull仍受同一执行边界影响，未进入guest命令或改变现场。保留该负向观察后，用获准的严格串行plain `utmctl file pull/list`重取crash-state、checkpoint、network和18台清单，三份字节与原证据同hash且目标仍唯一started。任务根未发现独立32位十六进制secret。
- 72项控制、回读、空stderr与上述失败/成功postverify从absent incoming原子发布为`RadishLex-L6-Crash-Install-Prepared-d75818f-v3-Checkpoint-Prepared`；加manifest共73文件，均`0600`、单link、零xattr，manifest `a36b0cbc…8396`逐项通过。当前不resume、不运行dpkg后续、不停止VM；下一步须另行授权guest内既有operation的exact resume及terminal postflight/archive，任一差异不retry。

## 2026-08-19：`install_prepared` exact resume 到 source terminal

- 从clean `de36188`、prepared manifest `a36b0cbc…8396`与唯一started/断网clone `3EC83EB9…593B9`开始；先逐项复验73文件manifest并串行回读checkpoint `3fd5df67…4a70`、crash-state `e18bde6c…8719`、network `711f850c…b9d15`与十八台清单。resume readiness `b3859f90…553a`再次证明同一operation hash `55af5046…1828`、receipt prepared、guard合法已解锁、package absent、dpkg status/log未漂移、`ActiveGuard`、terminal输出absent及XDG/进程/断网稳定。
- canonical case `ddfa7fac…c7e`保持未改写；terminal派生入口 `d1b0c27f…7310`只把dispatcher从`preflight/crash/inspect-crash`替换为`resume/postflight/archive`，wrapper `1c0732cc…e45`也只接受这三相。宿主控制链只有一次`run_phase resume`，没有start/stop/clone/delete、重试、第二次maintenance或下一case入口。
- production maintenance exact resume只调用一次，exit 0、stderr空、stdout精确为`maintenance_outcome=completed`；resume evidence `a3da8511…a8bf`绑定既有crash-state并记录invocation=1。terminal receipt `5277acb3…207f`为3,786 bytes、`install/not_applicable/completed/chain-1`，source `38-1` package installed，guard absent，dpkg status `33c4973d…cff1`；1,512-byte delta `fbe45435…d6a`只记录首次安装到installed。
- strict postflight/postverify `5319db07…52a0`/`001b5be1…e2cc`闭合dpkg audit/verify、28项MD5 inventory、20项依赖、字体、manifest `b1322933…467b`、同hash不同inode双FFI、Manager/Fcitx `AllowedProduct`、component 9负向、XDG/进程与loopback-only。guest archive `c365083d…ab72`的17项逐哈希通过，不含raw operation ID/receipt/status/full log。
- terminal后未再执行guest命令；严格串行file pull重取postverify、checkpoint、network均与原证据同hash，plain清单仍为目标唯一started、其余十七台stopped。117文件从absent incoming原子发布为`RadishLex-L6-Crash-Install-Prepared-d75818f-v3-Completed`，均`0600`、单link、零xattr，manifest `679b7e04…c544`逐项通过且任务根无原始32位operation secret。当前不得再执行guest命令、maintenance或其他case；下一步须另行授权正常停止并冻结config/EFI/qcow2、双重qcow2与零句柄。

## 2026-08-19：source terminal 正常停止与关机冻结

- 先逐项复验terminal manifest `679b7e04…c544`并以plain `utmctl list`确认仅`3EC83EB9…593B9` started；随后只发出一次正常host stop，没有进入guest、运行maintenance、调用dpkg或触发下一case。停止后连续清单均为十八台全部stopped。
- config/EFI/qcow2冻结为`62040cc9…eb9`/`f762ee52…e76`/`95e89df3…cb69`；10,306,846,720-byte qcow2严格串行复算两次一致，三项受控文件均为单link且零打开句柄。host summary明确记录terminal后guest command与maintenance invocation均为0。
- `RadishLex-L6-Crash-Install-Prepared-d75818f-v3-Stopped`从absent incoming发布；目录`0700`、十个manifest成员与`files.sha256`均`0600`、单link，逐项校验通过，manifest SHA-256为`036bace8…f3b8`。第三台clone现为冻结source terminal，不重启、不复用或承载下一checkpoint。

## 2026-08-19：今日提交回顾与文档复核

- 本次收尾提交前今日共有六笔提交。`c5d1116`是唯一代码/测试提交：新增repository-only `l6_guest_case_contract.py`与7项回归，把12项input固定为UTF-8 bytewise canonical order、环境文件固定为`build-environment.json`、guest-agent返回值降为观察量并要求有界canonical result/evidence文件回读，同时分离fresh absent与removed terminal；matrix、L6入口和`check-repo`均同步接入。审阅实现、测试与实际第三台case后未发现合同或production语义缺口。
- `39f660e`、`4da267d`、`633dc14`、`de36188`、`3307351`依次冻结第三台clone身份、运行态断网、canonical input/fresh-absent preflight、prepared checkpoint与exact resume terminal；这些提交只更新治理和真相源，没有修改production代码。各阶段都保持单VM、plain `utmctl`、文件回读、逐步授权与XDG零写入边界。
- 对照current、roadmap、technical plan、repository layout、Linux安装/Fcitx边界、L6 runbook及packaging/acceptance/product README后，发现repository layout尚未明确guest-case合同职责，且活跃入口仍停在“唯一started/等待关机”。现已补充模块归属，并统一切换为terminal/关机manifest、十八台all-stopped及下一case顺位。根README、privacy/sync、license、核心架构、协议与其他平台边界未变化，不追加重复流水。

## 2026-08-20：明日事项

1. 开始时复验Git clean、plain `utmctl list`十八台全部stopped、terminal/关机manifest `679b7e04…c544`/`036bace8…f3b8`、handoff record `c74fac12…9849`及DependencyFrozen三项身份；现有source terminal、旧checkpoint和失败现场均不得启动、改写或复用。
2. 明日唯一主切面为第二个crash scenario `install_artifacts_staged`。先在repository-only范围从matrix、production coordinator、acceptance scenario与现有测试推导并固定checkpoint时的receipt/staging/guard/package/dpkg/startup/XDG/process预期，以及相对`install_prepared` host case的最小差异；不能凭前一个case类推未验证状态。
3. 若现有合同不足以防止临时harness漂移，先写失败回归并扩展浅层guest-case/L6 contract；若已完整覆盖则只形成经静态复核的case派生入口，不增加production开关、空转wrapper或第二套真相源。定向Python/Rust、startup、L6/controller/release-pair与完整`check-repo`通过并单独提交前，不创建VM。
4. 系统动作继续分批授权：clone-only只从未改写DependencyFrozen建立独立clean clone，固定新UUID、config/EFI/qcow2与`Network=[]`，不得启动；启动后第一项guest动作仍是关闭非loopback接口并以文件回读证明仅`lo`、IPv4/IPv6 main route为空。
5. input switch与fresh-absent只读/mutation preflight须再次绑定同一`d75818f` handoff、12项canonical inventory、package/state/XDG absent、依赖/字体、`ReceiptMissing` startup和进程静止。通过前不生成operation ID；任一差异保留现场且不热修。
6. checkpoint再单独授权，只允许一次controller调用并要求命中`install_artifacts_staged/artifacts_staged`、完整process group `SIGKILL`、成员0与无dpkg child。checkpoint后的精确receipt/staging/startup必须匹配第2步冻结合同；不匹配即失败关闭，不resume、不调用dpkg后续。
7. 是否exact resume须在checkpoint与crash-state证据冻结后另行决定和授权；不得自动推进。正常停止、关机磁盘双重qcow2与零句柄也独立收尾。第二批repair清理、其余六个crash case、连续完整L6、P05C、发布、推送、真实同步和其他平台继续关闭。

## 2026-08-20：`install_artifacts_staged` repository-only 合同闭合

- 工作从clean `bdb8dc1`开始，`dev`与`origin/dev`一致；plain `utmctl list`只读确认18台注册VM全部stopped。全批没有创建、启动、停止或改写VM，没有传input、生成operation ID、运行acceptance/maintenance/dpkg，也没有触碰系统package或用户XDG。
- 现有matrix与acceptance只固定scenario/checkpoint/terminal，guest-case合同尚未表达第二个case的精确现场。先写调用缺失接口的失败回归，再在既有`l6_guest_case_contract.py`增加typed receipt/guard/checkpoint expectation：固定`install/not_applicable/artifacts_staged/chain-1`、target-only staging、package absent、dpkg status/log不变、root-owned `0600`零长度单link且worker退出后已解锁的guard、`ActiveGuard` startup及XDG/process/network零漂移；resume必须重验staged relationship、重新证明target quiescence、单次apply并完成。
- `l6_contract.py`继续单向导入guest-case规则并把typed expectation与既有matrix交叉校验，避免循环依赖和第二套scenario真相源；matrix checkpoint漂移有独立负向回归。production coordinator、startup或ABI均未修改。
- Linux product新增首次安装精确Rust回归：证明`artifacts_staged` checkpoint先于staged validation、quiescence和apply，receipt/proof/slot与absent package精确；fresh resume执行两次阶段化staging重验、消费一个move-only target permit、只apply一次并形成completed target proof，user data touch为0。package transaction、startup、L6 contract/controller、release-pair、maintenance-refresh及完整`check-repo.sh`均通过；下一步在独立提交后申请clone-only授权，不自动创建VM。

## 2026-08-20：`install_artifacts_staged` 独立 clone 身份冻结

- 从clean `5c278ed`与18台注册VM全部stopped开始，只取得clone-only授权。只读preflight逐项复验DependencyFrozen三项磁盘、reinstall注册壳与`Network=[]`、handoff `c74fac12…9849`、上一case terminal/stopped manifest `679b7e04…c544`/`036bace8…f3b8`及零句柄；首次固定环境内的plain `utmctl list`在应用沙盒边界exit 134并留下零字节原始输出与失败记录 `8c8e4fdf…298f`，未执行clone，随后只读提权重验通过。
- clone控制脚本经shell syntax与命令面扫描，唯一UTM mutation为一次plain `utmctl clone`，不含start/stop/delete/exec/file、dpkg或operation ID入口。新UUID为`B0B826F6-D7D3-433C-8987-E2D6993A87B3`，名称为`RadishLex-Debian13-ARM64-L6-d75818f-crash-install-artifacts-staged`；借用冻结reinstall terminal注册壳后，只把新clone EFI/qcow2替换为未改写DependencyFrozen的APFS clonefile。
- 启动前config/EFI/qcow2为`038274cb…af32`/`0b797641…418`/`4967234b…b18`，配置精确为`Network=[]`；qcow2独立复算一致，source/registration/clone三域零打开句柄，plain清单为19台全部stopped。全批没有启动VM、传input、进入guest、生成operation ID或运行acceptance/maintenance/dpkg，也未触碰产品state和用户XDG。
- 15项证据（含manifest）从absent incoming以Darwin `renameatx_np(RENAME_EXCL)`发布为`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-Clone-Prepared`；目录/文件为`0700`/`0600`、单link、零xattr，manifest `ce430efb…aeb8`逐项通过。下一步必须另行授权启动该clone；启动前复验十九台all-stopped与三项磁盘，启动后第一项guest动作只关闭非loopback接口并以独立文件回读证明仅`lo`且IPv4/IPv6 main route为空。

## 2026-08-20：`install_artifacts_staged` 首次启动失败关闭

- 从clean `58e33af`、clone-only manifest `ce430efb…aeb8`和19台全部stopped开始，取得仅启动/断网/文件回读授权。控制脚本先复验Git、manifest、clone/source三项磁盘与零句柄；静态命令面只有一次plain start、status/list、第一项guest断网exec与file pull，不含stop/clone/delete/push、input、preflight、acceptance、maintenance、dpkg或operation ID入口。
- 两份10GB级qcow2完整前置复哈希通过后，`utmctl start`在事件层输出OSStatus `-1712`；随后有界status轮询始终未观察到started，控制以`clone-did-not-start`退出。任务根只有控制脚本与启动前清单，没有started清单、exec stdout/stderr/result、pull或network evidence，证明控制未到达任何guest命令。没有自动retry，也不把该通用错误推断为bookmark、注册或磁盘问题。
- 失败后plain清单与status均确认目标及其余18台stopped；未发现UTM/QEMU进程或独立日志。config/EFI/qcow2仍为`038274cb…af32`/`0b797641…418`/`4967234b…b18`，qcow2严格复算两次一致，source/clone文件零句柄且`Network=[]`；没有guest、input、operation、transaction、产品state或用户XDG动作。
- 八项证据（含manifest）从absent incoming不可覆盖发布为`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-Start-Failed`；目录/文件为`0700`/`0600`、单link、零xattr，manifest `34c0918d…c8d8`逐项通过。下一步先离线固定能捕获start返回/诊断、逐次status和超时terminal的retry控制；再次start仍须新授权，不能沿用本次授权自动重试。

## 2026-08-20：L6 host 单次 start 控制闭合

- 新增repository-only `l6_utm_start_once.py`，把下一次start从临时shell流程收敛为单次状态机。CLI要求双显式授权，绑定clean 40位repository head、首次失败manifest及其`0700`/`0600`单link条目、精确VM数、全停清单和target独立stopped status；output根必须absent且位于repo和旧证据之外。
- 控制只通过PATH中的plain `utmctl`执行list/status/start，start源码入口唯一。start exit/timeout、stdout/stderr 64KiB前缀、完整size/hash、每次status及terminal list分别exclusive写入`0600` JSON并逐项`fsync`，根为`0700`，末尾生成`files.sha256`。status与list同时确认仅target started才返回0；全停失败、前置拒绝、其他不确定状态分别返回10/11/12。任何结果都不自动stop/retry，不进入guest、传input或生成operation ID。
- 八项合成回归覆盖正常started、`-1712`后全停、start timeout后started、status/list不一致、并发VM拒绝、双授权/absent输出、manifest漂移及executed-control固定路径/identity；`check-linux-l6-contract.sh`已接入且不调用真实UTM。真实失败manifest `34c0918d…c8d8`的7个条目通过新验证器。本批没有启动/停止VM、进入guest或触碰package、state、XDG；新的start与首条guest断网仍须单独授权。

## 2026-08-20：单次控制 retry 失败关闭并冻结

- 从clean `053018d`、首次失败manifest `34c0918d…c8d8`和十九台VM全部stopped开始，取得仅一次start及成功后首条断网/双回读授权。启动前复验committed控制/runtime `dd264acb…fb1`/`c015ab13…d61a`、clone/source config/EFI/qcow2、`Network=[]`与零句柄；orchestration静态入口只有committed start-once、成功分支的一次guest断网exec及两次file pull，不含stop/clone/delete/push、input、preflight、acceptance、maintenance、dpkg或operation ID。
- 唯一plain `utmctl start`在90秒内没有stdout/stderr并timeout；随后60次独立status及terminal list都只得到target stopped与其余十八台stopped。控制按冻结合同返回10，start manifest为`1bc2a5da…2f23`，terminal精确记录`failed-closed-stopped`、一次start、无自动stop/retry、guest/input/operation/transaction均未执行。因为返回非0，外层没有进入首条guest断网或任何file pull。
- 独立postfailure复验再次确认十九台all-stopped，clone config/EFI/qcow2仍为`038274cb…af32`/`0b797641…418`/`4967234b…b18`，qcow2严格复算两次一致，source/clone六项文件零句柄。含全部60份poll JSON、控制绑定、pre/post清单、启动诊断与磁盘证据的持久目录从absent incoming以`RENAME_EXCL`发布为`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-Start-Retry-Failed-Closed`；manifest `337007ff…7ebbb`逐项通过，目录/子目录`0700`、文件`0600`单link且零xattr。
- 离线差分确认该clone、已成功启动的第三台clean retry与reinstall壳在去除Name/UUID后config逐字段相同，bundle/config/Data权限、flags与xattr名称一致；UTM Registry可见字段也都为自洽UUID/Name/Path、`ReadOnly=false`、无external/shared/suspended差异。限定统一日志只看到TCC允许的AppleEvent start与后续status请求，没有QEMU启动记录或可归因的UTM/QEMU内部错误；因此不把timeout推断为bookmark、config、磁盘或权限根因。
- 该clone现冻结为两次start均未进入guest的宿主失败现场，不第三次start、修补注册/config、删除或复用。下一步只在新授权后从未改写DependencyFrozen建立另一台独立clean clone；clone-only批次不启动，后续start/首条断网、input/preflight、checkpoint与resume继续分别授权。

## 2026-08-20：v2 clone 假成功由后置条件失败关闭

- 从clean `992a307`、前序失败manifest `337007ff…7ebbb`、十九台注册VM全部stopped与未改写DependencyFrozen开始，只取得clone-only授权。单次控制先逐项复验前序/旧clone/上一caseterminal与关机manifest、handoff、source/reinstall/旧clone三域config/EFI/qcow2及零句柄；静态命令面只有一次plain `utmctl clone`与三次list，不含start/stop/delete/exec/file、input、operation ID、acceptance、maintenance或dpkg。
- 唯一clone进程exit 0、stdout空，但stderr精确报告Apple Event `OSStatus -1712`。紧随其后的list仍与preclone逐字节相同：没有新UUID、没有v2名称，目标`.utm` package也不存在；控制因此在`registered-vm-count-after`后置条件失败关闭，没有执行DependencyFrozen磁盘替换或任何guest动作，也没有retry或删除。
- 独立冻结再次确认十九台all-stopped、source/reinstall/旧clone磁盘hash未变且零句柄，新注册/package/prepared evidence均absent。16项证据（含manifest）从absent incoming以`RENAME_EXCL`发布为`RadishLex-L6-Crash-Install-Artifacts-Staged-d75818f-v2-Clone-Failed-Closed`；目录/文件为`0700`/`0600`、单link、零xattr，manifest `65160b12…c1859`逐项通过。
- 该观察只证明CLI exit 0不能替代注册与package后置条件，不归因bookmark、config、磁盘或具体UTM内部根因。下一步先在repository-only范围固定clone-once控制与合成回归，覆盖exit 0伴随stderr、正常注册、部分落地与并发状态；真实clone、启动/首条断网、input/preflight、checkpoint及resume继续分别授权。

## 2026-08-20：今日提交回顾与文档复核

- 本次收尾前今日共有十笔提交。三笔代码/测试提交中，`03e47ff`以typed guest-case、matrix交叉校验和首次安装Rust回归固定`install_artifacts_staged/artifacts_staged`的target-only staging、合法guard、package/dpkg未变与单次resume/apply；`90b5898`新增repository-only `l6_utm_start_once.py`及八项合成回归，把真实start收敛为单次、有界诊断、逐次status和四类terminal；`f8d237e`再绑定实际执行控制文件的固定路径、regular/single-link/权限与hash。其余七笔依次推进实机顺位、冻结clone/两次start失败与v2 clone假成功证据，没有修改production ABI、协议、隐私、许可或输入热路径。
- 对代码接线复核确认`check-linux-l6-contract.sh`现同时运行matrix、guest-case、UTM start-once与controller identity合同，`check-repo.py`也要求新增控制和测试文件。此前packaging入口仍写“只验证matrix”，technical plan、repository layout、Linux product/Fcitx/acceptance README与runbook局部状态仍停在首次start失败；现已统一到typed staged合同、两次start失败、v2 clone exit 0/stderr `-1712`且无注册/package、manifest `65160b12…c1859`以及先闭合clone-once控制的顺位。
- 根README只承担索引，privacy/sync、license、协议、engine/FFI/Manager边界和其他平台顺序均未变化，不为实机流水重复改写。收尾只读`utmctl list`确认十九台注册VM全部stopped，未发送多余stop，也未清理、恢复、重启或改写任何冻结现场。

## 2026-08-20：协作入口收敛为长期约束

- 新增 `docs/README.md` 作为文档导航与职责入口，集中说明项目定位、默认阅读顺序、文档分类和维护规则；当前里程碑、现场、停止线与下一步仍只在 `docs/status/current.md` 维护，不复制第二份阶段快照。
- `AGENTS.md` / `CLAUDE.md` 删除“项目与当前阶段”、P05B 逐实现合同和“当前顺位”等易变内容，把仍需长期执行的安装事务原则、冻结资产保护与系统授权边界改写为跨阶段规则。两份文件今后只在长期协作约束变化时同步修改，普通阶段、证据和顺位推进不再触碰。
- 根 README 与 repository layout 接入新的文档入口并明确职责。此次只调整文档治理，不改变架构、协议、隐私、许可、production 实现、当前系统现场或既有停止线；`check-docs.sh`、`check-text-files.sh`、`git diff --check`、两份协作文件逐字一致性与完整 `check-repo.sh` 均通过，文档门禁只保留既有 508 行预算提醒。

## 2026-08-21：明日事项

1. 开始时复验Git clean、十九台VM全部stopped、前序start/v2 clone失败manifest `337007ff…7ebbb`/`65160b12…c1859`、handoff `c74fac12…9849`与DependencyFrozen config/EFI/qcow2身份；现有clone、terminal和失败证据不得启动、重试、修补、删除或复用。
2. 唯一主切面先保持repository-only：以失败回归起步固定host clone-once控制，只允许PATH中的plain `utmctl list/clone`和精确一次clone调用；绑定clean head、前序manifest、全停清单、registered source、目标name/package absent与输出根，不承担start、guest、磁盘热替换或transaction。
3. 控制须分别记录exit/timeout、64KiB有界stdout/stderr、完整size/hash、pre/terminal list及注册/package后置事实；只有stderr空、未timeout、VM数量精确+1、唯一新UUID/name stopped且精确package存在才可判定created。exit 0伴随`-1712`且完全未落地归入failed-closed-absent；registry/package单侧、数量漂移、并发VM或命令不确定一律归入state-indeterminate，任何终态不自动retry/delete/start。
4. 合成回归至少覆盖正常创建、exit 0/stderr `-1712`且无落地、timeout后出现注册、仅registry、仅package、重复name/数量漂移、其他VM started、双授权/absent output、前序manifest与executed-control身份漂移；接入`check-linux-l6-contract.sh`和`check-repo.py`，补齐repository layout/packaging说明并通过定向Python、文档、文本及完整`check-repo.sh`后单独提交。
5. clone-once控制闭合不等于真实系统授权。只有新授权后才从未改写DependencyFrozen建立另一台独立clean clone并冻结UUID/config/EFI/qcow2/`Network=[]`；真实启动和首条guest断网继续另批，成功前不传input、不生成operation ID、不运行acceptance/maintenance/dpkg。
6. input/preflight、`install_artifacts_staged` checkpoint、exact resume、正常停止与关机冻结仍逐步授权；其余六个crash case、连续完整L6、P05C、发布、推送、真实同步和其他平台继续关闭。

## 2026-08-21：社区治理入口与仓库门禁补齐

- 参考 RadishAxiom 与 RadishLink 的社区治理结构，在根目录新增 RadishLex 专属 `SECURITY.md`、`CONTRIBUTING.md` 与 `CODE_OF_CONDUCT.md`；根 README 以单行紧凑入口链接三份文件并保持 180 行预算，不复制阶段状态。安全策略明确远程仓库已启用 GitHub Private Vulnerability Reporting，继续以 Security Advisory 私密入口和不可用时邮件作为报告路径。
- 新增 Bug 与变更提案两个 Issue Forms，分别固定合成复现、P0-P3、学习/删除、密文同步、FFI、平台和安装事务信息；Issue chooser 禁用空白 Issue，并把安全问题导向仓库 Security policy。贡献指南同步要求普通问题使用表单、漏洞保持私密，许可证仍以 RadishLex Source-Available License 为唯一真相源。
- 新增独立 `repository_governance.py`，让 `check-repo.py` 把三份社区文档、三个 Issue 配置和治理检查器本身列为必需文件，并扫描 tracked/untracked Markdown 的行内与引用式相对链接；远程、页内、代码围栏链接不误判，仓库逃逸和缺失目标失败关闭。社区文档同时纳入 `check-docs.py` 的入口预算。
- 六项聚焦单测覆盖完整/漂移治理合同、已有/编码/引用式/缺失/逃逸链接，以及 tracked、untracked、ignored 文件枚举；Issue YAML 解析、`check-docs.sh`、`check-text-files.sh`、`git diff --check` 与完整 `check-repo.sh` 均通过，只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。本批不改变架构、协议、隐私分级、产品实现、当前里程碑、实机现场或停止线，因此不修改 current、AGENTS/CLAUDE 和稳定技术专题。

## 2026-08-22：Agent 协作入口分层完善

- 参考 Radish 的协作入口治理结构，重新组织并逐字同步 `AGENTS.md` / `CLAUDE.md`。根入口从压缩的长段落扩展为可扫描的定位与优先级、任务授权、文档路由、项目长期边界、隐私/FFI/安装红线、风险分层验证及 Git/入口维护九类启动级契约；保留 RadishLex Source-Available License、本地输入热路径、P0-P3、tombstone、薄平台壳和失败关闭等长期红线。
- 新增 `docs/agent-collaboration.md`，以三层职责承接任务类型、基线确认、方案与范围、实现质量、文档归位、系统/实机授权、证据口径和交接细则；`docs/README.md` 只新增协作专题索引和维护说明。UTM、guest-agent、隔离网络、具体 transaction、冻结资产与阶段性“当前不做”继续只读对应 Boundary、runbook 和 `docs/status/current.md`，没有复制回根入口。
- `scripts/check-docs.py` 新增两份根协作文件逐字节一致性检查，`scripts/test_repository_checkers.py` 增加一致/漂移正负向回归。完整门禁首次捕获旧治理合同要求的许可证名称与核心短语，调整表述后既保留长期语义也继续满足仓库检查器。
- `python3 -m unittest scripts.test_repository_checkers`、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与完整 `./scripts/check-repo.sh` 均通过；只保留既有 `docs/sync-server-api-storage.md` 508 行预算提醒。本批不改变产品架构、协议、隐私分级、许可证、当前里程碑、实机现场或停止线，因此不修改 `current` 和稳定技术专题。

## 2026-08-22：L6 host clone-once 控制闭合

- 从clean `84567fa`开始，唯一开发切面保持repository-only。先提交调用尚不存在`l6_utm_clone_once`的合成测试并确认`ModuleNotFoundError`失败，再实现独立控制；本批没有调用真实`utmctl`、创建/启动/停止/删除VM、进入guest、替换磁盘、传input、生成operation ID或运行acceptance/maintenance/dpkg，也未触碰产品state与用户XDG。
- 新控制要求clone与禁止自动retry/delete/start两项显式授权，绑定clean 40位head、executed-control固定路径/regular single-link identity、前序失败manifest逐项identity/hash、canonical全停清单、精确registered source、目标name/package absent与create-new输出根。UTM命令面只有PATH中的plain list/clone，clone调用精确一次；命令exit/timeout、stdout/stderr 64KiB前缀与完整size/hash、pre/terminal list和目标package状态分别exclusive持久化到`0700`根下的`0600` JSON并逐项`fsync`，末尾生成manifest。
- created必须同时满足exit 0、未timeout、stderr空、既有注册项不漂移、数量精确+1、唯一新UUID/name stopped及精确`.utm`目录存在；命令确定结束且registry/package零变化为failed-closed-absent，前置拒绝与其他不确定状态分别独立返回。timeout后落地、registry/package单侧、重复name、数量漂移与其他VM started均不推断成功；任何终态都不自动retry/delete/start或进入guest。
- 十一项fake-runner/临时目录回归覆盖正常创建、exit 0伴随`-1712`零落地、超过64KiB输出的有界前缀与完整size/hash、timeout后完整落地、registry-only、package-only、重复name/数量漂移、并发VM、canonical清单hash漂移、双授权/absent输出/目标package absent，以及前序manifest和executed-control identity；控制和测试已接入默认L6与repository required-file门禁。下一步仍须另行授权从未改写DependencyFrozen建立新clean clone，clone授权不包含start或guest断网。
- 定向十一项测试、含matrix/guest-case/start-once/clone-once/controller identity共40项的`check-linux-l6-contract.sh`、`check-docs.sh`、`check-text-files.sh`、`git diff --check`与完整`check-repo.sh`均通过；只保留既有`docs/sync-server-api-storage.md` 508行预算提醒。current、roadmap、technical plan、repository layout、Linux Boundary、L6 runbook及packaging/platform入口已统一到clone-once闭合和“下一步另行授权真实clean clone”的顺位。
- 真实系统动作前再次静态核对本机二进制与UTM官方`UTMCtl.swift`，发现clone名称是`@Option var name`，命令必须为`utmctl clone UUID --name NAME`；原合成runner只验证了内部一致的错误位置参数，不能代表真实CLI语法。控制与测试现同时固定精确`--name` argv，定向十一项与L6合同重新通过；修正前没有消耗唯一clone调用，也没有改变十九台全停现场。

## 2026-08-22：`install_artifacts_staged` v3 clean clone 冻结

- `--name`修正以`b86d72f`独立提交，完整`check-repo.sh`通过且Git重新clean。真实调用前复验v2失败manifest `65160b12…c1859`、十九台canonical all-stopped hash `35db3c8f…fe6d`、registered reinstall壳 `E671DB9C…D465`、DependencyFrozen config/EFI/qcow2 `06d91dfe…a1568`/`0b797641…1418`/`4967234b…4b18`、注册壳三项磁盘和六文件零句柄；v3 name/package及两个输出根均absent。
- committed clone-once控制只发出一次`utmctl clone E671DB9C…D465 --name RadishLex-Debian13-ARM64-L6-d75818f-crash-install-artifacts-staged-v3`。命令exit 0、未timeout、stdout/stderr均空；terminal清单保留原十九台并只新增UUID `5B19AEF1-0F29-40B6-8F24-1B1929117DAB` stopped，精确`.utm` package存在，因此返回created。八个manifest条目逐项通过，manifest为`7ce53048…e5b7`；没有retry、delete、start、guest、input、operation ID或transaction。
- 新package初始config/EFI/qcow2为`d04b00e1…1ff4`/`76753750…a1c3`/`feb2ba7e…aaa5`且`Network=[]`，即注册壳副本而非clean盘。经单独精确系统授权，一次性控制只在该新package内生成DependencyFrozen APFS clonefile并原子替换EFI/qcow2；成功/失败输出根均create-new，失败不重试、不回滚、不删除也不启动。
- prepared manifest `b065c7ac…32db`固定最终config/EFI/qcow2 `d04b00e1…1ff4`/`0b797641…1418`/`4967234b…4b18`、qcow2两次复算、source/registration/target零句柄和20台前后同一all-stopped清单。独立只读复核再次逐项验证manifest、Name/UUID/`Network=[]`、DependencyFrozen与目标三项磁盘、零句柄和二十台全停；failure/incoming根absent，Git仍clean `b86d72f`。
- v3 clone现只作为第二个case的stopped clean起点，clone与磁盘物化授权均已结束。下一步必须另行授权单次start与首条guest断网；成功后才可分批进入canonical input/preflight，checkpoint、resume、正常停止和关机冻结仍分别授权。其余六个crash case、连续完整L6、P05C、发布、推送与其他平台继续关闭。

## 2026-08-22：v3 单次 start 失败关闭

- 从clean `27087c3`、clone/prepared manifest `7ce53048…e5b7`/`b065c7ac…32db`与二十台canonical all-stopped开始，取得仅一次start及成功后首条guest断网/双回读授权。一次性外层控制SHA-256为`d12c75ee…358b1`，前置逐项复验两份manifest、target/source config/EFI/qcow2、`Network=[]`、零句柄和固定全停清单；命令面只有committed `l6_utm_start_once.py`一次调用，以及成功门后的恰好一次guest断网exec和两次file pull，不含stop/retry/delete、input、operation ID、preflight、acceptance、maintenance或dpkg。
- 唯一plain `utmctl start`在90秒内没有stdout/stderr并timeout；随后60次独立status及terminal list始终为target stopped与其余十九台stopped。控制按合同返回10，start manifest `870f56dd…f3a5`逐项通过，terminal精确记录`failed-closed-stopped`、一次start、无自动stop/retry、guest/input/operation/transaction均未执行。
- 因started成功门未成立，外层在`start-controller`阶段停止，guest exec与file pull均为0次，没有创建network-ready证据。失败根以create-new方式冻结，manifest `59c62c14…bba05`绑定控制脚本、启动前/失败后同一二十台全停清单、target stopped与两份clone manifest；没有自动stop/retry/delete，也没有传input、生成operation ID或执行transaction。
- 独立只读postverify控制SHA-256为`801f7ecf…ccef5`，只调用`utmctl list/status`，不含任何start/stop/exec/file动作。它重新验证四份前序manifest，确认target config/EFI/qcow2仍为`d04b00e1…1ff4`/`0b797641…1418`/`4967234b…4b18`、qcow2两次完整复算一致、DependencyFrozen三项对照一致、`Network=[]`、source/target零句柄和二十台全停；manifest `8e3d5ced…8386`逐项通过。
- v3 `5B19AEF1…7DAB`现冻结为单次start失败现场，不第二次start、修补、删除或复用。现有证据只证明host launch未发生且现场完整，不把timeout归因为bookmark、config、磁盘、权限或具体UTM/QEMU缺陷。下一步先保持repository-only，以三份新manifest为输入收敛host launch诊断边界与合成回归；任何后续host诊断采集、新独立clone或start仍须单独授权。

## 2026-08-22：L6 host launch 只读诊断控制闭合

- 新增repository-only `l6_utm_launch_diagnostics.py`。控制要求host诊断与读取system log两项显式授权，并在任何观察命令前绑定clean committed head、executed-control identity及v3 start/failure/postverify manifest `870f56dd…f3a5`/`59c62c14…bba05`/`8e3d5ced…8386`；除逐项hash/identity外，还重验一次start、60次poll、全停terminal、guest/file/input/operation/transaction与自动动作均未执行。四个语义输入文件必须实际列入各自manifest，不能以未绑定旁路文件补充结论。
- 前置通过后命令面严格固定为`utmctl list`、目标`utmctl status`、`/bin/ps -axo pid=,ppid=,uid=,comm=`与最长15分钟、只覆盖UTM/utmctl/qemu的`/usr/bin/log show --style ndjson`；后者另固定no-pager、UTC、info/no-debug/no-signpost/no-loss、`log(1)`接受的带offset时间格式及合法`finished`终止记录。脚本不含clone/start/stop/guest exec/file、input、operation ID、transaction、retry或delete；目标或任一peer非stopped、命令timeout/非零/stderr/截断、格式与身份漂移均失败关闭。
- create-new输出根位于仓库和三份冻结证据之外，目录/文件为`0700`/`0600`并生成manifest。process证据只保留role、PID/PPID/UID、executable basename与path hash；log证据只保留受限metadata、完整message hash与home路径脱敏的有界prefix，原始stdout不持久化。任何terminal均固定`root_cause=unattributed`，单条日志文本不能自行证明bookmark、config、磁盘、权限或具体UTM/QEMU根因。
- 八项fake-runner/临时目录回归覆盖只读成功路径、target started前置拒绝、log失败、畸形/无关事件、双授权/时间窗/absent输出、start manifest语义、failure/postverify manifest语义与required-entry绑定、executed-control identity。实际冻结三份manifest也已由新绑定逻辑只读验证为67/14/11项；本批没有运行真实`utmctl`、`ps`或`log show`，没有创建真实诊断证据，也没有改变二十台全停现场。下一步须按`current`单独授权一次真实只读采集。
- 定向八项测试、含matrix/guest-case/start-once/clone-once/launch-diagnostic/controller identity的默认L6门禁、`check-docs.sh`、`check-text-files.sh`、`git diff --check`与完整`check-repo.sh`均通过；只保留既有`docs/sync-server-api-storage.md` 508行预算提醒。current、roadmap、technical plan、repository layout、L6 runbook及packaging/platform入口已统一到“控制闭合、真实只读诊断另行授权”的顺位。

## 2026-08-22：v3 首次 host launch 诊断失败关闭与 v2 收敛

- 获得真实只读host诊断和固定历史system log窗口授权后，从clean `a828be2`绑定start/failure/postverify manifest `870f56dd…f3a5`/`59c62c14…bba05`/`8e3d5ced…8386`，目标固定为`5B19AEF1…7DAB`，UTC窗口固定为08:12–08:18，输出写入新的私有`0700`根。前置逐项验证67/14/11个manifest条目、clean HEAD、控制identity、二十台all-stopped清单与目标stopped。
- 固定`/bin/ps -axo pid=,ppid=,uid=,comm=`exit 0、stderr空，但stdout为95,549 bytes并超过控制的64KiB捕获上限；因此控制按合同返回10与`diagnostics-incomplete`，reason为`host-process-observation-output-truncated`，没有继续执行`/usr/bin/log show`。六个payload的manifest `158fe177…77c`逐项通过，stdout完整hash为`65e0310d…be8`；terminal保持`root_cause=unattributed`，clone/start/stop/guest/input/operation/transaction/retry/delete均未执行。
- 首次输出根原样冻结，不补写日志、不覆盖或原地retry。repository-only v2把进程观察收敛为`/bin/ps -axo pid=,ppid=,uid=,ucomm=`，只保留无路径accounting name、role与PID/PPID/UID；同时新增独立binding模块，使主控制与binding模块都绑定committed identity，并要求下一次采集逐项验证首次诊断manifest、同一双授权/VM数量/历史时间窗、旧`comm=`命令、95,549-byte截断及日志未执行终态。
- 九项合成回归新增首次不完整诊断语义与时间窗漂移拒绝，真实四份冻结manifest也由v2逻辑只读验证为67/14/11/6项；定向测试、默认L6合同、文档/文本门禁、`git diff --check`与完整`check-repo.sh`通过，只保留既有`docs/sync-server-api-storage.md` 508行预算提醒。本批实现期间没有再次调用真实`utmctl`、`ps`或`log show`，没有改变VM现场。下一步须重新单独授权，以新absent输出根运行一次v2真实只读诊断；v3仍不得第二次start。

## 2026-08-22：v2 host launch 诊断失败关闭与 v3 收敛

- 获得新授权后，从clean `5ab391d`绑定start/failure/postverify/首次诊断manifest `870f56dd…f3a5`/`59c62c14…bba05`/`8e3d5ced…8386`/`158fe177…77c`，仍只针对`5B19AEF1…7DAB`和UTC 08:12–08:18窗口。前置验证67/14/11/6项、主控制与binding模块identity、二十台all-stopped及target stopped通过。
- 唯一`/bin/ps -axo pid=,ppid=,uid=,ucomm=` exit 0、stderr空，stdout 31,570 bytes、SHA-256 `9d21798f…ed62`且未截断；解析器随后以`host-process-identifier-invalid`失败关闭，未生成`host-processes.json`，也没有执行`/usr/bin/log show`。六项manifest `8ccdbb9f…dc7`逐项通过，root cause仍未归属，clone/start/stop/guest/input/operation/transaction/retry/delete均未执行。
- 原始进程清单按隐私边界未持久化，不能从该证据断言具体问题行。repository-only回归加入macOS全量清单允许的PID 0无关系统行，并继续拒绝负PID/PPID/UID与重复PID；v3控制同时强绑定首次截断诊断和v2标识拒绝诊断的授权、HEAD、窗口、命令、stream metadata与零mutation终态。
- 十项定向回归、默认L6合同、文档/文本门禁、`git diff --check`与完整`check-repo.sh`均通过，只保留既有`docs/sync-server-api-storage.md` 508行预算提醒；真实证据链由v3逻辑只读验证为67/14/11/6/6项。本批修复阶段没有再次调用真实`utmctl`、`ps`或`log show`。两份诊断目录均冻结且不得覆盖；下一步必须再次单独授权，以新的absent v3输出根执行只读采集，冻结目标仍不得第二次start。

## 2026-08-22：v3 host launch 诊断失败关闭与 v4 收敛

- 获得新授权后，从clean `86304b3`绑定start/failure/postverify/首次/v2诊断manifest `870f56dd…f3a5`/`59c62c14…bba05`/`8e3d5ced…8386`/`158fe177…77c`/`8ccdbb9f…dc7`，仍只针对`5B19AEF1…7DAB`和UTC 08:12–08:18窗口。前置验证67/14/11/6/6项、主控制与binding模块identity、二十台all-stopped及target stopped通过。
- 唯一`/bin/ps -axo pid=,ppid=,uid=,ucomm=` exit 0、stderr空，stdout 31,080 bytes、SHA-256 `a718ab88…397`且未截断；解析器仍以`host-process-identifier-invalid`失败关闭，未生成`host-processes.json`或执行`/usr/bin/log show`。六项manifest `f952953a…e5ddf`逐项通过，目录/文件为`0700`/`0600`，root cause仍未归属，clone/start/stop/guest/input/operation/transaction/retry/delete均未执行。
- 原始进程清单按隐私边界未持久化，不能断言具体失败行。本机`/etc/passwd`确认macOS legacy `nobody` UID为精确`-2`，这是对持续标识拒绝的高可信合同假设而非现场证明。repository-only v4只新增该精确UID，继续拒绝负PID/PPID、其他负UID、非精确PID 0与重复PID；同时强绑定三次不完整诊断的授权、HEAD、窗口、命令、stream metadata与零mutation终态。
- 十项定向回归、含matrix/guest-case/start-once/clone-once/launch-diagnostic/controller identity的默认L6合同、文档/文本门禁、`git diff --check`与完整`check-repo.sh`均通过，只保留既有`docs/sync-server-api-storage.md` 508行预算提醒；真实证据链由v4逻辑只读验证为67/14/11/6/6/6项。本批修复阶段没有再次调用真实`utmctl`、`ps`或`log show`。三份诊断目录均冻结且不得覆盖；下一步必须重新单独授权，以新的absent v4输出根执行只读采集，冻结目标仍不得第二次start。

## 2026-08-22：今日提交回顾与文档复核

- 本次文档收尾前今日共有十笔提交，其中`84567fa`为PR合并。`2e5261a`重整并逐字同步根协作入口，新增Agent协作专题及一致性门禁；`121642f`/`b86d72f`新增L6 clone-once控制并修正真实UTM `--name`参数；`27087c3`/`739b02e`分别冻结新v3 clean clone和唯一start失败现场；`a828be2`建立host launch只读诊断边界，`5ab391d`/`86304b3`/`7fdce24`再依次收敛紧凑`ucomm`、精确PID 0和macOS legacy UID `-2`兼容。今天没有修改production installer、FFI ABI、同步协议、隐私分级、许可证或输入热路径。
- 对今日代码接线逐项复核：协作治理已由`docs/README.md`、`docs/agent-collaboration.md`、根双入口和repository checker共同覆盖；clone/diagnostic控制已进入`check-linux-l6-contract.sh`与`check-repo.py` required-file门禁；阶段事实已同步到current、roadmap、technical plan、repository layout、Linux安装/Fcitx边界、L6 runbook及packaging/Linux product入口。根README、privacy/sync、engine、FFI、Manager、许可证和其他平台边界没有对应变化，不追加无关状态副本。
- 复核发现`platforms/linux-l6-acceptance/README.md`仍停在host诊断控制首次闭合、真实采集尚未发生的旧口径；现已补正为三次只读诊断manifest均在进程阶段失败关闭、system log未执行，以及v4只接受精确legacy UID `-2`并等待新授权。除此之外未发现代码与相关文档的有效口径漂移。

## 2026-08-23：明日事项

1. 主线继续留在M5-P05B的第二个`install_artifacts_staged` crash case，不切换P05C、其他crash case或其他平台。开始时确认Git clean、二十台VM全部stopped，逐项复验start/failure/postverify/三次诊断manifest `870f56dd…f3a5`/`59c62c14…bba05`/`8e3d5ced…8386`/`158fe177…77c`/`8ccdbb9f…dc7`/`f952953a…e5ddf`，并确认v4输出根仍absent；冻结v3 target不得第二次start、修补、删除或复用。
2. 取得新的精确系统授权后，只执行一次v4真实只读host诊断：目标仍为`5B19AEF1…7DAB`，UTC窗口仍为`2026-08-22 08:12:00+0000`至`08:18:00+0000`，输出写入新的`Host-Launch-Diagnostics-v4`私有根。命令面只含plain `utmctl list`、目标`status`、紧凑`ucomm`进程观察与固定predicate的`log show`；不含start、stop、clone、guest、file、input、operation、transaction、retry或delete。
3. v4返回后先完成证据审查：验证manifest、权限、stream size/hash、二十台与目标全停、脱敏进程结果和unified log终态。若采集完成，只依据结构化事件与独立系统事实形成判断，证据不足时继续保持`root_cause=unattributed`；若仍在解析或日志门失败，冻结新目录并回到repository-only，先让下一版证据给出隐私安全、可判定的失败类别，不连续发出猜测性真实调用。
4. 只有v4结果足以支持新的系统路径时，才设计下一台独立clean target。先在仓库内固定source、目标name/package、create-new输出根、clone后注册/package联合判定、DependencyFrozen磁盘物化和失败停止条件，复验现有clone-once控制是否可直接承载；需要扩展时先补负向回归、L6合同和文档并单独提交。
5. 新target的clone-only、磁盘物化、首次start与首条guest断网继续按系统影响拆分授权。clone成功只冻结UUID/config/EFI/qcow2/`Network=[]`与全停清单，不自动start；未来start成功后，第一项guest动作仍是关闭非loopback接口并双重文件回读证明仅`lo`且IPv4/IPv6 main route为空。
6. canonical input/preflight、`install_artifacts_staged` checkpoint、exact resume、正常停止与关机冻结继续按证据顺序推进，不为追赶进度合并授权或跳过后置验证。明日合理交付目标是完成v4诊断及其结论，条件允许时再完成新target的repository-only设计；不预设同日进入guest transaction。
7. 发布、推送、PR、tag/Release、真实同步、P05C以及Android/Windows/iOS继续关闭；只在当前主线形成可复核提交，并在结束时同步current、runbook和周志中确有变化的事实。

## 2026-08-23：v4 host launch 诊断失败关闭与 v5 收敛

- 获得精确系统授权后，从clean `bf42768`绑定start/failure/postverify/v1-v3诊断manifest `870f56dd…f3a5`/`59c62c14…bba05`/`8e3d5ced…8386`/`158fe177…77c`/`8ccdbb9f…dc7`/`f952953a…e5ddf`，仍只针对冻结目标`5B19AEF1…7DAB`与UTC 08:12–08:18窗口。v4输出根预先确认为absent，控制前置验证67/14/11/6/6/6项、clean HEAD、双控制identity、二十台all-stopped及target stopped通过。
- 唯一`/bin/ps -axo pid=,ppid=,uid=,ucomm=` exit 0、stderr空，stdout 31,605 bytes、SHA-256 `fbb7ca6d…8ed2`且未截断。精确legacy UID `-2`兼容使全量解析完成，脱敏`host-processes.json`确认相关UTM/utmctl/QEMU进程为0；原始进程清单未持久化。
- 固定predicate的`/usr/bin/log show`随后实际执行一次，exit 0、stderr空且未超时；stdout为3,916,860 bytes、SHA-256 `0dbed63a…e9c`，超过旧64KiB捕获上限。控制因此不解析截断前缀、不生成`unified-log.json`，以`diagnostics-incomplete`/`unified-log-observation-output-truncated`返回10。八项manifest `34ae0563…743e`逐项通过，目录/文件为`0700`/`0600`；root cause保持unattributed，clone/start/stop/guest/input/operation/transaction/retry/delete均未执行。
- v4目录已冻结且没有自动retry。repository-only v5现强绑定该八项manifest、3,916,860-byte日志metadata、进程解析完成/相关进程0及零mutation终态；普通list/status/process仍沿用64KiB捕获，只有system log显式使用8MiB。日志解析继续要求完整NDJSON `finished`，并固定16,384事件、16KiB单行、32KiB消息、1,024字符home-path脱敏prefix与16MiB脱敏序列化总量；原始日志正文仍不写入证据。
- 九项start、十项既有launch diagnostic、两项v5大输出/前序绑定定向回归、默认L6合同、文档/文本门禁、`git diff --check`与完整`check-repo.sh`均通过；v5逻辑也对真实v4八项证据完成只读语义校验，只保留既有`docs/sync-server-api-storage.md` 508行预算提醒。本批没有修改production installer、FFI ABI、同步/隐私协议、guest状态或产品热路径；下一次真实v5采集仍须新的单步授权和absent输出根。

## 2026-08-23：v5 host launch 诊断失败关闭与 v6 收敛

- 获得新的精确系统授权后，从clean `a111377`绑定start/failure/postverify/v1-v4诊断manifest `870f56dd…f3a5`/`59c62c14…bba05`/`8e3d5ced…8386`/`158fe177…77c`/`8ccdbb9f…dc7`/`f952953a…e5ddf`/`34ae0563…743e`，只针对冻结目标`5B19AEF1…7DAB`与UTC 08:12–08:18窗口。v5输出根预先absent，控制复验67/14/11/6/6/6/8项、clean HEAD、双控制identity、二十台all-stopped及target stopped。
- 唯一进程观察exit 0、stderr空，stdout 31,150 bytes、SHA-256 `cefe8ace…77ce`且未截断，脱敏结果确认UTM/utmctl/QEMU相关进程为0。唯一固定predicate `log show`也exit 0、stderr空、未超时，完整stdout为3,916,860 bytes、SHA-256 `0dbed63a…e9c`且未截断。
- 日志解析在可选`category`空字符串处以`unified-log-category-invalid`失败关闭，没有生成`unified-log.json`。八项manifest `9da78f78…08ae`逐项通过，目录/文件为`0700`/`0600`；terminal保持`root_cause=unattributed`，clone/start/stop/guest/input/operation/transaction/retry/delete均未执行。v5目录随即冻结，本批没有自动retry或第二次系统日志读取。
- repository-only v6现强绑定v5八项manifest与精确分类拒绝终态，只把可选`category`、`messageType`、`subsystem`空字符串规范化为缺失值；非字符串、必填timestamp/process/message、完整NDJSON终止记录、8MiB捕获、事件/单行/消息/脱敏总量与原始正文不持久化合同均保持不变。合成回归覆盖空字符串规范化、非字符串拒绝、v5语义绑定与terminal漂移拒绝；真实v5八项证据也由v6逻辑只读验证。
- v1-v6定向回归、默认L6合同、文档/文本门禁、`git diff --check`与完整`check-repo.sh`均通过，只保留既有`docs/sync-server-api-storage.md` 508行预算提醒。本批不修改production installer、FFI ABI、同步/隐私协议、guest状态或产品热路径。下一次真实v6采集须新的单步系统授权和absent输出根；冻结目标仍不得再次start。

## 2026-08-23：v6 host launch 诊断失败关闭与 v7 收敛

- 获得新的精确系统授权后，从clean `f47dbd2`绑定start/failure/postverify与v1-v5共八份前序manifest，只针对冻结目标`5B19AEF1…7DAB`与UTC 08:12–08:18窗口。v6输出根预先absent，控制复验67/14/11/6/6/6/8/8项、clean HEAD、双控制identity、二十台all-stopped及target stopped。
- 唯一进程观察exit 0、stderr空，stdout 31,605 bytes、SHA-256 `0b11cc43…e1e`且未截断，相关UTM/utmctl/QEMU进程为0。唯一固定predicate `log show`也exit 0、stderr空、未超时；完整stdout仍为3,916,860 bytes、SHA-256 `0dbed63a…e9c`且未截断。
- 可选空分类已由v6规范化，解析随后在NDJSON尾记录以`unified-log-finished-invalid`失败关闭，没有生成`unified-log.json`。八项manifest `44043282…4ae8`逐项通过，目录/文件为`0700`/`0600`；terminal保持`root_cause=unattributed`，clone/start/stop/guest/input/operation/transaction/retry/delete均未执行。v6目录随即冻结，本批没有自动retry或第二次system log读取。
- 本机`log(1)`手册只规定NDJSON末尾记录以包含`finished`字段标识结束，不限定其必须为JSON布尔。repository-only v7因此严格增加精确整数`1`与布尔`true`等价支持，继续拒绝其他值、重复或非末尾marker；解析前先持久化不含日志正文的record/marker数量、是否末尾和值类型分类，避免后续仍不匹配时再次缺少安全结构证据。原有字段、捕获、事件与隐私门均不放宽。
- v1-v7定向回归、默认L6合同、文档/文本门禁、`git diff --check`与完整`check-repo.sh`均通过，只保留既有`docs/sync-server-api-storage.md` 508行预算提醒；真实v6八项证据也由v7逻辑只读验证。本批不修改production installer、FFI ABI、同步/隐私协议、guest状态或产品热路径。下一次真实v7采集须新的单步系统授权和absent输出根；冻结目标仍不得再次start。

## 2026-08-23：v7 host launch 诊断闭合与启动传输停止线

- 获得新的精确系统授权后，从clean `5ab68b1`绑定start/failure/postverify与v1-v6共九份前序manifest，只针对冻结目标`5B19AEF1…7DAB`与UTC 08:12–08:18窗口。v7输出根预先absent；控制复验全部manifest、双控制identity、二十台all-stopped及target stopped后，只执行一次紧凑进程观察和一次固定predicate system log读取。
- 进程观察exit 0、stderr空，stdout 31,640 bytes、SHA-256 `1dd81e54…8cfa`且未截断，相关UTM/utmctl/QEMU进程为0。日志exit 0、stderr空、未超时，stdout仍为3,916,860 bytes与SHA-256 `0dbed63a…e9c`；4,041条NDJSON记录以唯一末尾整数`1`闭合，形成4,040条脱敏事件（UTM 1,067、utmctl 2,973、QEMU 0）。十项manifest `206aa335…7b56c`逐项通过，目录/文件为`0700`/`0600`。
- 脱敏时序显示08:13:48.432695由utmctl发送唯一`UTMv,star`，UTM在0.232ms后接收；约351ms后依次记录`NSWindow.m` assertion、`Invalid parameter not satisfying: [self canBecomeMainWindow]`、UTM栈与`NSInternalInconsistencyException`。证据窗口没有该start return ID的reply，也没有QEMU事件，随后既有start控制在90秒边界失败关闭。该组合把失败机制定位为host UTM/AppKit窗口路径，但不足以单独证明是谁建立了该窗口状态；terminal因此继续保守记录`root_cause=unattributed`。
- 本机`/Applications/UTM.app`为4.7.5 build 118。官方[v4.7.5 `utmctl`源码](https://github.com/utmapp/UTM/blob/v4.7.5/utmctl/UTMCtl.swift)显示CLI通过ScriptingBridge以`.andHide`拉起UTM，`start`直接调用`startSaving`；`--hide`只关闭名为UTM的主窗口。官方当前另有[4.7.5 `utmctl start`崩溃报告](https://github.com/utmapp/UTM/issues/7651)。这些材料支持把plain transport列为阻塞项，但不证明`--hide`一定修复本现场，也不授权更新UTM、GUI动作或试跑。
- clone/start/stop/guest/input/operation/transaction/retry/delete均未执行；v1-v7证据和原target全部冻结，本批没有第二次日志读取。文档/文本门禁、`git diff --check`与完整`check-repo.sh`通过，只保留既有508行文档预算提醒。下一批先repository-only设计launch transport v2：绑定精确UTM版本/build与v7 manifest，为新的独立clean target固定单一transport、pre/post inventory、进程残留与零自动补救门；通过评审后再分别申请clone与start系统授权。
